export const meta = {
  name: 'branch-thai-address-cascade',
  description: 'Add Thai province/district/subdistrict/zipcode cascade picker to the branch tree-view screen (backend model + frontend UI)',
  phases: [
    { title: 'Implement', detail: 'backend BranchOrgDoc geo fields + frontend cascade UI in company-branch-tree-view.tsx' },
    { title: 'Verify', detail: 'full CRUD UAT: forward cascade, reverse lookup, save/reload round-trip' },
  ],
}

const CONTEXT = [
  'Project: BC Ai Account, a Thai SME accounting and ERP system.',
  'Backend: Go, at D:\\bccode\\backend. Frontend: Next.js, at D:\\bccode\\frontend.',
  '',
  'Jead (project owner) asked to add the Thai province -> district -> subdistrict -> auto-zipcode cascade',
  'picker (plus reverse zipcode lookup) to the branch ("สาขา") screen, choosing explicitly to ADD it into the',
  'existing branch tree-view component rather than switching the branch screen onto a different component.',
  '',
  'Verified scope facts from direct source reading this session (do not re-derive, but verify anything you',
  'rely on against the real current file before changing it):',
  '',
  '1. The live branch screen (/branch) renders via',
  '   frontend/src/app/system-settings/company-branch-tree-view.tsx (about 1854 lines). It is backed by the',
  '   real active Go model backend/internal/organization/branch/models/branch_org.go, type BranchOrgDoc, whose',
  '   only address representation today is:',
  '   ```go',
  '   Addresses []BranchAddress `json:"addresses" bson:"addresses"`',
  '   type BranchAddress struct {',
  '       Code    string `json:"code" bson:"code"`',
  '       Address string `json:"address" bson:"address"`',
  '   }',
  '   ```',
  '   This is a per-language (Code = language code) free-text address string list. There are NO',
  '   Province/District/Subdistrict/Zipcode fields anywhere on the live model. A separate legacy',
  '   backend/internal/organization/branch/models/branch.go file has a dead, unreachable Contact struct with',
  '   old-style fields (ProvinceCode/DistrictCode/SubDistrictCode/ZipCode/Latitude/Longitude) — it is NOT wired',
  '   to any live route (confirm this yourself before assuming it can be reused; if it truly is fully dead,',
  '   ignore it, do not resurrect legacy dead code, write fresh fields on BranchOrgDoc instead).',
  '',
  '2. Design decision already made (do not re-litigate): the geo-address (province/district/subdistrict/',
  '   zipcode codes) is language-independent — it is just a set of codes, with the Thai/English display names',
  '   coming from the shared dataset lookup, not per-language storage. So add ONE top-level, single geo-address',
  '   field set on BranchOrgDoc, separate from and in addition to the existing per-language free-text',
  '   Addresses list — matching how creditor/debtor already separate their structured Address (with geo codes)',
  '   from other concerns. Concretely, add to BranchOrgDoc:',
  '   ```go',
  '   CountryCode     string `json:"countrycode" bson:"countrycode"`',
  '   ProvinceCode    string `json:"provincecode" bson:"provincecode"`',
  '   DistrictCode    string `json:"districtcode" bson:"districtcode"`',
  '   SubDistrictCode string `json:"subdistrictcode" bson:"subdistrictcode"`',
  '   ZipCode         string `json:"zipcode" bson:"zipcode"`',
  '   ```',
  '   (field names/tags illustrative — match this repo real naming convention exactly, no underscores, and',
  '   place them sensibly near other top-level scalar fields on BranchOrgDoc, read the real struct first).',
  '',
  '3. Backend routes: backend/internal/organization/branch/branch_http.go, RegisterHttp — POST/GET/PUT/DELETE',
  '   under /organization/branch. There is no separate request DTO; the handler unmarshals the wire payload',
  '   directly into BranchOrgDoc. UpdateBranch (around line 174-260) copies req fields onto existing and',
  '   persists via an explicit $set map — the 5 new fields need to be added to that $set map and to',
  '   CreateBranch the same way. sanitizeBranchAddresses (around line 461) only touches Code/Address today;',
  '   decide whether the new geo fields need similar trimming (they should at minimum be trimmed strings; the',
  '   province/district/subdistrict codes should be validated as real, resolvable dataset codes if you can do',
  '   that cheaply, but do not over-engineer — matching the same validation rigor already used by creditor/',
  '   debtor for their thai-address is enough, do not add anything stricter).',
  '',
  '4. Frontend: company-branch-tree-view.tsx uses plain useState per scalar field (e.g. formEmail line 566,',
  '   formCompanyRegNo line 565), merged directly into the body object inside handleSave (around lines',
  '   687-908, four save-path branches at 764/790/844/891). The address UI today is just',
  '   `<AddressesEditor addresses={formAddresses} onChange={setFormAddresses} languages={editorLanguages} .../>`',
  '   at line 1650-1659, backed by `const [formAddresses, setFormAddresses] = useState<Record<string,string>>({})`',
  '   at line 571, hydrated from the selected node in a useEffect around lines 577-626 via a helper',
  '   getAddressFromList (line 388).',
  '',
  '5. frontend/src/lib/thailand-addresses.ts exports plain functions callable directly from any React',
  '   component with local state, independent of the generic SystemSettingConfig/FieldEditor machinery used by',
  '   creditor/debtor: loadThailandAddressData(backendUrl?), findThailandProvince(data, provinceCode),',
  '   findThailandDistrict(data, provinceCode, districtCode),',
  '   findThailandSubdistrict(data, provinceCode, districtCode, subdistrictCode),',
  '   findThailandAddressMatchesByPostalCode(data, postalCode), getThailandDistrictPostalCode/',
  '   getThailandSubdistrictPostalCode, normalizeThaiPostalCode, onlyUniqueCodes, thailandAddressLabel,',
  '   getThailandCountry. The small dropdown-filtering/label helpers used by the generic system',
  '   (filterThailandProvinces/filterThailandDistricts/filterThailandSubdistricts/singleThailandAddressCode/',
  '   thailandAddressOptionLabel/postalAddressHint) are private to system-settings-screen.tsx (around lines',
  '   5507-5564) — either export them from there for reuse, or reimplement the same small logic locally in',
  '   company-branch-tree-view.tsx (a few lines each); prefer exporting and reusing over duplicating, since',
  '   this repo already has 3 duplicated Address structs and should not gain more duplicated address logic.',
  '   `ThailandAddressFieldEditor` (system-settings-screen.tsx, defined around line 5091) is the reference',
  '   implementation of the real cascade UX: loads the dataset via useState+useEffect, derives selections via',
  '   the find* functions, mutation handlers chooseProvince/chooseDistrict/chooseSubdistrict/applyPostalCode',
  '   (around lines 5190-5306) clear downstream selections on a parent change and auto-fill zipcode when a',
  '   selection uniquely determines one. Follow the same UX behavior in the new branch-tree-view picker.',
  '',
  'Project conventions: MongoDB field names in bson/json tags are lowercase, no underscores, no hyphens.',
  'Thai-First rule: write full correct Thai labels; other languages get English/placeholder text only. Full',
  'freedom is granted to change MongoDB schema/backend/frontend code without asking (pre-launch, disposable',
  'database) excluding deploy/push and the scopeofwork/ folder. Windows dev notes: host go build/test do not',
  'work directly, use the Docker builder image: MSYS_NO_PATHCONV=1 docker run --rm -v "D:/bccode/backend:/src"',
  '-w /src backend-mainapi-local-builder:latest sh -c "CGO_ENABLED=1 go build -tags musl ./..." (same pattern',
  'for go test). If the Docker Compose "build" path fails to find BuildKit on this machine, use',
  '"/c/Program Files/Docker/Docker/resources/cli-plugins/docker-buildx.exe" build --load directly instead —',
  'this was hit and worked around in a prior task this session, noted here so it does not need rediscovering.',
  'Frontend typecheck must run "node node_modules/typescript/bin/tsc --noEmit" directly. The frontend dev',
  'server must always be run as "next build" then "next start", never "next dev". Do NOT leave any Go build',
  'cache directories (like .gocache-build/.gocache-mod) in the repo root — use Docker named volumes or a',
  'location outside the repo for any build cache, and clean up anything you do create before finishing. Do',
  'not commit or push anything.',
].join('\n')

phase('Implement')

const implementReport = await agent(
  [
    CONTEXT,
    '',
    'Do the full implementation:',
    '',
    '1. Backend: read backend/internal/organization/branch/models/branch_org.go for real, add the 5 new geo',
    '   fields to BranchOrgDoc as specified above. Read backend/internal/organization/branch/branch_http.go for',
    '   real, wire the new fields through CreateBranch and UpdateBranch (including the explicit $set map used',
    '   by UpdateBranch), and through sanitizeBranchAddresses or an equivalent trim step for the new fields.',
    '   Verify: Docker builder image build command from context, must exit 0. Run any existing Go tests for the',
    '   touched package.',
    '',
    '2. Frontend: read company-branch-tree-view.tsx for real. Add 4-5 new useState hooks for the geo fields',
    '   (province/district/subdistrict/zipcode/countrycode codes), following the exact same plain-useState',
    '   pattern already used for formEmail/formCompanyRegNo etc. Build a cascade picker UI (4 selects: province,',
    '   district, subdistrict, then an auto-filled/manually-editable zipcode field, matching the UX behavior of',
    '   ThailandAddressFieldEditor in system-settings-screen.tsx: choosing a province clears district+subdistrict',
    '   +zipcode; choosing a district clears subdistrict+zipcode; choosing a subdistrict auto-fills zipcode when',
    '   it uniquely determines one; typing a zipcode directly attempts reverse lookup and auto-fills',
    '   province+district+subdistrict when unique, or shows a multi-match list to narrow down when ambiguous).',
    '   Place this picker in the same form area as the existing AddressesEditor. Import and call',
    '   frontend/src/lib/thailand-addresses.ts exports directly. For the small filtering/label helper functions',
    '   that are currently private to system-settings-screen.tsx, export them from that file and import them',
    '   here rather than duplicating the logic (read the real current private definitions first, they are',
    '   around line 5507-5564 as of this session, re-verify against the current file).',
    '   Hydrate the new fields from the selected branch node in the same useEffect that currently hydrates',
    '   formAddresses (around lines 577-626), and merge the new fields into the body object in handleSave in',
    '   all the same branches that currently set addresses (around lines 764/790/844/891).',
    '',
    '3. Verify: "node node_modules/typescript/bin/tsc --noEmit" must exit 0. Do a real live check: build fresh',
    '   ("next build" then "next start", never "next dev", killing any stale process on port 3000 first),',
    '   log in via the real UI, open the branch tree-view screen, and drive the actual cascade: select a real',
    '   province, then district, then subdistrict, confirm the zipcode auto-fills; separately, clear the form',
    '   and type a real 5-digit zipcode, confirm province/district/subdistrict auto-fill (or a multi-match list',
    '   appears for an ambiguous zipcode); save a branch with the new fields set, reload/reselect it, and',
    '   confirm the saved geo fields are correctly redisplayed (a genuine save-and-reload round-trip, not just a',
    '   client-side state check). This is the actual feature working end-to-end, verify it directly.',
    '',
    'Report back precisely: exact files changed, exact field/function names added, and the verbatim output of',
    'every verification command including the live cascade + save/reload check (describe exactly what you',
    'selected/typed and what you observed, with real values).',
  ].join('\n'),
  { label: 'implement-branch-cascade', phase: 'Implement' },
)

phase('Verify')

const verifyReport = await agent(
  [
    CONTEXT,
    '',
    '=== Implementation report to independently verify, do not just trust it ===',
    String(implementReport),
    '',
    'Your job is independent verification, following the uat-crud-mongo convention already used in this',
    'project: never trust a self-reported "done" without real evidence you generate yourself.',
    '',
    '1. Read the real git diff yourself to confirm what was actually changed matches what was reported.',
    '2. Do a full CRUD UAT pass on the branch screen: create a branch with real province/district/subdistrict',
    '   selections (verify the zipcode auto-fills), save, and verify three ways — real browser UI shows it',
    '   correctly after reload, a real API response for that branch includes the 5 new fields with the right',
    '   values, and the real MongoDB document (docker exec mongodb mongosh --quiet appdb --eval ...) has them',
    '   too. Then edit an existing branch changing only the geo fields, save, and confirm the existing',
    '   per-language free-text Addresses list is untouched (no regression to the pre-existing feature). Then',
    '   test the reverse zipcode lookup with a real ambiguous and a real unique zipcode.',
    '3. If you find any real bug, root-cause it, fix it with the smallest correct change, and re-verify with',
    '   fresh, unique test data.',
    '4. Add or extend a Playwright regression test for this (frontend/e2e/ — check whether a branch-specific',
    '   spec already exists to extend, or add a small new one following the existing patterns in this repo,',
    '   e.g. frontend/e2e/trade-partners-crud.spec.ts for DOM-interaction conventions). Then run the FULL',
    '   existing e2e suite (not just your new/changed file) and confirm no regressions.',
    '5. Run the backend build and frontend typecheck one more time as a final sanity check, report their real',
    '   exit output. Confirm no stray Go build cache directories were left in the repo root (check for anything',
    '   matching .gocache*/gopath-cache/similar at the repo root; if you find any, delete them and note it).',
    '6. Read D:\\bccode\\.agents\\worklog.md yourself first to find its real current last line, then append a new',
    '   entry after it (do not just claim you did this — re-read the file after your edit to confirm the new',
    '   entry is actually there before finishing, since this exact "claimed but did not land" gap has happened',
    '   twice already this session with other agents).',
    '',
    'Do not commit or push anything.',
    '',
    'End your report with the required Thai summary format: what changed, pros, cons/risks, and concrete next',
    'steps.',
  ].join('\n'),
  { label: 'verify-branch-cascade', phase: 'Verify', effort: 'high' },
)

return { implementReport, verifyReport }
