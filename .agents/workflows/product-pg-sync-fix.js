export const meta = {
  name: 'product-pg-sync-fix',
  description: 'Fix the Product list 500 error in DEV: dead ProductBarcode Kafka consumer + missing auto table-provisioning for new tenant PostgreSQL databases',
  phases: [
    { title: 'Implement', detail: 'debug+fix the dead consumer, fix tenant auto-provisioning to create all tables including inventorystockbalances' },
    { title: 'Verify', detail: 'create a product end-to-end, confirm it syncs to PostgreSQL, confirm /product list loads with real stock balance' },
  ],
}

const CONTEXT = [
  'Project: BC Ai Account, a Thai SME accounting and ERP system.',
  'Backend: Go, at D:\\bccode\\backend. Data Store Roles (already established project convention): MongoDB is',
  'the raw source of truth; PostgreSQL is a relational projection populated via Kafka after every Mongo',
  'create/update/delete; ClickHouse is a dimensional/BI projection. PostgreSQL and ClickHouse are disposable',
  'and rebuildable from MongoDB at any time (pre-launch, no real data at risk).',
  '',
  'Confirmed by Jead (the project owner) directly: the /product screen list view legitimately needs to read',
  'from PostgreSQL, not MongoDB, because it displays a precomputed stock balance figure (ยอดคงเหลือ) that only',
  'exists as a PostgreSQL projection (joined from product/productbarcode/inventorystockbalances tables). This',
  'is NOT a design mistake to revert — keep the PostgreSQL-backed list. The real bug is that the Mongo-to-',
  'PostgreSQL sync pipeline for Product/ProductBarcode is broken in this DEV environment, so those PostgreSQL',
  'tables never get populated, and the list 500s as a result.',
  '',
  'Verified root causes from direct investigation this session, do not re-derive them, but verify each one',
  'against the real current system state yourself before fixing (state may have changed):',
  '',
  '**Root cause A — the ProductBarcode Kafka consumer is registered but never actually consumes.**',
  '- The consumer code is real and complete, not a stub: `backend/internal/product/productbarcode/',
  '  productbarcode_consumer.go` (ProductBarcodeConsumer, handlers for Create/Update/Delete/BulkCreate),',
  '  backed by `backend/internal/product/productbarcode/services/productbarcode_consumer_service.go` and',
  '  `productbarcode_pg_repository.go` (real GORM-based PostgreSQL upsert logic). A dedicated standalone binary',
  '  also exists: `backend/cmd/productbarcodeconsumer/main.go`.',
  '- It is registered in the running mainapi process: `backend/main.go` (around line 726) calls',
  '  `productbarcode.NewProductBarcodeConsumer(ms, cfg)` inside a block gated by',
  '  `devApiMode == "" || devApiMode == "2"`; `backend/cmd/app/main.go` (around line 385) also registers it.',
  '  `docker-compose.yml`\'s mainapi service sets `DEV_API_MODE: "2"`, so the gate should be satisfied.',
  '- Live verification showed it is NOT actually running: `docker exec kafka kafka-consumer-groups --describe',
  '  --group consumer-productbarcode-group-01` returned `GroupIdNotFoundException` — this consumer group has',
  '  never connected to Kafka, ever, even though Kafka topics like `when-product-barcode-created` genuinely',
  '  receive real messages when a barcode is created via the API today (confirmed by directly consuming real',
  '  messages from that topic). Other consumers in the same mainapi process (creditor, debtor, warehouse,',
  '  inventory) DO show up as real active consumer groups, so this is specific to the ProductBarcode consumer',
  '  registration path, not a Kafka-wide problem.',
  '- This needs live debugging: find why `RegisterConsumer()`/`ms.Consume(...)` for this specific consumer',
  '  never actually takes effect at runtime, when structurally-similar consumers in the same process do work.',
  '  Compare the exact registration code/config for a working consumer (e.g. warehouse or creditor) against',
  '  the ProductBarcode one, line by line, to find the real discrepancy — do not guess, add temporary debug',
  '  logging if needed to see where the registration path silently fails or no-ops, then remove the debug',
  '  logging once the real fix is confirmed.',
  '',
  '**Root cause B — new tenant PostgreSQL databases never get their tables created, and',
  '  inventorystockbalances is never created by the normal provisioning path at all.**',
  '- `backend/internal/goapi/mydb/unified_database_manager.go` (`connectPostgreSQL`, around lines 95-128):',
  '  when a tenant\'s PostgreSQL database does not exist yet, `createPostgreSQLDatabase()` runs a bare',
  '  `CREATE DATABASE "<holdingcode>"` and stops — it never runs any table DDL afterward. Confirmed live: a',
  '  freshly auto-created tenant database (`bctest01`) has zero tables.',
  '- Separately, even the full DatabaseRebuildAll path (used for the older `test` tenant database, which does',
  '  have 20 tables including `product`) never creates `inventorystockbalances` — that table is only ever',
  '  created by `CreateInventoryCostingTables` (`backend/internal/goapi/inventory/database.go`, around line',
  '  12), which is only invoked from the manual HTTP endpoint `POST /inventory/create-tables`',
  '  (`backend/internal/goapi/inventory/handler.go`, around line 290) — it is missing from',
  '  DatabaseRebuildAll\'s table list entirely (`create-database.go`, `independentTables` list around lines',
  '  1857-1872).',
  '- Real fix needed: (1) make `createPostgreSQLDatabase()` (or its caller in `connectPostgreSQL`) run full',
  '  table provisioning immediately after creating a new tenant database, so a freshly auto-created tenant',
  '  gets all its tables, not just an empty database; (2) add `CreateInventoryCostingTables` (or equivalent',
  '  DDL) into `DatabaseRebuildAll`\'s table list so `inventorystockbalances` is provisioned automatically for',
  '  every tenant, old and new, closing the gap for the already-existing `test` tenant too.',
  '',
  '**Defensive improvement to consider (lower priority, only if time allows without scope creep):**',
  '`ProductSearchHandler` (`backend/internal/goapi/handlers/product_cache.go`) currently 500s outright when the',
  'underlying PostgreSQL tables/relations do not exist. Given root causes A and B are being fixed, this should',
  'become moot in practice, but a defensive check that returns an empty/degraded result with a clear log',
  'message instead of a raw 500 would be a reasonable belt-and-suspenders addition if it is a small, safe',
  'change — do not spend significant effort here if fixing A and B already makes this a non-issue.',
  '',
  'Project conventions: Windows dev notes — host go build/test do not work directly, use the Docker builder',
  'image: MSYS_NO_PATHCONV=1 docker run --rm -v "D:/bccode/backend:/src" -w /src',
  'backend-mainapi-local-builder:latest sh -c "CGO_ENABLED=1 go build -tags musl ./..." (same pattern for go',
  'test). To actually test a live fix, you need to rebuild and restart the real `mainapi` Docker container',
  '(the running container predates any backend code change) — if `docker compose build`/`docker-compose build`',
  'cannot find BuildKit on this machine, use',
  '"/c/Program Files/Docker/Docker/resources/cli-plugins/docker-buildx.exe" build --load -t backend-mainapi -f',
  'Dockerfile . directly (a prior task this session hit and worked around this exact issue), then',
  '`docker-compose up -d --no-deps mainapi` (or the equivalent compose file combination already used in this',
  'project — check `docker inspect mainapi` for the real compose file list if unsure) to actually pick up the',
  'change before testing through the API/UI. Do NOT leave any Go build cache directories in the repo root (a',
  'prior task left 3.3GB of .gocache-build/.gocache-mod there by mistake; use Docker named volumes or a',
  'location outside the repo). Do not commit or push anything.',
  '',
  'Full freedom is granted to change MongoDB/PostgreSQL schema and backend code without asking (pre-launch,',
  'disposable database), excluding deploying/pushing to a shared remote and the scopeofwork/ folder.',
].join('\n')

phase('Implement')

const implementReport = await agent(
  [
    CONTEXT,
    '',
    'Fix both root causes, in order:',
    '',
    '1. **Root cause A first** (the dead consumer). Read the real current source of',
    '   `backend/internal/product/productbarcode/productbarcode_consumer.go` and its registration in',
    '   `backend/main.go`/`backend/cmd/app/main.go`. Compare against a real, confirmed-working consumer',
    '   registration in the same file (for example the warehouse or creditor consumer, whichever you confirm',
    '   is genuinely active via `docker exec kafka kafka-consumer-groups --describe --group <its-real-group-id>`',
    '   first) to find the actual discrepancy — a typo in the group id, a config value read from the wrong',
    '   place, a missing method call, an early return, whatever it really is. Add temporary debug logging if',
    '   you need to see where the registration path silently fails, then remove the debug logging once you have',
    '   the real fix. Apply the smallest correct fix.',
    '   Verify: rebuild the mainapi Docker image (buildx workaround if needed) and restart the container, then',
    '   confirm live via `docker exec kafka kafka-consumer-groups --describe --group',
    '   consumer-productbarcode-group-01` that the group now shows up as an active consumer with real lag/offset',
    '   numbers (not GroupIdNotFoundException). Create a real barcode via the API/UI and confirm the mainapi',
    '   container logs show it actually being consumed (a real "Consume Product Barcode"-style log line or',
    '   equivalent), not just produced.',
    '',
    '2. **Root cause B** (tenant table auto-provisioning). Read the real current source of',
    '   `backend/internal/goapi/mydb/unified_database_manager.go` (`connectPostgreSQL`/',
    '   `createPostgreSQLDatabase`) and `backend/internal/goapi/.../create-database.go`',
    '   (`DatabaseRebuildAll`/`independentTables`, find the real current file and line numbers, they may have',
    '   shifted). Make the new-tenant auto-create path run full table provisioning (the same DDL',
    '   `DatabaseRebuildAll` runs) immediately after `CREATE DATABASE`, so a freshly auto-created tenant',
    '   database is not left empty. Add `CreateInventoryCostingTables` (from',
    '   `backend/internal/goapi/inventory/database.go`) into `DatabaseRebuildAll`\'s table list, so',
    '   `inventorystockbalances` gets created for every tenant automatically going forward, not only via the',
    '   manual `/inventory/create-tables` endpoint.',
    '   Verify: rebuild+restart mainapi, create a brand-new tenant (or use one that does not yet have these',
    '   tables), and confirm via `docker exec postgres psql ...` that `productbarcode` and',
    '   `inventorystockbalances` tables now exist for it. For the existing `test` tenant that already has some',
    '   tables but is missing `inventorystockbalances`, decide and apply the smallest correct way to backfill',
    '   just that missing table without re-running destructive DDL on the tables that already exist and may',
    '   have real dev data in them.',
    '',
    '3. **End-to-end proof**: create a brand new product + barcode via the real UI (or API), wait briefly for',
    '   the Kafka consumer to process it, then confirm the real PostgreSQL row exists for it',
    '   (`docker exec postgres psql`), and finally load the real `/product` screen in a browser and confirm the',
    '   list genuinely loads (200, real rows, no 500) including the product you just created, with whatever',
    '   stock-balance figure it shows (0 or a real number, either is fine, the point is the query succeeds).',
    '',
    '4. Only if the above is solid and you have time left without scope creep: add the defensive graceful-',
    '   degrade check in `ProductSearchHandler` described in the shared context. Skip it entirely if it risks',
    '   masking a real remaining issue — the goal is a real working query, not a hidden failure.',
    '',
    'Report back precisely: exact root cause of A (what was actually wrong, in your own words, not just',
    '"fixed it"), exact files changed for both A and B, and the verbatim output of every verification command',
    'including the live consumer-group check, the live PostgreSQL table check, and the live /product screen',
    'load.',
  ].join('\n'),
  { label: 'implement-pg-sync-fix', phase: 'Implement' },
)

phase('Verify')

const verifyReport = await agent(
  [
    CONTEXT,
    '',
    '=== Implementation report to independently verify, do not just trust it ===',
    String(implementReport),
    '',
    'Your job is independent verification, following the uat-crud-mongo convention already used in this',
    'project: never trust a self-reported "done" without real evidence you generate yourself.',
    '',
    '1. Read the real git diff yourself to confirm what was actually changed matches what was reported, and',
    '   that the fix is the smallest correct change (not a broad rewrite of the DB provisioning system).',
    '2. Independently confirm root cause A is genuinely fixed: run',
    '   `docker exec kafka kafka-consumer-groups --describe --group consumer-productbarcode-group-01` yourself',
    '   and confirm it shows a real active consumer, not GroupIdNotFoundException. Create a fresh barcode via',
    '   the real API/UI yourself (do not reuse the implementer\'s test data) and confirm via a real PostgreSQL',
    '   query that the row landed there within a reasonable time.',
    '3. Independently confirm root cause B is genuinely fixed: confirm via real `docker exec postgres psql`',
    '   queries that both the `test` tenant and a fresh/newly-created tenant have real `productbarcode` and',
    '   `inventorystockbalances` tables (not just that the implementer claims so).',
    '4. Do a full, fresh live UAT of the actual /product screen: log in, open /product, confirm the list loads',
    '   (200, not 500), create a new product with at least one barcode via the real UI, confirm it appears in',
    '   the list after a reasonable wait for the Kafka sync, and confirm whatever stock-balance figure is shown',
    '   is not an error state.',
    '5. Run the backend build (Docker builder image command) one more time as a final sanity check, and run the',
    '   existing product/barcode/productset Playwright specs',
    '   (frontend/e2e/product-crud.spec.ts, product-barcode-crud.spec.ts, product-set-crud.spec.ts) to confirm',
    '   no regression from this backend change. If any of those specs specifically asserted the previous',
    '   documented 500-error behavior as expected (a prior task deliberately left such an assertion pending this',
    '   exact fix), find it and flip/remove it now that the real bug is fixed, and re-run to confirm green.',
    '6. Confirm no stray Go build cache directories were left in the repo root.',
    '7. Read D:\\bccode\\.agents\\worklog.md yourself first to find its real current last line, then append a new',
    '   entry after it, and re-read the file after your edit to confirm the entry actually landed (this exact',
    '   "claimed but did not land" gap has happened multiple times this session with other agents, do not',
    '   repeat it).',
    '',
    'Do not commit or push anything.',
    '',
    'End your report with the required Thai summary format: what changed, pros, cons/risks, and concrete next',
    'steps.',
  ].join('\n'),
  { label: 'verify-pg-sync-fix', phase: 'Verify', effort: 'high' },
)

return { implementReport, verifyReport }
