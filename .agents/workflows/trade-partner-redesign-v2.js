export const meta = {
  name: 'trade-partner-redesign-v2',
  description: 'Redesign creditor/debtor: unified business-partner credit, generic Thai address cascade, multi-bank-account, multi-branch legal research',
  phases: [
    { title: 'Research', detail: 'web (Thai legal + competitor) + GPT/GLM/DeepSeek consult in parallel' },
    { title: 'Design Review', detail: 'Fable reads real source and writes final spec' },
    { title: 'Implement', detail: 'build the approved design' },
    { title: 'Verify', detail: 'full CRUD UAT across all touched screens' },
  ],
}

const SHARED_CONTEXT = [
  'Project: BC Ai Account, a Thai SME accounting and ERP system.',
  'Backend: Go, at D:\\bccode\\backend (module smlcloudplatform, Echo framework, MongoDB as the only raw data store).',
  'Frontend: Next.js, at D:\\bccode\\frontend.',
  '',
  'Earlier this session, 4 real screens were built and are live today: creditor (/creditor), creditorgroup',
  '(/creditorgroup), debtor (/debtor), debtorgroup (/debtorgroup), configured as "main-crud" entries in',
  'frontend/src/lib/system-setting-screens.ts, backed by backend/internal/debtaccount/{creditor,creditorgroup,',
  'debtor,debtorgroup}. Jead (the project owner) is now asking for a real redesign of this area, based on',
  'looking at the live menu group "คู่ค้า (ลูกค้า/ผู้ขาย)" (Trade Partners), which has 4 items: เจ้าหนี้ (Creditor),',
  'กลุ่มเจ้าหนี้ (Creditor Group), ลูกหนี้ (Debtor), กลุ่มลูกหนี้ (Debtor Group).',
  '',
  'Jead\'s exact requirements, verbatim intent preserved:',
  '',
  '1. "แยกเป็น ลูกหนี้ เจ้าหนี้ คนละ section แล้ว ต้องออกแบบ กรณี ลูกหนี้ เจ้าหนี้ เป็นกลุ่มเดียวกัน เพราะต้องมีเครดิตรวมกันในอนาคต"',
  '   Debtor and Creditor stay as separate menu sections/screens as they are today, BUT the design must handle',
  '   the case where a real-world business is BOTH a customer (debtor) and a vendor (creditor) at the same time,',
  '   because their credit exposure needs to be tracked/combined together in the future (a shared credit limit',
  '   or at minimum a shared identity link between a Creditor record and a Debtor record for the same real party).',
  '',
  '2. "ออกแบบระบบกลางด้วย เลือกจังหวัดในไทย พอเลือกจังหวัดเสร็จ เลือกอำเภอ/เขต แล้ว เลือก ตำบล/แขงวง แล้วจะขึ้นรหัสไปรณีย์เลย',
  '   หรือป้อนรหัสไปร๊ย์แล้ว ทุกอย่าง มาเองเลย"',
  '   Design a CENTRAL/SHARED (reusable) Thai address input system: province -> district/khet -> subdistrict/',
  '   khwaeng cascading select that auto-fills the postal code, AND the reverse (enter a 5-digit postal code and',
  '   province/district/subdistrict auto-fill). This exact capability already exists in this codebase today but',
  '   is hardcoded to only the "branch" screen (see verified findings below) — it needs to become a real, generic,',
  '   reusable capability any screen can use, starting with creditor and debtor.',
  '',
  '3. "ธนาคารมีได้หลายธนาคาร" — a creditor (and now also a debtor, if relevant) must support MULTIPLE bank accounts,',
  '   not just one.',
  '',
  '4. "รูปภาพมีได้หลายรูปภาพ" — already fully supported today (see verified findings below), carry it forward,',
  '   do not regress it.',
  '',
  '5. "ไม่แน่ใจ ลูกหนี้/เจ้าหนี้ ต้องมีรหัสาขาตามกฏหมายไทยหรือไม่ ถ้ามีหลายสาขาจะทำยังไง" — Jead is unsure whether a',
  '   creditor/debtor is legally required to carry a Thai tax branch code (the existing `branchnumber` field,',
  '   e.g. head office = 00000, branch 1 = 00001), and if a business partner has MULTIPLE real branches, how that',
  '   should be modeled/handled. This is a real Thai tax/legal question and must go through the project\'s',
  '   THAI-DOMAIN research process: real web research on Thai Revenue Department (กรมสรรพากร) rules for branch',
  '   numbering on tax invoices/withholding tax documents for a counterparty, PLUS a real cross-check consult',
  '   with GPT, GLM, and DeepSeek, before Claude Code synthesizes and decides. Do not answer this from training',
  '   data alone; cite real, current sources.',
  '',
  '6. "ที่อยู่ออกใบกำกับ กับที่อยู่จริง อาจจะไม่ตรงกัน" — the tax-invoice/billing address and the actual/operating',
  '   address of a business partner may be different and both need to be represented, not just billing address',
  '   alone as today.',
  '',
  '7. "และอื่นๆ ลองหาจาก คู่แข่ง" — research real competitor Thai and international accounting/ERP software for',
  '   anything else relevant to a complete, correct creditor/debtor master-data design that this list has not',
  '   already covered.',
  '',
  'Verified findings from direct source reading this session, do not re-derive them, but verify anything you',
  'rely on yourself before changing it:',
  '',
  'A. **Images**: both creditor and debtor configs already use `imageGalleryField("images", ...)` (multi-image),',
  '   backed by `Images *[]Image` on both Go models. Already correct, do not touch unless something is broken.',
  '',
  'B. **Bank accounts**: creditor UI exposes exactly 3 flat single-row fields',
  '   (`bankaccountbankcode`/`bankaccountnumber`/`bankaccountname` in frontend/src/lib/system-setting-screens.ts',
  '   around line 1025-1027) for ONE bank account. The real Go backend field',
  '   (backend/internal/debtaccount/creditor/models/creditor.go:33) is already `BankAccounts *[]BankAccount`, an',
  '   array — the UI just does not expose more than one row today, and it is not confirmed whether the current',
  '   single-row UI even round-trips correctly against that array field (verify this yourself, it may already be',
  '   subtly broken). Debtor has NO bank account field or backend array at all currently — decide, based on real',
  '   research plus GPT/DeepSeek advice, whether a debtor (customer) plausibly needs bank accounts too (for',
  '   refunds, for example) or whether this is creditor-only by nature.',
  '',
  'C. **Thai address cascade**: a real dataset exists at `backend/assets/address/thailand-addresses.json`,',
  '   served via `frontend/src/app/api/address/thailand/route.ts`, loaded client-side via',
  '   `frontend/src/lib/thailand-addresses.ts` (`loadThailandAddressData`). The cascade UI logic (forward',
  '   province->district->subdistrict->auto-zipcode, AND reverse zipcode->auto-fill) already exists and works,',
  '   but is hardcoded to `config.slug === "branch"` in roughly 10 places inside',
  '   `frontend/src/app/system-settings/system-settings-screen.tsx` (grep for that exact string to find every',
  '   occurrence). Creditor/debtor currently only have flat `addressforbilling.provincecode/districtcode/',
  '   subdistrictcode/zipcode` plain text fields with no cascade wired at all. The reverse-lookup function is',
  '   `findThailandAddressMatchesByPostalCode` (used by `applyPostalCode()`).',
  '',
  'D. **Branch number**: both models have a single plain string `BranchNumber` field (not an array), with a',
  '   fixed helper text "สำนักงานใหญ่ = 00000, สาขาที่ 1 = 00001". No existing multi-branch concept exists anywhere',
  '   in backend/internal/debtaccount today.',
  '',
  'E. **Addresses**: both models have `AddressForBilling Address` (single) and `AddressForShipping *[]Address`',
  '   (array), but only `addressforbilling.*` is exposed in either frontend config today —',
  '   `AddressForShipping` exists in the backend but has zero UI exposure on either screen.',
  '',
  'F. **A dormant, unwired "unified business partner" backend module already exists**, this is the single most',
  '   important finding for requirement 1 above: `backend/internal/debtaccount/customer/models/customer.go`',
  '   defines a `Customer` struct with `IsCreditor bool` and `IsDebtor bool` flags, has a complete CRUD stack',
  '   (`customer_http.go`, `customer_http_service.go`, `customer_mongo_repository.go`), and IS mounted in real',
  '   routing (`backend/main.go:421`, `customer.NewCustomerHttp(ms, cfg)`) — but there is NO frontend screen,',
  '   config entry, or route referencing it anywhere; it has apparently never been used. This may be exactly the',
  '   right foundation for requirement 1 (shared identity + combined credit for a business that is both customer',
  '   and vendor), or it may be an abandoned, wrong-shaped attempt that should be replaced instead — this decision',
  '   must be made deliberately with real evidence (read its actual full field list, compare it field-by-field',
  '   against the live Creditor/Debtor models, check whether it has ever been referenced by any Kafka consumer or',
  '   PostgreSQL projection, check git history for why it was built and abandoned), not guessed. Also note:',
  '   `Customer.Address` (customer.go:33) independently duplicates the same address shape used by',
  '   creditor.Address/debtor.Address — three near-identical struct definitions across three packages, worth',
  '   flagging/reconciling if this task touches address modeling.',
  '',
  'Existing conventions to follow:',
  '- MongoDB field names in bson/json tags are lowercase, no underscores, no hyphens, matching every model.',
  '- Business Code fields (Code, GroupCode) are uppercase, no spaces.',
  '- Thai-First rule: write full, correct Thai labels; other languages get English/placeholder text only.',
  '- Backend collections/indexes are created automatically by backend code; never create them manually.',
  '- Full freedom is granted to add/change MongoDB schema, backend code, and frontend code without asking',
  '  (pre-launch, disposable database) — excluding deploying/pushing to a shared remote and the scopeofwork/',
  '  folder, which must be read-only.',
  '- Windows dev notes: host go build/test do not work directly; use the Docker builder image:',
  '  MSYS_NO_PATHCONV=1 docker run --rm -v "D:/bccode/backend:/src" -w /src backend-mainapi-local-builder:latest',
  '  sh -c "CGO_ENABLED=1 go build -tags musl ./..." (same pattern for go test). Frontend typecheck must run',
  '  "node node_modules/typescript/bin/tsc --noEmit" directly. The frontend dev server must always be run as',
  '  "next build" then "next start", never "next dev".',
  '- This project follows a Multi-Model Role Division Rule: Claude Sonnet is the only agent that edits files;',
  '  Fable acts as chief architect and final design judge; GPT 5.5, GLM, and DeepSeek are external advisors',
  '  consulted through real CLI wrapper calls (never role-played by an agent persona pretending to be them).',
  '  There is also a mandatory scoring rule: every real consult of GPT/GLM/DeepSeek must be scored 1-10',
  '  afterward (accuracy verified against real source, usefulness/how much was adopted, reliability of the',
  '  call) and appended as a row to .agents/advisor-scorecard.md, with the score shown in the final report.',
  '- A 4th advisor is now available: OpenRouter, via `python ~/.claude/tools/openrouter-ask.py --model',
  '  <provider/model-id> "question"` (list options first with --list-models --filter <text>). Use it only if a',
  '  question genuinely needs a model outside GPT/GLM/DeepSeek\'s coverage; log which underlying model was used.',
  '- Thai-domain legal/tax questions (like the branch-number question, requirement 5 above) require real web',
  '  research first, plus cross-checking with GPT, GLM, and DeepSeek, before Claude Code synthesizes a final,',
  '  source-cited answer. Do not rely on any single model\'s untraceable claim about a Thai law/tax rate/rule.',
].join('\n')

function externalConsultInstructions(toolName, wrapperPath, question) {
  return [
    'You are consulting a real external AI model through a command line wrapper. This is not a role-play, you',
    'must genuinely invoke the external command and return what it actually outputs.',
    '',
    'Steps, in order:',
    '1. Use the Write tool to save the following context and question to a temporary file at a path such as',
    '   D:\\bccode\\.agents\\tmp\\' + toolName + '-consult-input.txt.',
    '2. The file content must include the shared project context below, followed by the specific question.',
    '3. Run this exact command using the Bash tool (adjust only the file path if you saved it elsewhere):',
    '   cat D:\\bccode\\.agents\\tmp\\' + toolName + '-consult-input.txt | python ' + wrapperPath + ' --system "You are acting as an external design advisor for a Thai SME ERP project. Read the attached context and question carefully, then give a direct, specific, real answer."',
    '4. Return the raw stdout of that command back as your final answer, verbatim, with no summarizing, no',
    '   added opinion of your own, and no reformatting. If the command fails or times out, report the exact',
    '   error text you saw instead of inventing a fallback answer. Retry up to 3 times with increasing timeouts',
    '   before giving up and reporting the real failure.',
    '5. Delete the temporary file when you are done.',
    '',
    '=== Context to include in the file ===',
    SHARED_CONTEXT,
    '',
    '=== Question to include in the file ===',
    question,
  ].join('\n')
}

phase('Research')

const researchPrompts = [
  {
    label: 'web-thai-legal-branch',
    phase: 'Research',
    prompt: [
      SHARED_CONTEXT,
      '',
      'Task: perform REAL web research (use WebSearch and WebFetch, cite real URLs you actually visited, do not',
      'invent sources) specifically on requirement 5: Thai tax law and Revenue Department (กรมสรรพากร) rules',
      'about branch numbering (รหัสสาขา, เลขที่สาขา) on tax invoices, receipts, and withholding tax certificates',
      '(หนังสือรับรองหักภาษี ณ ที่จ่าย) for a COUNTERPARTY (a vendor/creditor or a customer/debtor), not just for',
      'your own company. Specifically answer:',
      '1. Is a business legally required to record a specific branch number when issuing/receiving a tax invoice',
      '   to/from a counterparty that has multiple branches? What is the real rule (cite the source)?',
      '2. If a single creditor or debtor business entity operates multiple real branches, how do real Thai',
      '   accounting/ERP systems (and real accounting practice) typically record this: one master record with a',
      '   single default branch number, one master record with a list of branch numbers/addresses, or one master',
      '   record per branch?',
      '3. What does the 13-digit Tax ID (เลขประจำตัวผู้เสียภาษี) plus branch number combination actually represent',
      '   on a real Thai tax document, and does the branch number ever change independently of the head office',
      '   Tax ID?',
      '',
      'Report concrete findings with citations. If you cannot find a definitive authoritative answer, say so',
      'honestly rather than guessing, and note what is unclear.',
    ].join('\n'),
  },
  {
    label: 'web-competitor-research',
    phase: 'Research',
    prompt: [
      SHARED_CONTEXT,
      '',
      'Task: perform REAL web research (use WebSearch and WebFetch, cite real URLs you actually visited, do not',
      'invent sources) on how real competitor software handles these specific design questions:',
      '1. Unified customer/vendor ("business partner") entities with a combined/shared credit limit for a real',
      'business that is both a customer and a supplier at once: SAP Business One Business Partner (BP) master',
      'data, Odoo res.partner (single partner model with customer_rank/supplier_rank flags), QuickBooks/Xero (do',
      'they unify or keep separate?), and any real Thai software (PEAK, FlowAccount, Express Accounting) that',
      'does this.',
      '2. Multi-bank-account UX on a vendor/customer master record: how do SAP B1, Odoo, NetSuite, QuickBooks',
      'let a user add more than one bank account per party, and what fields does each bank account row carry',
      '(bank name/code, account number, account name, branch, currency, is-default flag)?',
      '3. Billing address vs actual/operating address vs shipping address as distinct concepts on a vendor/',
      'customer master record: how many distinct address types do real systems carry, and what are they called?',
      '4. Anything else about creditor/debtor/vendor/customer master data design that a first-version screen in',
      'a Thai SME ERP should have and this task has not already covered.',
      '',
      'Report concrete findings organized by question number, with citations. Synthesize a clear recommendation',
      'for each question at the end.',
    ].join('\n'),
  },
  {
    label: 'gpt-consult',
    phase: 'Research',
    prompt: externalConsultInstructions(
      'gpt',
      '~/.claude/tools/gpt-ask.py',
      [
        'From a Thai SME business-owner and UX point of view, answer:',
        '1. When a real business is both our customer and our vendor at the same time, should the user experience',
        '   be "one shared record with two role flags and one combined credit view", or "two separate records',
        '   that are linked/cross-referenced", or something else? What is the least confusing UX for a Thai SME',
        '   bookkeeper who is not a software engineer?',
        '2. How should multiple bank accounts be presented on the edit form without overwhelming a simple form',
        '   (e.g. a "add another bank account" repeatable row vs a separate sub-screen)?',
        '3. Should the debtor (customer) master record also support bank accounts (for refunds), or is that',
        '   creditor-only in practice for a typical Thai SME?',
        '4. How should "ที่อยู่ออกใบกำกับ" (billing/tax-invoice address) vs an actual/operating address be labeled',
        '   and laid out on the form so a non-technical user understands the difference immediately?',
        '5. Flag anything in this whole redesign that feels like scope creep for a first version and should be',
        '   deferred.',
      ].join('\n'),
    ),
  },
  {
    label: 'deepseek-consult',
    phase: 'Research',
    prompt: externalConsultInstructions(
      'deepseek',
      '~/.claude/tools/deepseek-ask.py',
      [
        'From a backend data-modeling point of view, answer, having read the shared context above which',
        'describes a real, already-built-but-unwired backend module `backend/internal/debtaccount/customer`',
        '(struct Customer with IsCreditor bool, IsDebtor bool flags, full CRUD, mounted in real routing but no',
        'frontend ever built for it):',
        '1. Should this existing Customer module be adopted as the foundation for a unified creditor/debtor',
        '   party with combined credit, or should Creditor and Debtor stay as two separate models with a simple',
        '   cross-reference field (for example a shared `linkedpartyguid` or matching normalized TaxId) added to',
        '   each instead? Give a clear recommendation with concrete migration/compatibility trade-offs given the',
        '   two live screens already exist and have real (if currently empty, pre-launch) data shape.',
        '2. Design the exact MongoDB schema change needed for combined credit tracking (a shared credit limit',
        '   record, or a computed/aggregated view across linked Creditor+Debtor records) — give the concrete',
        '   field names and types.',
        '3. Design the exact schema for multiple bank accounts (confirm the existing `BankAccounts *[]BankAccount`',
        '   array on Creditor is the right shape, and specify whether Debtor should get the same array field).',
        '4. Design the exact schema for multi-branch handling per your understanding of the Thai branch-number',
        '   question (an array of branch entries with branch number + address, versus a single branch number',
        '   staying as-is) — but flag clearly that this must be reconciled against real Thai tax research from a',
        '   separate research agent, not decided from assumption alone.',
        '5. Recommend indexing and duplicate-detection strategy if creditor and debtor records can now be linked',
        '   by shared Tax ID.',
      ].join('\n'),
    ),
  },
  {
    label: 'glm-consult',
    phase: 'Research',
    prompt: externalConsultInstructions(
      'glm',
      '~/.claude/tools/glm-ask.py',
      [
        'From a frontend architecture point of view, answer, having read the shared context above which',
        'describes an existing Thai address cascading picker (province -> district -> subdistrict -> auto',
        'zipcode, plus reverse zipcode lookup) that today only works because it is hardcoded to',
        '`config.slug === "branch"` in roughly 10 places inside system-settings-screen.tsx:',
        '1. Recommend the cleanest way to genericize this into a reusable field type (for example a new field',
        '   type "thai-address" in the SystemSettingConfig field list) that any screen, starting with creditor',
        '   and debtor, can opt into, without breaking the existing branch screen. Should the branch screen',
        '   itself be migrated to use the new generic field type in the same pass, or left on its old hardcoded',
        '   path for now to reduce risk? Give a clear recommendation.',
        '2. Recommend the UI pattern for a repeatable multi-bank-account editor (add/remove rows) that reuses',
        '   existing form patterns in this codebase rather than inventing a new one from scratch.',
        '3. Recommend how to expose a second address (actual/operating address, distinct from the billing',
        '   address) on the creditor/debtor form without cluttering it — collapsible section, tab, or something',
        '   else that fits the existing screen patterns.',
        '4. Flag any risk of this redesign growing beyond a reasonable scope, and what should be deferred.',
      ].join('\n'),
    ),
  },
]

const researchResults = await parallel(researchPrompts.map((r) => () => agent(r.prompt, { label: r.label, phase: r.phase })))
const [webThaiLegal, webCompetitor, gptConsult, deepseekConsult, glmConsult] = researchResults

phase('Design Review')

const fableJudge = await agent(
  [
    SHARED_CONTEXT,
    '',
    'You are the chief architect and final design judge for this task (Multi-Model Role Division Rule). You must',
    'read real source files yourself before writing the final spec. At minimum, read:',
    '- backend/internal/debtaccount/customer/models/customer.go and its full http/service/repository stack, and',
    '  check git log / git blame on that folder to understand when and why it was built and why it looks unused',
    '  (grep the whole frontend for any reference to it to confirm it is truly unwired).',
    '- backend/internal/debtaccount/creditor/models/creditor.go and backend/internal/debtaccount/debtor/models/',
    '  debtor.go (the current live models).',
    '- frontend/src/lib/system-setting-screens.ts, the full current creditor and debtor config entries.',
    '- frontend/src/lib/thailand-addresses.ts and frontend/src/app/api/address/thailand/route.ts.',
    '- frontend/src/app/system-settings/system-settings-screen.tsx, every place gated by',
    '  `config.slug === "branch"` (grep for it) to understand exactly what the address cascade logic does today.',
    '',
    'Here are the Research phase results (web research and real calls to GPT, DeepSeek, GLM). Treat them as',
    'input, not ground truth; verify any concrete claim about this repo against the real files yourself. For the',
    'Thai legal branch-number question specifically, treat the web-thai-legal-branch research as the source of',
    'truth over any single model advisor opinion, per the project THAI-DOMAIN rule.',
    '',
    '=== Thai legal/tax web research (branch numbers) ===',
    String(webThaiLegal),
    '',
    '=== Competitor web research ===',
    String(webCompetitor),
    '',
    '=== GPT consult ===',
    String(gptConsult),
    '',
    '=== DeepSeek consult ===',
    String(deepseekConsult),
    '',
    '=== GLM consult ===',
    String(glmConsult),
    '',
    'Write the definitive final design spec for this task. It must cover, precisely and unambiguously:',
    '1. **Business-partner unification decision** (requirement 1): adopt the existing dormant Customer module,',
    '   adapt it, replace it, or link Creditor/Debtor another way — with clear reasoning grounded in what you',
    '   actually read in customer.go and its wiring, plus advisor input. Specify the exact combined-credit',
    '   mechanism (shared field, computed aggregate, or linked-record lookup) and the exact migration/rollout',
    '   plan given creditor/debtor screens are already live (even though pre-launch data is disposable).',
    '2. **Generic Thai address field type**: the exact new field type name and its config shape, exactly how it',
    '   plugs into system-setting-screens.ts and system-settings-screen.tsx without duplicating the existing',
    '   branch-only logic, and whether the branch screen itself gets migrated onto it in this same task or left',
    '   alone (with reasoning either way).',
    '3. **Multi-bank-account**: the exact repeatable-row field type/shape, confirming (via your own source read)',
    '   whether the current single-row UI on creditor is really wired correctly against the backend',
    '   `BankAccounts` array or is subtly broken, and whether debtor should get bank accounts too.',
    '4. **Branch number / multi-branch**: the exact resolution based on the real Thai tax research above — do not',
    '   invent a legal fact; if the research is inconclusive, say so and propose the safest practical default.',
    '5. **Billing vs actual address**: the exact second-address field/section design.',
    '6. **Anything from the competitor research** worth adding, with clear reasoning, and an explicit "do not',
    '   build in this pass" list for anything that is a real scope-creep risk.',
    '7. A concrete test plan matching the uat-crud-mongo convention already used in this project.',
    '',
    'If requirement 1 turns out to require a genuinely large, hard-to-reverse rearchitecture (for example fully',
    'merging Creditor and Debtor into one collection), say so explicitly and flag it as something Claude Code',
    'should confirm with Jead before implementing, rather than silently deciding alone — this is exactly the',
    'kind of R1/R0 call the project dissent rule requires flagging.',
    '',
    'Write this as a complete, concrete, implementable specification. Do not leave placeholders like "TBD".',
  ].join('\n'),
  { model: 'fable', label: 'fable-judge', phase: 'Design Review', effort: 'high' },
)

return { webThaiLegal, webCompetitor, gptConsult, deepseekConsult, glmConsult, fableJudge }
