export const meta = {
  name: 'trade-partner-redesign-v2-implement',
  description: 'Implement the Fable-approved trade-partner redesign: generic thai-address field type + branch fix, bank-accounts field type + creditor bug fix, actual address, taxid link, branch-number law',
  phases: [
    { title: 'Implement Part 1', detail: 'backend additions + thai-address generic field type + branch migration' },
    { title: 'Implement Part 2', detail: 'creditor/debtor migration, bank-accounts type, taxid link, cleanup' },
    { title: 'Verify', detail: 'full CRUD UAT + Fable’s 8-point test plan' },
  ],
}

const SPEC = args.fableSpec

const CONTEXT = [
  'Project: BC Ai Account, a Thai SME accounting and ERP system.',
  'Backend: Go, at D:\\bccode\\backend. Frontend: Next.js, at D:\\bccode\\frontend.',
  '',
  'This is the approved final design spec (chief architect Fable, already verified against real source by',
  'Claude Code independently, including spot-checking the two most load-bearing claims: the branch Thai',
  'address cascade is genuinely dead code today due to a field-rename mismatch, and the creditor bank-account',
  'save path genuinely overwrites/destroys the array on every save). Implement it precisely. Where the spec',
  'gives exact line numbers, treat them as a strong hint, not gospel — re-read the real current file first,',
  'since line numbers shift; if something has moved or looks different from the spec description, trust',
  'the real file and adapt, noting the deviation in your report.',
  '',
  '=== FULL APPROVED DESIGN SPEC ===',
  SPEC,
  '',
  'Project conventions: MongoDB field names in bson/json tags are lowercase, no underscores, no hyphens.',
  'Thai-First rule: write full correct Thai labels; other languages get English/placeholder text only. Full',
  'freedom is granted to change MongoDB schema/backend/frontend code without asking (pre-launch, disposable',
  'database) excluding deploy/push and the scopeofwork/ folder. Windows dev notes: host go build/test do not',
  'work directly, use the Docker builder image: MSYS_NO_PATHCONV=1 docker run --rm -v "D:/bccode/backend:/src"',
  '-w /src backend-mainapi-local-builder:latest sh -c "CGO_ENABLED=1 go build -tags musl ./..." (same pattern',
  'for go test). Frontend typecheck must run "node node_modules/typescript/bin/tsc --noEmit" directly. The',
  'frontend dev server must always be run as "next build" then "next start", never "next dev". Do not commit',
  'or push anything.',
].join('\n')

phase('Implement Part 1')

const part1Report = await agent(
  [
    CONTEXT,
    '',
    'Do ONLY steps 1-2 of the spec\'s section 8 "Implementation order" in this part:',
    '',
    'STEP 1 (backend, all additive, no risk to existing data):',
    '- Add `AddressForActual Address` field to both Creditor (backend/internal/debtaccount/creditor/models/',
    '  creditor.go) and Debtor (backend/internal/debtaccount/debtor/models/debtor.go) structs, per spec section 5.',
    '- Add a package-local `BankAccount` struct and `BankAccounts *[]BankAccount` field to the Debtor model,',
    '  per spec section 3 (match the exact shape already used on Creditor: bankcode/accountnumber/accountname).',
    '- Add `taxid` to the searchable/filterable fields in the creditor and debtor Mongo repositories, per spec',
    '  section 1.1 (read the real repository files first, find the existing code/name search column list, and',
    '  add taxid the same way).',
    '- Verify: Docker builder image build command from context, must exit 0. Run any existing Go tests for the',
    '  touched packages.',
    '',
    'STEP 2 (frontend, the generic Thai address field type + branch migration):',
    '- Read frontend/src/app/system-settings/system-settings-screen.tsx yourself, find the real current',
    '  ThailandAddressFieldEditor / ThailandAddressReadOnlyDetail / ThailandAddressFreeTextEditor components,',
    '  THAILAND_ADDRESS_PRIMARY_FIELD / THAILAND_ADDRESS_SECONDARY_FIELDS constants, isThailandAddressPrimaryField',
    '  / isThailandAddressSecondaryField functions, and every call site of them (the spec lists line numbers as',
    '  of 2026-07-05, re-verify against the current file).',
    '- Parametrize these components/functions with a `prefix: string` (per spec section 2): every hardcoded',
    '  `"contact.province_code"` etc. becomes a prefix-relative lookup against the field key\'s own subkeys',
    '  (`provincecode`, `districtcode`, `subdistrictcode`, `zipcode`, `countrycode`), matching the backend',
    '  `Address` struct bson tags (no underscores). Do NOT touch frontend/src/lib/thailand-addresses.ts or the',
    '  /api/address/thailand route — those are already correct and untouched per the spec.',
    '- Add a new field type `"thai-address"` to the `SystemSettingField["type"]` union in',
    '  frontend/src/lib/system-setting-screens.ts, and a `thaiAddressField(key, th, en)` helper next to',
    '  `textField`, per spec section 2.',
    '- Wire the field-renderer switch in system-settings-screen.tsx: `case "thai-address"` renders the',
    '  parametrized editor with `prefix={field.key}`; do the same for the read-only detail view. Wire',
    '  `formFromRecord`/`buildPayload` to copy the 5 subkeys (countrycode/provincecode/districtcode/',
    '  subdistrictcode/zipcode) between the flat form state and the nested record path under `field.key`.',
    '- **Migrate the branch screen onto the new field type in this same step** (mandatory per spec, since this',
    '  is the bug fix, not just a refactor): replace the 4 flat `contact.*code` textFields in the branch config',
    '  (frontend/src/lib/system-setting-screens.ts) with one `thaiAddressField("contact", ...)`, and delete the',
    '  now-dead THAILAND_ADDRESS_PRIMARY_FIELD/SECONDARY_FIELDS constants, the old gate functions, their call',
    '  sites, and the 6 dead `branch.contact.*` rows in `fieldValueAliases`, per spec section 2 point 4.',
    '- Verify: "node node_modules/typescript/bin/tsc --noEmit" must exit 0. Then do a REAL live check (build',
    '  fresh with "next build" then "next start", never "next dev", killing any stale process on port 3000',
    '  first) proving the /branch screen cascade actually works now: select a real province -> district ->',
    '  subdistrict and confirm the zipcode auto-fills in the UI, AND type a real 5-digit zipcode and confirm',
    '  province/district/subdistrict auto-populate (reverse lookup). This is the proof the dead-code bug is',
    '  actually fixed, not just refactored in place — do not skip this live check.',
    '',
    'Report back precisely: exact files changed, exact functions/constants added or deleted, the exact prefix',
    'mechanism you implemented, and the verbatim output of every verification command including the live',
    'branch-screen cascade check (describe exactly what you selected/typed and what you observed).',
  ].join('\n'),
  { label: 'implement-part1-backend-and-thai-address', phase: 'Implement Part 1' },
)

phase('Implement Part 2')

const part2Report = await agent(
  [
    CONTEXT,
    '',
    '=== Real report from Part 1 (backend additions + generic thai-address type + branch migration) ===',
    String(part1Report),
    '',
    'Part 1 is done and reportedly verified (branch screen cascade live-checked as working). Use the REAL',
    'field/function names Part 1 actually implemented, not just the spec, if they ever differ.',
    '',
    'Do steps 3-4-5-6 of the spec\'s section 8 "Implementation order" in this part:',
    '',
    'STEP 3 (billing vs actual address on creditor/debtor):',
    '- Replace the 4 flat `addressforbilling.*code` textFields on the live creditor and debtor configs',
    '  (frontend/src/lib/system-setting-screens.ts) with the new `thaiAddressField("addressforbilling", ...)`',
    '  from Part 1. Keep the existing `stringListField("addressforbilling.address", ...)` and phone fields.',
    '- Add the actual-address block to both configs: `stringListField("addressforactual.address", ...)` +',
    '  `thaiAddressField("addressforactual", ...)`, per spec section 5.',
    '- Implement the `copyFromPrefix` convenience described in spec section 5: a small button in the',
    '  actual-address section that copies the 5 cascade subkeys plus the `.address` string list from',
    '  `addressforbilling` into `addressforactual` in the live form state (one state update, not a save).',
    '',
    'STEP 4 (multi-bank-account type + fix the creditor destroy bug + add debtor bank accounts):',
    '- Add a new field type `"bank-accounts"` (repeatable rows: bankcode/accountnumber/accountname, add-row',
    '  button, per-row delete, drop fully-empty rows on save) per spec section 3. Model the renderer on the',
    '  existing array-of-objects editor pattern already in this file (the spec references an existing similar',
    '  repeatable-row pattern — find and reuse it, do not invent a new primitive from scratch).',
    '- On the creditor config: replace the 3 flat bank fields with `bankAccountsField("bankaccounts", ...)`.',
    '  **Delete** the entire old slug-hack wiring this replaces: the creditor-specific fold in',
    '  `formFromRecord`, the `creditorBankAccountFieldKeys`/`BankAccountRecord` special-case lookup in',
    '  `recordValueForField`, and the creditor-specific unconditional-overwrite block in `buildPayload` (the',
    '  exact bug: `payload.bankaccounts = [...]` unconditionally replacing the whole array every save,',
    '  destroying any row beyond the first). Confirm after your change that a record with 2+ bank accounts',
    '  survives an unrelated-field edit and save (this is the literal regression test for the bug you are',
    '  fixing — verify it live, not just by reading your own diff).',
    '- On the debtor config: add the same `bankAccountsField("bankaccounts", ...)` wired to the new backend',
    '  `BankAccounts` field Part 1 added to the Debtor model.',
    '',
    'STEP 5 (taxid link/duplicate badge + branch-number law):',
    '- Implement the taxid cross-lookup behavior in spec section 1.2: on blur of the `taxid` field (or on',
    '  record load) when it is a real 13-digit value, query both collections (`GET .../list?q=<taxid>`) and',
    '  exact-match on the returned `taxid` field. Show the informational cross-link badge + combined-credit',
    '  line when a match exists in the OTHER collection; show the non-blocking duplicate warning when a match',
    '  exists in the SAME collection with the same `branchnumber`. Do not block saving in either case.',
    '- Update the `branchnumber` helper text on both configs to the exact law-grounded wording in spec section',
    '  4. Apply `normalizeThaiTaxBranchCode` (the existing function already used by the branch save path — find',
    '  and reuse it, do not reimplement) to `branchnumber` in the creditor/debtor `buildPayload`.',
    '',
    'STEP 6 (cleanup pass):',
    '- Do the dead-code deletions listed in spec section 2 point 4 if Part 1 did not already do them.',
    '- Re-run `tsc --noEmit` (must exit 0) and a fresh `next build` (must succeed) after all of the above.',
    '',
    'Report back precisely: exact files changed, exact bug-fix confirmation (the 2+ bank account survives an',
    'edit test, described with what you actually did and saw), and the verbatim output of every verification',
    'command.',
  ].join('\n'),
  { label: 'implement-part2-creditor-debtor-and-bugfix', phase: 'Implement Part 2' },
)

phase('Verify')

const verifyReport = await agent(
  [
    CONTEXT,
    '',
    '=== Part 1 report ===',
    String(part1Report),
    '',
    '=== Part 2 report ===',
    String(part2Report),
    '',
    'Independently verify everything, following the uat-crud-mongo convention already used in this project:',
    'never trust a self-reported "done" without real evidence you generate yourself. Run through the exact',
    '8-point test plan in spec section 7, using real browser actions, real API calls, and real MongoDB queries',
    '(docker exec mongodb mongosh --quiet appdb --eval ...) for every mutation. Pay special attention to test 1',
    '(the multi-bank destroy-bug regression test) and test 4 (branch cascade resurrection) since those are the',
    'two confirmed pre-existing bugs this task exists to fix — they must now demonstrably work, not just compile.',
    '',
    'If you find any real bug, root-cause it against the real code, fix it with the smallest correct change,',
    'and re-verify with fresh, unique test data.',
    '',
    'Add or extend Playwright regression tests (frontend/e2e/trade-partners-crud.spec.ts is the natural home',
    'for creditor/debtor cases; consider whether the branch cascade fix needs its own small spec too) covering',
    'at least the multi-bank round-trip, the address cascade forward+reverse on both branch and creditor, and',
    'the taxid link/duplicate-warning behavior. Then run the FULL existing e2e suite (not just your new/changed',
    'file) and confirm no regressions.',
    '',
    'Run the backend build and frontend typecheck one more time as a final sanity check, report their real',
    'exit output.',
    '',
    'Append one worklog entry to D:\\bccode\\.agents\\worklog.md (read the file first to find the real current',
    'end, then append after it) summarizing this whole task: the two confirmed pre-existing bugs found and',
    'fixed, the new generic field types, the taxid-link design, the branch-number legal resolution with its',
    'source citations, files changed, and test results. Also score every GPT/GLM/DeepSeek consult from the',
    'Research phase into `.agents/advisor-scorecard.md` if that has not already been done (check the file',
    'first; the research phase indicated some scores may already be logged).',
    '',
    'Do not commit or push anything.',
    '',
    'End your report with the required Thai summary format: what changed, pros, cons/risks, and concrete next',
    'steps.',
  ].join('\n'),
  { label: 'full-verify', phase: 'Verify', effort: 'high' },
)

return { part1Report, part2Report, verifyReport }
