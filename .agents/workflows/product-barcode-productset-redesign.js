export const meta = {
  name: 'product-barcode-productset-redesign',
  description: 'Test, research competitors, and design improvements for the Product / Barcode / Product Set screens (high blast-radius core master data)',
  phases: [
    { title: 'Research', detail: 'UAT test current screens + competitor research + GPT/GLM/DeepSeek consult, all in parallel' },
    { title: 'Design Review', detail: 'Fable synthesizes; splits safe-now improvements from anything needing Jead sign-off' },
    { title: 'Implement', detail: 'build only the Fable-cleared safe items; flag, do not silently build, anything R0/R1' },
    { title: 'Verify', detail: 'UAT the changes + add baseline Playwright regression coverage (currently zero)' },
  ],
}

const CONTEXT = [
  'Project: BC Ai Account, a Thai SME accounting and ERP system.',
  'Backend: Go, at D:\\bccode\\backend. Frontend: Next.js, at D:\\bccode\\frontend.',
  '',
  'Jead (the project owner) asked to test, improve, and redesign 3 screens in the menu group',
  '"สินค้าและบาร์โค้ด" (Products and Barcode): สินค้า (Product), บาร์โค้ด (Barcode), สินค้าชุด (Product Set),',
  'and to research competitor software for ideas. The 4th item in that menu group, product-unit, is NOT in',
  'scope, leave it untouched.',
  '',
  'Verified scope facts from direct source reading this session, do not re-derive them, but verify anything',
  'you rely on against the real current file before changing it:',
  '',
  '1. All 3 are real custom React screens, not generic config-driven screens:',
  '   - Product: frontend/src/app/menu/product-screen.tsx, about 3300 lines, a two-panel list+detail screen',
  '     with roughly 12 tabs on the detail side (basic info, classification, units, BOM, stock, media,',
  '     logistics, restaurant, time-for-sales, business, misc). This is the largest, most complex screen in',
  '     the whole system.',
  '   - Barcode: frontend/src/app/menu/product-barcode-screen.tsx, about 1618 lines, list+detail form+barcode',
  '     picker modal.',
  '   - Product Set: frontend/src/app/menu/product-set-screen.tsx, about 1994 lines, list+detail editor. It has',
  '     NO dedicated backend model — it calls the same `/product` endpoint as the Product screen, filtered',
  '     client-side by itemtype:2 / materialtype:3 on the Product model. It is a UI-layer convention, not a',
  '     real separate entity. Do not confuse this with the Product BOM module (a genuinely separate menu item,',
  '     "สูตรผลิต", route /productbom, its own collection productbarcodeboms) — Product Set and BOM are',
  '     different concepts even though both involve composing products from other products.',
  '',
  '2. Real backend modules involved:',
  '   - Product: backend/internal/product/product/models/product.go + product_http.go. Collection "products".',
  '     Routes: GET/POST /product, GET/PUT/DELETE /product/:guid.',
  '   - ProductBarcode: backend/internal/product/productbarcode/models/product_barcode.go +',
  '     productbarcode_http.go (+ a separate productbarcode_price_history_http.go). Collection',
  '     "productbarcodes". About 20 routes under /product/barcode/* (search, list, create, update,',
  '     delete-bulk, xsort, branch, business-type, units, groups, export, bom/:barcode, price-history,',
  '     import, import-refbarcode, bulk).',
  '   - Notable existing fields on Product/ProductBarcode worth knowing before proposing changes: multi-tier',
  '     Prices[] (KeyNumber+Price), unit conversion via DivideValue/StandValue/Condition, deep classification',
  '     hierarchy (Group/Groupsubone/Groupsubtwo/Category/Class/Brand/Design/Model/Pattern/Grade, each with',
  '     Guid+Code+Names), Images[]/ImageURI, RefBarcodes[] for variants/sub-SKUs, Dimensions[]/Options[] for',
  '     color/size, MarketplaceProducts[] (Shopee/Lazada/TikTok sync fields, possibly under-used on this',
  '     on-prem deployment, verify real usage before touching), and restaurant-specific fields',
  '     (IsALaCarte/Restaurant{}/OrderTypes[]/TimeForSales[]) embedded directly in the same struct even for',
  '     non-restaurant tenants.',
  '',
  '3. **Blast radius is HIGH.** Roughly 25 other backend modules reference Product/ProductBarcode directly:',
  '   transaction modules (saleinvoice, purchase, stocktransfer, stockadjustment, stockbalance,',
  '   stockpickupproduct, stockreturnproduct), the 10 smlaiproduct/* sub-modules (brand/category/class/design/',
  '   grade/group/groupsubone/groupsubtwo/model/patternproduct), POS (pos/shift, coupon), goapi/handlers',
  '   (product_search, kafka/inventory, barcode-product build), datatransfer, images, slipimage, productimport.',
  '   This count came from a grep pass, not a full manual read of every file — treat it as "real risk exists,',
  '   verify the specific ones you are about to touch," not as an exhaustive, already-confirmed list.',
  '   **Any change to the Product or ProductBarcode Go struct (renaming/removing a field, changing a type) is',
  '   R1 at minimum given this blast radius — it must be flagged to Jead for explicit confirmation before being',
  '   implemented, never done silently.** Purely additive backend fields, and any frontend-only UI/UX',
  '   improvement that does not change the wire contract, are lower risk and can proceed if clearly beneficial',
  '   and well-scoped.',
  '',
  '4. **Zero existing Playwright e2e coverage** for /product, /productbarcode, or /productset today (existing',
  '   specs cover BOM, product-category-list, and product-warehouse, none of these 3 screens). Adding baseline',
  '   CRUD regression coverage for these 3 screens is valuable on its own, independent of any redesign.',
  '',
  '5. Known rough edges already spotted (not exhaustive, use them as a starting point for testing, not a final',
  '   list): Product Set has no backend-level identity or validation beyond client-side filtering; restaurant',
  '   and marketplace-sync fields are mixed into the general Product/ProductBarcode struct for every tenant',
  '   regardless of whether they use those features.',
  '',
  'Project conventions: MongoDB field names in bson/json tags are lowercase, no underscores, no hyphens.',
  'Thai-First rule: write full correct Thai labels; other languages get English/placeholder text only. Full',
  'freedom is granted to change MongoDB schema/backend/frontend code without asking (pre-launch, disposable',
  'database) excluding deploy/push and the scopeofwork/ folder — BUT this project also has an explicit dissent',
  'rule: any change with wide blast radius or that is hard to reverse must still be flagged to Jead before',
  'being implemented, freedom to change data does not override the obligation to flag risk. Windows dev notes:',
  'host go build/test do not work directly, use the Docker builder image: MSYS_NO_PATHCONV=1 docker run --rm',
  '-v "D:/bccode/backend:/src" -w /src backend-mainapi-local-builder:latest sh -c "CGO_ENABLED=1 go build',
  '-tags musl ./..." (same pattern for go test; if docker compose build cannot find BuildKit, use',
  '"/c/Program Files/Docker/Docker/resources/cli-plugins/docker-buildx.exe" build --load directly instead,',
  'this was hit and worked around in a prior task this session). Frontend typecheck must run',
  '"node node_modules/typescript/bin/tsc --noEmit" directly. The frontend dev server must always be run as',
  '"next build" then "next start", never "next dev". Do NOT leave any Go build cache directories in the repo',
  'root (a prior task left 3.3GB of .gocache-build/.gocache-mod there by mistake; use Docker named volumes or',
  'a location outside the repo). Do not commit or push anything.',
  '',
  'This project follows the Multi-Model Orchestration Rule (see D:\\bccode\\AGENTS.md): Opus 4.8 is the',
  'orchestrator/lead that splits work, dispatches to the pool, evaluates results, and applies+verifies+owns',
  'the outcome. Pool: external chat advisors (opinion-only, never edit files) GLM 5.2, DeepSeek V4 Pro,',
  'ChatGPT 5.6, Kimi K3 Max via ~/.claude/tools/*-ask.py; Claude subagents Fable (hard/architecture/final',
  'review) and Sonnet (general implementation/verification) via the Agent tool. Verify every advisor claim',
  'against real source before use; advisors never own an R0/R1 decision.',
].join('\n')

function externalConsultInstructions(toolName, wrapperPath, question) {
  return [
    'You are consulting a real external AI model through a command line wrapper. This is not a role-play, you',
    'must genuinely invoke the external command and return what it actually outputs.',
    '',
    'Steps, in order:',
    '1. Use the Write tool to save the following context and question to a temporary file at a path such as',
    '   D:\\bccode\\.agents\\tmp\\' + toolName + '-consult-input.txt.',
    '2. The file content must include the shared project context below, followed by the specific question.',
    '3. Run this exact command using the Bash tool (adjust only the file path if you saved it elsewhere):',
    '   cat D:\\bccode\\.agents\\tmp\\' + toolName + '-consult-input.txt | python ' + wrapperPath + ' --system "You are acting as an external design advisor for a Thai SME ERP project. Read the attached context and question carefully, then give a direct, specific, real answer."',
    '4. Return the raw stdout of that command back as your final answer, verbatim, with no summarizing, no',
    '   added opinion of your own, and no reformatting. If the command fails or times out, report the exact',
    '   error text you saw instead of inventing a fallback answer. Retry up to 3 times with increasing timeouts',
    '   before giving up and reporting the real failure.',
    '5. Delete the temporary file when you are done.',
    '',
    '=== Context to include in the file ===',
    CONTEXT,
    '',
    '=== Question to include in the file ===',
    question,
  ].join('\n')
}

phase('Research')

const researchPrompts = [
  {
    label: 'uat-product',
    phase: 'Research',
    prompt: [
      CONTEXT,
      '',
      'Task: run a full CRUD UAT pass on the CURRENT, EXISTING /product screen (create, read/list, update,',
      'delete), following the uat-crud-mongo convention already used in this project: drive the real UI, verify',
      'UI + API + MongoDB agree after every mutation. Do not redesign anything yet, this is pure testing of what',
      'exists today. If you find a real, small, clearly-scoped bug (something objectively broken, not a design',
      'opinion), fix it with the smallest correct change and note it. If you find something that looks broken',
      'but touches the wide-blast-radius Product/ProductBarcode schema or many other modules, do NOT fix it,',
      'just document it precisely for the Design Review phase. Pay attention to: does each of the ~12 tabs',
      'actually save and reload correctly, do multi-tier prices round-trip, does the classification hierarchy',
      '(group/category/brand/etc) save and redisplay correctly, do images upload and persist. Report concrete',
      'findings with evidence (what you did, what you saw, any bug fixed and how).',
    ].join('\n'),
  },
  {
    label: 'uat-barcode',
    phase: 'Research',
    prompt: [
      CONTEXT,
      '',
      'Task: run a full CRUD UAT pass on the CURRENT, EXISTING /productbarcode screen (create, read/list,',
      'update, delete), following the uat-crud-mongo convention. Do not redesign anything yet. Fix small,',
      'clearly-scoped real bugs directly; document (do not fix) anything with wide blast radius. Pay attention',
      'to: barcode uniqueness/validation, unit conversion fields, price history, the barcode picker modal',
      'workflow (used by other screens like Product Set). Report concrete findings with evidence.',
    ].join('\n'),
  },
  {
    label: 'uat-productset',
    phase: 'Research',
    prompt: [
      CONTEXT,
      '',
      'Task: run a full CRUD UAT pass on the CURRENT, EXISTING /productset screen (create, read/list, update,',
      'delete), following the uat-crud-mongo convention. Remember this screen has no dedicated backend model —',
      'it is a client-side filtered view of Product (itemtype:2/materialtype:3); verify this real mechanism',
      'yourself and test whether it behaves correctly and safely (for example: can a "regular" product',
      'accidentally show up as/be edited as a product set due to a filter or validation gap? does deleting a',
      'product set actually only affect the intended itemtype:2 record?). Do not redesign anything yet. Fix',
      'small, clearly-scoped real bugs directly; document (do not fix) anything with wide blast radius. Report',
      'concrete findings with evidence.',
    ].join('\n'),
  },
  {
    label: 'web-thai-competitor',
    phase: 'Research',
    prompt: [
      CONTEXT,
      '',
      'Task: perform real web research (WebSearch and WebFetch, cite real URLs you actually visited, do not',
      'invent sources) on how real Thai POS/inventory/accounting software handles product master data, barcode',
      'management, and product bundles/sets: PEAK, FlowAccount, LoyverseX/Loyverse, Zort, or any other real Thai',
      'inventory/POS software you can find documentation for. Cover: how they structure product classification',
      '(categories/brands/units), how they handle multiple barcodes per product (variants, packs, cases), and',
      'how they model bundle/set/kit products (a product composed of other products) — is it a real separate',
      'entity, a flag on the product, or something else? Report concrete field lists and conventions with',
      'citations, organized by topic.',
    ].join('\n'),
  },
  {
    label: 'web-intl-competitor',
    phase: 'Research',
    prompt: [
      CONTEXT,
      '',
      'Task: perform real web research (WebSearch and WebFetch, cite real URLs you actually visited, do not',
      'invent sources) on how established international retail/inventory platforms handle product master data,',
      'barcode/SKU management, and bundle/kit products: Shopify (product + variants + SKU/barcode per variant),',
      'Square (item library, item variations), Lightspeed Retail, and Odoo Inventory/Sales (Bill of Materials',
      '"kit" products vs regular products). For each, note the exact field shapes for variants/SKUs/barcodes',
      'and how a bundle/kit is represented (separate model vs a flag/type on the product model). Synthesize a',
      'clear comparison and a recommendation on whether Product Set deserves a real backend identity of its own',
      'versus staying a UI-layer filter, given this repo already has a working BOM module for actual recipe/kit',
      'composition. Report concrete findings with citations.',
    ].join('\n'),
  },
  {
    label: 'gpt-consult',
    phase: 'Research',
    prompt: externalConsultInstructions(
      'gpt',
      '~/.claude/tools/gpt-ask.py',
      [
        'From a Thai SME user experience and business-owner point of view, review the Product screen (about',
        '3300 lines, roughly 12 tabs: basic info, classification, units, BOM, stock, media, logistics,',
        'restaurant, time-for-sales, business, misc) and the Barcode and Product Set screens described in the',
        'shared context:',
        '1. Is a 12-tab single-screen form a reasonable UX for a Thai SME user managing products day-to-day, or',
        '   is it overwhelming? What would you split out, hide behind an "advanced" toggle, or reorganize?',
        '2. Restaurant-only fields (IsALaCarte, Restaurant{}, OrderTypes[], TimeForSales[]) and marketplace-sync',
        '   fields (Shopee/Lazada/TikTok) are shown to every tenant regardless of whether they use those',
        '   features. From a UX standpoint, should these be conditionally hidden/shown based on tenant',
        '   business type, and is that worth doing in a first pass or is it scope creep?',
        '3. For Product Set (a product composed of other products, currently just a filtered view of Product,',
        '   not the same as the existing BOM/recipe module), what would a genuinely useful, SME-friendly Product',
        '   Set screen look like — what fields/behavior does a Thai SME actually need for bundles (for example a',
        '   "ชุดของขวัญ" gift set, or a "คอมโบ" combo meal that is NOT a manufactured recipe)?',
        '4. Flag anything in this whole area that should be deferred as out of scope for a first improvement',
        '   pass, given the wide blast radius of touching core product data.',
      ].join('\n'),
    ),
  },
  {
    label: 'deepseek-consult',
    phase: 'Research',
    prompt: externalConsultInstructions(
      'deepseek',
      '~/.claude/tools/deepseek-ask.py',
      [
        'From a backend data-modeling point of view, review the Product/ProductBarcode models and the Product',
        'Set situation described in the shared context (Product Set has no dedicated backend model, it is a',
        'client-side itemtype:2/materialtype:3 filter on Product; roughly 25 other backend modules reference',
        'Product/ProductBarcode directly):',
        '1. Is representing "Product Set" as a filtered view of the same Product collection (rather than a real',
        '   separate entity) a sound design given it already coexists with a genuinely separate BOM module for',
        '   recipes/manufacturing? What are the concrete risks of the current approach (for example: can a',
        '   generic query across all products accidentally include or exclude set-type products incorrectly;',
        '   is there any real validation preventing a normal product from having inconsistent set-only field',
        '   values)?',
        '2. Given the roughly 25-module blast radius, what is the safest way to make any improvement to the',
        '   Product/ProductBarcode schema (for example, splitting the restaurant-only or marketplace-sync',
        '   fields into a separate embedded sub-document) without breaking existing consumers? Is a purely',
        '   additive, backward-compatible change possible here, or does any meaningful improvement require a',
        '   migration step given this is pre-launch disposable data?',
        '3. Recommend indexing/query-performance considerations specific to Product/ProductBarcode given their',
        '   central role (search, filtering by classification, barcode lookup) — is there anything commonly',
        '   missing in a product master schema this large that would matter at real transaction volume?',
        '4. Flag any schema change you would explicitly NOT recommend for a first pass given the blast radius,',
        '   even if theoretically cleaner.',
      ].join('\n'),
    ),
  },
  {
    label: 'glm-consult',
    phase: 'Research',
    prompt: externalConsultInstructions(
      'glm',
      '~/.claude/tools/glm-ask.py',
      [
        'From a frontend architecture point of view, review the Product screen described in the shared context',
        '(about 3300 lines, a single component with roughly 12 tabs covering basic info, classification, units,',
        'BOM, stock, media, logistics, restaurant, time-for-sales, business, misc):',
        '1. Is a single ~3300-line component with 12 tabs a maintainability risk? If you were to recommend',
        '   splitting it, what is the smallest, safest refactor (e.g. extracting each tab into its own',
        '   component with shared state passed via props/context) versus a larger rewrite, and which would you',
        '   recommend for a first pass given this screen is live and heavily used?',
        '2. Should restaurant-only and marketplace-sync tabs be conditionally rendered based on tenant/company',
        '   settings rather than always shown? What is the smallest correct way to do that in this codebase,',
        '   given the existing patterns you can infer from the shared context (business-code fields, tenant',
        '   scoping conventions already used elsewhere in this project this session)?',
        '3. Zero Playwright e2e coverage exists today for /product, /productbarcode, /productset. What is the',
        '   highest-value first set of test cases to add given this screen is this large and this central —',
        '   what would you test first if you could only add a handful of tests?',
        '4. Flag any risk of this whole task growing beyond a reasonable first-pass scope, and what should be',
        '   explicitly deferred given the size and centrality of these screens.',
      ].join('\n'),
    ),
  },
]

const researchResults = await parallel(researchPrompts.map((r) => () => agent(r.prompt, { label: r.label, phase: r.phase })))
const [uatProduct, uatBarcode, uatProductSet, webThaiCompetitor, webIntlCompetitor, gptConsult, deepseekConsult, glmConsult] = researchResults

phase('Design Review')

const fableJudge = await agent(
  [
    CONTEXT,
    '',
    'You are the chief architect and final design judge for this task (Multi-Model Orchestration Rule; Opus routes architecture/review to Fable). You',
    'must read real source files yourself before writing the final spec. At minimum, read:',
    '- frontend/src/app/menu/product-screen.tsx, frontend/src/app/menu/product-barcode-screen.tsx,',
    '  frontend/src/app/menu/product-set-screen.tsx (at least skim the overall structure of each, you do not',
    '  need to read all ~3300 lines of product-screen.tsx line by line, but understand its real tab structure).',
    '- backend/internal/product/product/models/product.go,',
    '  backend/internal/product/productbarcode/models/product_barcode.go.',
    '- A sample of 2-3 of the highest-blast-radius consumers mentioned in the shared context (for example',
    '  saleinvoice, stockbalance, one of the smlaiproduct/* sub-modules) to get a real sense of how tightly',
    '  coupled they are to the exact current field shapes, before recommending any schema change.',
    '',
    'Here are the Research phase results: 3 real UAT test passes on the current screens, 2 real web research',
    'passes, and 3 real calls to GPT, DeepSeek, and GLM. Treat them as input, not ground truth; verify any',
    'concrete claim about this repo against the real files yourself.',
    '',
    '=== UAT: current /product screen ===',
    String(uatProduct),
    '',
    '=== UAT: current /productbarcode screen ===',
    String(uatBarcode),
    '',
    '=== UAT: current /productset screen ===',
    String(uatProductSet),
    '',
    '=== Thai competitor web research ===',
    String(webThaiCompetitor),
    '',
    '=== International competitor web research ===',
    String(webIntlCompetitor),
    '',
    '=== GPT consult ===',
    String(gptConsult),
    '',
    '=== DeepSeek consult ===',
    String(deepseekConsult),
    '',
    '=== GLM consult ===',
    String(glmConsult),
    '',
    'Write the definitive final design spec for this task. It must clearly separate two categories, this is',
    'the single most important structural requirement of this spec:',
    '',
    '**CATEGORY A — safe to implement now** (small, additive, reversible, does not change the wire contract of',
    'Product/ProductBarcode in a way that could break the ~25 other consumers, and does not require Jead to',
    'make a business-scope decision): for example small UI/UX improvements, adding a missing validation, fixing',
    'a confirmed bug from the UAT passes, adding Playwright test coverage, splitting a giant component into',
    'sub-components with no behavior change, conditionally hiding a tab based on an existing tenant setting if',
    'one already exists. For every item here, give exact file-level instructions precise enough for an',
    'implementer to build it without further research.',
    '',
    '**CATEGORY B — needs Jead confirmation before any code is written**: anything that changes the',
    'Product/ProductBarcode schema in a way existing consumers might depend on, anything that gives Product Set',
    'a real separate backend identity (a genuine architecture change), removing/relocating restaurant or',
    'marketplace fields, or any other R0/R1-class call per this project dissent rule. For every item here,',
    'explain the choice/tradeoff clearly enough that Jead can make a real yes/no decision, but do NOT write',
    'implementation instructions for these — flag them as blocked pending a decision, the same way a prior task',
    'this session correctly flagged the Project-vs-Job backend conflict and the branch address-cascade routing',
    'decision instead of guessing.',
    '',
    'Also include: an explicit "do not build in this pass" list for real scope-creep risks the research',
    'surfaced, and a concrete test plan (matching the uat-crud-mongo convention) for whatever Category A items',
    'get implemented, focused on closing the current zero-coverage gap for these 3 screens with real,',
    'high-value CRUD test cases.',
    '',
    'Write this as a complete, concrete, implementable specification for Category A. Do not leave placeholders',
    'like "TBD".',
  ].join('\n'),
  { model: 'fable', label: 'fable-judge', phase: 'Design Review', effort: 'high' },
)

phase('Implement')

const implementReport = await agent(
  [
    CONTEXT,
    '',
    'Here is the final design spec from the chief architect (Fable). Implement ONLY the items in',
    '"CATEGORY A — safe to implement now". Do NOT implement, and do NOT guess at, anything listed in',
    '"CATEGORY B — needs Jead confirmation" — those are blocked pending a real decision from the project',
    'owner, full stop, no matter how good an idea one of them might seem. If you are ever unsure whether',
    'something you are about to do falls in Category A or B, treat it as Category B and stop.',
    '',
    '=== Final design spec ===',
    String(fableJudge),
    '',
    'Implement every Category A item precisely as specified. Verify each change: run the Docker builder image',
    'backend build if you touch Go code (must exit 0), run "node node_modules/typescript/bin/tsc --noEmit" if',
    'you touch frontend code (must exit 0), and do a real live check in the browser for any UI change you make',
    '(build fresh with "next build" then "next start", never "next dev", killing any stale process on port',
    '3000 first).',
    '',
    'Report back precisely: exact files changed, exactly which Category A items you implemented (and confirm',
    'you did not touch any Category B item), and the verbatim output of every verification command including',
    'your live-check evidence.',
  ].join('\n'),
  { label: 'implement-category-a', phase: 'Implement' },
)

phase('Verify')

const verifyReport = await agent(
  [
    CONTEXT,
    '',
    '=== Implementation report to independently verify, do not just trust it ===',
    String(implementReport),
    '',
    'Your job is independent verification, following the uat-crud-mongo convention already used in this',
    'project: never trust a self-reported "done" without real evidence you generate yourself.',
    '',
    '1. Read the real git diff yourself and confirm it matches what was reported, and confirm nothing from',
    '   "CATEGORY B" in the design spec below was touched.',
    '2. Re-run a full CRUD UAT pass on all 3 screens (/product, /productbarcode, /productset) verifying UI + API',
    '   + MongoDB agreement for every mutation, confirming the Category A improvements actually work and',
    '   nothing regressed versus the original UAT passes from the Research phase.',
    '3. Add real Playwright regression tests for these 3 screens per the design spec\'s test plan — this repo',
    '   currently has ZERO e2e coverage for them, so this closes a real gap, not just extends existing tests.',
    '   Follow the DOM-interaction conventions already established in this repo\'s existing specs (for example',
    '   frontend/e2e/trade-partners-crud.spec.ts or frontend/e2e/product-bom-crud.spec.ts) rather than',
    '   inventing new conventions.',
    '4. Run the FULL existing e2e suite (not just your new files) and confirm no regressions to any other',
    '   screen, given the wide blast radius of this area.',
    '5. Run the backend build and frontend typecheck one more time as a final sanity check, report their real',
    '   exit output. Confirm no stray Go build cache directories were left in the repo root.',
    '6. Read D:\\bccode\\.agents\\worklog.md yourself first to find its real current last line, then append a new',
    '   entry after it, and re-read the file after your edit to confirm the entry actually landed before',
    '   finishing (this exact "claimed but did not land" gap has happened multiple times this session with',
    '   other agents, do not repeat it).',
    '7. Confirm every GPT/GLM/DeepSeek consult from the Research phase got scored into',
    '   .agents/advisor-scorecard.md; if any is missing, add it now based on how much of its input Fable',
    '   actually adopted.',
    '',
    '=== Design spec, for reference on what should and should not have been touched ===',
    String(fableJudge),
    '',
    'Do not commit or push anything.',
    '',
    'End your report with the required Thai summary format: what changed, pros, cons/risks, and concrete next',
    'steps — including a clear, distinct list of every Category B item still pending Jead\'s decision.',
  ].join('\n'),
  { label: 'full-verify', phase: 'Verify', effort: 'high' },
)

return { uatProduct, uatBarcode, uatProductSet, webThaiCompetitor, webIntlCompetitor, gptConsult, deepseekConsult, glmConsult, fableJudge, implementReport, verifyReport }
