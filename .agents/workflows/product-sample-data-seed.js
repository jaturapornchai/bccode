export const meta = {
  name: 'product-sample-data-seed',
  description: 'Build missing groupsubone/groupsubtwo screens, seed realistic connected classification master data, then seed Product/Barcode/ProductSet sample data through the real UI, verify UI+API+Mongo+PostgreSQL',
  phases: [
    { title: 'Build Missing Screens', detail: 'groupsubone + groupsubtwo frontend config (backend already exists)' },
    { title: 'Seed Master Data', detail: 'brand/class/design/model/pattern/grade/group/category/groupsubone/groupsubtwo — one coherent theme' },
    { title: 'Seed Products', detail: 'realistic interrelated Products + Barcodes + Product Sets referencing the real master data, via real UI' },
    { title: 'Verify', detail: 'UI + API + MongoDB + PostgreSQL 3/4-way agreement, relation-field completeness check, Playwright regression' },
  ],
}

const CONTEXT = [
  'Project: BC Ai Account, a Thai SME accounting and ERP system.',
  'Backend: Go, at D:\\bccode\\backend. Frontend: Next.js, at D:\\bccode\\frontend.',
  '',
  'Jead (project owner) asked to create sample data for the "สินค้าและบาร์โค้ด" (Products and Barcode) menu',
  'group — สินค้า (Product, /product), บาร์โค้ด (Barcode, /productbarcode), สินค้าชุด (Product Set,',
  '/productset) — with the data genuinely RELATED to each other (not random disconnected rows), created',
  '"เหมือน user ป้อนเองเลย" (as if a real user typed it in themselves — meaning drive the real UI/real API in',
  'the exact shape a real user submission takes, not raw MongoDB inserts), AND to check every relation field',
  '(a field referencing another master-data entity) for whether its reference is actually resolvable/complete,',
  'building any missing supporting screen if needed.',
  '',
  'Verified facts from direct source investigation this session, do not re-derive them, but verify anything',
  'you rely on against the real current system state yourself (data may have changed):',
  '',
  '**Relation fields on Product and ProductBarcode** (same set on both,',
  'backend/internal/product/product/models/product.go and',
  'backend/internal/product/productbarcode/models/product_barcode.go):',
  '',
  '| Relation | Field(s) | Master collection | Frontend route | Status |',
  '|---|---|---|---|---|',
  '| Unit | UnitGuid (unitguid) | `units` | /productunit (basePath /unit) | screen exists, 110 real docs |',
  '| Group | GroupCode/GroupNames (Code only, NO Guid field on Product) | `productgroups` | /productgroup (basePath',
  '  /product/group) | screen EXISTS and is a REAL TREE, not flat — verified backend model',
  '  backend/internal/product/productgroup/models/productgroup.go has ParentGUID, ParentGUIDAll, ChildCount,',
  '  XSorts fields, and the real screen config subtitle literally says "จัดกลุ่มสินค้าแบบลำดับชั้น กลุ่มหลักและ',
  '  กลุ่มย่อย ลากวางจัดลำดับได้" (organize hierarchically, main groups and subgroups, drag to reorder). Jead',
  '  confirmed this directly. Seed this as a REAL small tree (for example 1-2 root/parent groups, each with',
  '  2-3 child groups set via parentguid), never as flat unrelated siblings. 0 real docs today.',
  '| GroupSubOne | GroupsuboneGuid/Code/Names | `groupsuboneproductmaster` (via /aicloud/groupsubone) | NONE — proxy wiring exists in',
  '  frontend/src/app/api/product-barcode/master/[master]/route.ts but no system-setting-screens.ts entry | MISSING SCREEN, 0 docs |',
  '| GroupSubTwo | GroupsubtwoGuid/Code/Names | `groupsubtwoproductmaster` (via /aicloud/groupsubtwo) | same gap as GroupSubOne | MISSING SCREEN, 0 docs |',
  '',
  'Important, separately-discovered fact: GroupSubOne and GroupSubTwo (the smlaiproduct family) are actually',
  'the bottom two levels of their OWN separate 3-level hierarchy, unrelated to the productgroups tree above:',
  'backend/internal/smlaiproduct/groupsuboneproduct/models/groupsuboneproduct.go has an optional',
  '`GroupMainGuid` field referencing a third, ALSO-missing master `smlaiproduct/groupproduct` (collection',
  '`groupproductmaster`, no frontend screen or proxy mapping exists for it at all — verified, zero matches',
  'anywhere in frontend/src). backend/internal/smlaiproduct/groupsubtwoproduct/models/groupsubtwoproduct.go',
  'similarly has optional `GroupMainGuid` and `GroupSubGuid` fields. Confirmed via the real Go validate tags',
  'that ONLY the `Names` field is `validate:"required"` on both — GroupMainGuid/GroupSubGuid carry no',
  'validation tag, so they are genuinely optional at the backend level. Decision already made (do not',
  're-litigate, matches this project\'s YAGNI/minimal-scope convention): do NOT build a 3rd "Group Main"',
  'screen for this task, since Product itself never references `groupproductmaster` at all (confirmed via',
  'frontend/src/app/menu/tab-product-classification.tsx — the real Product classification tab only shows 3',
  'independent pickers, "group"/"groupsubone"/"groupsubtwo", each using its own code/names directly on the',
  'Product record; it never touches GroupSubOne\'s own internal GroupMainGuid). Build GroupSubOne/GroupSubTwo',
  'screens as simple Code+Names masters (matching the brand/class/etc. sibling pattern) and leave their',
  'optional GroupMainGuid/GroupSubGuid parent-link fields unset/blank for this pass — note this as a known,',
  'deliberately deferred gap in the final report (a real "Group Main" screen + full 3-level cascade picker',
  'UX would be a separate future task if ever needed), do not silently build it and do not silently skip',
  'mentioning it either.',
  '| Brand | BrandGuid/Code/Names | `brandproductmaster` (via /aicloud/brand) | /masterbrandscreen | screen exists, 0 docs |',
  '| Design | DesignGuid/Code/Names | `designproductmaster` (via /aicloud/design) | /masterdesignscreen | screen exists, 0 docs |',
  '| Model | ModelGuid/Code/Names | `modelproductmaster` (via /aicloud/model) | /mastermodelscreen | screen exists, 0 docs |',
  '| Pattern | PatternGuid/Code/Names | `patternproductmaster` (via /aicloud/pattern) | /masterpatternscreen | screen exists, 0 docs |',
  '| Grade | GradeGuid/Code/Names | `gradeproductmaster` (via /aicloud/grade) | /mastergradescreen | screen exists, 0 docs |',
  '| Category | CategoryGuid/Code/Names | `categoryproductmaster` (via /aicloud/category, "หมวดคุณลักษณะสินค้า" —',
  '  distinct from the separate menu-category-tree screens /productcategorygroupselectscreen +',
  '  /productcategorylist, basePath /product/category, do not confuse the two) | /mastercategoryscreen | screen exists, 0 docs |',
  '| Class | ClassGuid/Code/Names | `classproductmaster` (via /aicloud/class) | /masterclassscreen | screen exists, 0 docs |',
  '',
  'Jead directly confirmed and pointed at a real, working, already-built additional classification system that',
  'must also be used in this task, under the menu group "จัดกลุ่มสินค้า": screens "กลุ่มสินค้า" (the Group tree',
  'above), "จัดหมวดสินค้า" (/productcategorygroupselectscreen) and "สินค้าในหมวด" (/productcategorylist), both',
  'basePath /product/category. Verified backend: backend/internal/product/productcategory/models/',
  'productcategory.go, collection `productcategories`, ALSO a real tree (ParentGUID/ParentGUIDAll/ChildCount/',
  'XSorts, same shape as productgroups) PLUS a `CodeList *[]CodeXSort` field — meaning a category node holds',
  'the list of member product/barcode codes directly (a reverse relationship: category -> its products), not',
  'a field on Product pointing to this tree. This is a SEPARATE, real, working system from the flat',
  '"หมวดคุณลักษณะสินค้า" master above (categoryproductmaster/CategoryGuid on Product) — both are real and both',
  'must be exercised in this task, do not treat one as replacing the other unless you find solid evidence they',
  'are actually meant to be the same thing (verify by reading the real productcategory_http.go and the two',
  'screen configs yourself; if a real user genuinely would only use one of the two in practice, say so in your',
  'report, but seed both realistically regardless since both are live, working features).',
  '',
  'Seed `productcategories` as a real tree too (root categories + child categories, matching the same theme,',
  'for example a root "เครื่องดื่ม" with children "กาแฟ"/"นม") in the Seed Master Data phase — but the actual',
  'assignment of real sample Product codes into each category\'s `codelist` (via the "สินค้าในหมวด" screen)',
  'must happen in the LATER Seed Products phase, after real products exist, not here — trying to reference',
  'products that do not exist yet would be meaningless. This is the part that makes it a genuine relation',
  'check, not just another flat/tree master: verify via a fresh API read and a real MongoDB document that the',
  'category\'s codelist genuinely contains your real product codes afterward.',
  'Real current MongoDB Atlas document counts confirmed this session (holdingcode "test", database',
  '`bcaiclouddb` on `dev.brulkiz.mongodb.net`, connection string in gitignored backend/bootstrap.json,',
  'reuse it, never print/log/commit it): `units`=110, `products`=51, `productbarcodes`=29,',
  '`productbarcodespricehistory`=65. Every other master collection above genuinely does not exist yet',
  '(zero footprint, not just zero-for-this-tenant) — re-verify counts yourself before seeding since this may',
  'have changed since this session\'s discovery pass.',
  '',
  '**Product Set has no separate backend model.** It is a Product/ProductBarcode document with',
  '`ItemType=2` (json/bson `itemtype`) and `MaterialType=3` (json/bson `materialtype`) — a convention enforced',
  'server-side by `ValidateProductClassification`',
  '(backend/internal/product/productbarcode/models/product_classification.go). Components are referenced via',
  'real existing fields (verified this session, exact current shape):',
  '```go',
  '// product.go:84-85 (mirrored on ProductBarcode, product_barcode.go:182-183)',
  'RefBarcodes *[]RefProductBarcode `json:"refbarcodes" bson:"refbarcodes"`',
  'BOM         *[]BOMProductBarcode `json:"bom" bson:"bom"`',
  '// RefProductBarcode (product.go:102-120) key fields: GuidFixed, Names, ItemUnitCode, Barcode,',
  '// Condition, DivideValue, StandValue, Qty, SellerSKU + package dims (skip these for sample data)',
  '// BOMProductBarcode (product.go:122-133) key fields: BarcodeGuidFixed, Level, Names, ItemUnitCode,',
  '// Barcode, Condition, DivideValue, StandValue, Qty',
  '```',
  'Do NOT confuse this with `ProductOption`/`ProductChoice` (product.go:298-321) — that is a separate,',
  'unrelated restaurant-menu-modifier feature (item size/add-on choices), not the Product Set mechanism.',
  '',
  '**An existing seed-data skill and script already exist — read and reuse their conventions, do not',
  'reinvent from scratch:**',
  '- `D:\\bccode\\.agents\\skills\\dev-data-seeder\\SKILL.md` and',
  '  `D:\\bccode\\.agents\\skills\\dev-data-seeder\\scripts\\seed-restaurant-menu-dev.ps1` — the more complete,',
  '  more current copy (uses real field names holdingcode/itemtype matching the actual Go structs verified',
  '  this session). A second, older, slightly inconsistent copy exists at',
  '  `D:\\bccode\\.claude\\skills\\dev-data-seeder\\` (uses holding_code/item_type, which do NOT match the real',
  '  current field names) — read both, but trust the `.agents` copy\'s field-naming convention where they',
  '  disagree, and flag this inconsistency in your final report rather than silently picking one without',
  '  saying so.',
  '- That existing script creates normal products (itemtype=0/materialtype=0) and Product Sets',
  '  (itemtype=2/materialtype=3) via real API calls (POST /product, POST /product/barcode/bulk, PUT',
  '  /product/barcode/:guid for BOM), but does NOT seed any of the classification master data',
  '  (brand/class/design/model/pattern/grade/group/category/groupsubone/groupsubtwo) — that gap is exactly',
  '  what this task needs to close first, before the sample products can meaningfully reference real values.',
  '',
  'Project conventions: MongoDB field names in bson/json tags are lowercase, no underscores, no hyphens.',
  'Business Code fields (Code, GroupCode, etc.) are uppercase, no spaces. Thai-First rule: write full correct',
  'Thai labels; other languages get English/placeholder text only. Full freedom is granted to add/change',
  'MongoDB schema and backend/frontend code without asking (pre-launch, disposable database) excluding',
  'deploy/push and the scopeofwork/ folder. Windows dev notes: host go build/test do not work directly, use',
  'the Docker builder image: MSYS_NO_PATHCONV=1 docker run --rm -v "D:/bccode/backend:/src" -w /src',
  'backend-mainapi-local-builder:latest sh -c "CGO_ENABLED=1 go build -tags musl ./..." (same pattern for go',
  'test). Frontend typecheck must run "node node_modules/typescript/bin/tsc --noEmit" directly. The frontend',
  'dev server must always be run as "next build" then "next start", never "next dev" — if a stale process is',
  'already on port 3000, kill it and start fresh before testing any change. Do NOT leave any Go build cache',
  'directories in the repo root. Do not commit or push anything. Never print, log, or write full connection',
  'strings/secrets into regression tests, reports, or commit history — reference them by file name',
  '(backend/bootstrap.json) or env var, never paste the literal value into a file that could be committed.',
  '',
  'This task follows the project\'s uat-crud-mongo convention: never claim a flow works without evidence —',
  'UI state, a fresh API read, and the real MongoDB document (and now also the real PostgreSQL projection,',
  'given the Kafka sync pipeline was just fixed in a prior task this session) must all agree after every',
  'create/update/delete. Environment safety: confirm you are pointed at the real DEV/disposable',
  'MongoDB Atlas + local Docker PostgreSQL before writing anything (holdingcode "test", never anything that',
  'looks like real production/customer data) — this has already been confirmed safe this session, re-confirm',
  'quickly rather than skipping the check.',
].join('\n')

phase('Build Missing Screens')

const screensReport = await agent(
  [
    CONTEXT,
    '',
    'Build the 2 missing frontend screens: GroupSubOne (กลุ่มย่อยระดับ 1) and GroupSubTwo (กลุ่มย่อยระดับ 2).',
    'Backend already fully supports both (models + HTTP routes exist and work, reached via the existing proxy',
    'at frontend/src/app/api/product-barcode/master/[master]/route.ts which already maps groupsubone ->',
    '/aicloud/groupsubone and groupsubtwo -> /aicloud/groupsubtwo) — this is frontend-only work.',
    '',
    'Read the 6 real, working sibling screens first (brand/class/design/model/pattern/grade — find their exact',
    'config entries in frontend/src/lib/system-setting-screens.ts, they should all share a near-identical',
    'simple shape given they are all "smlaiproduct/*" family masters) and copy their exact pattern precisely',
    '(same kind, same field list shape, same basePath/listPath convention but pointed at',
    '/aicloud/groupsubone and /aicloud/groupsubtwo respectively). Read the real backend routes',
    '(backend/internal/smlaiproduct/groupsuboneproduct and .../groupsubtwoproduct, or wherever they really',
    'live — confirm the real package path yourself) to confirm the exact real route paths and field names',
    'before wiring, do not assume them from this summary.',
    '',
    'Add 2 new SystemSettingConfig entries: slug/route for each (pick sensible slugs matching the sibling',
    'naming convention, e.g. following whatever pattern masterbrandscreen/masterclassscreen/etc. use), with',
    'Thai titles "กลุ่มย่อยระดับ 1" and "กลุ่มย่อยระดับ 2" (or better Thai wording if you find the sibling',
    'screens use a clearer convention for hierarchy naming — match their style). These do NOT yet have menu',
    'entries in frontend/src/lib/menu-data.ts (the 6 siblings might also lack menu entries and be reached only',
    'via master-picker — check this and follow the same pattern; if the siblings DO have menu entries, add',
    'matching ones for these two as well, in the same menu group).',
    '',
    'Keep both new screens to a simple Code+Names(+Disabled) field list, matching the 6 flat siblings exactly',
    '— do NOT add a parent-picker field for GroupMainGuid/GroupSubGuid (per the shared context, this is a',
    'deliberately deferred decision, not an oversight; those fields are optional and unused by the real',
    'Product form). Do not build a 3rd "Group Main" screen either.',
    '',
    'Verify: "node node_modules/typescript/bin/tsc --noEmit" must exit 0. Do a real live check (build fresh',
    'with "next build" then "next start", killing any stale process on port 3000 first) — open both new',
    'routes directly in a browser, confirm they render a real form (not a 404), and do one real',
    'create+edit+delete round-trip on each via the actual UI, verifying via a fresh API read and a real',
    'MongoDB document each time (docker exec mongodb mongosh --quiet "<atlas-uri-from-bootstrap.json>" --eval',
    '"db.getSiblingDB(\'bcaiclouddb\').<collection>.find(...)"). Delete your own test records afterward so this',
    'phase leaves the collections empty again for the next phase to seed real sample data cleanly.',
    '',
    'Report back precisely: exact files changed, exact slugs/routes/basePaths chosen, and the verbatim output',
    'of every verification command including your live create/edit/delete evidence for both new screens.',
  ].join('\n'),
  { label: 'build-groupsub-screens', phase: 'Build Missing Screens' },
)

phase('Seed Master Data')

const masterDataReport = await agent(
  [
    CONTEXT,
    '',
    '=== Report on the 2 newly-built screens (groupsubone/groupsubtwo), for the real routes/field names ===',
    String(screensReport),
    '',
    'Your job: seed realistic, thematically-coherent Thai classification master data across ALL 11 relation',
    'types (brand, class, design, model, pattern, grade, group, category, groupsubone, groupsubtwo, and',
    'productcategories — the separate real tree system Jead pointed at directly, see below), through',
    'the real UI (Playwright, driving each screen like a real user would) or, where a repeatable bulk-style',
    'API call is the more realistic "how a real user\'s browser actually submits this form" shape, direct API',
    'calls matching the exact real request payload the UI itself sends (verify the real payload shape by',
    'reading the relevant screen/api.ts code first, do not guess it).',
    '',
    'Pick ONE coherent, realistic Thai SME business theme and apply it consistently across all 10 master-data',
    'types so that later Product/Barcode/ProductSet sample data built on top of it reads as a real, believable',
    'business, not randomly labeled test rows (for example, a small Thai grocery/beverage shop selling real',
    'recognizable-style Thai brands of coffee, milk, snacks — invent plausible generic-sounding Thai brand/',
    'product names, do not use any real trademarked brand name verbatim). Suggested minimum per type (adjust',
    'if a type genuinely does not fit the theme naturally, but do not skip any type entirely — every relation',
    'field should have at least 2-3 real, resolvable options afterward): 2-3 real entries each for',
    'brand/category/class/groupsubone/groupsubtwo, and at least 1-2 sensible entries each for',
    'design/model/pattern/grade even if their natural fit to a grocery theme is looser (for example grade could',
    'be "เกรดพรีเมียม"/"เกรดมาตรฐาน", pattern/design/model can be simple placeholder-appropriate values for a',
    'non-fashion business — use your judgment for what a real Thai SME might actually type into these fields,',
    'do not leave them meaninglessly blank if the field exists and is meant to be used).',
    '',
    '**"group" (กลุ่มสินค้า, /productgroup) is the one exception — seed it as a REAL TREE, not flat siblings.**',
    'This screen is confirmed hierarchical (parentguid/childcount/drag-to-reorder), matching the same theme.',
    'Create at least 2 root/parent groups (for example "เครื่องดื่ม" Beverages and "ของใช้" Supplies), and under',
    'each root create 2-3 real child groups via the UI\'s own parent-child mechanism (read the real',
    '/productgroup screen\'s form first to find how a parent is actually assigned — likely a parent-picker field',
    'or a tree-drag interaction; use whichever the real UI actually exposes, do not guess). Verify the parent-',
    'child relationship really persisted (the child\'s parentguid resolves to the real root\'s guidfixed, and the',
    'root\'s childcount reflects the real child count) via a fresh API read and a real MongoDB document check —',
    'this is exactly the kind of relation-completeness check the whole task cares about, so do not just create',
    'flat rows and call it done for this one type.',
    '',
    'Verify each master-data type after seeding: a fresh list-load on its own screen shows the real new rows,',
    'a fresh API read confirms them, and the real MongoDB document exists with the correct holdingcode "test"',
    'scope. Do this for all 11 types before moving on — this phase\'s whole point is giving Phase 3 a menu of',
    'real, referenceable classification values. For `productcategories`, only build the tree structure in this',
    'phase (root + child category nodes) — do NOT attempt to populate any category\'s `codelist` yet, since no',
    'real sample products exist until Phase 3; that assignment step happens in Phase 3 instead.',
    '',
    'Report back precisely: for each of the 11 master-data types, the exact codes/names you created (a short',
    'table is fine), how you created them (UI vs API, and why), and verification evidence for each.',
  ].join('\n'),
  { label: 'seed-master-data', phase: 'Seed Master Data' },
)

phase('Seed Products')

const productsReport = await agent(
  [
    CONTEXT,
    '',
    '=== Master data now seeded (real codes/names to reference) ===',
    String(masterDataReport),
    '',
    'Your job: create a small, realistic, genuinely interrelated set of sample Products, Barcodes, and at',
    'least one Product Set, through the REAL /product and /productbarcode UI screens exactly the way a real',
    'user would (Jead\'s explicit instruction: "ทำเหมือน user ป้อนเองเลย" — drive the actual UI form, do not',
    'write directly to MongoDB). Suggested scope (adjust for what genuinely makes a coherent demo, but keep it',
    'a curated SAMPLE, not a bulk 100+ row seed — this is meant to look hand-entered and demonstrably related,',
    'not mass-generated): 5-8 real Products, each assigned a real brand/category/class/group/groupsubone/',
    'groupsubtwo/design/model/pattern/grade value from Phase 2\'s real seeded master data (not left blank),',
    'each with at least one real Barcode (with a real unit from the existing 110 seeded units, a real price),',
    'and at least 1-2 Product Sets that each genuinely bundle 2-3 of the real Products/Barcodes you just',
    'created as components (via the real RefBarcodes[]/BOM[] mechanism, using the real barcode/guid values you',
    'get back from creating them, not placeholder strings).',
    '',
    'Every product/barcode/set must be created via the real UI (Playwright), following the exact real form',
    'interaction patterns already established in this session\'s Playwright specs',
    '(frontend/e2e/product-crud.spec.ts, product-barcode-crud.spec.ts, product-set-crud.spec.ts — read them',
    'first for the real DOM-interaction helpers, do not reinvent selectors from scratch).',
    '',
    'After creating everything, explicitly verify EVERY relation field you set actually resolved to a real',
    'value, not a dangling/unresolved reference: for each product, re-fetch it via a fresh API GET and confirm',
    'each classification Guid/Code you set corresponds to a document that genuinely exists in its master',
    'collection (cross-check the guid/code against the real master-data documents from Phase 2, do not just',
    'check the field is non-empty). This is the literal "ตรวจด้วยทุก field ที่มี relation อ้างอิงครบหรือไม่"',
    'requirement — do it for every relation field on every product you create, not a sample.',
    '',
    'Also, now that real products exist: go back to the real `productcategories` tree seeded in Phase 2 (the',
    '"จัดหมวดสินค้า"/"สินค้าในหมวด" system Jead specifically pointed at) and use the real "สินค้าในหมวด"',
    '(/productcategorylist) screen to assign your real new product codes into the appropriate category node(s)',
    '`codelist` (matching your chosen theme, e.g. put the coffee-related products into the "กาแฟ" child',
    'category). Verify via a fresh API read and a real MongoDB document that the category\'s codelist genuinely',
    'contains your real product codes afterward — this is a required part of this phase, not optional.',
    '',
    'Report back precisely: exact codes/names for every Product/Barcode/Product Set created, the exact real',
    'component barcodes each Product Set bundles, which category node(s) now list which real product codes in',
    'their codelist, and the relation-completeness check result for every single relation field on every',
    'product (a table: product code -> field -> value -> resolved-real y/n).',
  ].join('\n'),
  { label: 'seed-products-barcodes-sets', phase: 'Seed Products' },
)

phase('Verify')

const verifyReport = await agent(
  [
    CONTEXT,
    '',
    '=== Screens report ===',
    String(screensReport),
    '',
    '=== Master data report ===',
    String(masterDataReport),
    '',
    '=== Products/Barcodes/Sets report ===',
    String(productsReport),
    '',
    'Your job is independent verification, following the uat-crud-mongo convention already used in this',
    'project: never trust a self-reported "done" without real evidence you generate yourself.',
    '',
    '1. Read the real git diff yourself for the 2 new screen files, confirm it matches what was reported and',
    'is a small, additive, sibling-pattern-matching change (no unrelated changes).',
    '2. Independently re-verify a sample of the seeded master data (at least 3 of the 11 types, and MUST',
    'include `productcategories` specifically since Jead pointed at it directly) via a fresh UI load + fresh',
    'API read + real MongoDB query, using your own commands, not reused output from the reports above. For',
    '`productcategories`, also independently confirm the real product-code assignment into a category\'s',
    '`codelist` actually happened (re-fetch that category via API and check the codelist yourself).',
    '3. Independently re-verify at least 2 of the seeded Products (including their full relation-field set)',
    'and the Product Set(s) via a fresh UI load + fresh API read + real MongoDB query + real PostgreSQL query',
    '(the Kafka-driven Mongo-to-PostgreSQL sync was fixed in a prior task this session — confirm these new',
    'products/barcodes also show up correctly in PostgreSQL after a brief wait, the same way you would for any',
    'ProductBarcode create).',
    '4. Confirm the relation-completeness claim in the Products report is actually true for at least 3 products',
    'you re-check yourself — do not just trust the table, cross-reference at least a few entries against the',
    'real master-data documents.',
    '5. Confirm the actual /product, /productbarcode, /productset screens, when opened fresh in a real browser,',
    'visibly show this new sample data (not just that the API returns it) — take this as literally as the',
    'task asked: "ทำเหมือน user ป้อนเองเลย", so it should look and behave exactly like something a real user',
    'built by hand.',
    '6. Run the existing Playwright suite (product-crud.spec.ts, product-barcode-crud.spec.ts,',
    'product-set-crud.spec.ts, and the full suite for regressions) and confirm nothing broke from the new',
    'screens or the new data. If the new screens need their own regression test, add one following this',
    'session\'s established conventions (unique/generated codes, cleans up after itself) — but note that the',
    'SAMPLE data created in this task should NOT be cleaned up/deleted at the end (unlike a throwaway',
    'regression test\'s own data) since it is meant to persist as visible demo/sample data for Jead to look at.',
    '7. Run the backend build and frontend typecheck one more time as a final sanity check.',
    '8. Read D:\\bccode\\.agents\\worklog.md yourself first to find its real current last line, then append a',
    'new entry after it, and re-read the file after your edit to confirm the entry actually landed (this exact',
    '"claimed but did not land" gap has happened multiple times this session with other agents, do not repeat',
    'it).',
    '9. Note the two-inconsistent-copies-of-dev-data-seeder-skill finding in your final report as something',
    'Jead may want reconciled later (do not fix it yourself, it is a docs/skill-maintenance question, not part',
    'of this task\'s scope).',
    '',
    'Do not commit or push anything. Do not delete the sample data created in Phases 2-3 (that data is the',
    'actual deliverable Jead asked for).',
    '',
    'End your report with the required Thai summary format: what changed, pros, cons/risks, and concrete next',
    'steps — including a clear list of exactly what sample data now exists and where to see it (which screens,',
    'which codes) so Jead can look at it directly.',
  ].join('\n'),
  { label: 'full-verify', phase: 'Verify', effort: 'high' },
)

return { screensReport, masterDataReport, productsReport, verifyReport }
