export const meta = {
  name: 'product-image-auth-fix',
  description: 'Fix product/barcode image display: plain <img src> cannot carry the required Authorization header, so uploaded images never render (reuse the proven LogoAvatar auth-fetch pattern)',
  phases: [
    { title: 'Implement', detail: 'apply the proven authenticated-image-fetch pattern to all broken product/barcode image displays' },
    { title: 'Verify', detail: 'real upload + immediate display round-trip via UI, MongoDB, and the S3 proxy; add Playwright regression' },
  ],
}

const CONTEXT = [
  'Project: BC Ai Account, a Thai SME accounting and ERP system.',
  'Backend: Go, at D:\\bccode\\backend. Frontend: Next.js, at D:\\bccode\\frontend. Image storage: S3/MinIO,',
  'served through an auth-protected backend proxy route.',
  '',
  'Jead (project owner) reported: after uploading a product image, it shows as a broken/placeholder icon',
  '(shown twice now, with two different freshly-uploaded files, both broken immediately after upload — this',
  'is a live, currently-reproducible bug, not a one-off stale artifact).',
  '',
  'Root cause, confirmed by direct source investigation this session, do not re-derive it, but verify anything',
  'you rely on against the real current source yourself (it may have shifted line numbers):',
  '',
  '1. **The backend image-serving route genuinely requires an Authorization header.** Route:',
  '   `backend/internal/goapi/bootstrap.go` registers `authGroup.GET("/s3/file/*", handlers.S3FileProxyHandler)`',
  '   inside an authenticated route group (`createGoAPIAuthMiddleware`). A request with no valid bearer token',
  '   gets a clean `401 {"success":false,"message":"Unauthorized"}` from this route, confirmed live.',
  '',
  '2. **The frontend renders these images with a plain `<img src="/goapi/s3/file/...">` tag.** A browser',
  '   cannot attach a custom `Authorization` header to a plain `<img>` element request — so ANY real,',
  '   successfully-uploaded product/barcode image will ALWAYS render broken, unconditionally, regardless of',
  '   whether the upload itself succeeded. This is the primary, confirmed-real, currently-reproducible bug.',
  '   Confirmed broken locations (read each yourself first, line numbers are as of this investigation and may',
  '   have shifted):',
  '   - `frontend/src/app/menu/tab-product-media.tsx` — main product image (~line 87-93) AND the gallery',
  '     images (~line 157)',
  '   - `frontend/src/app/menu/product-screen.tsx` — list-row thumbnail (~line 1086)',
  '   - `frontend/src/components/product-barcode/barcode-form.tsx` — main + gallery images, 4 occurrences',
  '     (~lines 932, 954, 1288, 1356)',
  '   - `frontend/src/app/menu/product-barcode-screen.tsx` — list-row thumbnail (~line 1081)',
  '',
  '3. **A correct, proven, already-working pattern for this exact problem already exists in this codebase**,',
  '   but was never reused for product/barcode images:',
  '   - `frontend/src/lib/image-upload-proxy.ts` exports `imageNeedsAuthenticatedFetch(url)` (~line 166-178),',
  '     which detects a `/s3/file/` (or equivalent auth-required) path.',
  '   - `frontend/src/components/logo-avatar.tsx` exports a `useLogoImage()` hook (~line 111-198) that: checks',
  '     `imageNeedsAuthenticatedFetch(url)`; if true, does a real `fetch(url, { headers: { Authorization:',
  '     `Bearer ${token}` } })`, converts the response to a `.blob()`, creates an object URL via',
  '     `URL.createObjectURL()`, and caches object URLs with an LRU cache (revoking old ones to avoid leaking',
  '     memory) so repeated renders of the same image do not re-fetch every time. Used correctly today by',
  '     `company-branch-tree-view.tsx`, `holding-screen.tsx`, `system-settings-screen.tsx`,',
  '     `workspace-screen.tsx`, `main-menu-screen.tsx` (all for logos/avatars only).',
  '   - Read the real current implementation of `useLogoImage()` and `imageNeedsAuthenticatedFetch()` fully',
  '     before building anything — this is the exact mechanism to generalize and reuse, not reinvent.',
  '',
  '4. **A second, separate, smaller issue exists on top of the above**: the specific file referenced in the',
  '   first bug report (`test/products/20260707_235545_342e6dbb`) returns a genuine `404 File not found` from',
  '   the S3 proxy even with a valid, correctly-scoped auth token — meaning that particular upload never',
  '   actually landed in storage (or was later removed). A second background investigation into whether the',
  '   upload endpoint itself has a silent-failure bug (returning a success response with a URL even when the',
  '   underlying S3 write did not actually succeed) may still be in progress or may have already reported',
  '   findings elsewhere in this session — if you find its results, incorporate them; if not, independently',
  '   verify upload-then-immediate-fetch works correctly today as part of your own Verify phase (see below) —',
  '   if a real product upload done fresh today, through the real UI, still cannot be fetched back',
  '   immediately afterward even WITH the auth-header fix in place, that would indicate a genuine upload-side',
  '   bug requiring its own separate root-cause fix, not just a display-side one.',
  '',
  'Project conventions: Windows dev notes — frontend typecheck must run',
  '"node node_modules/typescript/bin/tsc --noEmit" directly. The frontend dev server must always be run as',
  '"next build" then "next start", never "next dev" — kill any stale process on port 3000 first. If you touch',
  'backend Go code for the upload-side investigation, host go build/test do not work directly, use the Docker',
  'builder image: MSYS_NO_PATHCONV=1 docker run --rm -v "D:/bccode/backend:/src" -w /src',
  'backend-mainapi-local-builder:latest sh -c "CGO_ENABLED=1 go build -tags musl ./..." — and to test a live',
  'backend fix, rebuild+restart the real mainapi Docker container (if docker compose build cannot find',
  'BuildKit, use "/c/Program Files/Docker/Docker/resources/cli-plugins/docker-buildx.exe" build --load -t',
  'backend-mainapi -f Dockerfile . directly, then docker-compose up -d --no-deps mainapi — this exact',
  'workaround was needed and used in a prior task this session). Do NOT leave any Go build cache directories',
  'in the repo root. Do not commit or push anything. Full freedom is granted to change backend/frontend code',
  'without asking (pre-launch, disposable database), excluding deploying/pushing to a shared remote and the',
  'scopeofwork/ folder.',
  '',
  'This project follows the uat-crud-mongo skill convention: never claim a fix works without evidence — a',
  'fresh real upload through the real UI must actually display correctly afterward, verified live in a',
  'browser, not just "the code looks right now."',
].join('\n')

phase('Implement')

const implementReport = await agent(
  [
    CONTEXT,
    '',
    'Fix the confirmed systemic bug (item 2/3 above): apply the proven authenticated-image-fetch pattern to',
    'every broken product/barcode image display location.',
    '',
    '1. Read `useLogoImage()` in `frontend/src/components/logo-avatar.tsx` and',
    '   `imageNeedsAuthenticatedFetch()` in `frontend/src/lib/image-upload-proxy.ts` fully, for real, first.',
    '2. Decide the cleanest way to reuse this for product/barcode images: either (a) extract the core',
    '   auth-fetch-to-blob-URL logic out of `useLogoImage()` into a small, generic, reusable hook (for example',
    '   `useAuthenticatedImage(url)`) in `image-upload-proxy.ts` or a new small shared file, that both',
    '   `useLogoImage()` and the product/barcode image components can call, or (b) if `useLogoImage()` is',
    '   already generic enough to accept any URL (not logo-specific), reuse it directly. Prefer (a) only if',
    '   `useLogoImage()` genuinely has logo-specific baggage that would be wrong to drag into product image',
    '   rendering (check for this — logo-specific defaults, logo-specific cache keys, etc.) — otherwise the',
    '   smaller diff is better. Do not build a second, parallel, slightly-different caching mechanism.',
    '3. Apply the fix to all 5 confirmed locations:',
    '   - `frontend/src/app/menu/tab-product-media.tsx` (main image + gallery images)',
    '   - `frontend/src/app/menu/product-screen.tsx` (list-row thumbnail)',
    '   - `frontend/src/components/product-barcode/barcode-form.tsx` (main + gallery, both barcode-form',
    '     locations)',
    '   - `frontend/src/app/menu/product-barcode-screen.tsx` (list-row thumbnail)',
    '   Each `<img src={rawUrl}>` that points at a `/s3/file/`-style path needs to resolve through the new/',
    '   reused authenticated-fetch mechanism instead of using the raw URL directly. Handle the loading state',
    '   sensibly (a lightweight placeholder while the blob fetch is in flight is fine, matching whatever',
    '   `useLogoImage()` already does for its own loading state).',
    '4. Grep the whole frontend one more time for any OTHER place rendering a raw `/s3/file/` or similar',
    '   auth-required image path directly as `<img src>` that was not in the original 5-location list (the',
    '   original investigation may not have been exhaustive) — fix any genuine additional occurrence you find,',
    '   using the same mechanism, for consistency. Do not touch image displays that are NOT auth-required',
    '   (public URLs, data URIs, etc.) — only fix genuinely broken auth-required cases.',
    '',
    'Verify: "node node_modules/typescript/bin/tsc --noEmit" must exit 0. Do a real live check: build fresh',
    '("next build" then "next start", killing any stale process on port 3000 first), log in via the real UI,',
    'upload a REAL test image (a real small image file, not a fake/empty one) to a product via `/product`,',
    'and confirm it actually renders as a real image (not a broken icon) immediately after upload, without',
    'needing a page reload. Then reload the page and confirm it still renders correctly (proves the fetch-on-',
    'read path also works, not just an optimistic post-upload preview). Repeat for the barcode gallery on',
    '`/productbarcode` if time allows, to confirm the fix generalizes correctly across both screens.',
    '',
    'Report back precisely: exact files changed, the exact mechanism you built/reused, and the verbatim',
    'output of every verification command including your live upload-and-display evidence (describe exactly',
    'what image you uploaded and what you observed, with real screenshots/snapshots if possible).',
  ].join('\n'),
  { label: 'implement-image-auth-fix', phase: 'Implement' },
)

phase('Verify')

const verifyReport = await agent(
  [
    CONTEXT,
    '',
    '=== Implementation report to independently verify, do not just trust it ===',
    String(implementReport),
    '',
    'Your job is independent verification, following the uat-crud-mongo convention already used in this',
    'project: never trust a self-reported "done" without real evidence you generate yourself.',
    '',
    '1. Read the real git diff yourself, confirm it matches what was reported and is a focused, additive fix',
    '   (no unrelated changes, no new competing caching mechanism if one was not needed).',
    '2. Independently do a FRESH real upload-and-display test yourself (do not reuse the implementer\'s',
    '   screenshots/evidence): log in via the real UI, upload a real image to a NEW product you create for',
    '   this test, confirm it renders correctly immediately, reload the page and confirm it still renders,',
    '   open the product again from the list and confirm the list-row thumbnail also renders correctly (not',
    '   just the detail-view image). Do the same for a barcode\'s image.',
    '3. Confirm via MongoDB that the real `imageuri`/image array field was actually persisted correctly for',
    '   your test records (docker exec mongodb mongosh against the real Atlas `bcaiclouddb`/`test` tenant,',
    '   same connection this session has used throughout — read `backend/bootstrap.json` for the real URI,',
    '   never print/log/commit the literal connection string).',
    '4. Confirm via a direct authenticated `curl` call to the real `/goapi/s3/file/...` path (with a real',
    '   bearer token, same pattern used elsewhere this session) that the uploaded file is genuinely retrievable',
    '   from storage (200, real image content-type) — this closes the loop on whether uploads are landing in',
    '   S3/MinIO correctly, addressing item 4 in the shared context.',
    '5. Run the existing Playwright product/barcode specs (product-crud.spec.ts, product-barcode-crud.spec.ts,',
    '   product-set-crud.spec.ts) to confirm no regressions. Add a new, focused regression test (or extend an',
    '   existing spec) that uploads a real image and asserts it renders (not a broken-icon state — for example',
    '   assert the `<img>` element\'s `naturalWidth`/`naturalHeight` are non-zero after load, or that a real',
    '   blob URL was set, whichever is the more robust real signal available) — this is exactly the kind of',
    '   bug that needs a regression test so it cannot silently come back.',
    '6. Run "node node_modules/typescript/bin/tsc --noEmit" one more time as a final sanity check.',
    '7. Clean up your own test data (delete the test product/barcode you created) unless it is meant to stay',
    '   as part of this session\'s existing sample data set — use your judgment, but do not leave orphaned',
    '   throwaway test records with no clear purpose.',
    '8. Read D:\\bccode\\.agents\\worklog.md yourself first to find its real current last line, then append a',
    '   new entry after it, and re-read the file after your edit to confirm the entry actually landed.',
    '',
    'Do not commit or push anything.',
    '',
    'End your report with the required Thai summary format: what changed, pros, cons/risks, and concrete next',
    'steps.',
  ].join('\n'),
  { label: 'full-verify', phase: 'Verify', effort: 'high' },
)

return { implementReport, verifyReport }
