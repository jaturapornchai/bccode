export const meta = {
  name: 'product-pg-sync-fix-v2',
  description: 'Consult advisors on the right design, then build the missing Product-to-PostgreSQL Kafka sync pipeline (mirroring the working ProductBarcode pipeline)',
  phases: [
    { title: 'Consult', detail: 'GPT/GLM/DeepSeek in parallel on the fix design, Fable synthesizes' },
    { title: 'Implement', detail: 'build the Product Kafka producer + consumer + PostgreSQL sync' },
    { title: 'Verify', detail: 'create/update/delete a real product, confirm /product list works end-to-end' },
  ],
}

const CONTEXT = [
  'Project: BC Ai Account, a Thai SME accounting and ERP system.',
  'Backend: Go, at D:\\bccode\\backend. Data Store Roles (established project convention): MongoDB is the raw',
  'source of truth; PostgreSQL is a relational projection populated via Kafka after every Mongo',
  'create/update/delete; ClickHouse is a dimensional/BI projection. PostgreSQL and ClickHouse are disposable',
  'and rebuildable from MongoDB at any time (pre-launch, no real data at risk).',
  '',
  'Confirmed by Jead (the project owner): the /product screen list view legitimately needs to read from',
  'PostgreSQL, not MongoDB, because it displays a precomputed stock balance figure (ยอดคงเหลือ). Keep this',
  'architecture — do not revert the list to MongoDB.',
  '',
  'Root cause, precisely confirmed by direct source investigation this session (do not re-derive, but verify',
  'anything you rely on against the real current system state yourself, since it may have changed):',
  '',
  '1. **The Product module (backend/internal/product/product/) has ZERO Kafka event pipeline.** No producer',
  '   call anywhere in product_http.go / services/product_http_service.go / repositories/',
  '   product_mongo_repository.go (grepped for kafka/Producer/Publish/Emit/SendMessage — zero matches). It',
  '   writes only to MongoDB. Contrast with ProductBarcode (backend/internal/product/productbarcode/), which',
  '   DOES have real producer calls emitting `when-product-barcode-created/updated/deleted` (this pipeline was',
  '   fixed in a prior task this session and is now confirmed working: Kafka consumer group',
  '   `biapi-inventory-consumer-v1` in `backend/internal/goapi/handlers/kafka/inventory.go` consumes these',
  '   topics and writes into the PostgreSQL `productbarcode` table, LAG=0, verified live).',
  '2. **No Kafka consumer exists that writes into the PostgreSQL `product` table either** —',
  '   backend/internal/goapi/handlers/kafka/ has no "product.go" sibling to "inventory.go". The topic constants',
  '   in backend/internal/goapi/handlers/kafka/constants.go are literally named for product-barcode events',
  '   only; the "biapi-inventory-consumer" group name is a historical misnomer, not evidence of a real',
  '   Product-level pipeline.',
  '3. **The PostgreSQL `product` table itself is correctly provisioned and has the right schema** — confirmed',
  '   via `docker exec postgres psql -U postgres -d test -c "\\d product"`: `id serial PK`, `itemcode text`',
  '   (unique), `name0 text not null`, `unitcode text not null`, `unitname text not null`,',
  '   `balanceqty numeric(18,4)`, `balanceqtyword text`, `pendingrecvqty/pendingrecvqtyword`,',
  '   `pendingsendqty/pendingsendqtyword`. DDL is `TableProductCreate` in',
  '   backend/internal/goapi/process/build/create-database.go (already wired into DatabaseRebuildAll from a',
  '   prior fix this session, table auto-provisions correctly) — the table exists, it is simply never written',
  '   to, confirmed 0 rows tenant-wide even after real products were created via the normal API.',
  '4. **`inventorystockbalances` (the 3rd table joined by the list query) is correctly, separately populated**',
  '   by real, working, unrelated code: backend/internal/goapi/inventory/costing/engine.go, triggered only by',
  '   real stock transaction HTTP endpoints (POST /inventory/receipt, /issue, /transfer, /adjustment,',
  '   /sales-return, /purchase-return, registered in backend/internal/goapi/inventory/handler.go). This is',
  '   deliberate and correct — a product with no stock transactions yet should show 0 balance, not be treated',
  '   as broken. Do not touch this part.',
  '5. **The list query itself is the smoking gun, not a query defect**: `buildProductSearchQuery` in',
  '   backend/internal/goapi/handlers/product_cache.go is `FROM public.product p LEFT JOIN',
  '   (...productbarcode...) pb ON pb.itemcode = p.itemcode LEFT JOIN (...inventorystockbalances...) st ON',
  '   st.itemcode = p.itemcode WHERE 1=1` — `product` is the driving FROM table. With `product` at 0 rows, the',
  '   whole query returns 0 rows regardless of `productbarcode` having real synced data (7 rows confirmed',
  '   present, structurally invisible to this query because of how the JOIN is anchored). The fix is squarely:',
  '   make something insert/update/delete real rows in `public.product` when a Product is created/updated/',
  '   deleted in MongoDB — mirroring the exact pattern already proven working for ProductBarcode.',
  '',
  'Project conventions: MongoDB field names in bson/json tags are lowercase, no underscores, no hyphens.',
  'Windows dev notes: host go build/test do not work directly, use the Docker builder image:',
  'MSYS_NO_PATHCONV=1 docker run --rm -v "D:/bccode/backend:/src" -w /src backend-mainapi-local-builder:latest',
  'sh -c "CGO_ENABLED=1 go build -tags musl ./..." (same pattern for go test). To test a live fix, rebuild and',
  'restart the real mainapi Docker container (if docker compose build cannot find BuildKit, use',
  '"/c/Program Files/Docker/Docker/resources/cli-plugins/docker-buildx.exe" build --load -t backend-mainapi -f',
  'Dockerfile . directly, then docker-compose up -d --no-deps mainapi — this exact workaround was needed and',
  'used in a prior task this session). Do NOT leave any Go build cache directories in the repo root. Do not',
  'commit or push anything. Full freedom is granted to change MongoDB/PostgreSQL schema and backend code',
  'without asking (pre-launch, disposable database), excluding deploying/pushing to a shared remote and the',
  'scopeofwork/ folder.',
  '',
  'This project follows a Multi-Model Role Division Rule: Claude Sonnet is the only agent that edits files;',
  'Fable acts as chief architect and final design judge; GPT 5.5, GLM, and DeepSeek are external advisors',
  'consulted through real CLI wrapper calls (never role-played by an agent persona pretending to be them).',
  'There is a mandatory scoring rule: every real consult of GPT/GLM/DeepSeek must be scored 1-10 afterward',
  '(accuracy verified against real source, usefulness/how much was adopted, reliability of the call) and',
  'appended as a row to .agents/advisor-scorecard.md, with the score shown in the final report.',
].join('\n')

function externalConsultInstructions(toolName, wrapperPath, question) {
  return [
    'You are consulting a real external AI model through a command line wrapper. This is not a role-play, you',
    'must genuinely invoke the external command and return what it actually outputs.',
    '',
    'Steps, in order:',
    '1. Use the Write tool to save the following context and question to a temporary file at a path such as',
    '   D:\\bccode\\.agents\\tmp\\' + toolName + '-consult-input.txt.',
    '2. The file content must include the shared project context below, followed by the specific question.',
    '3. Run this exact command using the Bash tool (adjust only the file path if you saved it elsewhere):',
    '   cat D:\\bccode\\.agents\\tmp\\' + toolName + '-consult-input.txt | python ' + wrapperPath + ' --system "You are acting as an external design advisor for a Thai SME ERP project. Read the attached context and question carefully, then give a direct, specific, real answer."',
    '4. Return the raw stdout of that command back as your final answer, verbatim, with no summarizing, no',
    '   added opinion of your own, and no reformatting. If the command fails or times out, report the exact',
    '   error text you saw instead of inventing a fallback answer. Retry up to 3 times with increasing timeouts',
    '   before giving up and reporting the real failure.',
    '5. Delete the temporary file when you are done.',
    '',
    '=== Context to include in the file ===',
    CONTEXT,
    '',
    '=== Question to include in the file ===',
    question,
  ].join('\n')
}

phase('Consult')

const consultPrompts = [
  {
    label: 'deepseek-consult',
    phase: 'Consult',
    prompt: externalConsultInstructions(
      'deepseek',
      '~/.claude/tools/deepseek-ask.py',
      [
        'From a backend data-modeling and event-driven-architecture point of view, design the fix for the',
        'confirmed root cause above (Product has no Kafka event pipeline, so PostgreSQL `product` table never',
        'gets populated):',
        '1. Should Product get its own dedicated Kafka producer + topics (when-product-created/updated/deleted),',
        '   mirroring ProductBarcode exactly, or is there a leaner design (for example, since Product and',
        '   ProductBarcode are tightly related, could a single combined event/topic cover both, or should they',
        '   stay fully separate as they are now)? Give a clear recommendation with reasoning.',
        '2. Design the exact consumer logic for writing into PostgreSQL `product`: on create, INSERT with',
        '   itemcode/name0/unitcode/unitname (balanceqty/pending columns should presumably start at 0/blank,',
        '   confirm this is right); on update, should it be a full UPSERT (INSERT ... ON CONFLICT itemcode DO',
        '   UPDATE) to also self-heal any product that is somehow still missing a row, or a plain UPDATE that',
        '   assumes the row already exists? On delete, should the PostgreSQL row be hard-deleted (matching how',
        '   the ProductBarcode consumer prior task confirmed to be a hard delete) or soft-deleted?',
        '3. Any race-condition or ordering concern given `product` and `productbarcode` are both Kafka-driven',
        '   and the list query LEFT JOINs productbarcode onto product — could productbarcode rows arrive before',
        '   their parent product row in some real scenario, and if so does that matter for this specific query',
        '   shape (LEFT JOIN, not INNER JOIN, on the productbarcode side)?',
        '4. Any indexing or performance recommendation specific to the real `product` table schema described',
        '   above, given it will now receive live INSERT/UPDATE/DELETE traffic for the first time.',
        '5. Flag anything about this fix that should NOT be done in this pass (over-engineering risk) given the',
        '   project is pre-launch and this is a targeted bug fix, not a new feature.',
      ].join('\n'),
    ),
  },
  {
    label: 'glm-consult',
    phase: 'Consult',
    prompt: externalConsultInstructions(
      'glm',
      '~/.claude/tools/glm-ask.py',
      [
        'From a Go backend code-structure point of view, design the fix for the confirmed root cause above',
        '(Product has no Kafka event pipeline at all, unlike its sibling ProductBarcode which has a fully',
        'working one):',
        '1. Recommend the exact file layout for the new Product Kafka producer (where in',
        '   backend/internal/product/product/ should the emit calls go — inside product_http.go directly like',
        '   ProductBarcode does, or in a new dedicated file — match whatever the real ProductBarcode producer',
        '   file structure actually looks like, read it first) and the new consumer (should it be a new file',
        '   backend/internal/goapi/handlers/kafka/product.go, sibling to the existing inventory.go that handles',
        '   ProductBarcode, given inventory.go is really a misnamed ProductBarcode consumer file per the shared',
        '   context)?',
        '2. Recommend how to register the new consumer so it actually runs (the prior ProductBarcode consumer',
        '   registration bug this session was a config-loading issue, not a registration-code issue — make sure',
        '   your recommendation actually gets picked up by the real running mainapi process, following whatever',
        '   pattern the now-fixed ProductBarcode consumer registration uses).',
        '3. Flag any risk that adding a new Kafka producer to the Product create/update/delete HTTP handlers',
        '   could break existing behavior or slow down the request (should the Kafka publish be synchronous',
        '   in the request path, matching ProductBarcode\'s existing pattern, or should it be fire-and-forget —',
        '   match whatever ProductBarcode already does for consistency unless there is a strong reason not to).',
        '4. Flag any scope-creep risk for this task and what should be explicitly deferred.',
      ].join('\n'),
    ),
  },
  {
    label: 'gpt-consult',
    phase: 'Consult',
    prompt: externalConsultInstructions(
      'gpt',
      '~/.claude/tools/gpt-ask.py',
      [
        'From a requirements and real-world-correctness point of view: the /product screen has been showing an',
        'empty list for every tenant in this environment because of the backend bug described above. Once',
        'fixed, real products created going forward will sync correctly, but products created BEFORE the fix',
        '(including real sample data seeded in a prior task this session) will still be missing from the',
        'PostgreSQL `product` table since no historical backfill/replay has happened.',
        '1. Should the fix include a one-time backfill (read all existing Product documents from MongoDB for a',
        '   tenant and insert them into PostgreSQL `product` directly) so existing/sample data becomes visible',
        '   immediately, or is it acceptable to only fix it prospectively (new products work, old ones stay',
        '   invisible until edited/re-saved, which would re-trigger the now-working sync)? Give a clear',
        '   recommendation from a real business-usability standpoint, not just "technically correct".',
        '2. If a backfill is warranted, should it be a one-off script run once by the developer, or a real',
        '   HTTP endpoint/admin action Jead could re-trigger later if this ever needs to happen again (for',
        '   example after a database reset)? Recommend the simplest option that is not a throwaway hack.',
        '3. Flag anything else a real Thai SME business owner would care about here (for example, would they',
        '   notice/care about a brief delay between saving a product and seeing it in the list, given Kafka',
        '   sync is asynchronous)?',
      ].join('\n'),
    ),
  },
]

const consultResults = await parallel(consultPrompts.map((r) => () => agent(r.prompt, { label: r.label, phase: r.phase })))
const [deepseekConsult, glmConsult, gptConsult] = consultResults

const fableJudge = await agent(
  [
    CONTEXT,
    '',
    'You are the chief architect and final design judge for this task (Multi-Model Role Division Rule). Read',
    'the real current source yourself before finalizing: the real ProductBarcode producer calls (wherever they',
    'live in the Product module), the real consumer file backend/internal/goapi/handlers/kafka/inventory.go',
    '(the working reference pattern), and the real registration wiring in backend/main.go/',
    'backend/cmd/app/main.go for that consumer.',
    '',
    'Here are 3 real advisor consultations on this fix. Treat them as input, not ground truth; verify any',
    'concrete claim about this repo against the real files yourself.',
    '',
    '=== DeepSeek consult ===',
    String(deepseekConsult),
    '',
    '=== GLM consult ===',
    String(glmConsult),
    '',
    '=== GPT consult ===',
    String(gptConsult),
    '',
    'Write the definitive final design spec for this fix. It must cover, precisely and unambiguously:',
    '1. Exact Kafka producer design for Product (topic names, where the emit calls go, exact payload shape).',
    '2. Exact consumer design (new file location, exact SQL for create/update/delete against the real `product`',
    '   table schema, registration wiring so it actually runs).',
    '3. Whether a one-time backfill for existing/sample Product data is included in this pass, with reasoning.',
    '4. An explicit "do not build" list for anything the advisors suggested that is over-engineering for a',
    '   targeted bug fix.',
    '5. A concrete verification plan: create a real product via the real UI, confirm it appears in',
    '   PostgreSQL `product` within a reasonable time, confirm the real /product screen list shows it.',
    '',
    'Write this as a complete, concrete, implementable specification. Do not leave placeholders like "TBD".',
  ].join('\n'),
  { model: 'fable', label: 'fable-judge', phase: 'Consult', effort: 'high' },
)

phase('Implement')

const implementReport = await agent(
  [
    CONTEXT,
    '',
    '=== Approved final design spec ===',
    String(fableJudge),
    '',
    'Implement this spec precisely. If you find the spec conflicts with something real you discover in the',
    'source, fix it yourself, keep going, and clearly flag the deviation and why in your final report.',
    '',
    'Verify: Docker builder image backend build must exit 0. Rebuild and restart the real mainapi Docker',
    'container (buildx workaround if docker compose build cannot find BuildKit) so the fix is actually live,',
    'not just compiled. Create a real product via the real UI/API, wait briefly for Kafka, and confirm a real',
    'row now exists in PostgreSQL `product` for it (docker exec postgres psql). Confirm the real /product',
    'screen list now genuinely shows real products (not 0/0) via a live browser check.',
    '',
    'If the spec includes a backfill step, run it for the `test` tenant and confirm the previously-invisible',
    'sample data from the prior task this session (product codes containing "152349") now appears too.',
    '',
    'Report back precisely: exact files changed/created, the verbatim output of every verification command,',
    'and your live browser evidence that /product genuinely works now.',
  ].join('\n'),
  { label: 'implement-product-sync', phase: 'Implement' },
)

phase('Verify')

const verifyReport = await agent(
  [
    CONTEXT,
    '',
    '=== Implementation report to independently verify, do not just trust it ===',
    String(implementReport),
    '',
    'Your job is independent verification, following the uat-crud-mongo convention already used in this',
    'project: never trust a self-reported "done" without real evidence you generate yourself.',
    '',
    '1. Read the real git diff yourself, confirm it matches what was reported and is scoped to this fix only.',
    '2. Independently create a fresh, new real product via the real UI (do not reuse the implementer\'s test',
    'data), wait for Kafka, and confirm via your own `docker exec postgres psql` query that it landed in',
    '`product`. Update it and confirm the PostgreSQL row updates. Delete it and confirm the expected delete',
    'behavior (hard or soft, per the approved spec) actually happens.',
    '3. Load the real /product screen fresh in a browser and confirm the list genuinely renders real rows,',
    'including the sample data from the prior task (codes containing "152349") if a backfill was part of the',
    'spec — confirm this specifically since it was a real, previously-reported user-visible problem.',
    '4. Run the existing Playwright specs (product-crud.spec.ts, product-barcode-crud.spec.ts,',
    'product-set-crud.spec.ts, product-barcode-sample-data.spec.ts if it still exists) and confirm no',
    'regressions — pay special attention to any assertion in product-crud.spec.ts that previously documented',
    'the empty-list bug as expected/known, and fix that assertion now that it is resolved.',
    '5. Run the backend build one more time as a final sanity check, confirm no stray Go build cache',
    'directories were left in the repo root.',
    '6. Read D:\\bccode\\.agents\\worklog.md yourself first to find its real current last line, then append a',
    'new entry after it, and re-read the file after your edit to confirm the entry actually landed.',
    '7. Confirm all 3 consults from this task are scored into .agents/advisor-scorecard.md.',
    '',
    'Do not commit or push anything.',
    '',
    'End your report with the required Thai summary format: what changed, pros, cons/risks, and concrete next',
    'steps.',
  ].join('\n'),
  { label: 'full-verify', phase: 'Verify', effort: 'high' },
)

return { deepseekConsult, glmConsult, gptConsult, fableJudge, implementReport, verifyReport }
