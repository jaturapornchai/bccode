# AI Work Journal (cross-model)

Append-only running log so any agent (Claude Code / ZCode / Codex / GLM / any) sees what was already done, the key decisions, and known-pending items BEFORE starting — because Jead runs multiple models that do NOT share memory. Read this first; append a short entry after finishing meaningful work.

Rules for this file:
- Newest entry on top. Keep each entry 1-4 lines, source-linked (`path:line`) when useful.
- Log: features/fixes done, decisions made, rules/skills changed, and bugs found (with status).
- Do NOT store secrets, tokens, or connection strings.
- Detailed cross-layer pending work goes in `.agents/handoffs/`; this file is the short shared history + open-decision list.
- Prune entries once fully superseded or older than ~30 days.

## Open decisions / known-pending (clear when resolved)
- **Pre-existing PNG-guard bug** (uncommitted from a prior session): `resizeLogoFile` in `frontend/src/app/system-settings/system-settings-screen.tsx` hard-rejects non-PNG. Breaks: crop on logo/gallery (cropper outputs WebP → rejected), gallery (`imageuris`) non-PNG upload, shop logo (no `acceptTypes`). company/branch logo PNG-only is intended. Awaiting Jead decision to fix (give crop/gallery/shop-logo a WebP-capable resize helper).
- ~~Permission/access-screen business-code handling~~ — **RESOLVED 2026-06-21** (see log): uppercase+no-space normalization, duplicate guard, and business-code (not guid) display now implemented for all atlas code screens. Remaining future item: deep Go-level (mainapi) enforcement — currently enforced at frontend + Next proxy only.
- **8-screen access audit DONE (2026-06-21)** — 51-agent workflow, 28 confirmed findings. CRUD works on all 8 screens; search works on 6 (activelanguages=singleton edit-only, company=tree, both by design). Relationships are referentially consistent (branch→company by `companyguid` validated; scope rules store UPPERCASE `businesscode` + 5-digit `branchcode`; user/group/approval code-joins resolve against real collections; useraccessaudit expands group→permissions correctly and does NOT have the guid-header bug). **Generic bugs to decide on fixing (1 root → many screens):**
  - **Code normalization incomplete**: `normalizeBusinessCode` in `frontend/src/lib/business-code.ts` does only `trim().toUpperCase()` — it does NOT strip INTERNAL spaces, so company code `AB 01` keeps the space (violates the new no-space rule). And the atlas screens (`permissiondefinition`/`permissiongroup`/`approvalsetting`) never normalize their code field at all — `buildPayload` routes `permissioncode`/`groupcode`/`approvalcode` through the generic `String(value).trim()` branch (system-settings-screen.tsx:15359-15361): no uppercase, no space strip. HIGH.
  - **No duplicate-code guard** on atlas code screens: the only check is hard-coded to `slug==='user'` (system-settings-screen.tsx:1935-1953); `permissioncode`/`groupcode`/`approvalcode` can be created twice. HIGH.
  - **Detail header shows `guidfixed`** instead of the business code on permissiondefinition/permissiongroup/approvalsetting/permissionlink — `recordId` returns `config.idField` (=`guidfixed`) first (system-settings-screen.tsx:15652-15653; header 4214-4217). MEDIUM. (useraccessaudit is correct — shows username.)
  - Minor: activelanguages save back-fills `codetranslator` from "" to the code (mutates data); company soft-vs-hard-delete mismatch; company/branch name not required; approval scope not required.
  - **Suggested root fix**: (1) add internal-whitespace strip to `normalizeBusinessCode`; (2) normalize code-type fields (uppercase+nospace) in `buildPayload` for atlas screens; (3) generalize the duplicate-code guard beyond `slug==='user'`; (4) make the detail header prefer the business-code field over `guidfixed`. Awaiting Jead decision.

## Log (newest first)

### 2026-07-03
- **BACKEND — warehouse master-data redesign: warehouse/warehouselocation/warehousebin as 3 separate MongoDB collections, replacing the embedded Warehouse.Location[]/Location.Shelf[] shape (build+test verified via Docker builder)**. Per `scopeofwork/warehouse.md` + explicit task spec. `backend/internal/warehouse/models/warehouse.go` — dropped `Location *[]Location` from `Warehouse`; added `Status`; kept `WarehousePG` (uppercase, the LIVE Kafka-consumer projection struct — separate from `warehouse_pg.go`'s `WarehousePg`/lowercase, which is the `shop_http.go` dual-write struct and was correctly left untouched) trimmed to master fields only, dropped the now-dead `LocationsPG`/`Location`/`Shelf`/`ShelfProductBarcode`/bulk-op types. New `models/location.go` (`WarehouseLocation`: warehouseguid, code, names, locationtype enum, allowedproductclasses[], hazardclasses[], allowputaway/pick, blockedin/out, sortcode, status, companyguids) and `models/bin.go` (`WarehouseBin`: warehouseguid, locationguid, code, name, barcode, bintype enum, aisle/rack/level/positioncode, widthmm/lengthmm/heightmm/maxweightgram/maxvolumecm3/mintemperaturedeci/maxtemperaturedeci as **int64** per the dimension-decimal rule, allowedproductclasses[]/hazardclasses[]/fixeditems[] with NO qty field, allowputaway/pick, blockedin/out, sortcode, status — no companyguids, inherits from location). New repos `warehouse_location_mongo_repository.go`/`warehouse_bin_mongo_repository.go` (CrudRepository+SearchRepository generics, `EnsureIndexes` creating unique `(holdingcode,warehouseguid,code)` / `(holdingcode,warehouseguid,locationguid,code)` / partial-unique `(holdingcode,barcode)` on bin — called from `NewWarehouseHttp` constructor per Backend-Owned Schema Rule, never hand-run). New services `warehouse_location_http_service.go` (re-homed `validateLocationCompanyScope` subset-check, now unit tested) / `warehouse_bin_http_service.go`. Rewrote `warehouse_http.go`: CRUD `/warehouse` (no location field in response), CRUD `/warehouse/:warehouseguid/location`, CRUD `/warehouse/:warehouseguid/location/:locationguid/bin`, new `GET /warehouse/tree` (3 unpaginated Go queries assembled into nested JSON, no aggregation pipeline). Removed the old embedded-array Zone/Shelf/ProductBarcode-in-shelf handlers+service methods (dead once Location was removed). **Found+fixed 2 compile breakages the task brief didn't flag:** (1) `warehouse_consumer.go`→`warehouse_phaser.go`→`WarehousePG` Kafka-consumer path (registered live in `cmd/app/main.go:384`, separate from the `warehouse_pg.go` dual-write the brief called out) read `*doc.Location` — fixed by dropping Location from the phaser output (matches new model); (2) `productbarcode_http_service.go`'s `getProductBarcodesFromShelves` (feeds `GET /productbarcode?warehousecode&locationcode&shelfcode`) read the deleted `warehouse.Location[].Shelf[].ProductItems` — verified unreachable from the frontend (no caller sends those query params) and stubbed to return no matches, with a flag comment that a real fix needs a new `IWarehouseBinRepository` dependency threaded through 4 constructor call sites (productimport/eorder/mastersync/productbarcode_http) — **flagged, not fixed, out of scope for this backend-only phase**. `frontend/e2e/product-warehouse-crud.spec.ts` (old contract) and `warehouse-locations-editor.tsx`/`warehouse-tree-view.tsx` (frontend) will break — expected, frontend phase not started. Verified: `CGO_ENABLED=1 go build -tags musl ./...` EXIT 0 (whole module); `go test -tags musl ./internal/warehouse/...` EXIT 0 (validateLocationCompanyScope 4/4, phaser, UpdateWarehouse 2/2); `go test ./internal/product/productbarcode/...` EXIT 0; `gofmt -l` clean on both touched packages.

### 2026-06-29
- **UX — login portrait compact (iPad ไม่ต้องเลื่อน) — เอา portrait-header ออก + field เป็น 2-col row + ซ่อน first-use-note/subtitle (frontend, build+dev-verified)**. ลุงจืด "หน้าจอ ipad ต้องเลื่อน อยากให้แสดงทุกอย่างเลยไม่ต้องเลื่อนลง ลดขนาด font หรือทำเป็น row ได้". ต้นเหตุ = portrait-header (logo+h1+tagline) ที่รอบก่อนเพิ่มเอง (~120px) + first-use-note (3 ขั้นตอน ~140px, ซ้ำซ้อน brand info) + password-heading subtitle. Fix ใน portrait bracket `(768-1023px)`: (1) **เอา `.login-portrait-header` ออกทั้ง JSX + CSS** (ซ้ำซ้อน + สูง); (2) **holding-code + username เป็น `grid-template-columns:1fr 1fr`** (ใช้แนวนอน iPad); (3) **ซ่อน `.first-use-note` + `.password-login-heading small`** portrait; (4) login-card max-width 540→**620px** + ลด gap 16→10 + ลด padding. Estimated compact height ~516px vs iPad mini portrait 1024px = FIT (เหลือ ~476px spare). Files: `frontend/src/app/globals.css` (portrait bracket rewrite + remove dead `.login-portrait-header` styles), `frontend/src/app/login-screen.tsx` (remove portrait-header JSX). Verified: production build CSS `11crn5e7fzrr0.css` มี `1fr 1fr`+`max-width:620px`+`first-use-note{display:none}`+`768-1023px` ครบ; dev server :3000 CSS chunk มี rules ใหม่ (5×`1fr 1fr`, 3×`620px`, 2×`holding-code-field`); :3000 status 200. **LESSON (recurring):** ห้ามรัน `next build` ขณะ `next dev` รันอยู่ — ทั้งคู่ share `.next` คนละ format → dev chunks 500. ถ้าจะ verify prod build ต้อง kill dev ก่อน แล้ว restart dev หลัง build. (ผมทำพัง 2 รอบรีบ verify — แก้ด้วย kill+rm .next+restart dev ทั้งคู่).
- **UX — login responsive สำหรับ iPad mini/Air และจออื่น (frontend, build-verified)**. ลุงจืด "แก้หน้า login ให้เหมาะกับ ipadmini,ipad air และจอขนาดอื่นๆด้วย" — ตรง handoff pending (1). ปัญหาเดิม (source-verified): `globals.css` L946 `@media (901-1180px)` ซ่อน `.brand-panel` → form 460px ลอยกลาง whitespace มหาศาลบน iPad mini landscape (1024) + iPad Air landscape (1180). Fix: ลบ `(901-1180)` ออก + 3 breakpoints ใหม่ (iPad-first): (1) **≥1024px** = 2-panel hero+form เสมอ `grid-template-columns:minmax(0,1fr) minmax(420px,460px)` (ไม่ซ่อน hero อีก); (2) **768-1023px** (iPad mini 768 / Air 820 portrait) = single column, login-card 460→**540px**, เพิ่ม `.login-portrait-header` (logo+tagline ด้านบนการ์ด เพื่อ balance แนวตั้ง — CSS-gated `display:flex` เฉพาะ bracket นี้, hidden ทุกจออื่น); (3) **<768px** (phone, ไม่ใช่ priority) = compact 460px ใช้ได้ไม่พัง. byte-preserve patch globals.css (mixed-EOL — เฉพาะ block เล็ก ไม่ format ทั้งไฟล์). Files: `frontend/src/app/globals.css` (ลบ media `(901-1180)` + เพิ่ม `(768-1023)`/`(≥1024)`/`(<767)` + `.login-portrait-header` styles), `frontend/src/app/login-screen.tsx` (เพิ่ม `<div className="login-portrait-header">` logo+h1+tagline ก่อน `.login-card`). Verified: `tsc --noEmit` EXIT 0 + `next build` EXIT 0 + build CSS chunk `0pp7y1dotvf74.css` มี `@media (768-1023)`/`(≥1024)`/`(<767)` + `.login-portrait-header{display:flex}` + `login-card max-width:540px` + OLD `(901-1180)` grep ว่าง (gone) + client JS bundle `04-iwxac9lcgx.js` มี JSX `login-portrait-header` compiled (renders after hydration — login เป็น client-only mount เหมือน brand-panel เดิม). ทดสอบบน isolated port :3939 (prod build) ไม่รบกวน dev server :3000 แล้ว kill. **NOT deployed** — ต้อง `deploy dev` ถ้าจะเอาขึ้น .202.

### 2026-06-26
- **REMOVAL — เอาระบบ MCP ออกจาก active instruction/skill/rules ทั้งหมด (คำสั่งลุงจืด; scope = active instruction only)**: โค้ด MCP ถูกลบไปก่อนแล้ว (backend `c75594bc`, frontend 3 commits). รอบนี้กวาด instruction/skill/prompt ที่ยังอ้าง BC's MCP feature — ลบ skill `.agents/skills/mcp-tool/` + `backend/.continue/skills/mcp-builder/` + prompts `backend/prompts/mcp_guide.md`/`mcp_apikey_permission_preset.md`; แก้ MCP mentions ใน `AGENTS.md`/`AI_INDEX.md`/`backend/CLAUDE.md` (ลบ MCP-Server section + tree) + `bc-account-core-rules.md`/`project-context.md` (db-mcp/github-mcp + MCP-Architecture) + api-designer/bc-account-expert/llm-index (naming-rule example) + qcrud/handoffs-README + `.clinerules`/`.cursorrules` + architecture×2 + pgvector script; regenerate `CODE-MAP.md` (38→37 files, mcp/ หาย); ตัด mcp-builder ออกจาก `skills-lock.json`. **เก็บไว้ (justified)**: historical records (security-audit handoff, worklog, naming-audit wiki), agent-tooling `.mcp.json` (mongodb/docker/codebase-memory = Claude's own MCP, คนละเรื่อง) + `.playwright-mcp/` ignore, backend `.go` คอมเมนต์เชิงอธิบาย, data dumps. **Flag (ไม่แตะ — รอตัดสิน)**: dead i18n labels ใน `languages.tsv`, `chatbot_agent.md` (chat-spec นอก scope), `next.config.ts` denylist `/goapi/mcp/*` (harmless defense). Verified: repo grep เหลือ 16 ไฟล์ = 0 active instruction; skills-lock/.mcp.json JSON valid; CODE-MAP 0 mcp. ไม่ commit frontend staged (tab feature = งานอื่น).

### 2026-06-24
- **UX — active menu tab background made obvious**: active open tab now uses the primary background with white foreground, adjusted route text/icon/close button contrast. Verified `npm run typecheck` EXIT 0 and localhost computed style: active tab background `rgb(129, 41, 32)`, foreground white. File: `frontend/src/app/menu/main-menu-screen.tsx`.
- **UX — menu tabs no longer duplicate by default**: main menu item clicks now reuse an existing tab with the same route and switch to it; new `+` icon buttons in left/top menu item rows force opening a fresh tab when the user wants duplicates. Verified in localhost UI: clicking `สีสินค้า` twice kept one `/productcolor` tab; clicking `+` added another; preview reset back to left menu/home. File: `frontend/src/app/menu/main-menu-screen.tsx`.
- **UX — product option set wording + basic description field**: `/productvariantmatrix` now uses plain screen copy "ชุดตัวเลือกสินค้า" and a business-friendly subtitle, plus a `description` textarea labeled "คำอธิบายเบื้องต้น". Verified in localhost UI (header + create form) and focused config test; `npm run typecheck` EXIT 0. Files: `frontend/src/lib/system-setting-screens.ts`, `frontend/src/lib/system-setting-screens.test.ts`.
- **FIX/VERIFY — CRUD test for product option screens**: tested `/productcolor`, `/productsize`, `/productvariantmatrix` create/edit/delete via localhost frontend with DEV workspace `test`; all three CRUD flows pass and test records were deleted. Found + fixed matrix structured code normalization: `optioncode`, `valuecode`, SKU/barcode/unit/serial/media/spec/import code fields now use `normalizeBusinessCode` (uppercase + strip whitespace) instead of uppercase-only, so `COLOR X`/`BLACK X` saves as `COLORX`/`BLACKX`. Also fixed Next image aspect-ratio warning for flag icons in system settings/NamesEditor. Verified in UI + `npm run typecheck` EXIT 0. Files: `frontend/src/app/system-settings/system-settings-screen.tsx`, `frontend/src/components/product-barcode/names-editor.tsx`.

### 2026-06-22
- **RULES — added 2 cross-agent rules (Jead)**: (1) **Plan-First Rule** — after receiving ANY task/command, form+state a brief plan (goal/steps/files/blast-radius/smallest-safe approach) BEFORE editing; scale to task size; confirm first for ambiguous scope or R0/R1. (2) **Timezone Iron Rule** — MongoDB/PostgreSQL/ClickHouse store all timestamps in **UTC+0** (backend `time.Now().UTC()`, UTC across Kafka+PG/CH, UTC ISO-8601 `Z` in API); **frontend converts UTC → active branch `timezone` for display** via `frontend/src/lib/date-time.ts` (reinforces the existing "Project Date Display Contract" + branch-owns-timezone). Written to `bc-account-core-rules.md` (2 new sections) + `AGENTS.md` (Quality #8 plan-first, Code Rules timezone bullet) + skills go-expert/db-expert (UTC storage) + nextjs-frontend (branch-tz display) + `wiki/llm-index.md` (2 routing lines). rg-verified present + consistent. NOTE: this SETS the rule; existing timestamp-write code wasn't audited for UTC compliance (follow-up if needed).
- **FEATURE — กลุ่มสินค้า redesigned as an arbitrary-depth drag-drop TREE (mirror จัดหมวดสินค้า); removed ประเภทสินค้า (multi-agent workflow build + integration fix; backend deployed .202 + verified, frontend tsc/build clean)**. Per Jead: replace the 4 flat tabbed collections (productgroup/mastergroup/mastergroupsub1/mastergroupsub2) with ONE tree (กลุ่มหลัก→ย่อย→ย่อยเรื่อยๆ) + drag-drop, own session, DEV data fresh. Built via a 6-agent workflow (Map ×3 → Build ×3: backend tree / frontend tree workbench / menu). **Backend** (`internal/product/productgroup/`): added `parentguid`+`parentguidall`+`xsorts(*[]XSort)`+`childcount` to the model + `PUT /product/group/xsort` (batch reorder) + reparent via `PUT /product/group/:id`, faithfully mirroring productcategory. **Frontend**: new `product-group-tree-view.tsx` (copy/adapt of the 1671-line category tree-view — same dnd-kit reorder/reparent/move-to-root + inline editor, no groupNumber/codelist), wired `/productgroup` to render it (replaced PRODUCT_GROUP_TABS), removed the producttype + mastergroup* configs from `system-setting-screens.ts` + dead `menu-icons.ts` keys. **Menu**: removed product-type (`/producttypescreen`) from `menu-data.ts` (test updated, 16/16 pass). **Integration fixes I made (parallel-build divergence the orchestrator owns):** (1) the backend agent first used `xorder(int)`+`/move`+`/xorder`+`/tree` — realigned it to the category contract (`xsorts` + `/xsort` + PUT/:id) that the frontend+generic Next proxy expect; (2) found+fixed a create bug — the create/save/update handlers lacked the nil-init `if docReq.XSorts == nil { … &[]common.XSort{} }` BEFORE `ctx.Validate` (the form doesn't send xsorts → `unique=Code,dive` rejected nil with "xsorts must contain unique values") — added to all 3 handlers mirroring productcategory. **Verified (live .202):** create (with+without xsorts), subgroup (parentguid), reparent (PUT/:id), xsort reorder → mongo persists parentguid/parentguidall/xsorts; the /productgroup screen renders the tree workbench (title + "ลากวาง" subtitle + เพิ่มกลุ่มหลัก/เพิ่มกลุ่มย่อย, NO old tabs); create form opens. **NOT verified (harness limit):** the NamesEditor controlled input can't be filled via preview eval, so the full UI save round-trip wasn't click-driven — but its parts (form opens + backend accepts the no-xsorts payload + generic proxy) are all verified. Frontend NOT deployed to .202 container (localhost:3000 has it). producttype backend model/handlers still exist (unreachable from menu) — left for a later cleanup.
- **CLEANUP — removed redundant MongoDB-config path (Seed Env + client-config + `systemconfig`); setup config now 100% file-based (deployed .202, verified)**. Per Jead (config = file-based JSON per backend, not DB): "Seed Env" (`config/seed`) wrote ENV→MongoDB `systemconfig`, and `GET /api/setup/client-config` read it (for legacy Flutter API-URL fetch) — both DISCONNECTED from the live file-based config (config/get+save use bootstrap.json+custom_config.json; runtime config-load never reads systemconfig — only stale comments claimed it). Removed: **frontend** Seed Env button + `handleSeedConfig` + `config/seed` proxy allowlist entry + dead `"seed"` LoadingAction (`settings-screen.tsx`, `api/setup/.../route.ts`); **backend** `SetupSeedConfigHandler` + `SetupClientConfigHandler` + both routes (`bootstrap.go`) + `setupConfigCollection` const (`setup_config_handler.go`) + the `config/seed` api_catalog entry; fixed stale "MongoDB system_config" comments → "custom_config.json" in both `config.go`. KEPT `envFirst`+`os` (shared helper also used by `mongo_copy_uat_to_dev.go` — initial over-removal caught by builder compile, restored). **Data:** dropped the `systemconfig` collection (21 stale docs) on .202. Verified: backend compiles (CGO/musl builder), deployed mainapi healthz 200, `config/get`/`save`/`test-connection` still 200, `config/seed`+`client-config` unregistered (bootstrap.go 0 refs), Seed Env button gone from the localhost:3000 bundle; frontend `tsc`+`build` clean. **CAVEAT:** `/api/setup/client-config` was PUBLIC + comment said "Flutter apps เรียกตอน startup เพื่อรับ API URLs" — the active Next.js frontend doesn't use it and it returned empty on .202 (serviceurls never seeded), but a live legacy Flutter client (if any) that fetched API URLs from it would lose that endpoint. MongoDB now only stores the setup password (`systemsetuppassword`).
- **UX — Settings "บันทึก Config" → reload popup → back to login (frontend, verified end-to-end in preview)**. Per Jead: after saving config the backend reloads/reconnects, so return the user to login to re-enter with fresh config. `frontend/src/app/settings/settings-screen.tsx handleSaveConfig`: on `config/save` success → `setReloadingConfig(true)` (renders a fixed full-screen popup "กำลังโหลด config ใหม่… บันทึกแล้ว — ระบบกำลัง reconnect ฐานข้อมูล แล้วจะกลับไปหน้าเข้าสู่ระบบ" with a `Loader2` spinner) → wait 2.2s (backend `ReloadAndReconnect` is async) → `router.push("/")`. Added `useRouter` + `reloadingConfig` state. **Verified in Claude Preview (localhost):** /settings → enter setup pw `12345` → setup-connected → click "บันทึก Config" → confirm → popup shows → ~2.2s → redirected to `/` (login). Also confirmed the save did NOT clobber mongo `replicaSet=rs0` (uri arrives masked `***` → backend skips it). `tsc`+`build` clean; frontend-only (not deployed to .202 frontend container).
- **FIX — Settings "ทดสอบทั้งหมด" showed MongoDB/PG/ClickHouse ✗ (masked-secret test bug; deployed .202, verified)**. The connection test sent the FORM values to `SetupTestConnectionHandler`; `config/get` masks secrets as `"***"`, so an unchanged secret arrived as `"***"` and the backend literally tried to connect with password/URI `"***"` → MongoDB/PG/CH ✗ (Kafka/storage ✓ = no secret). Fix (`backend/internal/goapi/handlers/setup_config_handler.go SetupTestConnectionHandler`): if the secret is masked (`""`/`"***"`), substitute the real stored value via `setupconfig.ReadBootstrapAsConfigEntries()` (unmasked server-side, merged bootstrap+custom) before testing — mongo uri ← `mongodb.uri`, pg/ch password ← `<category>.password`; a genuinely edited secret is used as-is. Deployed mainapi .202, **verified live with `"***"`: MongoDB ✓ (4 dbs), PostgreSQL ✓ (PG 18.4), ClickHouse ✓ (appdb)** → "ทดสอบทั้งหมด" now 5/5. Backend-only (no frontend change). Setup password = MongoDB `systemsetuppassword` collection or default `12345`.
- **FIX — Settings "save ไม่จำ" / DB connect X: frontend generated mongo URIs WITHOUT `replicaSet=rs0` → clobbered the correct config (deployed .202 + frontend, verified)**. System config is **file-based per backend, NOT in DB** (Jead confirmed: the config that connects to DBs/other systems can't live in the DB). Layers: `bootstrap.json` (host-mounted **ro** base, has correct `…&replicaSet=rs0`) + `custom_config.json` (host-mounted **rw** override — where `config/save` writes; `config/get` merges both; persists across recreate). **Root cause:** the Settings screen built mongo URIs without `replicaSet=rs0` — `frontend/src/app/settings/settings-screen.tsx` `DOCKER_DEFAULTS.mongodb.uri` (used by **Auto-fill**) and `frontend/src/lib/setup-config.ts:371` (fields-mode URI builder, also missing `authSource=admin`). Auto-fill/save wrote the non-rs0 URI into custom_config.json, which **overrides** bootstrap.json's correct URI → backend would connect MongoDB **standalone** → transactions (create-holding) fail with IllegalOperation + the connection test shows MongoDB X (PG/CH X are a separate masked-secret test artifact — the test can't use the `***`-masked password). Fixed both frontend spots to emit `?authSource=admin&replicaSet=rs0`. Fixed the live `.202 custom_config.json` (jq-added `&replicaSet=rs0`) + restarted mainapi → **verified create-holding transaction works (rs0 active)**. Frontend `tsc`+`build` clean, localhost:3000 restarted. **CAVEAT:** if `ReloadAndReconnect` doesn't recreate DB clients on `config/save`, a mainapi restart may be needed for DB-connection edits to take effect (not verified).
- **FIX — duplicate jaturapornchai user records (root cause of email-login fail) + re-added localhost-only DEV bypass button (frontend, verified)**. (a) **Data dedup (DEV reset rule):** `users` had TWO records for `jaturapornchai@gmail.com` — `6a386d2e` (username=`jaturapornchai`, has bcrypt pw `smlsoft`) and `6a3872e1` (username=`jaturapornchai@gmail.com`, NO password — created by `LoginWithGoogleEmail` since Google login keys username=email). `Login(email)` hit the passwordless dup first → "login failed". Deleted the dup + set the survivor's `username=jaturapornchai@gmail.com` (matches shopusers + the Google flow, so Google login no longer creates a dup). Now `/login(email, smlsoft)` → success. **Latent bug for any user: Google login (`LoginWithGoogleEmail`) creates a username=email record that can duplicate an existing username≠email record — consider keying users by email consistently.** (b) **DEV button:** per Jead "เพิ่มปุ่ม dev แสดงเมื่อเข้าผ่าน localhost … ทะลุเข้าทดสอบด้วย jaturapornchai@gmail.com" — re-added a "🧪 เข้าทดสอบระบบ (dev)" button in `login-screen.tsx` that renders ONLY when `window.location.hostname` is localhost/127.0.0.1 (set post-mount; never on the deployed/public frontend). It calls the existing `performLogin({username:"jaturapornchai@gmail.com", password:"smlsoft", holdingCode:""})` → `/api/auth/login` → redirect `/holding`. Frontend-only; reuses `/login` (no new backend endpoint/backdoor). **Verified via Claude Preview (localhost):** button renders + click → logged in → `/holding` empty-state. `tsc` + `next build` clean. localhost:3000 server restarted. NOT deployed to .202 (hostname check hides it there anyway).
- **UX — removed all DEV-only login buttons; Google login is now the sole provider (deployed .202, verified)**. Per Jead "ยกเลิกปุ่ม dev ทั้งหมด ให้เข้าผ่าน google และให้ใช้ localhost:3000": deleted both DEV buttons from `frontend/src/app/login-screen.tsx` — "Test Google login" (`local-google-test-login` → `/api/auth/google/dev-login`) and "ทดสอบระบบ (jaturapornchai)" (`local-dev-system-test-login` → POST `/login` with hardcoded creds). Dead-code cleanup: removed `handleLocalGoogleTestLogin`/`handleDevSystemTestLogin` handlers, `loadLocalTestLoginAvailability` effect+caller, `isLocalTestHost`/`isLocalHost` state, `Beaker` + `isLocalLoginHost` + `LOCAL_GOOGLE_TEST_EMAIL` imports, `ProviderLoginState` union trimmed (`"idle"|"google"`), `performLogin` `isDev?: boolean` flag, `dev-test-badge`/`local-google-test-login`/`local-dev-system-test-login` CSS blocks in `globals.css`, `localGoogleTestLogin` i18n key across all 12 locales, `localGoogleTestLogin` entry in `i18n.test.ts`, and the entire `/api/auth/google/dev-login` route folder. **Also set `NEXT_PUBLIC_GOOGLE_CLIENT_ID=501250317679-05usebjtla636rm1an1dcdv3d0aeb4v1.apps.googleusercontent.com` in `frontend/.env.local`** (was missing — that's why the real GIS button wasn't rendering; the button code was always present at `login-screen.tsx:564` `gis-button-host` div). Deployed to .202: rebuilt `bc-frontend:latest` via `Dockerfile.onprem` with `--build-arg NEXT_PUBLIC_GOOGLE_CLIENT_ID=...` + container env, recreated `bc-frontend`. **Verified:** local `:3000` GIS button renders + clicks through to `accounts.google.com/o/oauth2/v2/auth?client_id=501250317679-...&origin=http%3A%2F%2Flocalhost%3A3000` (browser log shows popup was tried — popup-blocked at browser setting, not a code issue); `.202:3000` returns the page chunk `11ziinh1mrggj.js` with `501250317679` baked in. Frontend `tsc` clean. NOTE: the email-as-login backend fallback added earlier today is still deployed (no rollback) — harmless now that no UI sends email, but remains available for future use.
- **FIX — login-by-email for DEV test button (deployed .202, verified)**. Symptom: "ทดสอบระบบ (jaturapornchai)" button failed after DEV DB wipe (2026-06-21) — the button sends `username=jaturapornchai@gmail.com` (email) but `/login` only queried `users.username`. Also discovered a pre-wipe residue record `_id=6a37ebf2` with `username="jaturapornchai@gmail.com"` but **password missing** that hijacked the lookup (matched before the real user, then password check failed). Fix: (a) **BE** `internal/authentication/services/authentication_service.go Login()` — added email-as-login fallback: if `FindUser(username)` returns empty AND input contains `@`, retry via `authRepo.FindByIdentity("email", ...)` (follows the FindOne-swallow rule — tests decoded result, not err). (b) **Data** — deleted the passwordless dup `6a37ebf2` and set `email="jaturapornchai@gmail.com"` on the working record `6a386d2e` (DEV destructive reset rule; 1 deleted, 1 modified). Deployed: FE `bc-frontend:latest` rebuilt (`Dockerfile.onprem`) + mainapi rebuilt via `docker compose up -d --build --no-deps --force-recreate mainapi`; both healthy. **Verified live:** direct `/login` with email → `{"success":true,"token":...}`; via frontend proxy `:3000/api/auth/login` → same. Frontend `tsc` clean. **[Superseded same day]** — DEV buttons removed entirely (entry above), so this fallback is now unused by UI but kept for safety.
- **Frontend `next dev` Turbopack panic resolved (local only)**: stray `D:\bccode\frontend\nul` 0-byte file (created 2026-06-21, likely from a `> nul` redirect run in bash which doesn't treat `nul` as Windows null device) caused `@tailwindcss/postcss` auto-scan to fail with "Incorrect function. (os error 1)" → Turbopack panic on `/`. Deleted `nul`, cleared `.next`, restarted `npm run dev` → `/` compiles HTTP 200. **Same stray `nul` was on .202 (`/home/smlsoft/bc-frontend/nul`)** — removed during this deploy. **Lesson for all agents:** in bash use `> /dev/null`, never `> nul`; bash creates a literal file named `nul`.

### 2026-06-21
- **DOCS/RULES handoff for next agent (GLM) — codified this session's conventions cross-agent**. Added two new rules to `.agents/rules/bc-account-core-rules.md` + concise mirrors in `AGENTS.md` Code Rules: **"Uploaded Image Format Iron Rule (PNG/JPG only)"** (user uploads PNG/JPG only — FE accept+guard, FE encoders PNG/JPG-no-WebP, BE `image_r2.go` ext+content-type guard, thumbnails kept; static `.webp` page-background assets exempt) and **"Global Toast Notification Rule"** (use `@/lib/toast`+`<ToastViewport/>`, never per-screen inline notice banners). Updated skills: `nextjs-frontend` (toast + image-format bullets), `go-api-handler` (image upload guard + `FindOne`-swallow not-found gotcha). Updated `.agents/wiki/llm-index.md` with image-format / toast / FindOne routing lines. Wrote `.agents/handoffs/2026-06-21-glm-handoff.md` (done vs pending: **frontend NOT deployed** — toast/empty-state/image-accept are local-verified only; old `.webp` S3 objects not migrated; rotate bootstrap secrets; report-execute handlers unregistered in mainapi). Verified rule headers present + no contradiction with the existing static-asset `.webp` rule via `rg`. No Claude private-memory entry (conventions now live in repo rules — avoid duplication).
- **UX — image upload now PNG/JPG only (drop WebP from INPUT), picker filtered (frontend)**. Per Jead on the system-settings shop image fields (รูปภาพ/รูปหน้าปก): in `frontend/src/app/system-settings/system-settings-screen.tsx` — file-picker `accept` default `"image/png,image/jpeg,image/webp,image/gif"` → `"image/png,image/jpeg"` (both ImageUploadField + the imageuris gallery, lines ~11799/12077); helper text `imageUploadHint` (all 12 languages) "JPG, PNG, WebP …" → "JPG, PNG …"; and the upload validation (both components) now rejects any type outside the field's `acceptTypes` (default PNG/JPG; PNG-only logo fields stay PNG-only) with a clear Thai/EN message instead of accepting any `image/*`. PNG-only company/branch logo (`company-branch-tree-view.tsx`, `accept="image/png"`) unchanged. Verified: `tsc --noEmit` clean + `next build` OK + grep confirms no WebP/gif left on the input side. NOT deployed to Jead's frontend (local verify only); live deep-settings render not exercised (needs an authed session + selected holding). **CAVEAT (not changed):** the resize/thumbnail/crop pipeline still encodes STORED files to WebP internally (smaller/faster; thumbnails intentionally WebP) — input/filter only, per the literal request. Product-menu image upload (`product-screen.tsx`, `accept="image/*"`) is a separate component, left as-is. **[UPDATE — Jead "ทุกจอเลยนะ เอาแค่ png/jpg": extended to ALL screens]** Changed the remaining permissive `accept="image/*"` → `image/png,image/jpeg` (+ a `file.type` PNG/JPG guard with Thai/EN message) in `menu/product-screen.tsx` (TabProductMedia: main + gallery) and `components/product-barcode/barcode-form.tsx` (TabMedia: main + gallery; threaded a `language` prop in for the message). Audited every `type="file"` in the app: all image inputs now PNG/JPG only; the PNG-only transparent logo (`company-branch-tree-view.tsx`, `accept="image/png"`) stays PNG-only by design (still within png/jpg); CSV bulk-import inputs (marketplace, tab-product-marketplace) are unrelated. `tsc` clean + `next build` OK. **[UPDATE 2 — Jead "file ที่เก็บใน s3 ต้องเป็น png หรือ jpg เท่านั้น, thumbnail ก็ต้องมี": storage side now PNG/JPG only, deployed + verified .202]** The upload pipeline previously stored WebP. Backend `internal/goapi/handlers/image_r2.go ImageUploadHandler` is now the authoritative guard: ext allowlist tightened `jpg/jpeg/png/gif/webp/bmp` → `jpg/jpeg/png`, PLUS a `http.DetectContentType` check rejecting anything but `image/jpeg`/`image/png` (catches a webp/gif renamed `.jpg`). Frontend client encoders changed WebP→**JPG (PNG source stays PNG for transparency)**: `resizeImageFile`, `makeThumbnailFile` (thumbnails kept, just JPG/PNG now), and `ImageCropDialog` outputType/ext (`system-settings-screen.tsx`); `resizeLogoFile` already emitted PNG; image-upload-proxy fallback filename `image.webp`→`image.jpg`. Verified live on .202 (real uploads): PNG→200, JPEG→200 (stored key `...hash.jpg`), WebP→400 (ext), webp-renamed-`.jpg`→400 (content-type). Backend deployed (mainapi healthy, healthz 200); frontend `tsc`+`next build` clean but NOT deployed to Jead's running frontend.
- **FEATURE — global bottom-right toast notifications (replaces per-screen inline notice banners; frontend, verified in preview)**. New `frontend/src/lib/toast.ts` = framework-agnostic singleton store (`notify(kind,text,duration=5000)`, `toast.{success,error,info,warning}`, `dismissToast`, `useToasts()` via `useSyncExternalStore`, max 5 stacked, auto-dismiss 5s) + `frontend/src/components/toast-viewport.tsx` (`"use client"`, fixed bottom-right, reuses themed `.message`/`.success`/`.error`/`.info` classes so no globals.css edit, role=alert for errors, manual close, `motion/react` animation) mounted once in `src/app/layout.tsx`. Compatibility shim `pushNotice({type,text}|null)` lets every screen migrate with near-zero churn: `const setNotice = pushNotice` (or a `useCallback` wrapper where `textKey` is used, e.g. workspace). Migrated **13 screens** (workspace manual + a 12-agent workflow): currency, holding, line-oa, main-menu (2 notices), marketplace, product-barcode, product-barcode-shelf, product-price-history, product, product-set, product-category-items-editor, system-settings (notice was plumbed as a prop through 3 sub-components — removed 3 render blocks + 6 prop passes + types). All inline `{notice ? <div className="message …"> … }` banners deleted (0 residual `notice` reads), dead imports/types trimmed. Verified: `tsc --noEmit` clean + `next build` OK + preview (temp debug hook) — 4 kinds render bottom-right (16px/16px), correct themed classes + `role` (error→alert), present 700ms after fire, auto-dismiss confirmed (count→0 after duration); debug hook removed + rebuilt clean. Frontend-only; NOT deployed to Jead's running frontend (local verify only). Added `frontend-prod` (`npm run start`) config to `.claude/launch.json`. NOTE: interactive/persistent "info" notices (e.g. LINE "waiting") now also auto-dismiss at 5s per Jead's directive ("ทุกสถานะ 5 วินาที").
- **FIX/UX — workspace "เลือกบริษัท" empty-state setup button was dead (frontend, verified in preview)**. `frontend/src/app/workspace/workspace-screen.tsx` empty-state button "เริ่มตั้งค่าระบบ" was `disabled={busy || accessShopOptions.length === 0}` — but it renders exactly when the user has 0 accessible holdings, so it was permanently un-clickable (the screenshot case: holding `bcdemo01` selected, 0 shops). Its `onClick` (`openAccessSettings`) also can't work with 0 holdings (it needs a representative shop to select). Redesigned: new `handlePrimarySetup()` — when `accessShopOptions.length === 0` route to `/holding` (the real create/select-กลุ่มกิจการ screen), else open the access/language setup; both the empty-state button and the header button now use it, are context-aware ("เลือกหรือสร้างกลุ่มกิจการ" vs "เริ่มตั้งค่าระบบ" / "เลือกกลุ่มกิจการ" vs "ตั้งค่าระบบ"), and are `disabled={busy}` only. Heading/desc also switch to the no-holding wording. Verified: `tsc` clean + `next build` OK; preview (injected google auth + holdingcode, 0 shops) → empty state renders, both setup buttons `disabled:false`, clicking "เลือกหรือสร้างกลุ่มกิจการ" navigates to `/holding`, 0 console errors. Frontend-only; no backend change. (NOT deployed to Jead's running frontend — local verify only.)
- **SKILL hardening + codebase audit for the `FindOne`-swallow bug class (skills updated; 2 latent instances found+guarded, deployed .202)**. Encoded the lesson from the select-holding fix into skills so no agent repeats it: **go-expert** ("Mongo not-found semantics — `FindOne` SWALLOWS, `FindByID` does NOT": `PersisterMongo.FindOne` returns nil err + zero struct on no-match, so existence/fallback/duplicate/not-found decisions must test the decoded result for emptiness, never `err != nil` alone) + **bc-account-expert** ("Membership / shop-access resolution": resolve shopuser by `useruid` then fall back to `username` on an EMPTY result, not only on error, because token uid drifts from stored useruid). Then ran a **17-agent audit workflow** over all 232 `FindOne` call sites (8 areas, map→adversarial-verify): **9 suspects → 2 REAL_BUG (same class), 7 false-positive**. Both reals are in the report-execute service: `reportquerym/services/reportquery_http_service.go:304` (HIGH — `FindOneByCode` miss → `*findDoc.Fields` nil-ptr **panic**) + `reportqueryc/.../:263` (LOW — empty SQL → confusing ClickHouse error). FIXED both with a `findDoc.Code == ""` not-found guard (+ nil `Fields` guard). **Compile-verified** (CGO+musl builder image, both packages OK) and redeployed mainapi (healthy, healthz 200). **HONEST caveat:** these handlers are registered only in `cmd/app` (port 8080; reportqueryc is even commented out there) — the deployed **mainapi (root `main.go`, 8888) does NOT register them**, so `/reportm/execute` + `/report/execute` 404 and the panic is **NOT reachable in the running build**; the guards are defense-in-depth for if those handlers are ever wired into mainapi. Obsidian: `bugs/2026-06-21-select-holding-holdingcode-invalid.md`.
- **FIX — select-holding (and login-with-holding) wrongly returned "holdingcode invalid" for valid members (DONE + deployed .202, verified)**. Root cause: uid-drift + `PersisterMongo.FindOne` swallows "no documents" → returns a zero struct with NIL error. So `findShopUser` (and 2 sibling call sites in `internal/authentication/services/authentication_service.go`) only fell back to the username lookup on `err != nil`; when the token's uid ≠ the `useruid` stored on the membership (happens for a by-email membership created before first login, or a stale/old token), the uid query "succeeded empty", the fallback was skipped, and `AccessShop` saw `shopUser.ID == NilObjectID` → "holdingcode invalid". Fix: 3 spots now also fall back when the uid lookup matched nothing (`|| shopUser.ID == primitive.NilObjectID`) — `findShopUser:391`, `processUserLogin:323`, `UpdateFavoriteShop:913` (mirrors the same defensive `Username==""` guard already in `shopuser_repository.FindByHoldingCodeAndUsername`). VERIFIED live with a stale token (token uid `3EjpEvCK…` ≠ DB uid `3FRtbEfip…`): create-holding `bctestflow` → ok; **select-holding `bctestflow` → `{"success":true}` (was `holdingcode invalid`)**; token holdingcode set → downstream `/goapi/*` passes the "Shop not selected" middleware (handler-level 400, not 401). create-holding writes shop/shopuser/warehouse/organizationbranches/shopuseraccesslogs/organizationbusinesstypes (no PG tenant db). Test holding cleaned up; token holdingcode reset.
- **MongoDB converted to single-node REPLICA SET `rs0` (.202) — fixes "(IllegalOperation) Transaction numbers are only allowed on a replica set member or mongos"** on create-holding (and any flow using `pkg/microservice/persister_mongo.go` transactions; the DEV mongo was a standalone `mongod` which can't do transactions). Chose replica-set (matches production, no code change) over removing the framework-level transaction. Steps: created a keyFile (owner mongodb/999, chmod 400) in `/data/mongodb/keyfile` via a one-off root container; recreated the `mongodb` container (mongo:7.0, same volume/network `bc-backend_app-network`/port 27017) with `--replSet rs0 --keyFile /data/db/keyfile --bind_ip_all`; `rs.initiate({_id:"rs0",members:[{host:"mongodb:27017"}]})` → PRIMARY; appended `&replicaSet=rs0` to `bootstrap.json` mongodb.uri; restarted mainapi. VERIFIED: rs.status PRIMARY, mainapi healthy/healthz 200, a real Mongo transaction commits OK (was IllegalOperation). Admin mongosh from host must use `&directConnection=true` (RS member host `mongodb:27017` only resolves on the app-network). Data preserved (host volume `/data/mongodb`).
- **DEV DATABASE WIPE / fresh start (DESTRUCTIVE — DEV reset + Disposable-DEV-DB rule; deployed .202)** — Jead: "ล้างข้อมูลทั้งหมด เริ่มใหม่ (mongodb,pgsql,clickhouse) ห้ามกระทบตัวอื่น". Scoped to BC only: **MongoDB `appdb` — dropped all 25 collections = 7191 docs** (shopuseraccesslogs 6855, shopusers 21, users 19, organizationbranches 38, units 118, organizationcompanies 8, approvalsettings 10, permissiondefinitions 30, images 27, systemconfig 19, mcpapikeys 1, etc.); **PostgreSQL `appdb` (1 table) + `bctest01` (0 tables) TRUNCATEd**; **ClickHouse BC `appdb` = 0 tables (already empty, nothing to wipe)**. **UNTOUCHED ("ห้ามกระทบตัวอื่น"):** ClickHouse **`altcoin` (16 tables — separate project)**, all system DBs (mongo admin/config/local, pg `postgres`, ch system/information_schema), and infra (redis/kafka/minio — Jead named only the 3 DBs). mainapi restarted (clears pools + in-mem aiprovider cache) → healthy, healthz 200, appdb auto-recreates collections on first write (fresh). NOTE: all users gone → next Google web login re-creates the owner via find-or-create; existing Redis tokens are now useless. New data will be written with the corrected bson keys from today's fixes.
- **MongoDB↔source AUDIT + round-2 fixes (DONE + deployed .202, mainapi healthy + frontend tsc clean)** — read-only audit workflow (46 agents, 5 dims, adversarial-verified) found **61 ADDITIONAL** silent mismatches beyond the 44 (3 critical, 49 high, 7 med, 4 low); a 6-cluster fix workflow then fixed them (**35+ changes**, each struct-confirmed). Highlights: **approval/PO workflow was silently broken** — `handlers.go:1512` `required_level`→`requiredlevel` (pending-approvals filter always returned empty), + `current_approved_level`/`last_comment`/`source_*`/`purchase_type_name`/`cust_name`/`opened_at`/`opened_from`/`sent_at`(sort) across handlers+notification (approval level/comment/read-tracking never persisted); **datahistory** `screen_type`→`screentype` ×4 (PO create-vs-update detection broken); **image_r2 slip-verify** ~13 `$set` keys + `file_name`→`filename` ×6 (slip OCR data silently lost); productimport (`is_main_barcode`/`category_names`/`brand_code`), smlaiproduct (`groupMainGuid`/`groupSubGuid` camelCase), department `branch_key`, documentimage `doc_guid_ref`/`uploaded_at`/$pull `document_image_guid`, aiprovider `last_error_at`; **frontend** change-password (`current_password`/`new_password`→backend `currentpassword`/`newpassword` — was fully broken), test-conn `error_code`/`latency_ms`/value `databasenotexist`, createDefaultBranch (phone/address/22 business flags snake_case→json tags), dev-login keys, `delete_many`, dead `company_name`. Bonus latent fix: notification `line_userid`→`lineuserid`. Agents correctly LEFT HTTP query-params + c.JSON response keys (separate FE↔BE contract) + keys with no struct tag. Audit confirmed CLEAN: bson struct tags (1272 scanned, 0 column-leak/uppercase; `bson:"inline"` verified valid), collection names (no new mismatch). Verified: full CGO Docker build + mainapi healthy + frontend tsc EXIT 0 + redeployed both. **NOTE:** these were SILENT bugs (no errors) → pre-existing DEV data under wrong keys is disposable per the Disposable-DEV-DB rule.
- **MongoDB↔source mismatch fixes (round 1, DONE + deployed .202, mainapi healthy + frontend tsc clean)** — 8-agent workflow, **82 changes** across 7 clusters; each agent read the struct **bson tag = source of truth** before editing; SQL/ClickHouse columns left untouched (correct snake_case). (1) CRITICAL bson tags: `saleinvoicereturn.go` `csalechannelode`→`salechannelcode`; `transaction.go`/`saleinvoice.go` 5× stripped leaked GORM `column:` prefix. (2) HIGH Mongo filter keys: auth `phone_number`/`country_code`→`phonenumber`/`countrycode` (login-by-phone was broken); `goapi/process/build/*` `holding_code`→`holdingcode` + `deleted_by`→`deletedat` (productbarcodes→`deletedby`, verified per-collection); lineoa `lineoa_type`/`channel_*`/`session_id`→struct tags; coupon `*_id`/`used_at`/`canceled_at` + **3 extra found** (`sale_invoice_id`/`sale_invoice_number`/`coupon_code`); accountperiodmaster/documentimage(`task_guid`)/member(`line_uid`)/filestatus(`job_id`)/shopuser(`$shopInfo.main_holdingcode`)/approval(`employee_code`). (3) Dead code: deleted `handlers/dataimport/` + `product_balance.go` + `transaction_check_reference.go` + 3 orphan funcs (caller-grep-verified; kept thai_nlp helpers used by product_search.go). (4) Frontend: `workspace-models.ts` `is_favorite`/`last_accessed_at`/`is_creator`→`isfavorite`/`lastaccessedat`/`iscreator` across all readers + `system-settings route.ts` holdingcode dedupe. `branch.go:109 addressx` LEFT (no handler uses "address" — naming-only, not corruption). Verified: full CGO Docker build OK, `up -d --build --force-recreate mainapi` healthy, frontend `tsc --noEmit` clean + redeployed.
- **bson `id`→`_id` Mongo fix (DONE)**: changed all 10 MongoDB ObjectID structs from `bson:"id,omitempty"` to `bson:"_id,omitempty"` so the primary key unmarshals (was always NilObjectID after decode). 5 were REAL bugs shipping zero-id to clients → by-id calls 404'd: MCP API keys (`keys_repo.go:25/41`, list→get/update/delete/export), attachments (`attachment-model.go` download/delete), images (`image-model.go` delete/verify), pdf history (`pdf-history-model.go` reprint). 4 approval structs (`approval/handlers.go`,`notification.go`) + datahistory were LATENT (identity carried by guidfixed/token). Left untouched: `internal/models/index.go` (string+gorm PG model, bson vestigial), test/cmd. Built+deployed mainapi .202; PROVEN: create MCP key→list returns real `_id` (was `000…`)→delete-by-id works.
- **SECURITY FIXES (round 3, DONE + deployed .202, mainapi healthy)**: #5 setup config/get masks ALL secrets (broadened `isSecretKey`→token/api_key/uri/*key; verified 0 unmasked) + get-raw requires a key; #11 LINE webhook HMAC-SHA256 signature verify (per-tenant ChannelSecret); #13/#16 MCP searchcustomers keyword QuoteMeta + real `names[]` decode (phantom name0 fixed). **14/16 audit findings + denylist now addressed** (all criticals + all highs + most mediums). LEFT: #12 ClickHouse fails-open (latent, CH disabled), #14 MCP hard→soft delete (low value under the new Disposable-DEV-DB rule), #13 other MCP-tool regex variants. **Jead still: rotate bootstrap.json secrets.**
- **Rule added — No-Migration / Disposable DEV Database Rule** (`bc-account-core-rules.md` + `AGENTS.md` + `backend/CLAUDE.md`): in DEV the mongo/pgsql/clickhouse data+structures are disposable — change BE+FE freely (models/collections/fields/contracts), no migration/backfill/dual-write/compat shims; DB recreated from code. Overrides the "keep existing fields / dual-read-write" cautions in DEV. PRODUCTION still needs real migration.
- **SECURITY FIXES (round 2, DONE + deployed .202, mainapi healthy)** — 8 more findings fixed (see `.agents/handoffs/2026-06-21-security-audit.md`): #2/6 goapi tenant guard now inspects `database`/`dbname` (cross-tenant PG 403); #3 reportm forces holdingcode filter; #4 MCP /invoke forces holdingcode from key; #7 MCP key creation gated to owner/admin; #8 MCP key RNG→crypto/rand; #9 MCP-key IDOR (Export/Get/Update/Delete tenant-checked); #10 /goapi/getdoc SQLi parameterized; #13 product-barcode `q` regex escaped+capped; #15 MCP tool catalogs require auth. Built+deployed in 3 batches. STILL OPEN: #5 (LAN+default-pw→Jead rotate secrets), #11 LINE HMAC, #12 CH latent, #14 MCP soft-delete, #16 name0, #13 MCP-tool variants.
- **SECURITY FIXES (round 1, DONE + deployed .202)**: (1) **#1 /googlelogin account-takeover CLOSED at backend** — now requires + verifies a real Google ID token (tokeninfo: aud/iss/exp/email_verified), email derived from verified claims, email-only→401 (verified), bogus→401, mainapi→Google outbound confirmed. `authentication_http.go` verifyGoogleIDToken + `models/authentication.go` Credential + `config.go` GoogleClientId + FE `auth/google/verify/route.ts` forwards credential. **Jead must re-test web Google login in-browser** (interactive). (2) **Stopgap denylist** (`frontend/next.config.ts`) blocks public `/backend/{goapi/get,goapi/exec,goapi/getdoc,reportm/*,goapi/api/setup/*,goapi/api/mcp/*,goapi/mcp/*}` → verified 404 via tunnel, benign goapi/version→200. Closes PUBLIC surface for #2-5,7,9,15 (LAN still open). NOTE: minting a test token via `curl /googlelogin -d '{email}'` NO LONGER WORKS (needs a real Google credential now).
- **SECURITY & ROBUSTNESS AUDIT (26-agent workflow) — see `.agents/handoffs/2026-06-21-security-audit.md`**: 16 confirmed findings (6 CRITICAL, 5 high, 3 med, 2 low). Headlines: /googlelogin account-takeover (no Google token verify); cross-tenant PG read/write via body `database` field on /goapi/get+exec; NoSQL-injection+cross-tenant on /reportm/playground; MCP /invoke caller-controlled holdingcode → raw SQL; **unauthenticated bootstrap.json secret dump (default pw "12345")**; MCP key RNG ~62 keyspace; cross-tenant key IDOR /export; SQLi /goapi/getdoc; unauth LINE webhook DoS. Root theme: the Next denylist is the only public boundary & covers only login routes → most criticals reachable at app.bcaicloud.com/backend/**. **NONE fixed — awaiting Jead priority.** Recommend: rotate all bootstrap.json secrets now; extend denylist as stopgap; then server-side tenant-from-token + Google-token verify.
- **Naming rule CHANGED → lowercase, underscore ALLOWED** (Jead; supersedes the old no-underscore rule) in `AGENTS.md`/`bc-account-core-rules.md`/`AI_INDEX.md`/5 skills/3 wiki. Project-wide case audit (workflow + codebase-memory-mcp): only **7** mongo collection-name violations, ALL goapi — fixed all 7 const→lowercase (productGroups→productgroups, productCategories→productcategories, transactionPurchaseOrder/Requisition→…, aiProviderConfigs→aiproviderconfigs ×2 files, kbDocumentMetadata→kbdocumentmetadata) + 4 MCP-tool `bson:"id"`→`bson:"_id,omitempty"`. bson(5959 tags)/json/pg/ch tags all clean. mainapi rebuilt+deployed .202 (healthy, regression OK). All camelCase target collections were absent → no data migration.
- **DEV DB standardized to lowercase (runtime, .202) [DESTRUCTIVE — DEV reset rule]**: MongoDB appdb had 3 UPPERCASE orphan collections `shopUsers`/`organizationBranches`/`organizationBusinessTypes` (1 stale doc each; active data is in lowercase twins shopusers=21 / organizationbranches=38 / organizationbusinesstypes=6; runtime code references none — only `cmd/branch_code_audit/main.go:118` lists one) → **DROPPED all 3**. Now appdb=24 collections, 0 uppercase; doc top-level field keys clean (sampled 30/coll). PostgreSQL (appdb, bctest01) + ClickHouse (appdb) audited = 0 uppercase. Also: `barcode_list` schema-probe now process-cached.
- **Still open**: ~10 `bson:"id,omitempty"` (should be `_id`) in goapi active features (approval ×4, datahistory, mcp/mongodb/keys_repo ×2, models attachment/image/pdf-history) — separate `_id`-exemption rule, minor behavior change, flagged NOT fixed.
- **Feature — user avatar thumbnail on EVERY user-facing screen** (24-agent workflow found 11 unique gaps; 10 fixed frontend-only, 1 needed backend). `LogoAvatar` gained a `fallbackIcon` prop (people screens show a person icon, not Building2) + a `useProfileAvatar(auth)` hook (best-effort GET /profile). Wired avatars into: system-settings user **detail header** + **employee** list/detail (via `profilepicture`) + **permissionlink** list/picker/chip + **useraccessaudit** list; **menu** account trigger+dropdown (widened `ProfileResponse` to carry avatar/avatarthumb from /profile); **workspace / holding / line-oa** "signed-in-as" (hook). All data already present (`/holding/users` ShopUserProfile, `/profile`, employee `profilepicture`) → no backend change for these 10. Verified live: user detail header renders the real avatar image; workspace signed-in layout OK; /profile returns avatar+avatarthumb; typecheck+build clean, 0 console errors.
- **Feature — price-history "ผู้ทำรายการ" avatar (REQUIRED backend)**: `ProductPriceHistoryInfo` +`CreatedByAvatarThumb json:"createdbyavatarthumb" bson:"-"` (`models/product_price_history.go`); new handler helper `enrichPriceHistoryAvatars` (`productbarcode_price_history_http.go`) batch-looks-up `users` collection by `createdby` username (`$in`, projection username/avatar/avatarthumb — no password), best-effort, called in both GetPriceHistory + GetPriceHistoryByBarcode. Frontend `product-price-history-screen.tsx` renders `<LogoAvatar uri={createdByAvatarThumb}>`. **Deployed mainapi 192.168.2.202 (docker build OK, healthz 200).** Verified end-to-end by inserting a temp price-history record (createdby=bcseed.user01) → endpoint returned the correct `createdbyavatarthumb` path (`enrichWorked:true`) → temp record deleted (DEV restored to 0). Note: DEV bctest01 has 0 products / 0 price-history naturally.
- **DEV server IP migrated `45.144.166.112` → `192.168.2.202`** everywhere (0 occurrences remain): `frontend/src/lib/backend-url.ts` (dev-backend host allowlist) + `backend-url.test.ts`, plus rules/AGENTS/backend CLAUDE.md DEV-deploy sections rewritten to the on-prem SSH (`smlsoft@192.168.2.202`, key-based) tar+docker method. (Pre-existing unrelated `backend-url.test.ts` failure at the "accepts any host" case re port `:8888` — not caused by the IP edit.)
- **Cleared all remaining pending items** (4): (1) **serial/pricecode enforcement** — marked `productserialregistry.serialno` + `channelprice.pricecode` as `businessCodeField` (FE) and added `productserialregistries`/`productchannelprices` to the backend registry; verified `sn ab 01`→`SNAB01`/409, `pr 99`→`PR99`/409. (2) **pre-existing PNG-guard bug FIXED** — `ImageCropDialog` now takes an `outputType` (logo fields crop to PNG, others to WebP) and the image GALLERY uses a new generic `resizeImageFile` (any format → WebP, no PNG-only guard); shop logo got `acceptTypes:"image/png"` for consistency. So gallery non-PNG uploads and logo/gallery crops work again. (company/branch logo in the tree view has no crop and stays PNG-only-by-design.) (3) **Dead `SettingCard` component removed** (206 lines, was unused; typecheck passes). (4) **Owner dummy avatar cleared** — `jaturapornchai` `avatar:"test-avatar-url"`→`""` via PUT /profile (other fields preserved); the user-screen 404 console error is gone. All deployed (mainapi 192.168.2.202) + frontend rebuilt + verified.
- **UI regression test (8 access screens) + fix**: walked all 8 menus — all render OK, no GUID leaked anywhere, business codes shown correctly. Found + FIXED a plain-language violation: `loadRecordDetail` (system-settings-screen.tsx ~1388/1406) threw user-facing errors containing the word "MongoDB" ("ไม่พบรายละเอียดจาก MongoDB" / "Missing record ID for MongoDB detail loading") — rewritten to plain Thai/English without the DB jargon. Remaining (NOT a code bug): the owner account `jaturapornchai` has a leftover dummy avatar value `test-avatar-url` that 404s (2 console errors on the user screen) — it's creator-account data; clearing it safely needs an owner profile edit, left for Jead to overwrite with a real image.
- **Rule added — DEV Destructive Reset Rule** (`AGENTS.md` + `bc-account-core-rules.md`): in DEV the agent may delete old data / drop old structures (collections, indexes, fields) and recreate them, and change backend+frontend together to stay consistent, WITHOUT asking — when needed for correctness. DEV-only; must confirm DEV + log deletions. R0 still guards production.
- **Coverage: extended business-code enforcement to product-master atlas screens.** Marked `productcolor.code`, `productsize.code`, `productvariantmatrix.code` as `businessCodeField` (frontend) and added `productcolors`/`productsizes`/`productvariantmatrices` → `code` to the backend `atlasBusinessCodeFields` registry. Verified live: productcolors `red 01` → stored `RED01`, duplicate `RED 01` → HTTP 409. Scanned ALL 6 holdings × all code collections — 0 dirty, 0 duplicate, so the unique indexes build cleanly (the earlier "best-effort index" worry is moot — no destructive cleanup was needed). NOT enforced (intentional, uncertain): `channelprice.pricecode` (uniqueness unclear — could false-block) and `productserialregistry.serialno` (serial case-sensitivity) — flag for Jead before enforcing.
- **Identity-code duplicate guard for `employeecode` (permissionlink)** — NOT uppercased (email/username exempt), only trimmed, with a CASE-INSENSITIVE duplicate guard. Frontend: new `uniqueCode?` field flag + `uniqueCodeField` helper (`system-setting-screens.ts`), `employeecode` marked, `saveRecord` case-insensitive guard. Backend: `atlasIdentityCodeFields` registry (employeepermissions→employeecode) in `mongodb_atlas.go` — trim-only + case-insensitive `$regex` duplicate check + partial-unique index. Verified live (direct /atlas/update): `QAtest.Dup@Example.com` stored as-is (not uppercased); a 2nd `qatest.dup@example.com` → HTTP 409. Deployed mainapi 192.168.2.202 + frontend rebuilt. This closes the employeecode gap noted in the previous backend-enforcement entry.
- **Backend (Go mainapi) now ENFORCES the business-code rule** — closes the "future item" from the no-space fix. `backend/internal/goapi/handlers/mongodb_atlas.go`: added `atlasBusinessCodeFields` registry (permissiondefinitions→permissioncode, permissiongroups→groupcode, approvalsettings→approvalcode), `normalizeAtlasBusinessCode` (uppercase + strip whitespace), an in-handler duplicate-code guard (409), and a best-effort partial-unique index `(holdingcode, codeField)` via `ensureAtlasCodeIndex`. So even MCP/AI/mobile clients calling `/atlas/update` directly (bypassing the Next proxy) get normalized + duplicate-checked. Email/username code fields (employeepermissions.employeecode) intentionally excluded (email exempt from uppercasing). Deployed mainapi 192.168.2.202 (healthy). Verified live by calling `/goapi/atlas/update` directly: `qa direct 99` → stored `QADIRECT99`; a 2nd `QA DIRECT 99` → HTTP 409 "duplicate code: QADIRECT99". This is approach A + B from the A/B/C question; C (split permission/approval into dedicated non-generic handlers) was deliberately NOT done — it would duplicate A and require changing the frontend `/atlas/*` proxy contract for every atlas screen (redundant). The schema-aware part of C lives in the registry.
- **UX/UI audit + fixes on the 8 access screens** (37-agent workflow, 26 confirmed). FIXED: (a) truncation of business names/codes → `break-words` in ~10 spots (detail header `system-settings-screen.tsx:4232/4235`, generic atlas cell 4165, user list cells 3991/3997/4003, user picker 6127/6130/6177/6181, menu matrix 6503/6506, audit list 6918/6919, branch sidebar 13363, branch scope 7898; company/branch tree names `company-branch-tree-view.tsx:650/752`). (b) GUID leak — `recordTitle` fallback now lists business codes before `guidfixed`, and `recordDisplayCode` falls back through `recordBusinessLookup` when idField is a GUID, so permissionlink shows `employeecode` not a GUID. NOTE: deliberately did NOT flag `employeecode` as `businessCode` because that would uppercase emails (email is exempt). (c) a11y — `.bc-list-row:focus-visible` ring + reduced-motion for utility animations (`globals.css`), aria-label on back/remove-scope/chevron buttons, `role="dialog"`+aria-modal on the confirm modal. Verified live: permissionlink detail shows `bcseed.user01@example.com` not a guid; typecheck + test pass. DEFERRED (not done): Edit3→Pencil consistency, deleting the dead `SettingCard` component (system-settings-screen.tsx:3211-3415, unused — spawned a task), toolbar ตัวกรอง + เลือกเพื่อลบ multi-delete (new features), and refactoring NamesEditor/RadioOptionGroup into shared modules.
- **Fixed generic access-screen bugs (enforces the no-space rule in code)**: all 4 root fixes done frontend-side + server-proxy. (1) `normalizeBusinessCode` (`frontend/src/lib/business-code.ts`) now uppercases + strips ALL whitespace. (2) new `businessCode?` flag on `SystemSettingField` + `businessCodeField` helper; `permissioncode`/`groupcode`/`approvalcode` marked; `buildPayload` normalizes flagged fields (uppercase+no-space) and the Next proxy `route.ts` re-normalizes server-side (`normalizeBusinessCodeFields`). (3) duplicate-code guard generalized beyond `slug==='user'` (uses `recordMatchesBusinessLookup`). (4) new `recordDisplayCode` → detail/card header shows the business code, never `guidfixed`. Verified live: `qa test 99` → stored `QATEST99`; `QA TEST 99` → "รหัสนี้มีอยู่แล้ว"; detail header shows the code not a GUID. typecheck + business-code.test pass. NOTE: Go mainapi atlas handler is still generic (not schema-aware) — server-side enforcement currently lives in the Next proxy + frontend; deep Go-level enforcement is a future item.
- **Rule added — Business Code No-Space Rule**: business/reference codes must be uppercase AND contain no whitespace (e.g. `qatest perm 01` → `QATESTPERM01`). Added to `.agents/rules/bc-account-core-rules.md`, `AGENTS.md`, `.agents/skills/bc-account-expert/SKILL.md`. Documentation only — not yet enforced in code.
- **Tested permission screens (menu 4 กำหนดสิทธิ์หน้าจอ)** read-only-ish via UI+API: Add/Delete/confirm-dialog/structured-accessrules-editor work; found bugs → see open-decisions above. Test data cleaned up.
- **Feature — User avatar thumbnail**: avatar now keeps the ORIGINAL image untouched (full-res, any format) and uploads a small 256px WebP thumbnail; lists/preview use `avatarthumb`, detail uses `avatar`. Added `thumbnailKey` to `SystemSettingField` + `imageUploadField`; `makeThumbnailFile`, `uploadImageFile` 2-file upload, `buildPayload` carries `thumbnailKey`, crop disabled for thumbnail fields. Backend: `UserDetail`/`UserRoleRequest`(*ptr)/`ShopUserProfile` +`avatarthumb`, `saveUserLoginProfile` writes it, service copies in 2 read paths. Deployed mainapi on 192.168.2.202 (healthy). Verified upload/save/clear/no-touch/persist + thumb 4.3KB vs original 549KB.
- **Fix — User avatar not saving**: root cause was avatar dropped in 3 places (not a missing endpoint). `UserRoleRequest` lacked `Avatar` (now `*string`, nil-safe), `saveUserLoginProfile` didn't write avatar, `ShopUserProfile`/read paths didn't return it. `/holding/permission` already saved user profile (name/email). Frontend unchanged. Deployed 192.168.2.202.
- **Governance**: model-agnostic — all agents read the same rules, do the whole task themselves, no advisor/dual-track/cross-AI routing. A real second opinion = Jead (human).

## 2026-06-22 — Timezone Iron Rule audit + fixes (DB=UTC+0, FE=branch tz)
- Scope: full-codebase audit for the new Timezone Iron Rule; fix all real violations (DEV data disposable, ignored existing data).
- Frontend (only 2 real date-display violations — rest of 46 `toLocaleString` are number formatting, left untouched):
  - `app/system-settings/system-settings-screen.tsx` user-access-audit "report generated" timestamps (loadedAt 6943, generatedAt 6993): `new Date(x).toLocaleString(th-TH|en-US)` → `formatDefaultDateTime(x, resolveWorkspaceDateTimeDisplayOptions(workspace, language))` (lib/date-time.ts → active branch `timezone`). Added the two imports.
  - Non-violations confirmed & left: `timezoneUtcOffset` (16515, tz-offset util), `datePart`/`datePartsForLanguage` (lib/system-setting-screens.ts 1889, fixed-sample format-pattern preview, intentional UTC), all `*.toISOString()` (UTC data serialization to backend / date-only form values), `*.length/total*.toLocaleString(localeOf(language))` (thousands separators).
  - Verified: `npx tsc --noEmit` EXIT 0.
- Backend (PG session timezone is the only real storage misalignment; Mongo bson + ClickHouse DateTime already persist UTC by driver):
  - `internal/config/config_postgresql.go` `TimeZone()` default `Asia/Bangkok` → `UTC` (mainapi DSN via pkg/microservice/persister.go). Keeps now()/CURRENT_TIMESTAMP/`::timestamp` casts in UTC. Override per-deploy via bootstrap.json postgres.timezone.
  - Added explicit `TimeZone=UTC` to all goapi PG DSNs (were relying on server default): mydb/connection_pool_manager.go, mydb/unified_database_manager.go (x2 incl. admin `dbname=postgres`), mydb/brand_db.go, mypg/utils.go, mypg/fast_utils.go.
  - Confirmed compliant (no change): mypg/timezone_utils.go (UTC-boundary date filters via explicit `AT TIME ZONE`), productimport_http.go `time.Local=time.UTC`, transaction.go `Timezone` metadata field.
  - Verified: `go build ./internal/config/... ./internal/goapi/mydb/... ./internal/goapi/mypg/...` EXIT 0, `go vet ./internal/config/` EXIT 0. (pkg/microservice fails only on CGO/librdkafka kafka symbols — host constraint, unrelated.)
- Known minor (display-layer, NOT a storage violation): approval/notification.go formats LINE-push timestamps with hardcoded ICT (UTC+7) instead of branch tz; defensible for a Thai-centric ERP, left as-is.
- Not deployed (backend redeploy + frontend .202 rebuild needed to observe; DEV deploy requires explicit "deploy dev").

## 2026-06-22 — Product-group (กลุ่มสินค้า) CRUD test + fixes + DEV deploy
- Tested the product-group tree screen CRUD end-to-end against live DEV (192.168.2.202): full lifecycle (create root/child, list-tree, get, rename Thai-UTF8, reparent, xsort reorder+swap, delete, verify-gone) all PASS.
- Adversarial code-review flagged a HIGH "only one group creatable (code-uniqueness on empty code)" — REFUTED empirically: guard is `if findDoc.Code != ""`, so empty codes are never blocked; creating multiple groups works. Lesson: verify static-review findings against the running system before acting.
- Confirmed + fixed real bugs (DEV, disposable data):
  - #2 (HIGH): DeleteProductGroup / DeleteProductGroupByGUIDs only blocked on product-barcode ref, not child groups → deleting a parent silently orphaned children (FE re-roots dangling parentguid). FIX: block delete when a child exists (FindByDocIndentityGuid "parentguid"==guid; excludes soft-deleted), error "ไม่สามารถลบกลุ่มสินค้าที่มีกลุ่มย่อยได้ กรุณาย้ายหรือลบกลุ่มย่อยก่อน". (User chose block over cascade.)
  - #4 (MED): config sent `isdisabled` but models.ProductGroup had no IsDisabled field → silently discarded (dead toggle). FIX: added `IsDisabled bool json/bson:"isdisabled"` (mirrors productcategory).
- Not fixed (by decision): #3 childcount not recomputed server-side (UI derives from in-memory tree), #5 drag-error inline banner vs global toast, #6 no server-side reparent cycle guard.
- Verified: models pkg `go build` EXIT 0; full productgroup pkg compiled in Docker (golang:1.26-alpine + librdkafka-dev, `-tags musl` CGO) EXIT 0.
- Deployed to .202 (user-authorized): tar backend EXCLUDING bootstrap.json/custom_config.json (verified server bootstrap md5 unchanged before/after extract = not clobbered), patched server bootstrap.json postgresql.timezone Asia/Bangkok→UTC (completes the mainapi Timezone fix), `docker compose up -d --build --no-deps --force-recreate mainapi`, HEALTHZ 200.
- Post-deploy functional test on live .202: #4 isdisabled true/false round-trips; #2 delete-parent-with-child → HTTP 400 block + parent retained; regression delete child→parent ok; cleanup total=0.
- NOTE: changes are deployed to .202 but NOT committed to git (uncommitted working tree). Commit before any git-based redeploy or the fixes are lost.

## 2026-06-22 — Fix false-positive "ยังไม่ได้บันทึก" unsaved-changes guard (system-settings CRUD)
- Symptom (ลุงจืด): the unsaved-changes confirm dialog fired every time you switched records even WITHOUT editing anything.
- Root cause: frontend/src/app/system-settings/system-settings-screen.tsx had two guards keyed only on `formOpen` (openEdit ~line 1777, list onSelect ~line 3223): `if (formOpen && idChanged)` warned whenever a form was merely OPEN and another record was selected — no dirty check.
- Fix: added `isFormDirty` (useMemo) that compares the canonical payload of the current form vs its baseline — `buildPayload(form, editing, ...)` vs `buildPayload(baseline, ...)` where baseline = formFromRecord(editing) for edit mode or defaultForm(+parentguid) for create; raw-form fallback if workspace/auth null. Both guards now gated `formOpen && isFormDirty && idChanged`. buildPayload normalizes both sides (ignores readOnly fields like parentguid + transient shape), avoiding re-introducing false positives from benign mount normalization. Tree screens reuse the parent openEdit, so they are covered too.
- Verified: tsc --noEmit EXIT 0; production `next build` EXIT 0; live UI on Claude Preview (autoPort, fresh build → .202 backend): (1) open group A untouched → click edit B = NO dialog, switches directly (false-positive gone); (2) toggle the disabled checkbox on B (genuinely dirty) → click edit A = dialog DOES appear (real protection intact). Seed/cleanup of DEMO groups done; preview stopped.

## 2026-06-23 — Fix "กลุ่มสินค้า ย้ายไปมา หายหมด" (product-group tree vanishes after reparent)
- Symptom (ลุงจืด): dragging/reparenting product groups made ALL groups disappear from the tree.
- Root cause (verified via live API + UI): a reparent can create a CYCLE in parentguid (A→C→B→A). Confirmed empirically — backend has no cycle guard (#6, PUT accepts it, data NOT lost) and the FE move-guard checked `targetGroup.parentguidall` (a string that goes STALE on descendants after a move, so the cycle slips through). frontend/src/app/system-settings/product-group-tree-view.tsx treeRoots builder put every node with a present parent into childGroups and only parent-less nodes into `roots`; in a cycle EVERY node has a present parent → `roots = []` → tree renders empty = "หายหมด" (groups are fine in DB, just unrenderable).
- Fix (FE, minimal, 2 parts):
  1. Cycle-tolerant builder: walk parentguid upward with a visited-set (`chainReachesRoot`); a node whose chain loops never reaches a root → surface it as a root (flat) instead of dropping it, and never link cyclic nodes into childGroups (no render-recursion loop). Tree can no longer vanish regardless of cycle source.
  2. Cycle prevention: new `isWithinSubtreeOf(nodeGuid, ancestorGuid)` walks the LIVE override-aware parentguid graph (not the stale parentguidall string). moveGroupAsChild blocks when target is inside the dragged subtree; reorderGroup blocks when the new parent is the dragged node or inside its subtree.
- Verified: tsc EXIT 0; production next build EXIT 0; live Claude Preview (fresh build → .202) with a deliberately seeded cycle (A→C→B→A) → /productgroup renders all 3 cyclic groups flat as roots (5 rows incl. 2 pre-existing) with แก้ไข/ลบ — NOT empty. Pre-fix the same data rendered 0 rows. Cyclic test data cleaned up (break-cycle then delete; note the #2 child-guard blocks deleting cyclic nodes until parent is cleared).
- Recommend (optional, deferred earlier as #6): add a backend cycle guard in UpdateProductGroup so non-UI clients/API can't store a cycle. FE is now safe both ways (prevents + tolerates).

## 2026-06-28 — Rule expanded: No-Migration / Disposable Database now covers Kafka + ALL environments
- Jead set rule: "ไม่ต้องสนใจข้อมูลเก่า (mongodb, pgsql, clickhouse, kafka) เปลี่ยนได้เลย เดินหน้าอย่างเดียว". Confirmed scope = ALL environments incl. production.
- Updated `.agents/rules/bc-account-core-rules.md` line 69: renamed "Disposable DEV Database Rule" → "Disposable Database Rule"; added Kafka (topics/events/consumer-offsets) to disposable stores; scope expanded DEV-ONLY → ALL env incl. prod (rationale: pre-launch, no real customer data).
- Safety kept (DISSENT/R0): added a **go-live trigger** — the instant real customer/prod data exists, the disposable rule STOPS for prod data and real migration + R0 stop resume. Unsure if real prod data exists → ask Jead, do not assume disposable.
- Net effect: schema/contract/topic changes go straight to the correct shape in code; no migration/backfill/dual-read/compat-shim scaffolding for old data anywhere, until go-live.

## 2026-06-28 — Multi-format document number builder (per branch) + advisor consult
- Jead redesign: คำนำหน้าเลขที่เอกสารต่อสาขา จาก prefix เดี่ยว/ประเภท → หลายรูปแบบ/ประเภท. ปรึกษา GLM+DeepSeek (reset-mode, gap/void, ปีภาษี, branchcode token) แล้วสังเคราะห์.
- Backend (`organization/branch`): `BranchDocPrefix{doctype,prefix}` → `BranchDocFormat` 13 fields (doctype,name,prefix,usebranch,yearmode[none|be2|be4|ce2|ce4],usemonth,useday,separator,runlength,resetmode[never|yearly|monthly|daily],startnumber,enabled,isdefault). `DocumentPrefixes`→`DocumentFormats`. `normalizeAndValidateDocFormats` ใน prepareBranchUpdate: prefix uppercase+A-Z0-9, กันว่าง, กัน prefix ซ้ำทั้ง branch (ข้ามทุก doctype). Compile EXIT 0 (Docker golang:1.26-alpine CGO -tags musl).
- Frontend (`company-branch-tree-view.tsx`): `DocFormatBuilder` component — cards ต่อประเภท, เพิ่ม/ลบรูปแบบ, ดาว=ค่าเริ่มต้น+badge, ตัวอย่างเลขสด (buildDocExample), กัน prefix ซ้ำ real-time (แดง)+กัน save. ลบ helper เก่า normalizeDocPrefix(2-char)/cleanDocPrefixOrEmpty. tsc 0, UI verified (add SO → SO26062800001).
- **DEFERRED (config-only ตอนนี้):** generator เลขจริง, counter table + period_key + row-lock, gap/void no-reuse, ปีภาษี vs ปฏิทิน reset, book/series counter. ก่อน lock กฎภาษี (รัน per ปีภาษี) ต้อง verify rd.go.th.
- **NOT deployed:** commit b36b4127 บน working tree เท่านั้น. ต้อง `deploy dev` (.202) เพื่อให้ backend ใหม่เก็บ documentformats — backend .202 ปัจจุบันยัง schema เก่า (frontend save documentformats จะถูก ignore จนกว่า deploy).

## 2026-06-28 — Deploy dev (.202): documentformats backend + UI (drawer/sidebar/wide layout)
- Deployed BOTH to on-prem .202 per Jead "deploy dev".
- **Backend mainapi:** tar (exclude bootstrap.json/custom_config.json) → scp → extract → `docker compose up -d --build --no-deps --force-recreate mainapi`. Verified: healthz 200, **bootstrap.json md5 unchanged 7dbfef35 (not clobbered)**, login regression OK, branch API now returns `documentformats` (new struct live). mainapi healthy.
- **Frontend bc-frontend:** tar (exclude node_modules/.next) → scp → `docker build -f Dockerfile.onprem --build-arg NEXT_PUBLIC_GOOGLE_CLIENT_ID=...` → `docker rm -f` + `docker run` recreate with same config (port 3000, network bc-backend_app-network, env BCAI_LOCAL_BACKEND_URL/GOOGLE_CLIENT_ID/NODE_ENV, restart unless-stopped). Verified .202:3000 = 200 serving new UI (drawer + collapsible sidebar + wide form).
- **ISSUE (separate from deploy, FLAGGED):** public `https://app.bcaicloud.com` unreachable — `nslookup` = **Non-existent domain** (DNS record gone at Cloudflare). cloudflared service is `active` on .202, container serves locally fine. So the Cloudflare DNS/tunnel record for app.bcaicloud.com needs re-checking — NOT caused by this deploy. Jead to verify Cloudflare DNS if public access is wanted.
- Pushed 8 commits to GitHub origin/dev (1014ce93).

## 2026-06-28 — Handoff to ZCode + dev fully-local stack running
- Set up DEV fully-local stack (per new topology): frontend localhost + backend/mongo(rs0)/pg/ch/kafka/redis/minio all on local Docker Desktop via `docker-compose.local.yml` (committed) + gitignored `bootstrap.local.json`/`Dockerfile.local`/`custom_config.local.json`. Seeded dev user `jaturapornchai@gmail.com`/`smlsoft` + holding `test` + company/branch. Verified end-to-end (dev login → /holding). `.env.local` → `localhost:8888`.
- Also deployed this session's work to `.202` (backend documentformats + frontend drawer/sidebar/wide-form UI), bootstrap md5 unchanged.
- **Wrote full handoff for ZCode: `.agents/handoffs/2026-06-28-zcode-handoff.md`** — current state, done-list, and PENDING (priority): (1) Login UX for iPad mini/Air [breakpoint hero <1024, portrait form 520-560px; ⚠️ globals.css mixed-EOL = byte-preserving patch], (2) air hot-reload for backend, (3) stale R2/Atlas doc cleanup.

## 2026-06-30 — Marketplace scope rule + field-mapping doc; flagged duplicate channelmappings
- Jead: set rule "system = accounting + POS + back-office + close (ปิดงบ) + marketplace (Shopee/Lazada/TikTok/AliExpress), 100% data in/out, must reference official docs"; then find real Shopee data as example.
- Web-researched (IRON RULE 6) the 4 marketplaces' product/variation/SKU API field structures (Shopee tier_variation/model_id/tier_index/get_model_list; Lazada SPU→Product→SKU/SellerSku/SkuId/SaleProp; TikTok skus[].sales_attributes/sku_id/inventory; AliExpress aeop_ae_product_sku/sku_code/sku_attr/ipm_sku_stock).
- Added rule `.agents/rules/bc-account-core-rules.md` "System Scope & Marketplace Support". Wrote `.agents/wiki/marketplace-field-mapping.md` (contract→API field table for all 4 + Shopee worked example + official doc URLs + honest status: schema ready, connectors NOT built). Added llm-index pointer.
- **KEY FINDING (DISSENT):** a COMPLETE marketplace contract already exists on the product/SKU layer — Go `MarketplaceProductMap`/`MarketplaceSKUMap`/`MarketplaceDimensionStock` (`product/models/product.go` + `productbarcode/models/product_barcode.go`, w/ tests), frontend `product-barcode/types.ts`, UI `tab-product-marketplace.tsx`/`marketplace-screen.tsx`, `/channelprice`. Comments map fields to Shopee/Lazada/TikTok directly. The `channelmappings` field I added earlier on `productvariantmatrix` (option-set TEMPLATE) DUPLICATES this on the wrong entity (marketplace listings are per product/SKU, not per template).
- **PENDING DECISION (Jead):** revert `channelmappings` (recommended — use existing contract) vs keep. Anchored rule+doc to the existing contract regardless. Earlier kept changes still valid: dimension items `[]` default fix, placeholder-aware json default.

## 2026-06-30 (cont.) — Resolved: single-master marketplace principle; reverted channelmappings
- Jead decision: "master ตัวเดียว รองรับทุก marketplace, ยอดคงเหลือเดียวกัน, ราคาต่างกันต่อ channel, ยอดพร้อมขายต่างกันต่อ channel" = exactly what the existing MarketplaceProductMap/SKUMap/DimensionStock contract does (customprice/platformprice per channel; availableqty/oversellbufferqty per channel; shared accounting stock).
- REVERTED the duplicate `channelmappings` (field + structured editor + nested skuref + select-kind) from `system-setting-screens.ts` + `system-settings-screen.tsx`. tsc EXIT 0, no leftover refs. KEPT (independently useful): dimension items `[]` default fix; placeholder-aware json default (`[`-placeholder → `[]`).
- Rule updated with the one-master principle (System Scope & Marketplace Support). Obsidian decision note rewritten to the final decision.
- Lesson: check product/productbarcode datamodel before adding any marketplace field — the canonical contract already exists.

## 2026-06-30 (cont.) — Tested marketplace screen with REAL Shopee data → found producttype contract bug
- Jead: open Shopee, get real data, enter it into our /marketplace screen, see if it works.
- Got REAL Shopee item_id/shop_id from live listing URLs (pattern `-i.{shopid}.{itemid}`): e.g. MEMEE cotton tee shop_id 395187627 / item_id 43252260289.
- Seeded 1 product-barcode (MEMEE tee, barcode 8859MEMEE0001) in tenant `test` (dev DB was empty). Drove `/marketplace/shopee` (MarketplaceMappingsScreen) via Stagehand: menu-search → open "เชื่อม Shopee" → tab "ตารางจับคู่สินค้า" → "นำเข้าไฟล์จับคู่สินค้า (Bulk Import)" → paste real Shopee import row → "นำเข้ารหัสจับคู่ทั้งหมด". Nav + barcode MATCH worked (Row 2 matched).
- **BUG (confirmed, real, blocking):** import save fails — `json: cannot unmarshal number into Go struct field ProductBarcodeRequest...producttype of type models.ProductType`. Root cause = **frontend/backend contract mismatch**: frontend `product-barcode/types.ts` `export type ProductType = number` (line 395) + `rawToProductBarcode` coerces `producttype` via `getNumber` (utils.ts:403, default 0); backend `models.ProductType` is a STRUCT `{DocIdentity, Code, Names}` (`productbarcode/models/product_type.go`). So load(object)→coerce(number 0)→save(number)→backend 400. Any product re-saved through the marketplace import hits this (not seed-specific). Note: `product-barcode-screen.tsx:1440` also READS producttype as a number → fixing needs care (consumed-as-number elsewhere); possible cascade (other struct fields in the full re-save).
- PENDING DECISION (Jead): fix is R1 (shared product-barcode contract). Options: (a) align FE producttype to backend struct, (b) marketplace-screen-local: preserve raw producttype object before updateBarcode, (c) backend tolerant unmarshal. Seed MEMEE product left in `test` (disposable) — can delete.

## 2026-06-30 (cont.) — Marketplace import: producttype fix verified; remaining blockers = seed-incompleteness
- Applied targeted fix in `marketplace-screen.tsx` import: preserve raw `producttype` struct from the GET payload before `updateBarcode` (FE types it as number → coercion broke backend unmarshal). tsc EXIT 0; the producttype 400 is eliminated (verified via Stagehand import re-run).
- After that, save hit `names must contain at least 1 item` (validator, not a literal Go string) — traced to my minimal API-seeded product (producttype.names null; a complete product wouldn't hit it). Raw-API product seeding is unreliable: product-barcode DELETE returns `{success:true}` but does NOT remove the row (left duplicate MEMEE barcodes in tenant `test`, disposable). Did NOT chase further (scope/whack-a-mole).
- CONCLUSION: screen UI+logic verified working with REAL Shopee data (nav, connected-account display [Ban Chiang, 142 maps], mappings tab, barcode list, bulk-import paste+parse+MATCH). One real bug (producttype) fixed. Full green save needs a product created via the real Product UI (complete nested fields), not a minimal API seed. Root producttype fix (align FE `type ProductType = number` → backend struct, + `product-barcode-screen.tsx:1440` reads it as number) still pending — R1 shared contract.
- Note: product-barcode DELETE not actually deleting is a separate suspected bug (flagged, not investigated).

## 2026-06-30 (cont.) — Marketplace import: root producttype fix + raw-resave fix done; green-proof needs a real product
- TASK 1 (root producttype): FE `type ProductType = number` → struct `{guidfixed,code,names}` (`product-barcode/types.ts`); `rawToProductBarcode` now preserves the struct (`utils.ts`); empty default → `{code,names:[]}`; `product-barcode-screen.tsx` reads producttype.code (was getFirstNumber→0). Removed the earlier local marketplace workaround. tsc 0.
- TASK (import save): rewrote the Bulk Import save in `marketplace-screen.tsx` to modify the RAW product doc from GET directly (add marketplaceproducts + refbarcodes.marketplaceskumappings) and PUT it back, instead of re-saving through the lossy display transform `rawToProductBarcode` (which dropped producttype + names). Removed now-unused `rawToProductBarcode` import. tsc 0.
- TASK 3 (delete "not working"): ROOT CAUSE = (a) create IGNORES client-sent `guidfixed` and the create-response `id` is NOT the queryable guidfixed; the real guid comes from the LIST. (b) the list is a goapi read-projection that LAGS (shows already-deleted rows). DELETE works at the store when given the list guidfixed (verified: DELETE 200 → subsequent GET 400 not-found). The UI uses list guids so its deletes work; the stale-list display is the projection-lag symptom.
- GREEN-PROOF BLOCKER (not a screen bug): API-seeded barcodes have stored `names: []` because **barcode.names derives from the product master (itemguid)**; my seeds used a fake itemguid (no master) → empty names → the update-time `names must contain ≥1` validator fails on re-save. itemunitnames + producttype.names (sent inline) DID store. So the import save needs a REAL product (master+barcode, e.g. created via the Product UI), which I have not yet run. Code fixes are sound + verified compile; full green e2e still pending a real product.
- Test tenant `test` polluted with duplicate/stale MEMEE/UITEST barcodes (disposable; projection lag means list won't fully clear).

## 2026-06-30 (cont.) — Clarified: barcode uniqueness WORKS; the "duplicates" were projection ghosts
- Jead flagged: barcode must be unique per holding — why did the tests show dups?
- VERIFIED uniqueness IS enforced: back-to-back create of the same barcode → create#1 201, create#2 **400 "barcode is exists"** (backend `productbarcode_http_service.go:138` rejects when FindByDocIndentityGuid finds an existing barcode in the holding). Projection showed count=1.
- So the earlier "duplicate" MEMEE/UITEST rows were **goapi list-projection GHOSTS**: my reseed scripts did delete-then-create; DELETE removed the row from the Mongo store (uniqueness check then passes → new doc created) but the **goapi read-projection (`/api/product/barcode/list`) does NOT drop deleted barcodes**, so it showed the deleted ghost + the new row = looked like a dup. Mongo store integrity is fine (no real dups).
- REAL underlying bug (single root for both "delete ไม่ลบ" and "barcode ซ้ำ" symptoms): **the goapi list read-projection is not updated on delete** (delete propagates to Mongo but the projection keeps stale rows). Creates DO reflect promptly. This is a CQRS projection/delete-propagation gap worth a backend look (Kafka projection pipeline) — NOT investigated yet.

## 2026-06-30 (cont.) — FIXED the "barcode ซ้ำ"/ghost bug (goapi list missing deletedat filter)
- Jead: screens show duplicate barcodes; find the projection-delete bug; rule = must check MongoDB every time.
- ROOT CAUSE (definitive): the frontend barcode list calls goapi `BarcodeListHandler` (`internal/goapi/handlers/barcode_list.go`), which reads MongoDB `productbarcodes` but its filter was `{holdingcode}` ONLY — **missing `deletedat: {$exists:false}`**. Soft delete sets `deletedat` (doc kept), so deleted barcodes stayed in the list as GHOSTS that looked like duplicates (uniqueness only blocks LIVE docs, verified: 2nd create of same barcode → 400 "barcode is exists"). mainapi `SearchRepository.FindStep` already filtered deletedat; this goapi handler didn't. Only this one goapi handler reads `productbarcodes` directly (audited).
- FIX: added `deletedat: {$exists:false}` to the filter (one filter var, used for find+count). Built+deployed to local mainapi via `deploy-mainapi-fast.ps1` (compile 12.7s, healthz OK).
- VERIFIED: clean ghost test — create→list(1)→delete→list now **0** (was 1 before fix), recreate same barcode 201. Ghost gone.
- RULE added: `.agents/rules/bc-account-core-rules.md` "Barcode / Soft-Delete Read Integrity" — every read of a soft-deleted collection via raw bson must filter `deletedat:{$exists:false}` (find AND count); read live MongoDB state.
- STILL PENDING: Task 2 (marketplace import green via real product) — API-seeded barcodes have empty `names` (derives from product master/itemguid which my seed faked); needs a product created via the real Product UI (or a seeded product master) to fully demo the producttype + raw-resave import fixes green.

## 2026-06-30 (cont.) — Task 2 GREEN: marketplace import works with real Shopee data + real product
- Final root of the names-empty blocker: **barcode `names` is DERIVED from the product MASTER** (`UpdateProductBarcode`/create: `repoMaster.FindByGuid(ItemGuid)` → `findDoc.Names = master.Names`, `productbarcode_http_service.go:739`). My earlier seeds used a FAKE itemguid → master not found → names overwritten to [] → re-save failed `names must contain ≥1`. NOT a code bug — the marketplace import + producttype/raw-resave fixes are sound.
- GREEN proof: created a real Product MASTER (POST /api/product, `code` field; guid from create response `data.guidfixed` since the /api/product LIST reads a lagging PG projection) → created a barcode with `itemguid`=masterGuid → barcode.names auto-derived ("เสื้อ GREEN MASTER", len 1) → drove the real /marketplace/shopee Bulk Import (real Shopee item_id 43252260289 from the MEMEE listing) → **"ประมวลผลสำเร็จ 1 รายการ"**; verify: `marketplaceproducts=[{shopee, 43252260289}]` PERSISTED. The producttype(struct)+raw-resave import fixes are confirmed working end-to-end.
- MINOR follow-up: the per-SKU `refbarcodes[].marketplaceskumappings` (model_id) came back empty in verify even though the item-level map saved + import reported success — the sub-barcode SKU-map step may need a look (refbarcode match after master-derivation). Item-level Shopee link is green.
- Also confirmed quirks (not blockers): product/barcode create IGNORE client guidfixed + return an id/0-ObjectId ≠ queryable guidfixed (use create-response data.guidfixed or the list); /api/product LIST reads PG projection (lags Mongo creates).

## 2026-07-01 — Warehouse screen (คลัง) UAT: 3 real backend bugs found + fixed + deployed
- Jead: "ทดสอบหน้าจอ คลัง" (test the warehouse screen). Full Stagehand-driven UAT of the 3-level tree (Warehouse -> Location/Zone -> Shelf), every button.
- **Bug 1 (frontend, empty-state):** `warehouse-tree-view.tsx` `formType` state defaulted to `"editwarehouse"` (not `"createwarehouse"`), and `handleSaveForm` silently `return`s (no error shown) when `formType !== "createwarehouse"` but no warehouse record exists yet. Net effect: **a tenant with zero warehouses could never create its first one** — the Add form looked normal but every save silently no-op'd. Fixed: initial state → `"createwarehouse"`; the existing `useEffect` still correctly flips it to `"editwarehouse"` once a real record loads.
- **Bug 2 (frontend, wrong backend base):** `handleSaveForm` (create/edit warehouse) and `handleDeleteWarehouse` fetched `${auth.backendUrl}/warehouse...` directly — `auth.backendUrl` resolves to the **goapi** base, but `/warehouse` routes are registered on **mainapi** (`cmd/app/main.go`). Same bug class as the earlier company/organization fix this session. Fixed: wrap with `deriveMainApiUrl(auth?.backendUrl ?? "")` (already imported in this file for the earlier company fix). Location/Zone/Shelf CRUD were unaffected — they go through the `/api/system-settings/productwarehousescreen/[id]` proxy which already resolves mainapi correctly server-side.
- **Bug 3 (backend, table/column name typos):** `internal/warehouse/warehouse_http.go` had **hardcoded snake_case raw-SQL table/column names that don't match the actual GORM names** on `WarehousePg`/`ZonePg`/`CompanyWarehousePg` (all single-word, no underscore, per explicit `TableName()`/`gorm:"column:..."` tags): `"company_warehouses"` should be `"companywarehouses"`, `"warehouse_guid"` should be `"warehouseguid"`, `"warehouse_zones"` should be `"warehousezones"`, `"zone_guid"` should be `"zoneguid"`. This caused `relation "company_warehouses" does not exist` / effectively-broken company-linkage and zone/shelf cascade-delete queries on UpdateWarehouse and several other handlers. Fixed all occurrences (sed global replace per pattern, verified via `gofmt -l` + real compile via `deploy-mainapi-fast.ps1`).
- **Also expanded** `ensureTenantSchema(holdingCode)` (the per-tenant defensive AutoMigrate added earlier this session for Search/Create) to **all 13 remaining warehouse/zone/shelf handlers** (Info/Update/Delete × 3 levels) — defense-in-depth, since the framework's own global `PersisterTenant` AutoMigrate (over ALL `tenantModels` in the app) can fail early on an unrelated model and then **permanently caches the broken persister without retry** (`pkg/microservice/microservice.go:342-370` caches unconditionally even on AutoMigrate error) — confirmed via container logs this exact failure mode occurred today (`column "deletedat" does not exist` from some other model, unrelated to warehouse) and warehouse's own defensive migrate is what actually recovers it per-tenant.
- **Verified end-to-end (Stagehand, `scratch/stagehand-uat/uat-warehouse-final.js`):** Warehouse create/edit/delete ✅, Location create/edit/delete ✅ (10/12 real assertions green, real 200 responses captured). Shelf create ✅; shelf edit/delete assertions were inconclusive due to a TEST-SCRIPT scoping bug (the "แก้ไข" button title is reused for both Location-edit and Shelf-edit in a nested tree, and the test's ancestor-walk was too broad) — not confirmed as a real app defect, flagged for a future narrower test pass if needed.
- Deployed to **local mainapi only** (`deploy-mainapi-fast.ps1`, 2 rounds, both healthz-verified) — NOT yet on `.202`. Also researched-and-declined `magnitudedev/browser-agent` as a Stagehand alternative this session (see memory `stagehand-deepseek-uat-harness.md`) — DeepSeek has no vision API, GLM-5V-Turbo needs separate billing beyond the Coding Plan; staying on Stagehand+DeepSeek.

## 2026-07-01 — Created global skill `uat-crud-mongo`, ran it end-to-end on masterbrandscreen
- Jead: build a reusable, project-agnostic Claude Code skill for autonomous CRUD+MongoDB UAT (no hardcoded module/table/collection/route/field names), then run it on this project.
- Created `~/.claude/skills/uat-crud-mongo/` (global, works on any project): `SKILL.md` (13-phase workflow: read project conventions -> discover app/routes/CRUD/Mongo -> drive CRUD -> verify UI+API+DB three-way -> root-cause+fix -> add/update Playwright regression -> repeat) + `references/{discovery,verification,regression}-guide.md` (progressive disclosure, loaded only per-phase) + `templates/{bug-report,uat-summary}-template.md`. verification-guide.md distills real bug patterns found earlier in this session (silent stuck-in-edit-mode form, projection-lag ghost records, sibling-not-ancestor labels, native-confirm-vs-modal) generalized with zero bccode-specific names.
- **Ran it on `/masterbrandscreen` (ยี่ห้อสินค้า)** as the demo scope. Discovery (no assumptions): screen is `kind:"main-crud"` -> `/aicloud/brand`, actual MongoDB collection is **`appdb.brandproductmaster`** (single shared DB, tenant-scoped by a `holdingcode` field — NOT a separate DB per tenant like Postgres warehouse tables are). Local Mongo is `mongodb://localhost:27017` replica set `rs0`, confirmed healthy via `docker exec mongodb mongosh` (the connected MongoDB MCP was flaky mid-session — fell back to mongosh per the skill's own documented fallback).
- Full CRUD verified three-way for Create/Update/Delete: UI (Stagehand-driven) + API (captured POST 201/PUT 201/DELETE 200 with real bodies) + **MongoDB direct query** (confirmed the persisted `names[0].name` matches the UPDATE value, not the stale CREATE value; confirmed `deletedat` is set on delete AND that a deletedat-filtered query correctly excludes it — no ghost-record bug on this collection, unlike the goapi barcode-list bug found+fixed earlier this session on a different collection).
- **Added Playwright e2e infra to the frontend project for the first time** (previously only had Vitest unit tests, scoped to `src/**/*.test.ts` — Playwright added as a separate, non-conflicting `e2e/` layer): `playwright.config.ts` + `e2e/master-brand-crud.spec.ts` (real Create->Update->Delete flow, unique per-run test data, API-based cleanup safety net). Installed `@playwright/test` + chromium browser (`--ignore-scripts` + direct `node node_modules/@playwright/test/cli.js install chromium`, same Windows npx-broken workaround used for Stagehand). Added `test:e2e` script to `package.json`.
- Iterated the test to green (several real DOM-structure discoveries fixed along the way, all now documented as durable comments in the spec + in the skill's verification-guide.md): dev-login button text is `"🧪 เข้าทดสอบระบบ (dev)"` not literally "dev"; the company-selection card is a long composite-text button, not the first clickable element on the page; two responsive variants of the menu search input coexist (desktop header + mobile complementary region) — strict-mode-safe to grab via `querySelector` not a Playwright locator; per-row action buttons ("แก้ไข"/"ลบ") hold only their own short label text, the row's code/name lives in sibling elements — must scope-click by walking up to the row container; **multiple "ลบ" buttons coexist on screen at once (one per row + one inside the delete-confirm modal)** — must scope the confirm click to the modal specifically (by proximity to the "ต้องการลบจริงหรือไม่" heading), not just match on button text.
- **Test passes green, twice in a row (idempotent)** — `node node_modules/@playwright/test/cli.js test e2e/master-brand-crud.spec.ts`. Verified via direct Mongo query after both runs: 0 live leftover test records (all soft-deleted correctly), confirming the cleanup-safety-net works.
- **Pre-existing, unrelated finding (not fixed, out of scope):** `npm run test` (vitest) has 31 pre-existing failing tests across 10 files (mostly `src/app/api/auth/line/...` route tests hitting 504s) — confirmed via `git status` these files were never touched by this session's work; flagging, not fixing.
- Added `/test-results/`, `/playwright-report/`, `/blob-report/` to `frontend/.gitignore` (Playwright's own trace/report output dirs).

## 2026-07-01 — /uat-crud-mongo run #2: warehouse screen — closed known gaps, corrected a false alarm
- Jead: `/uat-crud-mongo หน้าจอคลัง` — run the new skill on the warehouse screen (already had 3 real backend bugs fixed earlier today, but shelf edit/delete was left inconclusive and no direct DB verification or Playwright regression test existed yet for it).
- **Discovered (fresh, not from memory):** backing store is **PostgreSQL** (per-tenant DB "test"), not MongoDB — the skill's own Phase 2 discovery correctly adapted rather than forcing a Mongo assumption. Confirmed live: `warehouse`/`warehousezones`/`warehouseshelves`/`companywarehouses` tables, current state 1 live warehouse + 2 soft-deleted leftovers from earlier testing.
- **Closed the shelf edit/delete gap:** root cause of the earlier inconclusive result was a too-broad DOM ancestor search — the `title="แก้ไข"` button is reused at BOTH the Location and Shelf nesting levels, and a Location's DOM subtree contains its nested Shelf rows' text, so "first ancestor within N levels containing rowText" could match the wrong button. Fixed by picking the candidate button whose NEAREST (shallowest) matching ancestor contains the target row text, not the first DOM-order match. Full 3-level chain (Warehouse+Location+Shelf, all 4 CRUD ops each = 18 real assertions) now passes 100% via Stagehand, confirmed again via a clean Playwright run.
- **Jead caught a wrong inference mid-run:** I initially flagged `deleted_at` (with underscore) on the `warehouse` table as maybe-the-same-bug-class as the earlier `company_warehouses`/`warehouse_guid` typos. Jead asked to check the actual rule — `.agents/rules/bc-account-core-rules.md` "Lowercase Database Naming Iron Rule" explicitly allows underscores (`created_at` is cited as an acceptable example); the only violation is uppercase letters. `deleted_at` is fully compliant — not a bug, just a style difference (GORM's default naming for timestamp fields without an explicit `gorm:"column:..."` tag). Corrected and moved on — lesson: verify against the actual rule text before flagging, don't pattern-match on surface similarity to an unrelated prior bug.
- **Caught and then corrected my own false alarm (exactly the kind of premature conclusion IRON RULE 7 warns against):** after the full CRUD+delete run showed 0 rows (not even soft-deleted) in `warehousezones`/`warehouseshelves`, I initially suspected location/shelf data was never actually persisting anywhere. Root-caused before "fixing" anything: `UpdateWarehouse`'s zone/shelf sync uses `Unscoped()` (hard delete) as part of its replace-all pattern on every warehouse save — so checking table state *after* a full delete sequence can't distinguish "never wrote" from "wrote correctly, then hard-deleted by design". Verified with an isolated no-delete run + direct Postgres query mid-flow: location and shelf rows exist correctly, right FKs, right data — **not a bug**, the create/update path works exactly as designed.
- **Added Playwright regression test:** `frontend/e2e/product-warehouse-crud.spec.ts` — full Warehouse+Location+Shelf create/edit/delete (8 actions), passes clean on 2 consecutive runs, verified 0 live leftover data in Postgres after each run (only soft-deleted, as expected). Documents the native-`window.confirm()` delete pattern (differs from most other datacrud screens which use an in-page modal) and the tightest-ancestor-match technique for reused button titles in nested trees.
- tsc EXIT 0 after adding the new spec file.

## 2026-07-01 — Fixed React uncontrolled-to-controlled input warning on warehouse "เพิ่มกลุ่มชั้นวาง" (Bulk Add Shelves)
- Jead pasted a Next.js dev-overlay console error: "A component is changing an uncontrolled input to be controlled" at `warehouse-tree-view.tsx:1745` (an Input inside the Bulk Add Shelves form — an area not covered by earlier UAT passes today).
- **Root cause:** `formFields` state has 5 optional bulk-shelf fields (`bulkPrefix`/`bulkStartNum`/`bulkEndNum`/`bulkPadding`/`bulkNamePattern`). The `useEffect` that resets `formFields` on `formType`/`selectedNode` change has 7 branches (createwarehouse/editwarehouse/createlocation/editlocation/createshelf/editshelf/bulkshelf), each doing a FULL object replacement (`setFormFields({...})`, not a merge). Only the `bulkshelf` branch included the 5 bulk fields (with real default values like `"SH-"`) — the other 6 branches omitted them entirely, so `formFields.bulkPrefix` reverted to `undefined` every time the user did ANYTHING else first (open/close any other form) before reaching Bulk Add Shelves — then flipping to `"SH-"` when bulkshelf's branch ran, triggering React's warning on that transition.
- **First fix attempt was incomplete:** added the 5 bulk fields to the initial `useState()` default only — this didn't work (verified empirically, warning still fired) because the OTHER 6 branches' full-object-replacement still wiped the fields back to `undefined` before the user ever reached the bulk form in a normal flow (create warehouse → create location → open bulk-add).
- **Real fix:** added the same 5 bulk-field defaults (`""`) to all 6 other branches, so `formFields` is always a complete, fully-string-typed shape at every `setFormFields` call site, with no branch capable of reintroducing `undefined`.
- **Verified via Stagehand (in-page console.error/warn override, since this Stagehand version's CDP wrapper rejects `page.on("console")`):** before fix, opening Bulk Add Shelves fired the exact reported warning; after the complete fix, zero console warnings/errors on the same repro path, and the bulk form's default values (`bulkPrefix="SH-"`, name-pattern field) still populate correctly — behavior unchanged, only the warning is gone.
- Re-ran `frontend/e2e/product-warehouse-crud.spec.ts` (added earlier today) — still passes, confirming this fix didn't regress normal Warehouse/Location/Shelf CRUD.
- tsc EXIT 0.

## 2026-07-01 — Rule: no-ask for MongoDB structure + screen/field changes
- Jead: "ตั้งกฏเลย แก้โครงสร้าง mongodb ได้ แก้หน้าจอได้ เพิ่ม field ลบ field ได้ ไม่ต้องถาม" — set a rule: don't ask before changing MongoDB structure, changing screens, or adding/removing fields.
- Added "No-Ask Schema + Screen Change Rule" to `.agents/rules/bc-account-core-rules.md` (right after the existing Disposable Database rules, which already covered DB resets — this explicitly extends the same "no need to ask" to UI screen/field changes too). Scope stays the same as the existing disposable-DB rules (pre-launch, DEV+.202+prod-until-go-live) and does NOT sweep in separately-gated actions (deploy, git push/force-push, other R0s) that weren't part of this grant.

## 2026-07-01 (cont.) — Rule expanded: also covers backend/frontend code changes generally
- Jead expanded the just-set rule: "...แก้ backend, frontend โดยไม่ต้องถาม" — no-ask now explicitly covers backend/frontend code changes generally (not just DB structure/screen/field), when needed to support (a)-(c). Renamed to "No-Ask Schema + Screen + Code Change Rule" in bc-account-core-rules.md. Same scope/exclusions as before (pre-launch disposable data; deploy/git push/other R0s still gated).

## 2026-07-01 (cont.) — `/uat-crud-mongo` full sweep across all 16 "จัดกลุ่มสินค้า / สี ไซซ์ และตัวเลือก / รายละเอียดประกอบสินค้า / คลังและการผลิต" menu items
- Jead: "ตรวจเมนูทั้งหมดนี้อีกรอบ" (re-check this whole menu) with a screenshot of all 16 items across 4 menu groups.
- **Discovered structure (no assumptions):** 7 screens share the `aicloudConfig`/`codeNameConfig` generic factory (`/aicloud/{path}`, code+names only) — brand, model, pattern, category(attribute), class, design, grade. 3 share `atlasMasterConfig` (direct-collection generic engine) — productcolor (`productcolors`), productsize (`productsizes`), productvariantmatrix (`productvariantmatrices`). Remaining 6 are hand-rolled `main-crud` configs with their own basePath: productgroup, productcategorygroupselectscreen, productcategorylist, productdimension, productbom, productwarehousescreen (warehouse already fully verified earlier today).
- **Verified working (UI + Mongo ground truth):** all 7 aicloud screens (full CRUD on model as representative + brand already done, list-loads-clean smoke check on pattern/category/class/design/grade), all 3 atlas screens (productcolor/productsize/productvariantmatrix full create/edit/delete, MongoDB-confirmed persisted correctly incl. the edit path — an early apparent "edit failed" result was Stagehand/DeepSeek act() flakiness, not a real bug, corrected via direct Mongo read before concluding), productgroup, productdimension (re-verified full CRUD; the `items` field `[]` default fix from earlier today holds — confirmed via live placeholder="[]" DOM + Mongo), productcategorygroupselectscreen (full CRUD once the REAL button text "เพิ่มหมวดหลัก" was discovered live — menu-data.ts's display label ≠ actual rendered button/nav text in a few places, a recurring test-authoring trap worth remembering).
- **Not a bug (working as designed):** productcategorygroupselectscreen requires selecting one of the 20 fixed usage/device/channel groups (per core-rules.md "Product category group semantics") before its category CRUD form appears — matches the documented rule exactly.
- **REAL BUG #1 FIXED:** `productcategorylist` ("สินค้าในหมวด") field `codelist` was a raw JSON textarea (`jsonField`, no placeholder, no structured UI) — violates the "no raw {}/[] in user-facing screens" rule Jead set earlier this session. Fixed by switching to the existing `stringListField` (chip/tag editor, same component already used for color/size "aliases") in `frontend/src/lib/system-setting-screens.ts`. tsc clean.
- **REAL BUG #2 FIXED:** `product-category-tree-view.tsx`'s empty-state hint text unconditionally told the user to click "เพิ่มหมวดหลัก" even when the tree renders `readOnly={config.slug === "productcategorylist"}` (where that button never renders) — a dead UI reference. Fixed by branching the hint text on the `readOnly` prop (own screen keeps the original working copy). tsc clean, verified live both ways.
- **OPEN FINDING (found, NOT fixed — flagging per DISSENT, not silently working around it):** a category created via `productcategorygroupselectscreen` for a given group does not show up on the sibling `productcategorylist` screen for the SAME group/basePath, even after a full page reload + up to 30s of polling. Checked and ruled out: the Next.js API proxy (`/api/system-settings/[[...settingPath]]/route.ts`) uses `cache: "no-store"` for its forwarded requests, so it isn't an obvious HTTP-layer cache; both slugs build an identical `group-number`-filtered request per `loadRecords`. Root cause not yet isolated — worth a dedicated follow-up session (likely a React state/effect timing issue in `system-settings-screen.tsx`'s `loadRecords`/`groupNumber` wiring, or a backend-side scoping difference not yet found). Data itself is correct in MongoDB throughout (confirmed via direct query) — this is a read-freshness/display issue, not data loss.
- **Added regression tests:** `frontend/e2e/product-category-list-crud.spec.ts` (2 tests: readOnly-aware empty-state text on productcategorylist, and the sibling screen's own unaffected empty-state — both pass twice in a row). Did not add a live UI regression test for the codelist chip-editor render itself, since doing so needs the open cross-screen-staleness finding above resolved first; static/type-level coverage (tsc + `stringListField` has no raw-JSON render path anywhere in the codebase) stands in for now.
- Full `frontend/e2e/` suite (4 specs) passes clean. All test/leftover Mongo data cleaned (`productcategories`, `dimension`, `productcolors`, `brandproductmaster` all 0 live E2E-prefixed docs after the run).

## 2026-07-01 (cont.) — Migrated คลัง (warehouse/zone/shelf) backend from PostgreSQL to MongoDB
- Jead: crystallized the Data Store Roles rule ("mongodb = เก็บข้อมูลดิบ, pgsql = คำนวณแบบ relation") and ordered the warehouse data model redesigned — must be in MongoDB, closing the violation found earlier this session.
- **Discovered before writing anything new:** a correct MongoDB implementation for warehouse already existed in the codebase (`internal/warehouse/services/warehouse_http_service.go` + `repositories/warehouse_mongo_repository.go` + `models/warehouse.go` — full CQRS boilerplate matching every other Mongo module, plus a Kafka-consumer projection path to a flat Postgres JSONB table) but was never wired to the live `/warehouse` HTTP routes; a separate ad hoc PostgreSQL-direct handler (3 relational tables, per-tenant DB) was built and registered instead. The existing Mongo service also had 2 real, never-fixed bugs: `CreateShelf` built its update in memory but never called `repo.Update(...)` (silent no-op — the exact reason it likely looked "broken" and got bypassed originally), and `UpdateShelf` appended the same shelf object twice (copy-paste bug).
- **Also discovered (precise frontend contract audit before touching backend):** the frontend (`warehouse-tree-view.tsx`) does NOT use the granular `/warehouse/:guid/zone/...`/`/warehouse/:guid/zone/:guid/shelf/...` sub-routes at all — ALL zone/shelf create/edit/delete goes through a single whole-document `PUT /warehouse/:id` with a full `location` array (replace-all pattern), while a SEPARATE warehouse-name-only edit path also hits `PUT /warehouse/:id` but WITHOUT a `location` key at all. This meant the migration had one critical correctness requirement: the update handler must tell "location key absent" (preserve existing zones/shelves) apart from "location key present, even empty" (replace) — solved with pointer-typed request fields (`*[]models.Location`, `*[]string` for CompanyGuids), matching a pattern the original (unused) Mongo model already anticipated (`Warehouse.Location` was already `*[]Location`, a pointer, for exactly this reason).
- Ran a workflow (implement + adversarial-review agents) to do the migration; the implement agent did the real work correctly (confirmed via diff review after the fact) but the workflow run itself failed on a harness-level StructuredOutput formatting error at the end (not a code-correctness issue) — recovered by reviewing the actual diff directly and completing verification myself rather than re-running blind.
- **Changes:** `models/warehouse.go` (+`CompanyGuids []string` on `Warehouse`, +`GuidFixed string` on `Location` and `Shelf`), `services/warehouse_http_service.go` (fixed both bugs above, removed leftover `log.Printf("DEBUG: ...")` statements + the now-unused `log` import), `warehouse_http.go` (fully rewritten: wraps `services.IWarehouseHttpService` backed by `ms.MongoPersister(cfg.MongoPersisterConfig())` instead of GORM/PostgreSQL; constructor `NewWarehouseHttp(ms, cfg)` and `RegisterHttp()` signatures unchanged so `main.go`/`cmd/app/main.go` needed ZERO edits; removed the `ensureTenantSchema`/per-tenant-AutoMigrate machinery entirely — MongoDB here is a single shared `appdb`, tenant-scoped via a `holdingcode` field, no per-tenant connection dance needed).
- **Verified (real evidence, not claimed):**
  - Docker build+deploy: `deploy-mainapi-fast.ps1` → compiled in 1.95s, healthz OK, 14.56s total.
  - The EXISTING, UNMODIFIED Playwright regression test (`frontend/e2e/product-warehouse-crud.spec.ts`) passes twice in a row against the new MongoDB-backed API — strongest proof the frontend contract (same routes, same JSON field names) survived the migration with zero frontend changes.
  - Direct MongoDB query: `appdb.warehouse` collection populated correctly, `holdingcode: "test"` scoping correct, nested `location[].shelf[]` with auto-assigned `guidfixed` values present.
  - **CRITICAL partial-update property, tested directly via curl against the real running API (not just claimed): create warehouse → PUT with a location array (adds zone+shelf) → PUT again with ONLY code/names/latitude/longitude (no `location` key, no `companyguids` key) → confirmed via a fresh Mongo query that `location` and `companyguids` were UNCHANGED (same guidfixed values), only the name/lat/long fields updated.** This is exactly the failure mode that would have silently deleted a warehouse's zones/shelves on an unrelated name edit if gotten wrong.
  - Soft-delete verified: `DELETE /warehouse/:id` sets `deletedat`, doc excluded from live counts afterward.
  - `SearchWarehouse`'s companyguid filter confirmed correct by reading the shared `repositories.SearchRepository.FindPageFilter` — a plain scalar filter value against an array field (`companyguids`) uses MongoDB's native element-match semantics, no custom code needed, no bug.
- **Left alone (per Disposable Database Rule, pre-launch, no migration needed):** the old PostgreSQL `warehouse`/`warehousezones`/`warehouseshelves`/`companywarehouses` tables and `internal/warehouse/models/warehouse_pg.go` are now orphaned/legacy — left in place, not deleted, not migrated.
- **Not yet done (flagging, not silently skipping):** the zone/shelf sub-routes (`/warehouse/:guid/zone/...`) were rewired for API-surface parity but only smoke-level reasoned about, not independently UAT'd (the frontend doesn't call them, so risk is low, but they weren't part of this task's verification loop). The Kafka-projection-to-Postgres path (`warehouse_consumer_service.go` → flat `WarehousePG` JSONB table) was NOT touched or verified this session — it's the "pgsql = relational calculation" half of the new rule and would need its own pass if/when something actually needs to calculate relationally over warehouse data.
- Updated `bc-account-core-rules.md`'s Data Store Roles section with the crystallized one-liner rule + a note closing this exact violation. Updated `frontend/e2e/product-warehouse-crud.spec.ts`'s header comment to describe the new MongoDB backing store (test body itself untouched).

## 2026-07-02 — /uat-crud-mongo run #4 บน หน้าจอคลัง (post-MongoDB-migration): 4 บั๊กจริงใน zone/shelf sub-routes พบ+แก้+verify
- Jead: `/uat-crud-mongo หน้าจอคลัง` — re-run UAT หลัง migrate ไป MongoDB เมื่อวาน. รอบนี้ทำ three-way (UI+API+Mongo mid-flow ทุก step) เต็มรูปแบบ + ปิด gap ที่ flag ไว้ (zone/shelf sub-routes ไม่เคย UAT).
- **Main UI flow: 8/8 เขียว three-way** — warehouse/zone/shelf create/edit/delete ผ่าน UI + Mongo doc ตรงทุก step (script `scratch/stagehand-uat/uat-warehouse-mongo-3way.js`), partial-update (แก้ชื่อคลังอย่างเดียว location รอด — guidfixed เดิม), soft-delete (`deletedat` set, live count 0).
- **Sub-routes (`/warehouse/:guid/zone[...]`): พบบั๊กจริง 4 ตัว ซ่อนกันเป็นชั้นๆ** (ตัวหลังโผล่ได้ต่อเมื่อแก้ตัวหน้าแล้ว — เหตุผลที่ code path นี้ดู "ใช้ได้" มาตลอดคือมันไม่เคยถูกเรียกจริง):
  1. **POST zone panic (nil-deref)** — `CreateLocation` ทำ `range *locations` โดย warehouse ที่สร้างแบบไม่มี `location` key เก็บเป็น `null` (ทุก warehouse ที่สร้างผ่าน UI ก็เก็บ null เหมือนกัน แค่ UI รอดเพราะ whole-doc replace). ยืนยันด้วย stack trace จริงจาก `docker logs mainapi` (panic ที่ warehouse_http_service.go:185). **Fix ที่ choke point**: `normalizeWarehouse()` (Location nil → `[]`) ในทุก write path ของ service (CreateWarehouse/UpdateWarehouse/SaveInBatch×2) — DB disposable เลย wipe docs เก่าที่มี null แทนการเติม nil-guard 10 จุด.
  2. **POST shelf ตอบ "document not found" เสมอ** — `CreateShelf` ใช้ `FindWarehouseByShelf` ซึ่ง filter หา `location.shelf.code == <โค้ดที่กำลังจะสร้าง>` → shelf ใหม่ไม่มีทาง match. Fix: เปลี่ยนเป็น `FindWarehouseByLocation` (ตัว dup-check ข้างใน loop มีอยู่แล้ว).
  3. **Zone rename งอก entry ซ้ำ + ทิ้ง entry เก่า** — `UpdateLocation` (same-warehouse rename branch) loop หา `doc.Code` (โค้ดใหม่ที่เพิ่ง dup-check ว่าไม่มี) แทน `locationCode` เดิม → append ใหม่เสมอ entry เก่าค้าง. Fix: rewrite branch ให้หาด้วยโค้ดเดิม, update in-place (guidfixed เดิมรอด).
  4. **Cross-warehouse zone move ตอบ 200 แต่ไม่ย้าย** — บั๊กที่ผมสร้างเองตอน migrate เมื่อวาน: HTTP handler ทับ `req.WarehouseCode = warehouseInfo.Code` เสมอ ทำลาย move target → เข้า same-warehouse branch ตลอด. Fix: default เฉพาะเมื่อว่าง (`if req.WarehouseCode == ""`), เหมือนกันกับ `UpdateShelf` (WarehouseCode+LocationCode).
- **เสริมตามน้ำ (in-scope, ไม่ drift):** zone/shelf ที่สร้าง/ย้ายผ่าน sub-routes ได้ `guidfixed` จริงแล้ว (เดิม `""`) — `ensureShelfGuids()` + gen ตอน append + **preserve ข้าม move/rename** (`movedShelf`/`movedLocation` carry ทุก field ที่ ShelfRequest ไม่มี: min/max/productitems/dimensions — เดิม rebuild จาก Code+Name ล้วนๆ = ล้างเงียบ). Verify แล้ว: zone move A→B guidfixed เดิมเป๊ะ.
- **Verify (evidence จริง):** deploy 3 รอบ (fast-binary-swap, healthz OK ทุกรอบ) · curl chain เต็ม: zone create 201, shelf create 201, dup reject, shelf edit, zone rename (ZA หายเกลี้ยง), cross-move (A ว่าง, B ได้ zone+shelf+guid เดิม), delete ครบ · Mongo ตรวจตรงทุกจุด · **เพิ่ม Playwright test ใหม่** ใน `e2e/product-warehouse-crud.spec.ts`: "warehouse zone/shelf sub-route API" (Node-side request context + token จาก UI login, cover ทั้ง 4 บั๊ก) — **ทั้ง 2 test ผ่าน 2 รอบติด**, live leftovers = 0.
- หมายเหตุ minor ที่ไม่แก้ (flag): sub-route error ตอบ 500 แทน 400/409 สำหรับ business error ("code is exists") — pattern เดิมทั้ง codebase, ไม่ใช่ regression; DELETE zone/shelf ที่ไม่มีอยู่ตอบ 200 (idempotent no-op) — ยอมรับได้.

## 2026-07-02 (cont.) — /uat-crud-mongo สูตรผลิต (/productbom): UAT + ปรับปรุงรองรับร้านอาหาร/โรงงาน + พบบั๊ก tenant-corruption ใน product PUT
- Jead: "แก้ไขหน้าจอนี้ให้ด้วย สูตรผลิต รองรับธุรกิจ ร้านอาหาร โรงงาน ฯลฯ ตรวจ mongodb แก้ได้ เพื่อให้สูตรผลิตสมบูรณ์" — จอสุดท้ายที่ค้างจาก sweep 16 เมนู.
- **Discovery:** backend BOM (`internal/product/bom/`, collection `productbarcodeboms`) สมบูรณ์กว่าที่จอเปิดใช้มาก — reftype product/recipe (สูตรย่อย, กัน self-reference), ตรวจ materialtype วัตถุดิบ (1=Material/2=Semi-Finished/4=Agricultural เท่านั้น), yieldpercent, averagecost, **ระบบ version (boms[] + startdate/enddate + iscurrentuse)**. จอ editor (`product-bom-editor.tsx`) มี versions/yield/exploded-tree/rollup-cost อยู่แล้ว.
- **API UAT เขียว:** create สูตร+วัตถุดิบ+yield, สูตรย่อยข้ามสูตร, self-reference โดน reject, barcode ผีโดน reject — Mongo ตรงทุกจุด.
- **ปรับปรุง 3 จุด (ตามโจทย์ร้านอาหาร/โรงงาน):**
  1. **ปุ่มลบสูตร** — backend DELETE /product/bom/:id มีอยู่แล้วแต่ UI ไม่มีจุดเข้าเลย (สร้างได้แต่ลบไม่ได้) → เพิ่ม "ลบสูตร" ใน header + confirm dialog (useConfirmDialog ไม่ใช่ native confirm ตามกฎ) + รองรับ virtual record.
  2. **ต้นทุน/หน่วยแก้ไขได้** ในแถววัตถุดิบ (field `averagecost` มีใน backend แต่ UI render read-only ค่า 0 ตลอด → rollup cost/margin ใช้ไม่ได้จริง) — สูตรย่อยยังคำนวณจากลูกเหมือนเดิม.
  3. **ผลิตได้ต่อสูตร (`outputqty`)** — backend ใหม่: field ใน SaveRequest → recipe root `qty` (เดิม hardcode 1); UI field ใหม่ + สรุป "ต้นทุน/หน่วยผลิต = rollup ÷ N" ใน exploded tree (เค้ก 1 สูตร 10 ชิ้น = ฿9.43/ชิ้น — เลขที่ร้านอาหาร/โรงงานใช้ตั้งราคาจริง). Verified: rootqty=10 + averagecost=25 persist ใน Mongo, สูตรคำนวณถูก (rollup ฿94.33 = 3÷0.9×25 + 0.5×22).
- **REAL BUG (backend, ร้ายแรง): `PUT /product/:guid` ล้าง `holdingcode`+`guidfixed` เป็นค่าว่าง** — `Update` service ทำ `docData.ProductData = doc.ProductData` (embedded ทั้งก้อนรวม HoldingCodeentity+DocIdentity) → สินค้าหลุดจาก tenant ทันทีที่ update ด้วย payload ที่ไม่ echo ค่าเดิมกลับมา (เจอจริง: 2 seed products หายจาก list ทุก query ที่ scope ด้วย holdingcode). Fix: restore identity จาก findDoc หลัง assign. ซ่อมข้อมูล corrupt (ลบ 2 docs ที่ tenant-detached, สร้างใหม่ผ่าน API). Regression-check ผ่าน: PUT แล้ว holdingcode/guidfixed อยู่ครบ + list เจอ.
- **ไม่ใช่บั๊กแอป (harness artifact):** ข้อมูล seed ไทยกลายเป็น `????` — curl.exe บน Windows แปลง argv ไทยเป็น `?` (classic Windows argv mangling) → แก้ pattern การ seed เป็น `--data-binary @file.json` (UTF-8 ไฟล์) เท่านั้น. จอ render ตามข้อมูลถูกต้องตลอด.
- **Regression test ใหม่:** `frontend/e2e/product-bom-crud.spec.ts` — full UI flow: สร้างสูตร (code+ชื่อไทย+outputqty) → เพิ่มวัตถุดิบผ่าน MasterPicker (search ไทย) → ใส่ต้นทุน 40 → save → reopen ตรวจค่า persist → ลบสูตรผ่าน UI → cleanup safety net. ผ่าน 2 รอบติด. **DOM gotcha ใหม่ (จดใน spec):** NamesEditor ใช้ `input[placeholder="th"]` (ancestor-walk หา "TH" ใช้ไม่ได้ — label เป็น flag icon). Full e2e suite 6/6 ผ่าน.
- **Seed data คงไว้ใน dev DB** (MAT44941/MAT244941 + BMAT* barcodes + RCP44941/RCPMAIN44941): เป็น prerequisite ของ BOM spec + ตัวอย่างสูตรใช้งานจริง. หมายเหตุ minor: barcode itemguid ชี้ master guid เก่า (ไม่กระทบ — enrichment ใช้ itemcode). GREENM2 มี doc ซ้ำ 2 ตัวใน products (ของ session marketplace เก่า) — flag ไว้ ไม่แตะ (นอก scope).

## 2026-07-02 (cont.) — สูตรผลิต: เปลี่ยนสูตรย่อยเป็น live reference (แก้ครั้งเดียว ปรับสูตรใหญ่ทุกตัว) + กัน loop + สูตรตัวอย่างไทย
- Jead: "สูตรย่อย ทำเป็น BOM ซ้อน BOM ได้หรือไม่... เปลี่ยนเลย แต่ต้องระวัง อ้างอิงกัน จนติด loop ทำให้การคำนวณค้างด้วย แล้วสร้างสูตรตัวอย่าง ผัดไทย ข้าวผัด ส้มตำ ฯลฯ"
- **ก่อนเปลี่ยน**: สูตรย่อยถูก **ก็อปปี้สแนปช็อต** ตอนบันทึก (ทั้ง frontend `addRecipeToBOM` และ backend `prepareRecipeBOMItems`) — แก้สูตรย่อยแล้วสูตรใหญ่ที่เคยเพิ่มไปแล้วไม่อัปเดตตาม
- **เปลี่ยนเป็น**: สูตรใหญ่เก็บแค่ reference (barcode+qty+yield) ไม่ฝัง bom[] — `InfoBOM` resolve สดทุกครั้งที่อ่าน (`resolveRecipeTree`, recursive, ผ่านทั้ง active bom และทุก version ใน `boms[]`)
- **กัน loop 2 ชั้น**: (1) save-time `checkNoCycle` — เดินสายโซ่ของสูตรที่กำลังจะเพิ่ม เช็คว่าไม่วนกลับมาที่สูตรที่กำลังบันทึกทั้งทางตรง/ทางอ้อม (ลึกได้ถึง 15 ชั้น) ก่อนยอมรับ (2) read-time `resolveRecipeTree` มี depth-cap + visited-path เหมือนกัน เป็น defense-in-depth เผื่อข้อมูลหลุดมาเป็นวงแล้ว — ยืนยันจริง: พยายามปิดวง reject ใน 0.28s (API) / 0.315s (demo) ไม่ค้าง
- **แก้บั๊กคำนวณที่เจอระหว่างออกแบบ**: ต้นทุนสูตรย่อยเดิมเอา rollup ทั้งแบทช์มาคิดตรงๆ ไม่หารด้วย outputqty ของสูตรย่อยเอง (เช่น ใช้น้ำจิ้ม 0.05 LTR จากแบทช์ 1 LTR ควรได้ 5% ของต้นทุน ไม่ใช่ 100%) — เพิ่ม field `subrecipeoutputqty` (resolve สดจาก sub-recipe's Qty) แก้ `componentUnitCost` ให้หารถูก
- **สูตรตัวอย่างไทยจริง** (คงอยู่ใน dev DB ให้ดู): วัตถุดิบ 15 รายการ (เส้นจันทร์ กุ้งสด ไข่ไก่ ฯลฯ) + สูตรย่อยใช้ร่วม "น้ำปรุงรสส้มตำ-ผัดไทย" (RCPSAUCE197609) + ผัดไทยกุ้งสด (RCPPADTHAI197609) + ส้มตำไทย (RCPSOMTAM197609) ทั้งคู่อ้าง sauce ตัวเดียวกัน + ข้าวผัดกุ้ง (RCPKHAOPHAD197609, ไม่ใช้สูตรย่อย โชว์ความหลากหลาย)
- **Verify 3 ระดับ**: API (แก้ sauce → GET ทั้ง 2 จานเห็นค่าใหม่ทันทีโดยไม่ resave), MongoDB (พิสูจน์ document ของผัดไทยเก็บแค่ reference จริง ไม่มี snapshot ค้าง), UI browser จริง (ต้นทุน/หน่วยที่จอโชว์ตรงกับสูตรคำนวณมือเป๊ะ ฿3.00)
- **Adversarial review เจอจริง 3 จุด**: (1) **race condition** — 2 สูตรบันทึกพร้อมกันอ้างกันเองอาจหลุดผ่าน cycle-check ทั้งคู่ได้ (ไม่มี transaction/lock) — confidence: medium, **ยังไม่แก้** (ต้องใช้ Mongo transaction/optimistic lock เพิ่ม เป็นงานแยก) แต่ read-time depth-cap ยังกันไม่ให้ค้างแม้เกิดจริง (2) **ต้นไม้กว้างมาก (ไม่ใช่ loop) อาจช้า** — bound ด้วย depth-cap+context-timeout อยู่แล้ว ไม่ค้างถาวร แค่ช้า — confidence: medium, low priority (สูตรจริงไม่น่ากว้างขนาดนั้น) (3) **ไม่มี Go unit test คุ้มครอง logic กันลูป** — confidence: high, **แก้แล้วในรอบนี้**: เพิ่ม 6 unit test ใน `bom_http_service_test.go` (diamond-dependency ต้องผ่าน, direct/transitive cycle ต้อง reject, 2 tests ยิงเข้า pre-existing cycle ตรงๆ ผ่าน goroutine+timeout 3s พิสูจน์ไม่ค้างจริง, live-cascade ข้าม 2 parent) — รันผ่าน Docker builder (`-tags musl`, host build ใช้ไม่ได้เพราะ kafka CGO) ผ่านหมด รวมกับ test เดิมก็ผ่าน
- Full e2e suite (7 test รวม cascade test ใหม่) ผ่าน 2 รอบ, ไม่มีขยะทดสอบเหลือ
- **คงไว้ ไม่แก้ (flag ต่อ)**: race condition ข้อ (1) ข้างต้น — ความน่าจะเป็นต่ำมาก (ต้อง 2 user แก้ 2 สูตรที่เกี่ยวข้องกันพร้อมกันเป๊ะ) และมี safety net (read-time ไม่ค้าง) แต่ยังเป็นช่องโหว่ทางทฤษฎีจริง ถ้าต้องการปิดสนิทต้องเพิ่ม Mongo transaction/unique-constraint รอบ save — ยังไม่ทำเพราะนอก scope งานนี้ (จะทำถ้า Jead สั่ง)

## 2026-07-02 (cont.) — สูตรผลิต: เพิ่ม Finished Good/Labor/OH/Scrap/Standard-Cost toggle + calculator ขายตามจำนวน (3 ที่ปรึกษาออกแบบ) + แก้บั๊กที่ adversarial review เจอ
- Jead: ถาม GLM+DeepSeek+GPT 5.5 เรื่อง "ขายส้มตำ 10 จาน ต้องรู้ต้นทุน+วัตถุดิบ, ต้องเลือกได้ระหว่างต้นทุนอัตโนมัติ/Standard Cost" — ทั้ง 3 ตัวเห็นตรงกัน (independent, ไม่เห็นคำตอบกัน): (A) ต้องมี Finished Good link แยกจาก identity สูตร (B) Standard vs Current cost mode ที่หัวสูตร, standard = ค่าตายตัวอัปเดตด้วยมือเท่านั้น (C) explode+snapshot ตอนขาย ไม่ต้องมีใบสั่งผลิตเต็มรูปแบบ
- Jead confirm "สร้างเลย" → รัน workflow 2 phase (implement+verify, adversarial review)
- **Implement**: เพิ่ม 6 field บน `BOMProductBarcode` (costmode, standardcost, laborcost, overheadcost, scrappercent, finishedgoodbarcode) + สูตรคำนวณ CostMode-aware (`standard`→ใช้ standardcost ตรงๆ, `current`→(rollup+labor+OH)/outputqty/(1-scrap%)) + calculator "จำนวนที่จะขาย/ผลิต" reuse หน้าจอ Exploded BOM เดิม (ไม่สร้างใหม่) + ฟอร์ม Finished Good picker (reuse MasterPicker เดิม) + Labor/OH/Scrap%/Cost Mode radio (ตามกฎ ≤4 ตัวเลือก=radio ไม่ dropdown)
- **บั๊กที่ implement agent เจอ+แก้เอง**: field ใหม่ใช้ `bson:"...,omitempty"` ตาม pattern เดิม (SubRecipeOutputQty) แต่ `omitempty` ทำให้ reset ค่ากลับเป็น 0/ว่างไม่ได้ (Update() ทำ full-document $set, omitempty ตัด field ค่า 0 ทิ้งจาก $set) → แก้เอา `,omitempty` ออกเฉพาะ 6 field ใหม่
- **Verify ตรงตัวอย่างจริงของ Jead** (ส้มตำไทย 10 จาน): ตั้ง Labor=20 OH=10 Scrap=5% → cost/unit=฿30.24, ขาย 10 จาน=฿302.38, วัตถุดิบ scale ตาม scrap ถูกต้อง (ตรวจมือ), toggle เป็น standard cost=999 → หัวข้อเปลี่ยนเป็น ฿999 ทันที วัตถุดิบยังโชว์ไว้อ้างอิง — ตรงสเปกทุกจุด
- **Adversarial review เจอบั๊กจริง confidence สูง 1 จุด**: `resolveRecipeTree` (backend) ไม่ propagate LaborCost/OverheadCost/ScrapPercent/CostMode/StandardCost ของสูตรย่อยไปให้สูตรใหญ่ที่อ้างอิงมัน — แก้สูตรย่อย (เช่น RCPSAUCE) ให้มี labor/OH/scrap แล้ว สูตรใหญ่ (ส้มตำ/ผัดไทย) ที่อ้างอิงจะเงียบๆ **ไม่เห็น**ค่าพวกนี้เลย ต้นทุนคำนวณต่ำกว่าจริง — เกิดเพราะ demo verification ใช้ sauce ที่ labor/OH/scrap=0 ทั้งหมด เลยไม่เจอตอนนั้น
- **แก้เอง (นอก workflow, ทำเองต่อทันที)**: (1) backend `resolveRecipeTree` เพิ่ม propagate 5 field จาก subDoc ไปยัง resolved item (2) frontend: สร้าง shared function `effectiveUnitCost()` ใช้สูตรเดียวกันทั้งที่ root (`costPerOutputUnitEffective`) และที่ nested sub-recipe (`componentUnitCost`) — แก้ตรงจุดที่ adversarial review ชี้ (line duplication ที่อาจ desync ด้วย) — เพิ่ม 5 field ใหม่ใน `BOMItemInput` interface + `mapItems`
- **Verify การแก้จริง**: ตั้ง sauce labor=5/OH=3/scrap=10% → ยิง API เช็คว่า field propagate เข้า parent's resolved bom item ถูกต้อง → เปิด browser จริงเช็คแถวสูตรย่อยในหน้าส้มตำ: unit cost ฿75.61 บรรทัดรวม ฿3.02 ตรงกับคำนวณมือ (68.05/0.9=75.611, ×0.04=3.024) เป๊ะ — เดิม (ก่อนแก้) จะโชว์แค่ ฿60.05/฿2.40 (raw rollup ไม่รวม labor/OH/scrap)
- **เจอปัญหาแทรก**: frontend server (`next start`) รันด้วย build เก่าค้างอยู่ ทำให้หน้าจอ crash "This page couldn't load" ตอน verify — root cause คือลืม rebuild หลังแก้โค้ด ไม่ใช่บั๊กจริง — kill process เก่า, `next build` ใหม่, `next start` ใหม่ แก้หาย
- รีเซ็ต RCPSAUCE197609 กลับ baseline (labor/OH/scrap=0) หลัง verify เสร็จ
- **Full regression หลังแก้ครบ**: Go test 8/8 ผ่าน (ใน Docker builder), e2e suite 8/8 ผ่าน (รวม 3 test ของ BOM: CRUD พื้นฐาน, live cascade, labor/OH/scrap/costmode ใหม่)

## 2026-07-03 — กฎใหม่: อ่าน scopeofwork/ เสมอ, ห้ามแก้/ลบเด็ดขาด
- Jead: ถามว่าจะใช้ Google Doc ตั้งกรอบการทำงานให้ Claude Code อ่านได้ไหม → แนะนำ sync เข้าไฟล์ในโปรเจกต์แทน (ตรงกับกฎ model-agnostic เดิมของโปรเจกต์ที่ห้ามพึ่ง connector เฉพาะเจ้า) → Jead สร้างโฟลเดอร์ `D:\bccode\scopeofwork\` แล้ว (มีไฟล์ `warehouse.md` ว่างอยู่ตอนนี้ รอ Jead ใส่เนื้อหา)
- ตั้งกฎ: (1) ทุก agent ต้องอ่านทุกไฟล์ใน `scopeofwork/` ทุก session — เพิ่มเป็น step 3 ใน "Read in this order" ของ `CLAUDE.md`/`GEMINI.md` (2) ห้าม create/edit/delete/rename ใน folder นี้เด็ดขาด — เป็น R0 (ต้องถาม Jead ก่อนเสมอ ไม่ครอบคลุมโดย No-Ask rule) เพราะ Jead เป็นคนสร้าง/แก้เองเท่านั้น
- แก้ 3 ไฟล์: `CLAUDE.md`, `GEMINI.md` (read-order list), `AGENTS.md` (เพิ่ม 1 บรรทัดใน Forbidden list), `.agents/rules/bc-account-core-rules.md` (rule เต็มพร้อมเหตุผล — "Scope Of Work Folder" section)

## 2026-07-03 (cont.) — ตั้งกฎ Multi-Model Role Division (แทน Single-Agent, No-Advisor Rule เดิม)
- Jead สั่ง 2 รอบ ("ตั้งกฏตามนี้" + spec ละเอียดเต็มรูปแบบ) ให้ตั้งบทบาท: Claude Code=executor, GLM=frontend/fullstack advisor, DeepSeek=backend/DB/algorithm advisor, GPT 5.5=requirement/UX/ภาษาไทย/owner-logic advisor, Fable=Chief Architect/final judge — เคย flag conflict กับ "Single-Agent, No-Advisor Rule" (2026-06-19) ไว้ 1 ครั้งก่อนตั้ง
- แก้ `.agents/rules/bc-account-core-rules.md`: bullet เดิม (line 66) เปลี่ยนเป็น pointer + เพิ่ม section ใหม่เต็มรูปแบบ "Multi-Model Role Division — Executor + Advisors" (ตาราง role, when-to-consult, advisor template ข้อความ) และ "Task Protocol — Frontend / Backend / MongoDB" (10 ข้อ core principle + checklist ต่อ layer + end-of-task summary format 7 บรรทัด) — ระบุชัดว่า rule นี้ scope เฉพาะ `D:\bccode` เท่านั้น ไม่กระทบ global `~/.claude/CLAUDE.md` default ของโปรเจกต์อื่น
- อัปเดต memory `machine-speed-and-model-parity.md` ให้สะท้อนว่า bccode มี override เฉพาะโปรเจกต์ (กัน future session สับสนว่า global governance เปลี่ยนไปด้วย)
- **ยังไม่แตะโค้ด/ทดสอบ** — งานนี้เป็น docs/rule เท่านั้น ไม่มี Frontend/Backend/MongoDB ให้ verify

## 2026-07-03 (cont.) — อ่าน scopeofwork/aimodel.md ใหม่ (ไฟล์ต้นทางจริงของกฎ governance) + เติม rule ที่ขาด
- Jead สั่งอ่าน scopeofwork ใหม่ → พบไฟล์ใหม่ `scopeofwork/aimodel.md` (ต้นทางจริงของ "Multi-Model Role Division" ที่ตั้งไปแล้ว) มีรายละเอียดที่ยังไม่ได้เก็บ 2 จุด: (1) PostgreSQL/ClickHouse ยัง "ไม่ใช้งานจริง" ตอนนี้ (แค่เตรียมไว้) (2) "Frontend Testing Methodology" — Playwright/Stagehand เป็นหลัก, Computer Use เป็น fallback เท่านั้น, ห้ามเดา, ตรวจ MongoDB ทุกครั้งหลัง action, ทำต่อเนื่องจนผ่าน UAT หรือเจอ blocker
- เพิ่ม 2 ส่วนใน `.agents/rules/bc-account-core-rules.md`: "Frontend Testing Methodology" section ใหม่ + บรรทัด PostgreSQL/ClickHouse status ใน Task Protocol section

## 2026-07-03 (cont.) — เริ่ม redesign warehouse master data: embedded -> collection แยก (warehouse/warehouselocation/warehousebin)
- Jead สั่งสเปกละเอียด: แยก warehouse.location[].shelf[] (embedded) ออกเป็น 3 collection ระดับบนแยกกัน — warehouse / warehouselocation / warehousebin. ขอบเขตรอบนี้ = master data เท่านั้น ห้าม stockmovement/stockbalance/costing/ledger/qty-on-hand, ห้ามเก็บยอดคงเหลือใน warehousebin.fixeditems[]
- ก่อนเริ่ม: re-read scopeofwork ใหม่ (พบไฟล์ใหม่ `aimodel.md` = ต้นทางจริงของ Multi-Model Role Division rule ที่ตั้งไปแล้ว + เจอ 2 จุดที่ยังไม่ได้เก็บ — Postgres/ClickHouse ยังไม่ใช้งานจริง, Frontend Testing Methodology — เพิ่มเข้า core-rules.md แล้ว), re-read AGENTS.md + AI_INDEX.md เต็ม, อ่าน `warehouse-locations-editor.tsx` ที่ยังไม่เคยอ่าน
- **พบ finding สำคัญก่อนเริ่ม**: (1) `WarehouseLocationsEditor` (frontend) เป็น dead/unreachable code จริง — `productwarehousescreen` render `WarehouseTreeView` ตรงๆ เสมอ (system-settings-screen.tsx:2787) ทำให้ branch ที่ embed WarehouseLocationsEditor (line 8908, เงื่อนไข field.key==="location") ไม่มีทางถูกเรียกจริง — จะลบทิ้งเป็นส่วนหนึ่งของงานนี้ (2) `warehouse_pg.go` (PostgreSQL types เก่าจากก่อน migration) **ยังไม่ตายจริง** — `shop_http.go` ยังใช้ `WarehousePg`/`CompanyWarehousePg` ทำ dual-write เข้า PostgreSQL ตอนสร้าง shop ใหม่ (แยกจาก + เกินความจำเป็นจาก warehouse Mongo creation ที่ถูกต้องอยู่แล้วบรรทัดก่อนหน้า) — เป็น pre-existing bug/smell ที่ผิด Operational CRUD Pipeline Iron Rule (CRUD handler ห้าม write PostgreSQL ตรงๆ) แต่ **นอกขอบเขตงานนี้ ไม่แตะ** — flag ไว้เฉยๆ ห้ามลบ `warehouse_pg.go` เพราะ shop_http.go ยัง compile ต้องใช้
- ตรวจ blast radius: ไม่มี Go package อื่นอ้างอิง embedded Location/Shelf struct โดยตรง (เฉพาะใน `internal/warehouse/**`); module อื่น (stockbalanceimport, product_search, productbarcode) อ้างอิงแค่ string `warehousecode` เท่านั้น — ปลอดภัย ไม่กระทบ
- Checkpoint ก่อนเริ่ม (Checkpoint-Before-Large-Change Rule): commit `8ccbcdbd` + push แล้ว (รวมงาน warehouse company-scope + BOM standard-cost + governance rules ทั้งหมดที่ verify ผ่านของ session นี้)
- **เริ่ม Workflow 4 phase** (task `ws58f0rmn`, run `wf_227b8647-539`): Backend Implement -> Frontend Implement -> Verify -> Adversarial Review (4 lens ขนาน: multi-tenant security, data correctness, frontend UX contract, scope/regression) — ผลจะรายงานเมื่อเสร็จ

## 2026-07-03 (cont.) — warehouse redesign เสร็จ workflow 4 phase + แก้บั๊กจริงที่ adversarial review เจอ 5 จุด
- Workflow `ws58f0rmn` (Backend Implement -> Frontend Implement -> Verify -> Adversarial Review 4 lens) จบครบ: build/test/deploy/typecheck/e2e เขียว, Verify phase เจอ+แก้เอง 2 บั๊กแล้ว (stale build artifact ไม่ใช่โค้ด, Update* handler 500->400 ผิด status code)
- **Adversarial Review 4 lens เจอบั๊กจริงเพิ่ม 5 จุด (ยังไม่ได้แก้ตอนนั้น) — ผมแก้เองต่อทันที + verify ซ้ำครบ**:
  1. **[สูง] Full-document overwrite บน PUT ทุกตัว (Warehouse/Location/Bin)** — handler เดิม unmarshal JSON ใส่ struct ว่างเปล่าตรงๆ แล้ว service เขียนทับเอกสารทั้งก้อน ทำให้ field ที่ omit จาก request body หายไปเป็นค่า zero-value ทุกครั้งที่ PUT (ไม่ preserve เหมือน field ที่มีอยู่จริง) — แก้โดยให้ handler fetch เอกสารเดิมก่อน (`h.svc.InfoX(...)`) แล้ว unmarshal JSON ทับบน struct ที่มีค่าเดิมอยู่แล้ว (ไม่ใช่ struct ว่าง) — `encoding/json` จะเขียนทับเฉพาะ field ที่มีจริงใน JSON เท่านั้น ที่เหลือคงค่าเดิม — แก้ 3 handler ใน `warehouse_http.go` (UpdateWarehouse/UpdateLocation/UpdateBin)
  2. **[กลาง] Warehouse companyguids แคบลงไม่ revalidate location ลูก** — ถ้าแก้ companyguids ของ warehouse ให้แคบลง (ตัดบริษัทออก) แต่ location ที่มีอยู่แล้วยังอ้างอิงบริษัทนั้น จะไม่มีการเช็คซ้ำ ทำให้ location ค้างอยู่ในสภาพผิด invariant (ไม่ใช่ subset ของ parent อีกต่อไป) — แก้โดยเพิ่ม `repoLocation` เข้า `WarehouseHttpService`, ใน `UpdateWarehouse` fetch location ทั้งหมดของ warehouse นี้ก่อนเขียน แล้ว reuse `validateLocationCompanyScope` เดิมเช็คทีละตัว ถ้ามีตัวไหนหลุด subset ให้ reject 400 พร้อมชื่อ location ที่เป็นปัญหา (ต้องแก้ constructor signature 3 จุด: `warehouse_http.go`, `shop_http.go` bootstrap flow, test file)
  3. **[กลาง] Bin form ขาด `maxvolumecm3` ทั้ง form/payload** — backend model มี field นี้แต่ frontend ไม่เคยส่ง ทำให้ทุกครั้งที่แก้ bin จะ zero ค่านี้ทิ้ง (ผลจากบั๊ก #1 ผสมกับช่องโหว่นี้) — แก้โดยเพิ่ม input ในฟอร์ม + state + payload ให้ครบ (แก้บั๊ก #1 ที่ backend ทำให้ปลอดภัยขึ้นแล้ว แต่ user ยังควรตั้งค่าผ่าน UI ได้จริง)
  4. **[ต่ำ] `loading` state ค้าง true ถ้า auth ยังไม่พร้อมตอน mount** — `loadTree` early-return ก่อนถึง `setLoading(false)` — แก้โดย init `loading` เป็น `false` แทน (ตาม pattern เดียวกับ `company-branch-tree-view.tsx`)
  5. **[ต่ำ] `getProductBarcodesFromShelves` คืนค่าว่างเงียบๆ บน public API ที่ยังมีอยู่จริง** — filter `warehousecode/locationcode/shelfcode` บน `GET /productbarcode` ยัง Swagger-documented และ caller ภายนอก (mobile/integration) เรียกได้จริง แต่ตอนนี้ได้ผลลัพธ์ว่างเงียบๆ แทนที่จะ error ชัดเจน — ผิด Database No Silent Fallback Rule — แก้ให้คืน explicit error แทน `nil` error
- **Re-verify ครบทุกจุดด้วย evidence จริง**: backend build (`EXIT:0`), targeted test (`internal/warehouse/...` + `internal/product/productbarcode/...` ผ่านหมด — `internal/shop` test build fail จาก `utilmock` undefined **ยืนยันแล้วว่าเป็นของเดิมก่อนงานนี้** ไม่เกี่ยวกับที่แก้), deploy fast-binary-swap + healthz 200, frontend typecheck ผ่าน, e2e 2/2 ผ่าน, และยิง curl จริงยืนยันทั้ง 3 case: (1) warehouse name-only PUT companyguids รอด (2) location name-only PUT companyguids/sortcode/locationtype รอด (3) narrow companyguids ที่ location ยังต้องใช้ → reject 400 ไม่บันทึก (4) bin name-only PUT ทุก spec field รวม maxvolumecm3 รอด
- **ไฟล์ที่แก้เพิ่มจาก workflow เดิม**: `warehouse_http.go` (3 handler fetch-merge pattern), `services/warehouse_http_service.go` (repoLocation field + narrow-revalidation), `services/warehouse_http_service_test.go` (constructor call site), `shop_http.go` (constructor call site — ไม่แตะ logic เดิม), `product/productbarcode/services/productbarcode_http_service.go` (explicit error แทน silent empty), `frontend/warehouse-tree-view.tsx` (maxvolumecm3 + loading init fix)
- Commit + push ทันทีตาม Auto-push rule (multi-file schema/model redesign ที่ verify ครบแล้ว) — มี checkpoint ก่อนหน้า (`8ccbcdbd`) เป็นจุด rollback อยู่แล้ว

## 2026-07-03 (cont.) — เพิ่ม field ประเภทธุรกิจ (businesstypes) ลง warehouse
- Jead อัปเดต scopeofwork/warehouse.md เพิ่มส่วน "คุณสมบัติ" ระบุ 6 ประเภทธุรกิจที่ต้องรองรับ (ทั่วไป/ฝากขาย/Drop Ship/โกดังสินค้า/ซื้อมาขายไป/ขนส่ง) — ถามกลับว่าจะทำอะไรกับ 6 ข้อนี้ (master data ที่เพิ่งทำเสร็จเป็น structure กลางๆ ใช้ได้กับทุกธุรกิจอยู่แล้ว ไม่ผูกกับประเภทใดประเภทหนึ่ง) → Jead เลือก "เพิ่ม field ประเภทธุรกิจ/โหมดลงใน warehouse หรือ location"
- ตัดสินใจใส่ที่ระดับ **warehouse** เท่านั้น (ไม่ใส่ location ด้วย — เริ่มจากจุดเล็กสุดที่พอ, ขยายทีหลังได้ถ้าต้องการ per-location granularity)
- Backend: เพิ่ม `BusinessTypes []string` (`businesstypes`) ใน `models.Warehouse` — เป็น multi-select (คลังหนึ่งอาจให้บริการหลายโมเดลพร้อมกันได้ เช่น ทั่วไป+dropship) ไม่ใช่ enum เดี่ยว, เป็นแค่ classification ไม่มี downstream logic ผูกอยู่ (ต่างจาก companyguids ที่เป็น access restriction) — ไม่ต้องแก้ handler เพิ่มเพราะ Create/Update ทั้งคู่ unmarshal JSON เข้า struct เต็มอยู่แล้ว (field ใหม่ตามไปอัตโนมัติ รวมถึง partial-update preserve pattern ที่เพิ่งแก้ไปก่อนหน้า)
- Frontend: เพิ่ม `BusinessTypeScopePicker` component (chip multi-select ตาม pattern เดียวกับ `CompanyScopePicker`) + wiring ครบ (interface, form state, empty-form, edit-mapping, payload, UI)
- Verify: backend build+test ผ่าน, deploy+healthz ผ่าน, frontend typecheck ผ่าน, ทดสอบผ่าน browser จริง (สร้างคลัง เลือก "ฝากขาย"+"Drop Ship" → บันทึก → เช็ค API ตรง `businesstypes:["consignment","dropship"]` ถูกต้อง) แล้วลบข้อมูลทดสอบทิ้ง, e2e suite 2/2 ผ่านซ้ำ (ไม่ regression)

## 2026-07-03 (cont.) — /uat-crud-mongo ทดสอบหน้าจอคลังเต็มรูปแบบ (fresh pass หลัง redesign+businesstypes)
- ทำ full CRUD matrix ใหม่ทั้งหมดผ่าน browser จริง (ไม่ใช่แค่ curl) verify UI+API+MongoDB สามทางทุกจุด: Warehouse/Location/Bin ทั้ง Create/Read/Update/Delete — เชื่อม MongoDB ตรงผ่าน `docker exec mongodb mongosh` (ไม่มี MongoDB MCP ต่ออยู่)
- **พบ+ตรวจสอบ 1 จุดที่ดูเหมือนบั๊กตอนแรกแต่ไม่ใช่**: หลังลบ bin ผ่าน UI, MongoDB ยังมี document อยู่ (count=1) — ตรวจละเอียดพบเป็น **soft-delete ที่ถูกต้อง** (`deletedat`/`deletedby` ถูก set) ตาม pattern เดียวกับกฎ Barcode/Soft-Delete Read Integrity ที่ตั้งไว้ก่อนหน้านี้ในโปรเจกต์ — ตรวจต่อว่า read path (raw filtered query + `GET /warehouse/tree`) exclude soft-deleted ถูกต้องทั้งคู่ ไม่มี ghost/duplicate — สรุปว่า**ไม่ใช่บั๊ก** ผ่านครบ
- ยืนยัน index จริงใน MongoDB ตรงกับที่ adversarial review อ้างไว้ก่อนหน้า (unique compound index ทั้ง 3 collection + partial unique index บน `barcode`) ผ่าน `db.<collection>.getIndexes()` โดยตรง — เดิมยืนยันแค่ผ่าน code inspection + behavioral proof เท่านั้น รอบนี้เห็นจริงในฐานข้อมูล
- **เจอ test data ค้าง 58/11/6 documents** ใน `warehouse`/`warehouselocation`/`warehousebin` (holdingcode test) จากการทดสอบสะสมทั้ง session (curl manual test, e2e run ที่ interrupt กลางทาง ฯลฯ) — ตรวจแล้วเป็น test artifact ล้วนๆ (โค้ดขึ้นต้น E2EW/SRA/SCPWH/VFYWH/UAT* ทั้งหมด) → ลบทิ้งหมดตาม Post-Task Cleanup rule
- **พบ gap จริง**: `frontend/e2e/product-warehouse-crud.spec.ts` ไม่มี regression coverage สำหรับ field `businesstypes` ที่เพิ่งเพิ่ม → เพิ่ม assertion ใน test "create/edit/delete for warehouse, location, and bin": เลือก 2 ประเภทตอนสร้าง + ยืนยันว่ารอดหลัง edit (ตรงจุดที่เคยมีบั๊ก full-document-overwrite มาก่อน)
- Re-verify: e2e 2/2 ผ่าน, frontend typecheck ผ่าน, MongoDB สะอาด (0 live document ทุก collection หลังเทสจบ — e2e cleanup ตัวเองถูกต้อง)

## 2026-07-03 (cont.) — จัดระเบียบเมนู "ข้อมูลหลัก" (67 รายการ) ใหม่ เป็นขั้นตอน
- Jead ส่งภาพหน้าจอเมนู "ข้อมูลหลัก" (13 กลุ่ม, 67 รายการ) สั่งจัดใหม่ให้เป็นขั้นตอน ใช้งานง่าย เข้าใจง่าย — "ปรับได้ใหม่ทั้งหมด ย้ายได้เลย"
- **เริ่มด้วย Workflow แต่พบข้อผิดพลาดของตัวเอง**: launch ไปแล้วพบว่า phase "ปรึกษา GPT" ที่เขียนไว้จริงๆ แล้วเป็นแค่ subagent Claude อีกตัว (agentType อื่น) ไม่ใช่ GPT จริงผ่าน gpt-ask.py ตามกฎ Multi-Model Role Division ที่เพิ่งตั้ง — **หยุด workflow ทันที** (`TaskStop`) แล้วแก้ปัญหาด้วยการเรียก GPT จริงเองผ่าน `gpt-ask.py` โดยตรง
- **ปรึกษา GPT 5.5 จริง**: ได้ feedback ละเอียด — เห็นด้วยว่าดีขึ้น แต่ชี้ว่า (1) ชื่อกลุ่มบางอันควรปรับให้เป็นภาษาร้านค้ามากขึ้น (2) การแยก "ตั้งค่าการขาย" ควรแยกละเอียดกว่าที่เสนอ (จาก 4 เป็น 5 กลุ่ม เพราะ "ช่องทางขายและการชำระเงิน" ยังปนกันเกินไป) (3) **ไม่ควรย้าย "คลังความรู้"/"ผู้ช่วยแจ้งเตือน" ไปไว้ใน "เครื่องมือสินค้า"** เพราะยังผิดหมวดอยู่ดี ควรมีกลุ่มแยกของตัวเอง
- **ปรึกษา Fable (chief architect, final judge) จริงผ่าน Agent tool model=fable**: อ่านซอร์สโค้ดจริง (menu-data.ts, menu-data.test.ts, languages.tsv) แล้วเจอ 4 จุดวิกฤตที่แผนของผมพลาด: (1) **item id ในแผนร่างเป็นแค่ shorthand ไม่ตรงกับ id จริงในโค้ด** (เช่น "sales-color" จริงคือ "color", "serial-registry" จริงคือ "product-serial-registry") ต้องย้าย object เดิมแบบ verbatim ห้าม retype (2) **มี `menu-data.test.ts` ที่ pin โครงสร้างเดิมไว้** ต้องอัปเดตให้ตรงกับโครงสร้างใหม่ (3) **`languages.tsv` ต้องอัปเดต/เพิ่ม key ด้วย** เพราะ `menuText()` ใช้ backend dictionary ก่อน inline text (4) ควร reuse group id เดิมถ้า membership ไม่เปลี่ยน (กัน user's collapse-state ใน UI รีเซ็ต)
- **แก้เอง (ไม่ผ่าน workflow อีกรอบ เพราะ risk สูงเรื่อง exact id ต้องระวังเอง)**: จัดกลุ่มใหม่จาก 13→18 กลุ่ม, 67 รายการเท่าเดิม (นับซ้ำยืนยัน 67 ทั้งจาก test file และนับ `tx(` เอง) — สรุปการเปลี่ยน:
  - ย้าย "คู่ค้า" (เดิม "เจ้าหนี้/ลูกหนี้") ขึ้นมาอันดับ 6 (จาก 8) + เปลี่ยนชื่อเป็น "คู่ค้า (ลูกค้า/ผู้ขาย)"
  - "ข้อมูลหลักองค์กร" เปลี่ยนชื่อเป็น "งาน โครงการ และศูนย์ต้นทุน" (ชัดกว่าเดิม)
  - แยก "ตั้งค่าการขาย" (15 รายการก้อนเดียว) เป็น 5 กลุ่ม: ช่องทางขาย ราคา และขนส่ง / การรับเงินและบัญชีธนาคาร / หน้าร้าน POS / สมาชิก คูปอง และโปรโมชัน / เอกสารและบิล
  - ย้าย "คลังความรู้"+"ผู้ช่วยแจ้งเตือน" ออกจาก "นำเข้า/ส่งออกข้อมูล" (จัดผิดหมวดมานาน) ไปกลุ่มใหม่ "ผู้ช่วย AI และคลังความรู้"
  - "ร้านอาหาร/คาเฟ่" อยู่ท้ายสุด (เฉพาะธุรกิจร้านอาหาร ไม่ใช่ทุกธุรกิจต้องใช้)
  - ตั้งใจ**ไม่**เปลี่ยนชื่อ "รายละเอียดประกอบสินค้า"/"เชื่อมข้อมูลตลาดออนไลน์" ตามที่ร่างไว้ตอนแรก เพราะพิจารณาแล้วชื่อเดิมชัดพอ และกันชื่อซ้ำซ้อนกับ item label ข้างใน (self-correction ระหว่างแก้)
- อัปเดต `menu-data.test.ts` (assertion `sales-settings` group id → เช็คผ่าน `sales-loyalty`/`sales-channel-pricing` แทน) + `languages.tsv` (แก้ 2 key เดิม + เพิ่ม 6 key ใหม่ ตาม Thai-First No Auto-Translate Rule — Thai เขียนเต็ม, ภาษาอื่นเป็น English placeholder ชั่วคราว)
- **เจอ+แก้ deploy gotcha ระหว่าง verify**: แก้ `languages.tsv` แล้ว `deploy-mainapi-fast.ps1` (fast binary-swap) **ไม่ทำให้เห็นผล** เพราะไฟล์ asset ถูก bake เข้า Docker image ตอน build (`COPY ./assets/language/ /app/language/` ใน Dockerfile) ไม่ใช่ live-mount — ต้องทำ full rebuild (`docker-compose -f docker-compose.yml -f docker-compose.local.yml up -d --no-deps --build mainapi`) ตามกฎ deploy ของโปรเจกต์เอง ("runtime assets changed → full rebuild") ถึงจะเห็นคำแปลใหม่จริงใน backend
- **Verify ครบ**: vitest 16/16 ผ่าน, typecheck ผ่าน, นับ item จริง 67, ทดสอบผ่าน browser จริง (ขยายเมนูเห็นครบ 18 กลุ่มตรงตามแผน, คลิกนำทางจริงเข้า 3 item จาก 3 กลุ่มต่างกัน — โปรโมชั่น/เจ้าหนี้/คลังความรู้ ทำงานถูกต้องหมด), search เมนูยังหาเจอปกติ, ไม่มี console error

## 2026-07-03 (cont.) — ลบ field businesstypes ที่เพิ่งทำ (Jead ยืนยันไม่ต้องการ)
- Jead อัปเดต scopeofwork/warehouse.md เพิ่มคำชัดเจน "ให้เอาคุณสมบัติมาผสมกัน เพื่อให้โครงสร้างครอบคลุม **ผู้ใช้ไม่ต้องเลือกประเภทธุรกิจ**" — ถามยืนยันแล้วได้คำตอบ: ต้องการ **ลบ checkbox ที่เพิ่งทำออก** ไม่ใช่แค่เลือกได้หลายอัน — โครงสร้าง warehouse/location/bin (companyguids, locationtype, bintype, dimensions ฯลฯ) ที่มีอยู่แล้วต้องครอบคลุมทุกโมเดลธุรกิจได้เองโดยไม่ต้องมี business-type tag ให้ user จัดการเลย
- **ลบครบทุกจุด** (ย้อนกลับ commit `e7063f2a` ทั้งหมด): backend `Warehouse.BusinessTypes` field, frontend `BusinessTypeScopePicker` component + `WAREHOUSE_BUSINESS_TYPES` constant + ทุก state/payload/JSX wiring, e2e test assertion 2 บล็อก (create-time chip select + edit-persistence check)
- **Verify ครบหลังลบ**: backend build+test ผ่าน, frontend typecheck ผ่าน, deploy fast-binary-swap+healthz ผ่าน (ไม่ต้อง full rebuild รอบนี้เพราะไม่แตะ languages.tsv — businesstypes label เดิม hardcode ในคอมโพเนนต์ ไม่เคยผ่าน tsv), e2e 2/2 ผ่าน, เช็ค browser จริงยืนยัน checkbox หายไปจากฟอร์มคลังแล้วจริง (เหลือแค่ picker บริษัท), ไม่มี console error, ลบ MongoDB document ทดสอบ 2 ตัวที่มี businesstypes ค้างอยู่ (UATFULL1, E2EW586145 — ยืนยันเป็น test data ทั้งคู่)
