# Advisor Scorecard — Historical Record (GPT / GLM / DeepSeek / OpenRouter)

Historical consultation results only. **Superseded 2026-07-17 by the Multi-Model Orchestration
rule** (see `.agents/rules/bc-account-core-rules.md` "Multi-Model Orchestration"): Opus 4.8 now
orchestrates a 6-member advisor/subagent pool (GLM 5.2, Kimi K3 Max, DeepSeek, ChatGPT 5.6, Claude
Fable, Claude Sonnet). This scorecard is a past record, not active routing — do not use it to
auto-route or auto-score. (The intervening 2026-07-15 "must not consult any advisor" policy it
previously referenced is itself superseded.)

**Scoring rubric (1-10 per consult):**
- Accuracy (0-4): did Claude's own verification against real source/runtime confirm the advice, or
  did it turn out wrong/hallucinated?
- Usefulness (0-4): how much of the answer was actually adoptable/adopted as-is vs discarded?
- Reliability (0-2): did the call succeed cleanly, or did it need retries/time out/error?

10 = fully correct, fully adopted, first try. 0 = wrong and unusable. Show the score to Jead every
time an advisor is consulted, inline in the task report (for example "GLM: 8/10 — ...").

## Summary (running average per advisor x domain, update after each new row below)

| Advisor | Domain | Avg score | # consults | Notes |
|---|---|---|---|---|
| GLM | Frontend/React/Next.js/full-stack | 3.9/10 | 20 | Reliability remains uneven; narrow reviews can surface useful checks, but exact code/data decisions still require source/runtime verification. |
| DeepSeek | Backend/API/MongoDB | 4.0/10 | 21 | High-level direction can help, but recent answers often exhaust the output budget before an actionable conclusion. |
| GPT | Requirement/UX/Thai copy | 8.0/10 | 5 | Most consistent advisor so far across general UX calls and a real Thai tax-law citation check (9/10) — candidate to expand into more Thai-legal/domain research going forward per the adaptive-routing rule. |
| GLM | Documentation/data-model wiki | 4.5/10 | 2 | Found valid classification/wording risks, but answers were truncated and source/runtime evidence remained decisive. |
| DeepSeek | (other domains) | — | 0 | Expand here if DeepSeek proves strong outside its default domain. |
| GPT | (other domains) | — | 0 | Expand here if GPT proves strong outside its default domain. |
| OpenRouter | Wildcard/model-picker (any domain) | — | 0 | Added 2026-07-05, working via `~/.claude/tools/openrouter-ask.py` (verified live: `--model openai/gpt-5.2` answered correctly, `--list-models --filter deepseek` returned real pricing). Log the underlying model name in the domain/task column below (e.g. "OpenRouter → anthropic/claude-sonnet-5 — ..."), not just "OpenRouter", since the average here spans many different underlying models. |

## Log (append one row per consult, most recent last)

| Date | Advisor | Domain/task | Score | Reason |
|---|---|---|---|---|
| 2026-07-04 | GPT | Requirement/UX — creditor/debtor screen design | 8/10 | SME-friendly naming, 7-section layout, and the Auth-field UX call were sound and substantially adopted; call succeeded cleanly. |
| 2026-07-04 | DeepSeek | Backend — creditor/debtor schema | 7/10 | Correctly flagged the Auth plain-text-password risk and proposed WHT/bank-account/credit-limit fields that were adopted; also proposed PaymentTermType/DiscountDays fields that were judged out of scope and not adopted. |
| 2026-07-04 | GLM | Frontend/fullstack — creditor/debtor screen design | 2/10 | Call timed out after 3 retries (120s/180s/300s); honestly reported as a failure rather than fabricated, but delivered zero usable content this round. |
| 2026-07-05 | GPT | Requirement/UX — cost-center/project/job/channel screens | 7/10 | Reasonable SME UX and scope-trim suggestions, partly folded into the do-not-build list; nothing repo-specific to verify against. |
| 2026-07-05 | DeepSeek | Backend — cost-center/project/job schema | 5/10 | Correctly backed the 3-separate-modules direction, but proposed a `parentcode`+Job-FK shape and a non-repo `Names{Th,En}` struct that Fable rejected as not matching real source (YAGNI/ungrounded specifics). |
| 2026-07-05 | GLM | Full-stack architecture — cost-center/project/job module split | 8/10 | Converged on the same no-parentcode/no-new-renderer/leave-channelprice-alone position Fable ultimately adopted almost verbatim; call succeeded cleanly this time. |
| 2026-07-05 | GPT | Thai tax law — counterparty branch number on tax invoice/WHT cert | 9/10 | Correctly distinguished Section 86/4 (general minimum fields) from Notification No. 199 ข้อ 7-9 (specific branch/HQ requirement, VAT-registered buyer only) exactly matching primary-source text pulled directly from rd.go.th; correctly said WHT cert has no branch-code field; recommended option (b) one-master-with-branch-list, matching the final synthesis. |
| 2026-07-05 | DeepSeek | Thai tax law — counterparty branch number on tax invoice/WHT cert | 6/10 | Right on WHT cert (no branch requirement) and right that option (c)/(b) dominate over (a); overstated invoice rule as unconditional ("Yes, mandatory") and missed the VAT-registered-buyer-only condition confirmed in Notification No. 199 by primary source. |
| 2026-07-05 | GLM | Thai tax law — counterparty branch number on tax invoice/WHT cert | 5/10 | Correctly said WHT cert has no branch requirement and correctly favored (b)/(c) over (a); wrongly asserted counterparty branch is merely "best practice, not legally required" on the tax invoice, contradicted by Notification No. 199 ข้อ 9 which is a real legal mandate (conditional on buyer being VAT-registered) — its own Section 86/4 citation was also imprecise (invoice minimum fields don't literally require branch; that's ฉบับที่ 199's addition). |
| 2026-07-05 | GPT | Requirement/UX — unified business-partner design (creditor/debtor as same real party) | 8/10 | Shared partner identity + role flags + badge/combined-view (no auto-netting) + repeatable bank-account rows + defer list matched Fable's final spec almost exactly. |
| 2026-07-05 | DeepSeek | Backend — unified business-partner schema | 6/10 | Correctly identified `taxid` as the natural shared key (adopted); also proposed a new `creditlimit` collection and a `Branches` array restructure that Fable explicitly rejected as premature machinery/unnecessary churn on live screens. |
| 2026-07-05 | GLM | Frontend architecture — Thai address genericization + unified-partner scope boundaries | 9/10 | Migrate-branch-first, reuse-image-gallery-pattern-for-bank-accounts, separate `addressforactual` field (not reusing shipping array), and the exact phase-1/phase-2 scope split were all adopted near-verbatim by Fable; only its `variant` config option and one branch-code legal claim were rejected. |
| 2026-07-06 | GPT | Requirement/UX — product/barcode/product-set screen redesign (12-tab overload, bundle-vs-BOM) | 8/10 | Daily-use-vs-advanced tab grouping, "Product Set = ขายเป็นชุด / BOM = ผลิต" clarifier, frontend-only restaurant/marketplace hiding, and the defer list were all adopted into the final spec; its Product Set feature wish-list (stock-availability preview, COGS rollup) correctly self-deferred but slightly over-scoped for a first pass. |
| 2026-07-06 | DeepSeek | Backend — Product/ProductBarcode schema strategy + index audit | 4/10 | High-level "strictly additive, never rename" stance is correct but already project law; claimed Product Set has "no structural validation" when backend ValidateProductClassification actually double-guards itemtype/materialtype (verified in UAT); index recommendations cite fields (`isactive`, `searchkeyword`, `nameen`) that don't exist on the real Product model; dual-write RestaurantDetail migration machinery is over-engineered for a pre-launch disposable DB. |
| 2026-07-06 | GLM | Frontend architecture — product-screen decomposition + tenant gating + test plan | 6/10 | Extraction-order plan, hide-don't-disable, and scope-creep defer list adopted in modified form; but its form-context refactor is unnecessary (real code already has per-tab components taking {value,onChange} props — mechanical file moves suffice), and its tenant-gating step assumes an existing business-type flag that verification shows does not exist on the Shop model. |
| 2026-07-07 | DeepSeek | Backend — Product Mongo→Kafka→PG pipeline design | 6/10 | Dedicated `when-product-*` topics, metadata-only writes (never touch stock columns), and the JOIN-ordering race analysis were all correct and adopted; but "extend inventory.go + reuse biapi-inventory-consumer group" contradicts the repo's real per-entity registration pattern in handlers/kafka.go, its plain-INSERT-no-ON-CONFLICT breaks idempotent resync, and it missed the DatabaseRebuildAll-recreates-empty-table trap entirely. |
| 2026-07-07 | GLM | Backend — Product Kafka pipeline file layout + registration | 9/10 | Nearly everything verified true against source and adopted: emit via a small producer-side helper (real pattern = MessageQueueRepository), new sibling consumer file kafka/product.go, new `biapi-product-consumer` group, registration at the exact same site as biapi-inventory-consumer (handlers/kafka.go StartConsumers — verified), upsert-on-itemcode leaving stock columns alone, and flagging rebuild-backfill as mandatory; only deviation is Fable includes the minimal resync endpoint in this pass instead of deferring it. |
| 2026-07-07 | GPT | Requirement — backfill existing Product data or prospective-only | 8/10 | Correct business call that prospective-only looks like "สินค้าหาย" to the owner and that repair must be a reusable tenant-scoped action (not a one-off script) because PG is disposable/rebuildable — adopted as POST /product/resync reusing the new pipeline; honestly reported its sandbox read failure instead of fabricating source claims; per-count reporting suggestion adopted in reduced form. |
| 2026-07-09 | GLM | Frontend — product-group tree undo/redo + up/down move-button design | 7/10 | Valid, adopted catches: Undo/Redo (and the move buttons) must also be disabled while a save/apply is in flight, not just its own busy flag; history index must start at -1; snapshot must be captured before this move's own optimistic setState so undo chains correctly. Several other "bugs" were false alarms once checked against the real source already read this task: `updateList` in `reorderGroup`/`moveGroupAsChild`/`moveGroupToRoot` already unions old-parent + new-parent sibling payloads (so the before/after snapshot already covers both sides); `onRefresh` is wired to fire only from this component's own error catch blocks, never on a successful move, so resetting undo history on `[records]` change isn't "too aggressive" in practice; the sibling array used for up/down index math is already the real per-parent `childGroups`/`nodes` array (not a flattened global list), so first/last-sibling detection is correct as designed; the xsort `code` field is a hardcoded literal `"X"` everywhere, not server-derived, so there's no staleness risk on undo. Likely due to the prompt summarizing the design rather than pasting raw source. |
| 2026-07-09 | GLM | Frontend — product-group tree drag-interaction jank redesign (throttling + reparent disambiguation) | 9/10 | Validated the 5-piece redesign (kill double-dispatch, rAF-coalesce, one-shot cached geometry snapshot, stop live-reflow-during-hover, chip-as-explicit-reparent-target) as sound and sufficient to eliminate every measured cause. Contributed 3 concrete, correct, adopted refinements not in the original draft: drive the drag-ghost overlay via a ref + direct `style.transform` write inside the rAF tick instead of React state (the single biggest catch — without it, cursor-follow alone reintroduces per-move re-renders of the whole tree even after every other fix); `touch-action: none` on draggable rows (confirmed absent via grep); re-snapshot cached rects on container `scroll` + use a half-open interval (`top <= y < bottom`) for boundary hit-testing to avoid flicker. Correctly identified virtualization/auto-scroll as risks in the abstract; verified against real source that neither currently exists in this component, so those specific concerns don't apply here (not a wrong claim, just conditional/hypothetical, honestly framed as "if X exists"). One raised concern (ambiguous "after the last child" semantics) was checked against the real `reorderGroup` code and found to be a non-issue — existing behavior already unambiguously derives the new parent from the target row's own `parentguid` regardless of hit-test mechanism, so no change was needed there. Call succeeded cleanly, single try. |
| 2026-07-10 | GLM | Rule governance — mandatory dual advisors with Thai SME/ERP/POS/accounting focus | 6/10 | Correctly recommended non-trivial-only consultation, read-only boundaries, source/runtime verification, Thai tax/SME context, and trivial/incident exceptions; advice was generic rather than repo-specific, but the core controls were adopted. |
| 2026-07-10 | DeepSeek | Rule governance — mandatory dual advisors with Thai SME/ERP/POS/accounting focus | 6/10 | Correctly recommended the same non-trivial/read-only/source-verification boundary plus anonymization and decision accountability; generic rather than repo-specific, but the core controls were adopted. |
| 2026-07-11 | GLM | Frontend — inline CRUD form field flow and flicker review | 7/10 | Correctly identified `content-start` as the root CSS fix for stretched grid rows and reminded to preserve reachable actions; browser evidence confirmed the compact field flow. |
| 2026-07-11 | DeepSeek | Frontend — inline CRUD form field flow and flicker review | 1/10 | Did not return usable output before the 90-second advisor timeout; no recommendation was adopted. |
| 2026-07-11 | GLM | Frontend — Product Group record-switch flicker | 1/10 | Returned no usable output before the 90-second timeout; no recommendation was adopted. |
| 2026-07-11 | DeepSeek | Frontend-only — Product Group record-switch flicker | 3/10 | Correctly confirmed no API/MongoDB change is needed and advised keeping the form mounted, but diagnosed an already-removed code path and did not identify the real `main-crud` hydration unmount. |
| 2026-07-11 | GLM | Frontend — generic hydrated CRUD record-switch stability | 1/10 | Returned no usable output before the 55-second timeout; no recommendation was adopted. |
| 2026-07-11 | DeepSeek | Async/API review — generic hydrated CRUD record-switch stability | 7/10 | Correctly confirmed no contract change, endorsed one request token incremented for every open, and highlighted stale-response protection; executor verified the implementation against the real shared hydration branch. |
| 2026-07-11 | GLM | Frontend — Product Category steady detail pane | 1/10 | Returned no usable output before the 55-second timeout; no recommendation was adopted. |
| 2026-07-11 | DeepSeek | Async/API review — Product Category steady detail pane | 6/10 | Correctly supported retaining the prior hydrated detail beneath a non-interactive loading overlay and leaving API/MongoDB unchanged; additional API metadata/cache suggestions were out of scope. |
| 2026-07-11 | GLM | Frontend — Product Category zero-frame flicker correction | 1/10 | Returned no usable output before the 55-second timeout; no recommendation was adopted. |
| 2026-07-11 | DeepSeek | Async review — Product Category zero-frame flicker correction | 5/10 | Correctly requested rapid-click/out-of-order regression coverage, which was added and passed; incorrectly claimed the stale-response guard was absent although the real effect already uses a cleanup `cancelled` flag, and its changing-key suggestion would reintroduce remount flicker. |
| 2026-07-11 | GLM | Frontend — Products-in-Category zero-frame editor switching | 1/10 | Returned no usable output before the 55-second timeout; no recommendation was adopted. |
| 2026-07-11 | DeepSeek | Async/API review — Products-in-Category zero-frame editor switching | 1/10 | Returned no usable output before the 55-second timeout; no recommendation was adopted. |
| 2026-07-11 | GLM | Frontend/full-stack — Product-only category membership | 4/10 | Correctly reinforced Product `code` as the key and removal of Barcode from search/dedupe, but the answer was truncated after the opening checklist; only source-confirmed high-level points were adopted. |
| 2026-07-11 | DeepSeek | Backend/API/MongoDB — Product-only category membership | 3/10 | The call completed but exhausted its output budget before a concise actionable checklist; no advisor-specific implementation detail was used beyond direction already proven from local source. |
| 2026-07-11 | GLM | Frontend/data integrity — Product Category phantom count and targeted repair | 3/10 | The call completed but was truncated before its requested checklist. It raised mapping/normalization checks already proven from Mongo/API evidence; no advisor-specific implementation detail was needed. |
| 2026-07-11 | DeepSeek | Frontend/API — Product Category phantom count and targeted repair | 3/10 | The call completed but was truncated while discussing duplicate and replace semantics. Those concerns were already settled by the source-verified Product-only backend PUT contract. |
| 2026-07-11 | GLM | Frontend UX — fixed top actions for inline editors | 7/10 | Correctly flagged narrow-screen Thai header crowding and dirty/validation visibility. The responsive action group now takes its own row below `sm`; other concerns were existing workflow behavior, not regressions from the move. |
| 2026-07-11 | DeepSeek | Frontend UX — fixed top actions for inline editors | 2/10 | The call completed but exhausted its output budget before producing the requested concise bullets; no recommendation was adopted. |
| 2026-07-12 | GLM | DEV data — Product option/color/size sample records | 4/10 | Correctly flagged the visible soft-deleted color as a lookup/list integrity risk and questioned the intentionally empty SKU combinations, but the answer was truncated; no unrelated cleanup or SKU seed was adopted. |
| 2026-07-12 | DeepSeek | DEV data — Product option/color/size sample records | 2/10 | The call completed but exhausted its output budget before a concise actionable list; no advisor-specific change was adopted. |
| 2026-07-12 | GLM | DEV data — comprehensive Product Option Set seed review | 3/10 | The short follow-up still spent the budget on analysis and was truncated; no source-specific change was adopted. |
| 2026-07-12 | DeepSeek | DEV data — comprehensive Product Option Set seed review | 7/10 | Returned five concrete Thai POS risks (sample data leaking into POS/marketplaces/reports and lacking ownership); retained the deliberate no-SKU/no-price/no-stock boundary and did not perform cleanup without instruction. |
| 2026-07-14 | GLM | Frontend UX — compact top-menu item polish | 3/10 | Correctly supported retaining the shared row and existing design tokens, but the response was truncated before actionable detail; no GLM-specific change beyond the source-backed plan was needed. |
| 2026-07-14 | DeepSeek | Frontend UX — compact top-menu item polish | 8/10 | Correctly flagged border-layout shift, long Thai labels, fixed minimum height, focus visibility, and button shrink risk. Adopted transparent persistent borders, two-line labels, min-height, fixed action sizing, and a focus ring while preserving the compact menu density. |
| 2026-07-15 | GLM | Frontend UX — Thai/English Jarvis model-browser UI | 4/10 | Correctly flagged Thai date/locale and text-overflow checks; locale handling and live visual verification were adopted, but the answer was truncated before repo-specific detail. |
| 2026-07-15 | DeepSeek | Frontend UX — Thai/English Jarvis model-browser UI | 4/10 | Correctly recommended strict query-language validation and noted reload state; strict `lang=th|en` handling was adopted, while reload persistence was unnecessary for this read-only screen. |
| 2026-07-15 | GLM | Frontend/data scope — MongoDB-only model browser | 1/10 | Returned only an opening sentence before truncation; no actionable recommendation was available or adopted. |
| 2026-07-15 | DeepSeek | Frontend/data scope — MongoDB-only model browser | 3/10 | Partially raised graph-link endpoint normalization before truncation; the implementation already handled string/object endpoints and remained source/runtime-driven. |
| 2026-07-14 | GLM | Frontend/WebGL — first-entry graph readiness | 5/10 | Correctly identified opaque `no-cors` readiness, timer cleanup, and keeping the iframe unmounted while the API is unreachable. Its suggestion to wait for graph `ready` was rejected because source proves `idle` must mount the iframe to trigger `/api/graph`. |
| 2026-07-14 | DeepSeek | Frontend/WebGL — first-entry graph readiness | 3/10 | Flagged cross-origin response readability, but the response was truncated before concise advice. Runtime verified the status endpoint already returns CORS `*`; no DeepSeek-specific code was adopted. |
| 2026-07-14 | GLM | Documentation/data-model wiki — mainapi product models | 4/10 | Correctly flagged duplicate-H1 navigation and unresolved external-link risks, but the answer was truncated and no scope-expanding recommendation was adopted; module-qualified filenames and source-resolved internal links were already in the plan. |
| 2026-07-14 | DeepSeek | Backend/API/MongoDB — mainapi product-model wiki | 5/10 | Correctly reinforced explicit embedded-field handling, import-aware cross-module links, repository-backed collection evidence, and validation; the answer was truncated and those controls were already source-verified before editing. |
| 2026-07-14 | GLM | Frontend/data-model wiki — Mongo root vs general classification | 5/10 | Correctly warned that embedded types should not be described as wholly unrelated to MongoDB and that two visual groups need clear labels; adopted the precise GENERAL / EMBEDDED wording. The response was truncated. |
| 2026-07-14 | DeepSeek | Backend/MongoDB — Mongo root vs general classification | 5/10 | Correctly distinguished direct root-document persistence from embedded serialization and prompted repository-backed validation; adopted the explicit non-standalone-collection wording. The response was truncated. |
| 2026-07-14 | GLM | Thai SME/business — accounting SaaS outlook | 1/10 | Call completed but spent the entire response restating the prompt and was truncated before a usable recommendation; no advisor detail was used. |
| 2026-07-14 | DeepSeek | Backend/compliance — accounting SaaS outlook | 1/10 | Call completed but spent the entire response restating the prompt and was truncated before a usable recommendation; no advisor detail was used. |
