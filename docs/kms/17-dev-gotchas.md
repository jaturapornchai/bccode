# กับดักตอนพัฒนา (dev gotchas) ที่เคยเสียเวลาไปแล้ว — อย่าเจอซ้ำ
> ตรวจล่าสุด: 2026-09-07 (commit d93a210d) · แถวที่ระบุวันที่ 2026-09-09 ตรวจซ้ำที่ commit 098107e1 — รวมจาก memory ของ Claude + handoff; แต่ละข้อมีวันที่ที่ยืนยันจริง · 2026-09-25: ลบ/ปรับแถวที่อ้าง Kafka/Mongo/librdkafka/collection และโมดูล `/transaction/*` ที่ถูกถอดออกจากระบบ 2026-09-23 (ADR `decisions/2026-09-23-remove-mongo-kafka-redis-clickhouse.md`) และแถวผู้ช่วย AI ภายนอกที่ปลดระวางวันเดียวกัน; เลขบรรทัดอ้างอิงตรวจใหม่ที่ HEAD `c58f0b62`

## Frontend

| กับดัก | อาการ | วิธีที่ถูก | ยืนยัน |
|---|---|---|---|
| `frontend/next-env.d.ts` เด้งไปมา | ไฟล์นี้ถูก Next generate ใหม่ทุกครั้ง: `next dev` เขียน `./.next/dev/types/*`, `next build` (เช่น `sh tools/verify.sh frontend-build`) เขียน `./.next/types/*` → ขึ้น modified ทั้งที่ไม่ได้แก้เอง | `git checkout -- frontend/next-env.d.ts` ก่อน commit (ห้ามแก้ไฟล์นี้เอง ตามที่ Next กำกับไว้ในหัวไฟล์) | 2026-09-09 |
| `frontend/src/app/globals.css` มี EOL ผสม (CRLF/CR/LF) | Edit/Write tool normalize ทั้งไฟล์ → diff 1,400+ บรรทัดจากการแก้ 1 บรรทัด | แก้ด้วย Node script แบบ byte-preserving (`readFileSync` → `String.replace` ตรง ๆ → `writeFileSync`); ตรวจ `git diff --stat` ต้องเล็ก | 2026-08 |
| ปุ่มไม่รับ `text-xs`/`text-sm` | มี reset แบบ unlayered `button{font-size:inherit}` ชนะ Tailwind v4 utilities เสมอ | ใช้ `text-xs!` (important) หรือ inline style | 2026-06-29 |
| dark mode ทดสอบผิดวิธี | flip `data-theme` ด้วย JS แล้วดูเหมือน bug | palette ถูกเขียน inline บน `<html>` โดย `applyVisualTheme()` (`src/lib/theme-data.ts`) → ต้อง**กดปุ่มสลับธีมจริง** | 2026-09-02 |
| แก้ CSS login แล้วไม่เปลี่ยน | block กลางไฟล์ (design-pass 2026-08-28) hard-code สีขาว ชนะ tail override | selector ท้ายไฟล์ต้อง `.login-shell .login-card` ขึ้นไป และใช้ตัวแปร `--login-*` | 2026-09-02 |
| `localhost:3000` โชว์ UI เก่า | มี `next start` (production build) ค้างอยู่ | `netstat -ano \| grep :3000` → kill → `npm run dev` | 2026-09-02 |
| `npm`/`npx` ใน Git Bash บน Windows พัง ("cannot find the path specified") | postinstall/npx ล้ม | `npm install --ignore-scripts`; รัน `node node_modules/next/dist/bin/next dev` ตรง ๆ ถ้า `npm run dev` ไม่ขึ้น | 2026-06-29 |
| `next.config.ts` ต้องไม่มี `output: "standalone"` | `next start` พังใน Docker on-prem | build ด้วย `frontend/Dockerfile` (multi-stage, จบที่ `CMD ["npm","run","start"]`) และส่ง build-arg `BCAI_LOCAL_BACKEND_URL` ให้ตรงกับ backend ของ environment นั้น (ถ้าไม่ส่ง rewrites ใช้ค่า fallback `http://mainapi:8888` เมื่อ `NODE_ENV=production` — `frontend/next.config.ts:19`) — **ไม่มีไฟล์ `frontend/Dockerfile.onprem` ในรีโป** (`git ls-files frontend/Dockerfile*` มีแค่ `frontend/Dockerfile`) | 2026-09-09 |
| viewport | ออกแบบไว้สำหรับ **iPad (≥768px) ขึ้นไป** — มือถือแค่ไม่พัง | ใช้ grid หลายคอลัมน์บนจอกว้าง ไม่ stack ทุก field แนวตั้ง | กฎ 2026-06-28 |
| Popover โดนตัด | `overflow: hidden` บน panel ที่มี popover ลูก | clip ที่ shell ชั้นนอกสุดเท่านั้น (กฎ premium ข้อ 7 ใน AGENTS.md) | 2026-09-02 |

## Backend / Go

| กับดัก | รายละเอียด | ยืนยัน |
|---|---|---|
| build/test บน Windows native เคยพังเพราะ Kafka CGO | ก่อน 2026-09-23: `CGO_ENABLED=0` + ไม่มี gcc → confluent-kafka undefined ต้องพึ่ง container `golang:1.26` เท่านั้น — **แก้แล้ว**: ถอด Kafka/librdkafka ออกทั้งระบบ (ADR `decisions/2026-09-23-remove-mongo-kafka-redis-clickhouse.md`) ทำให้ `go build ./... && go vet ./... && go test ./...` รันตรงบน Windows ได้แล้ว; `tools/verify.sh backend`/`postgres` ยังใช้ container `golang:1.26` สำหรับ compile-all + integration เหมือนเดิม | 2026-09-23 |
| image ของ `mainapi` เก่ากว่าโค้ด → **จอขึ้น "ไม่พบข้อมูล" แทนที่จะ error** | image สร้าง 09-14 แต่ HTTP ของสินทรัพย์ถาวร (`/fa/v2/*`) เข้ามา 09-15 (`a79b5279`) ทุก request ได้ 404 จอจึงดูเหมือนไม่มีข้อมูล UAT ที่ "ผ่าน" จึงพิสูจน์ได้แค่ป้ายบนจอ — เทียบ `docker image inspect backend-mainapi --format '{{.Created}}'` กับวันที่โมดูล แล้ว rebuild ด้วย `cd backend && docker compose -f docker-compose.yml -f docker-compose.local.yml -f docker-compose.dev.yml up -d --build mainapi` | 2026-09-16 |
| แยก "ไม่มี route" กับ "token ไม่ผ่าน" | `curl -s -o /dev/null -w "%{http_code}" localhost:8888/<route> -H "Authorization: Bearer <token>"` → **404 = ไม่มีใน build, 401 = มี route แต่ token/shop ไม่ผ่าน**; ขอ token โดยไม่ใช้รหัสผ่าน: `POST /demo-login` แล้ว `POST /select-shop {"holdingcode":"demo","businesscode":"C01"}` — `/demo-login`/`/select-shop` เป็น route ของ mainapi จึง**ไม่มี** prefix `/goapi` (ดูแถว "path ฝั่ง backend" ด้านล่าง) | 2026-09-16 |
| `authFetch()` **ไม่ได้แนบ token ให้** | `client-auth-session.ts:174-192` แค่ *refresh* header ที่มีอยู่แล้ว ถ้า request ไม่มี `Authorization` มันจะ `fetch()` เปล่า ๆ → backend 401 → จอที่อ่าน `res?.items` ได้ undefined แล้วขึ้น "ไม่พบข้อมูล" เงียบ ๆ (เจอที่ `frontend/src/lib/fixed-assets.ts` ทุก endpoint) → ใช้ `apiFetch(path, init)` จาก `client-auth-session.ts` แทนทุกที่ (แนบ `Authorization` + `x-bc-backend-url` ให้เอง, ไม่มี session แล้ว throw ข้อความไทย); ตรวจด้วย `node tools/audit-auth-fetch.mjs` | 2026-09-16 |
| จอทั้งแท็บงาน "ไม่มีสิทธิ์ดูข้อมูลนี้" ทั้งที่สิทธิ์ครบ | ที่จริงคือคำขอออกไปแบบไม่มี token → 401 แล้ว `fetchErpTransactions` แปลง 401/403 เป็น `error: "unauthorized"` ซึ่งจอแสดงเป็นข้อความเรื่องสิทธิ์ — เจอพร้อมกัน 5 ไฟล์ (`erp-transaction/operations/reports/tools`, `thai-tax`) ไม่มีคำว่า Authorization เลยสักบรรทัด | 2026-09-16 |
| `beforeEach` ใน `describe` หนึ่ง **ไม่ทำงานให้ `describe` อื่นในไฟล์เดียวกัน** | เทสต์ครึ่งไฟล์ผ่าน ครึ่งไฟล์ล้มด้วย error คนละเรื่อง (เช่น `connection_error` เพราะไม่มี session) → helper ที่ลงทะเบียน hook ต้องเรียกที่ **ระดับไฟล์** เช่น `setupTestAuthSession()` ใน `frontend/src/lib/test-auth-session.ts`; และห้ามตั้งชื่อขึ้นต้น `use…` ไม่งั้น eslint `react-hooks/rules-of-hooks` ฟ้อง | 2026-09-16 |
| เทสต์ที่ stub `fetch` ให้ตอบ 401 จะ **ล้าง session ทิ้ง** | `authFetch` เจอ 401 → เรียก refresh → stub ตอบ 401 อีก → `clearAuthSession()` → assertion ถัดไปในเทสต์เดียวกันพังเป็น `connection_error` → เรียก `signInTestSession()` (`frontend/src/lib/test-auth-session.ts:12`) คั่นก่อน scenario ต่อไป | 2026-09-16 |
| จอว่างเพราะไม่มีข้อมูลตัวอย่าง | `node tools/seed-demo.mjs fa` สร้างประเภทสินทรัพย์ 3 + สินทรัพย์ 5 ผ่าน API จริง (ไม่ใช่ mock บน frontend) รันซ้ำได้ ไม่สร้างซ้ำ; ล้างด้วย `--wipe` ซึ่งลบเฉพาะรหัสที่ tool สร้างเอง (กฎ UAT ข้อ 4 ห้าม regex กว้าง) | 2026-09-16 |
| ไม่รู้ว่าจอไหนมี API จริง | `node tools/probe-endpoints.mjs` ยิงทุก endpoint ที่หน้าจอเรียกด้วยบัญชี demo (ค่าเริ่มต้น holding `demo`/บริษัท `C01`, เปลี่ยนด้วย `BC_PROBE_HOLDING`/`BC_PROBE_BUSINESS`) แล้วบอกเป็น 200 / 404 / 401; ตั้งแต่ 2026-09-23 API `/transaction/*` เดิมถูกลบทั้งชุด จอกลุ่มนั้นขึ้น "ยังไม่พร้อม" ผ่าน `isMenuBackendRetired` (`frontend/src/lib/menu-screen-status.ts`) — 404 ของกลุ่มนี้จึงเป็นสภาพปกติ | 2026-09-16 (ปรับ 2026-09-25) |
| path ฝั่ง backend: route ของ mainapi (`/gl/v2/*`, `/fa/v2/*`) **ไม่มี** prefix แต่ route ของ goapi (`/api/*`) **ต้องมี** `/goapi` | goapi ถูก mount ที่ `ms.Echo().Group("/goapi")` (`backend/main.go:172`) เช่น `/api/report/tax/wht` ต้องเรียกเป็น `/goapi/api/report/tax/wht`; ดูของจริงที่ `toGoApiPath()` ใน `frontend/src/app/api/goapi/[...goPath]/route.ts:57` — เดาผิดแล้วจะอ่าน 404 ว่า "ยังไม่มี API" ทั้งที่มี | 2026-09-16 |
| แถวค้างใน `stock_dirty` หลังแก้บั๊กแล้ว | worker เลิกลองเมื่อ `attempts >= MaxAttempts` (=5, `stockengine/dirty.go:20`) แถวจะค้างพร้อม `lasterror` เดิมแม้สาเหตุถูกแก้ไปแล้ว → ปลดล็อกด้วย `update stock_dirty set attempts=0, lasterror='', leaseowner='', leaseuntil=NULL where itemcode='...'` (คอลัมน์ NOT NULL ใส่ NULL ไม่ได้) — หมายเหตุ 2026-09-25: ไม่มีโค้ด production สร้างตาราง `stock_dirty` แล้ว ดู KI-30 ใน `15-known-issues.md` | 2026-09-16 |
| รายงานขายตอบ `QUERY_ERROR` เสมอ | `sales_report.go` เคยอ้าง `d.name0` บนตาราง `debtor` ซึ่ง**ไม่มีคอลัมน์นี้** (เก็บชื่อเป็น `names JSONB` แบบ `[{code,name}]` — ต่างจาก `product`/`productbarcode` ที่มี `name0` จริง) และ handler โยน error ทิ้งโดยไม่ log จึงไม่มีทางรู้สาเหตุ; แก้แล้ว 2026-09-16 พร้อมเพิ่ม `logger.Error` ทั้งสองจุด — หมายเหตุ 2026-09-25: ตาราง `doc`/`debtor` ไม่มีโค้ดสร้างแล้ว ดู KI-30 ใน `15-known-issues.md` | 2026-09-16 |
| 404 จาก API = จอยังไม่เปิดใช้งาน ไม่ใช่ error ที่กดใหม่แล้วหาย | `statusErrorKey()` ใน `erp-transaction.ts` และ `errorKeyFor()` ใน `erp-operations.ts` แปลง 404 เป็น key `module_not_available`; `erp-crud-workbench.tsx` แสดงเป็นข้อความเทา (ไม่ใช่กรอบแดง) และ**ซ่อนปุ่มสร้างเอกสาร** — อย่าปล่อยให้ผู้ใช้กดปุ่มที่บันทึกไม่ได้ | 2026-09-16 |
| gofmt import order | แทรก import เองแล้วลืม sort → **ไม่มีอะไรจับให้เลย** (ไม่มี CI ตั้งแต่ 2026-09-09 และ `tools/verify.sh` ก็ไม่รัน gofmt — workflow เดิมก็ไม่เคยรัน) ต้องรัน `gofmt -l .` เองก่อน commit | 2026-09-09 |
| ~~ไฟล์ log ถูก track ใน git~~ (แก้แล้ว) | log ของ backend ใต้ `backend/internal/**/logs/` + `frontend/.next-dev.log`/`.next-dev.err.log` เคยถูก track จึงเปลี่ยนทุกครั้งที่รัน test/dev server — `git rm --cached` แล้ว 2026-09-09 และ `.gitignore:54-55` คุมไว้แล้ว ไม่ต้อง `git checkout --` ก่อน commit อีก | 2026-09-09 |
| curl.exe บน Windows กับข้อความไทย | `curl -d "…ไทย…"` inline กลายเป็น `?????` ใน DB → เขียน JSON ลงไฟล์ UTF-8ก่อนแล้ว `--data-binary @file.json` | 2026-07-02 |
| Dev Login 401/404/503 | ดู `16-environments-and-servers.md` §DEV (secret drift ระหว่าง env กับ container) | 2026-09-02 |
| `backend/assets/language/languages.tsv` ต้องมี **13 คอลัมน์ต่อบรรทัดพอดี** (`key,th,en,cn,ja,km,ko,lo,my,vi,ms,id,fil` ตามบรรทัดหัวไฟล์) | tab ขาดแล้วไม่มีอะไรเตือน: แถวที่มีน้อยกว่า 2 คอลัมน์ถูกข้ามเงียบ ๆ (`backend/internal/goapi/language/language.go:166-168`) → `Text()` คืน key ดิบให้ผู้ใช้เห็น (`:69-78`); แถวที่ขาดคอลัมน์กลางทำให้ค่าภาษา**เลื่อนช่อง** (เคยเจอที่ `line_oa_qrcode` มี 12 คอลัมน์ ขาด `km` → ช่อง `km` เก็บข้อความเกาหลี, `ko` เก็บลาว, `lo` เก็บพม่า … `fil` ว่าง); test ที่มี (`frontend/src/lib/menu-data.test.ts:356-368`) ตรวจครบทุกภาษาเฉพาะ key ที่ผูกกับเมนูเท่านั้น จึงไม่จับแถวเหล่านี้ — ตรวจทั้งไฟล์ก่อน commit ด้วย `awk -F'\t' 'NF!=13 && NF>0 {print NR": "NF}' backend/assets/language/languages.tsv` — **2026-09-16 ล้างครบแล้ว: ทุกแถวมี 13 คอลัมน์ ไม่มี key ซ้ำ** (ลบแถวที่มีแต่ key ไม่มีคำแปล 9 แถว, เติมคอลัมน์ `km` ให้ `line_oa_qrcode`, เติมคำแปล 11 ภาษาให้ `cost_calculation_test_result`) และลบแถว key ซ้ำที่ไม่มีวันถูกใช้อีก 16 แถว — ตัวโหลดเขียน `nextCache[key] = …` จึง **แถวล่างสุดชนะเสมอ** (`backend/internal/goapi/language/language.go:181`) เพิ่มแถวใหม่แล้วต้องตรวจทั้งจำนวนคอลัมน์และ key ซ้ำ | 2026-09-16 |
| ข้อความจาก dictionary ถูก**ตัดคำว่า ERP ทิ้งอัตโนมัติ** | `sanitizeBackendLanguageText()` (`frontend/src/lib/backend-language-sanitize.ts`) ลบคำนี้ออกจากทุกข้อความที่ผ่าน `backendText()` ทั้งค่าจาก dictionary และ fallback ในโค้ด → ใส่คำนี้ในแถว tsv ไปก็ไม่ขึ้นบนจอ (เช่น `system_standard` เก็บ "ERP会計標準システム" แต่จอแสดง "会計標準システム") อย่าเสียเวลาไล่หาว่าคำหายไปไหน | 2026-09-16 |
| **404 ไม่ได้แปลว่า "ยังไม่ได้เขียน"** | โมดูลที่มี handler ครบแต่ไม่ถูก register ใน `backend/main.go` จะตอบ 404 ตลอด (เคยเจอกับโมดูลลูกหนี้อื่นซึ่งถูกลบไปแล้ว 2026-09-23) — เจอ 404 ให้ `grep -n RegisterHttp backend/main.go` ดูว่าโมดูลถูก register หรือไม่ ก่อนตัดสินใจเขียนใหม่ทั้งโมดูล | 2026-09-16 |
| **CSS Reset ใน `globals.css` บังคับ `label { width: 100% }`** | Checkbox หรือ Radio ที่ห่อด้วย `<label>` (เช่น `<Check>`) จะกว้างเต็ม 100% เสมอ แม้อยู่ในคอนเทนเนอร์ `flex flex-wrap` ก็จะกินเต็มแถวและดันตัวถัดไปตกบรรทัดใหม่ กลายเป็นแนวตั้ง — **วิธีที่ถูก:** ใน `globals.css` ต้องยกเว้น `label:not(.inline-flex):not(.w-auto):not(:has(input[type="checkbox"])):not(:has(input[type="radio"]))` และกำหนดให้ `label.inline-flex, label:has(input[type="checkbox"]), label:has(input[type="radio"])` มี `width: auto !important; max-width: none !important;` และในคอมโพเนนต์ `<Check>` ให้ใส่ `!w-auto !max-w-none shrink-0 whitespace-nowrap` เสมอ | แก้ 2026-09-17 (`frontend/src/app/globals.css:383-399`, `frontend/src/app/gl/gl-common.tsx:58-62`) |
| **regex ตัวอักษร `\p{L}` ไม่รับสระ/วรรณยุกต์ไทย** (Go + JS) | รหัส/ชื่อภาษาไทยที่มีสระบน-ล่างหรือวรรณยุกต์ถูกปฏิเสธ เช่น `สมุดซื้อ` ไม่ผ่าน `^[\p{L}\p{N}_.-]+$` (มี `ุ` U+0E38, `ื` U+0E37, `้` U+0E49) แต่ `กขค` ผ่าน จึงหลุดเทสต์ที่ใช้แค่พยัญชนะ; เคยทำให้สร้างสมุดรายวันไทยไม่ได้และ BFF GL ตอบ 404 กับ path ที่เป็นรหัสไทย — **วิธีที่ถูก:** สระบน-ล่าง ไม้หันอากาศ ไม้ไต่คู้ วรรณยุกต์ และทัณฑฆาต (U+0E31, U+0E34–U+0E3A, U+0E47–U+0E4E) เป็น General Category **Mn (combining mark)** ไม่ใช่ `L`; ส่วน ะ า ำ เ–ไ เป็น `Lo` และ ๐–๙ เป็น `Nd` → ใช้ `[\p{L}\p{M}\p{N}…]` แต่ห้ามให้ตัวแรกเป็น `\p{M}` (สระลอย), นับความยาวเป็น rune ไม่ใช่ byte (ไทย 3 byte/ตัวใน UTF-8), และ NFC ก่อนเทียบ (NFC ไม่สลับลำดับวรรณยุกต์กับสระบนเพราะ combining class 0 ทั้งคู่ — พิมพ์สลับยังเป็นคนละรหัส); อ้างอิง: Unicode Character Database `https://www.unicode.org/Public/UCD/latest/ucd/UnicodeData.txt` (ฟิลด์ที่ 3 = General Category), แผนผัง Thai `https://www.unicode.org/charts/PDF/U0E00.pdf`, ไวยากรณ์ Go regexp (RE2) `https://github.com/google/re2/wiki/Syntax`, JS Unicode property escapes `https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Regular_expressions/Unicode_character_class_escape`; ตัวอย่างที่ถูก `checkCode` (`backend/internal/generalledger/models.go:268`), `frontend/src/app/api/gl/[...glPath]/route.ts:16`; path segment ของ BFF `erp-transaction`/`fa`/`goapi` ยังใช้ regex เดิม | 2026-09-24 (ตรวจด้วย `node`: `/^[\p{L}\p{N}_.-]+$/u.test("สมุดซื้อ")` = false, เพิ่ม `\p{M}` = true) |

## UAT ผ่าน browser (harness)

- เปิดหลายแท็บงานที่มีปุ่ม "แก้ไข/ลบ" พร้อมกัน (เคยเจอกับแท็บสินค้า+บาร์โค้ด ซึ่งตอนนี้ขึ้น "ยังไม่พร้อม") จะมีปุ่มหลายชุด — ref ใหม่กว่าเป็นของแท็บที่ active; เช็ค header ก่อนกด (dirty guard "ทิ้งการแก้ไข" ช่วยได้)
- หน้าจอ datacrud มีปุ่ม แก้ไข/ลบ รายแถว; ลบมี modal "ต้องการลบจริงหรือไม่" ต้องกด `ลบ` ซ้ำ; **ทุกจอใช้ confirm dialog ในแอปแล้ว ไม่มี `window.confirm` เหลือใน `frontend/src`** (ตั้งแต่ commit b4a4898f 2026-07-03; `grep -rn "window.confirm(" frontend/src` = 0 แถว 2026-09-25) → ตัวขับเบราว์เซอร์ไม่ต้อง monkey-patch `window.confirm` แต่ต้องกดปุ่มยืนยันใน modal จริง
- harness Stagehand + DeepSeek (`scratch/stagehand-uat`) **ปลดระวาง 2026-09-23** — ห้ามใช้ AI ภายนอก ทดสอบด้วย Playwright/Browser pane + ปุ่ม Demo แทน (`AGENTS.md`)
- **ล้างข้อมูลทดสอบด้วย id/code ที่ระบุเป้าเท่านั้น** — เคยใช้ regex กว้างแล้วลบสาขาจริงไป 2 ตัว

## สคริปต์ / PowerShell

| กับดัก | อาการ | วิธีที่ถูก | ยืนยัน |
|---|---|---|---|
| `Get-Date -Format yyyy-MM-dd` บนเครื่องที่ตั้ง culture ไทย | ได้ปีพุทธศักราช — stamp ใน `CODE-MAP.md` ออกมาเป็น `2569-09-09` แทน `2026-09-09` | pin culture เสมอ: `[datetime]::Now.ToString('yyyy-MM-dd', [System.Globalization.CultureInfo]::InvariantCulture)` | เจอจริง 2026-09-09 ตอนทำ `-Check` mode ของ `tools/gen-code-map.ps1` (คอมเมนต์ `:152`, การ stamp `:166`) |
| สคริปต์ `.ps1` ที่มีข้อความไทยต้องมี BOM | Windows PowerShell 5.1 อ่านไฟล์เป็น ANSI → ข้อความไทยเพี้ยน | เขียนไฟล์ด้วย `utf-8-sig` (ห้ามใช้ Write tool ที่ตัด BOM ทิ้ง) | `tools/gen-code-map.ps1` ขึ้นต้นด้วย BOM |
| hook ใน `.githooks/` ไม่ทำงานเองหลัง clone | git อ่านเฉพาะ `.git/hooks/` เท่านั้น ไม่อ่านโฟลเดอร์ที่ track ไว้ | `npm run hooks:install` ครั้งเดียวต่อ clone (คัดลอกไฟล์เข้า `.git/hooks/`) — ต้องรันซ้ำทุกครั้งที่ pull การแก้ `.githooks/` | `tools/install-hooks.mjs` |
| **ห้ามใช้ `git config core.hooksPath .githooks`** | hooksPath **แทนที่** `.git/hooks` ทั้งโฟลเดอร์ — hook ส่วนตัวที่มีอยู่เดิมหยุดทำงานเงียบ ๆ ไม่มี error | ใช้วิธีคัดลอก (`npm run hooks:install`) เท่านั้น; ถ้าเคยตั้งไปแล้ว `git config --unset core.hooksPath` | เจอจริง 2026-09-09: การตั้ง hooksPath ปิด `post-commit` ของลุงจืด (sync Obsidian vault) โดยไม่แจ้ง |
| hook ที่ commit จาก Windows ไม่มี exec bit | repo ตั้ง `core.fileMode=false` → git เก็บเป็น `100644` → บน Linux/macOS รันไม่ได้ (การ์ดเงียบ) | `git update-index --chmod=+x .githooks/<hook>` ก่อน commit; ตรวจด้วย `git ls-files -s .githooks` ต้องเห็น `100755` | `.githooks/pre-commit`, `.githooks/pre-merge-commit` |
| merge commit ไม่วิ่ง `pre-commit` | git เรียก `pre-merge-commit` แทน — สอง branch ที่ต่างก็ผ่าน พอ merge แล้วแผนที่ผิด | มีไฟล์ `.githooks/pre-merge-commit` ที่ `exec` ต่อไปยัง pre-commit | `.githooks/pre-merge-commit:5` (ทดสอบ merge จริงแล้ว 2026-09-09) |
| ไฟล์ชื่อ `nul.go` / `con.go` / `aux.go` / `prn.go` / `com1.go` ฯลฯ | Git Bash สร้างได้และ `go build` ผ่าน แต่ `git add` ล้ม `open("…/nul.go"): No such file or directory` เพราะ NUL/CON/AUX/PRN/COM1–9/LPT1–9 เป็นชื่อสงวนของ Windows (มีนามสกุลก็ยังสงวน) — clone บน Windows ก็ checkout ไม่ได้ | ตั้งชื่อไฟล์ตามหน้าที่ของแพ็กเกจ (เช่น `textguard.go`) ห้ามใช้ชื่อสงวนเป็นชื่อฐาน; `nul_test.go` ใช้ได้ (ชื่อฐานคือ `nul_test`) | 2026-09-24 `backend/pkg/textguard/nul.go` → `textguard.go` (commit `6a88057f`) |
| query อ่าน `gl_records`/`fa_records` โดยไม่กรองรายการที่ลบ | เอกสารที่ลบแล้วยังโผล่ในรายการ/รายงาน — GL และสินทรัพย์ถาวรลบแบบ soft delete โดยตั้ง `isdeleted: true` ใน `payload` (JSONB) แถวยังอยู่ในตาราง | ทุก query ต้องกัน `NOT COALESCE((payload->>'isdeleted')::boolean, false)` แบบ `backend/internal/fixedasset/records.go:82` และ `backend/internal/generalledger/journal_books.go:230` | ลบเอกสารทดสอบแล้วรายการ/รายงานต้องได้ 0 แถวทันที |

## ผู้ช่วย AI / เครื่องมือ

- **ห้ามใช้ผู้ช่วย AI ภายนอก** (DeepSeek/Kimi/GLM/ChatGPT/OpenRouter) ตั้งแต่ 2026-09-23 — AI ที่ทำงานใน repo ทำเองทุกขั้น ใช้ subagent ของเครื่องมือเองได้ (กฎใน `AGENTS.md`)
- สร้างรูป = Codex CLI `gpt-image-2` เท่านั้น (ห้าม SVG) — รายละเอียดใน `~/.claude/refs/image-gen-codex.md` ของเครื่องลุงจืด
- Magnitude (browser agent) ประเมินแล้ว 2026-07-01 **ไม่ใช้** (ต้องการ vision LLM ที่ไม่มี) — อย่าประเมินซ้ำถ้าไม่มีข้อมูลใหม่
