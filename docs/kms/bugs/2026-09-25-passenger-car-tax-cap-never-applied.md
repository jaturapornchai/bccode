---
date: 2026-09-25
severity: high
component: [backend, frontend]
tags: [bc-account, go, postgres, fixed-asset, tax, pnd50]
fixed: true
---

# Symptom

รายงานกระทบยอดค่าเสื่อมราคาทางบัญชี-ทางภาษี (ภ.ง.ด.50, `GET /fa/v2/reports/tax-reconciliation`) ไม่เคยจำกัดค่าเสื่อมของรถยนต์นั่งที่ราคาทุนเกิน 1,000,000 บาท เมื่อสินทรัพย์ถูกสร้างจากหน้าจอหรือ MCP — ค่าเสื่อมทางภาษีเท่ากับทางบัญชี ผลต่างที่ต้องบวกกลับเป็น 0

# Root Cause

1. `reports.go` เช็คเพดานจาก `AssetTypeCode == "PASSENGER_CAR"` แต่ปุ่มแม่แบบในฟอร์มบันทึก `VEHICLE_PASSENGER` และช่องประเภทเป็นข้อความอิสระ (ผู้ใช้ตั้งรหัสเอง) — รหัสไม่ตรงกันจึงไม่เข้าเงื่อนไขเลย; MCP `fa_create_asset` ก็ไม่มีทางบอกได้ว่าเป็นรถยนต์นั่ง
2. แม้เข้าเงื่อนไข สูตรเดิมให้ 200,000 บาทตายตัว (20% × 1 ล้าน) แล้วจำกัดไม่เกินค่าเสื่อมบัญชี — ปีที่ซื้อกลางปีและปีสุดท้ายจึงหักเกิน (รวมตลอดอายุเกิน 1 ล้าน) และผิดเมื่ออัตราทางบัญชีต่ำกว่า 20%
3. ข้อยกเว้นตามกฎหมาย (รถของกิจการให้เช่ารถ, รถยนต์ต้นแบบวิจัย — พ.ร.ฎ. 145 ม.5) เป็นรายคัน จึงตัดสินจากรหัสประเภทไม่ได้ตั้งแต่ต้น

# Fix

- เพิ่ม `passengercartaxcap` (bool) ที่ตัวสินทรัพย์ (`Asset.PassengerCarTaxCap`) — ผู้ใช้ติ๊กเอง; ปุ่มแม่แบบ "รถยนต์นั่งไม่เกิน 10 ที่นั่ง" ติ๊กให้ แม่แบบอื่นเอาออก; MCP `fa_create_asset` รับ param เดียวกัน
- ค่าเสื่อมทางภาษี = ค่าเสื่อมบัญชีของปี × 1,000,000 ÷ ราคาทุน (ปัด 2 ตำแหน่ง) ตามข้อหารือ กค 0702/5605 ("คำนวณหักตามระยะเวลาที่ได้ทรัพย์สินนั้นมา")
- ที่มาทางกฎหมาย + ถ้อยคำที่ใช้ตัดสิน: [[21-thai-tax-form-references]] §13

# Regression Test

- `backend/internal/fixedasset/tax_reconciliation_test.go` — สร้างจาก JSON แบบที่ฟอร์มส่ง (`VEHICLE_PASSENGER` + flag), เต็มปี ≈ 200,000, ซื้อ 1 ก.ค. = สัดส่วน, รหัส `PASSENGER_CAR` ที่ไม่ติ๊ก (รถกิจการให้เช่า) ไม่ถูกจำกัด, ราคาทุน 900,000 ไม่ถูกจำกัด
- `backend/internal/fixedasset/mcp_create_asset_test.go` — สร้างผ่าน `fa_create_asset` แล้วรายงานถูกจำกัด (อยู่ใน test binary เดียวกับ package หลัก เพราะถ้าแยก package สองโปรเซสจะชนกันตอน `CREATE TABLE IF NOT EXISTS fa_records`)
- ทั้งสองเทสต์ fail กับโค้ดเดิม (ภาษี = บัญชี 299,999.97) — ต้องมี `BC_GL_TEST_POSTGRES_DSN` (`tools/verify.sh postgres`)

# Follow-up 2026-09-25: คืนเพดานอัตราร้อยละ 20 ต่อปี

- ช่องโหว่ของสูตรข้างบน: สเกลตามทุนอย่างเดียวทำให้เพดาน 200,000 บาท/ปี (20% × 1 ล้าน) ของโค้ดเดิมหายไป — อัตราบัญชีเกิน 20% (อายุ < 5 ปี) หักภาษีเกินกฎหมาย เช่น ทุน 1,500,000 อายุ 3 ปี ได้ 333,333.33 แทน 200,000
- แก้: `passengerCarTaxDeprec` (`backend/internal/fixedasset/reports.go`) = min(บัญชี × 1,000,000 ÷ ทุน, 20% × min(ทุน, 1,000,000) × วันที่ถือ ÷ วันของปี) — ใช้กับทุกคันที่ติ๊กแม้ทุน ≤ 1,000,000 ตาม พ.ร.ฎ. 145 ม.4 วรรคหนึ่ง + (5); ถ้อยคำกฎหมายและข้อยกเว้นวิธีอัตราไม่เท่ากัน (ม.4 วรรคสอง) อยู่ใน [[21-thai-tax-form-references]] §13
- เทสต์: `TestPassengerCarTaxDeprec` (ไม่ต้องมีฐานข้อมูล) + รถอายุ 3 ปีใน `TestTaxReconciliationPassengerCarCap` (เต็มปี 200,000.00, ทุน 800,000 → 160,000.00, ซื้อ 1 ก.ค. → 100,821.92)
- แก้ต่อ: "วันที่ถือ" เดิมบวก `Days` ของงวดค่าเสื่อมทางบัญชี แต่ `calculator.go` หยุดสร้างงวดเมื่อมูลค่าเหลือเท่าราคาซาก — ปีสุดท้ายของบัญชี (อายุ < 5 ปี) เพดานจึงถูกหั่นทั้งที่ยังถือรถทั้งปี เช่น ทุน 1,500,000 อายุ 3 ปี ซื้อ 1 ก.ค. 2569 ปี 2572 ได้ 116,164.38 (212 วัน) แทน 165,296.81 → บวกกลับ ภ.ง.ด.50 เกิน; ตอนนี้นับจากวันที่ได้มาถึงวันจำหน่าย/31 ธ.ค. (`taxHeldDays`) — เทสต์ `TestTaxHeldDays` + `TestPassengerCarTaxLastBookYear` (ไม่ต้องมีฐานข้อมูล) + ปี 2572 และขาย 31 มี.ค. 2570 ใน `TestTaxReconciliationPassengerCarCap`

# Follow-up 2026-09-25 (รอบ 2): ส่วนที่เพดานกันไว้ ยกไปหักปีถัดไป

- อาการ: UAT ทุน 1,500,000 อายุ 3 ปี อัตรา 33.33 ซาก 1.00 ซื้อ 1 ก.ค. 2569 — ค่าเสื่อมทางภาษีหยุดพร้อมงวดบัญชี (2569 100,821.92 / 2570 200,000 / 2571 200,000 / 2572 165,380.19 / 2573 0) รวม 666,202.11 ทั้งที่ต้นทุนส่วนไม่เกิน 1 ล้านยังหักไม่ครบ — ส่วนที่บวกกลับใน ภ.ง.ด.50 ไม่เคยได้หักคืน
- ต้นเหตุ: สูตรเดิมดูทีละปีจากค่าเสื่อมทางบัญชีของปีนั้น (`passengerCarTaxDeprec` = min(บัญชีปีนี้ × 1M/ทุน, เพดาน)) ไม่มีที่จำส่วนที่เพดานกันไว้ และปีที่ไม่มีงวดบัญชีได้ 0 เสมอ
- แหล่งทางการ: กค 0702/570 ("ต้องหักค่าสึกหรอ และค่าเสื่อมราคาต่อไปจนหมด"), กค 0811/09658 (ส่วนที่บวกกลับหักตามอายุที่เหลือได้), ป.3/2527 ข้อ 2 และ 8, พ.ร.ฎ. 145 ม.8 — ถ้อยคำเต็มใน [[21-thai-tax-form-references]] §1 + §13; ไม่พบข้อหารือเรื่องนี้ของรถยนต์นั่งโดยตรง
- แก้: `passengerCarTaxInYear` (เดิมชื่อ `passengerCarTaxByYear`; `backend/internal/fixedasset/reports.go`) ไล่งวดบัญชีทุกปีของคัน — ปีนี้ = min(min(บัญชีสะสม × 1M/ทุน, ฐานสูงสุด) − ที่หักไปแล้ว, เพดานปีนี้); ฐานสูงสุด 1,000,000 (ทุน ≤ 1M: ทุน − 1 บาท); หลังวันจำหน่าย 0; `GetTaxReconciliationReport` ใช้ผ่าน `passengerCarTaxForYear` (อ่านงวดทุกปีของคันที่ติ๊กด้วย query เดียว) — แถวปีที่บัญชีหมดแล้ว บัญชี 0 ภาษี > 0 ผลต่างติดลบ; `passengerCarTaxDeprec` และ `TestPassengerCarTaxDeprec` / `TestPassengerCarTaxLastBookYear` ข้างบนถูกแทนแล้ว
- ผลหลังแก้ (UAT เดิม): 2569 100,821.92 / 2570–2573 ปีละ 200,000.00 / 2574 99,177.41 รวม 999,999.33 (= 1,499,999 × 1M/1.5M — ส่วนซาก 1 บาททางบัญชีไม่ได้หัก ดูข้อที่ยังไม่ทำใน §13); ซาก 0 รวม 1,000,000.00 พอดี
- เทสต์: `TestPassengerCarTaxCarryForward` (`passenger_car_tax_carry_test.go` — UAT, ปีอธิกสุรทิน, ขายกลางอายุ, ทุน 800,000 เหลือ 1 บาท, อายุ 10 ปีไม่มีส่วนยก) + `TestPassengerCarTaxPieces` + ปี 2572/2573/2574 และปีหลังขายใน `TestTaxReconciliationPassengerCarCap` (PostgreSQL)

# Follow-up 2026-09-25 (รอบรีวิว): ปีรายงานไม่มีขอบเขต + งวดหลังวันขาย

- อาการ 1 (DoS): `GET /api/fa/reports/tax-reconciliation?fiscalyear=100000000` — ตัวไล่ทีละปีของรอบ 2 เดินจากปีแรกของงวดบัญชีถึงปีที่ขอ เก็บทุกปีลง map และไม่ดู ctx: ประมาณ 150 วินาที CPU + 16 GB ต่อคันที่ติ๊ก (`fiscalyear=9223372036854775807` ไม่จบเลย เพราะ y++ วนล้น) — ผู้ใช้ที่เปิดรายงานได้ (รวม Demo บน prod) ทำ mainapi ล่มได้
- แก้ 1: `taxReportYear` (`reports.go`) รับเฉพาะปี ค.ศ. 1900–2400 (ตัดช่องว่าง, "02026" → "2026") ก่อนเปิดฐานข้อมูล ไม่งั้นคืน field error 400 code/key `fa_err_fiscal_year_invalid` field `fiscalyear` ข้อความจาก `languages.tsv` (ปี พ.ศ. 2569 ถูกปฏิเสธพร้อมบอกให้ใส่ ค.ศ. แทนที่จะได้รายงานว่าง); `passengerCarTaxInYear` คืนเฉพาะปีที่ขอ และหยุดเมื่อพ้นปีบัญชีสุดท้ายแล้วยอดที่หักได้หมด หรือพ้นปีที่ขาย
- อาการ 2: ปีที่ขายรถที่อายุบัญชียาว (อัตราบัญชี < 20%) ยอดคุมตาม ป.3/2527 ข้อ 2 รวมงวดหลังวันขาย (`DisposeAsset` ไม่ลบงวดที่เหลือ) → หักเกิน เช่น อายุ 10 ปี ขาย 30 มิ.ย. 2570 ได้ 99,178.08 แทน 49,589.05
- แก้ 2: `passengerCarTaxInYear` ข้ามงวดที่ `stopdate` > วันจำหน่าย (กติกาเดียวกับค่าเสื่อมสะสมที่ `gl_poster.go` ใช้ตอนจำหน่าย)
- เทสต์: `TestTaxReportYear` (ปีผิด 9 แบบ → error 400 ก่อนเปิดฐานข้อมูล, ข้อความมีขอบเขตทั้งสอง), `TestPassengerCarTaxInYearStopsEarly` (ปี MaxInt จบทันที — ถ้าเอาจุดหยุดออก เทสต์ตกที่ 5 วินาที), เคส "10-year book sold 30 Jun 2027" ใน `TestPassengerCarTaxCarryForward` (ถ้าเอาตัวกรองวันขายออก ได้ 99,178.08 และเทสต์ตก)
- ยังค้าง: handler `reportTaxReconciliation` (`httpapi/http.go`) ส่งทุก error ของรายงานเป็น 500 — field error ปีผิดจึงออกเป็น 500 พร้อมข้อความไทย จนกว่าจะเปลี่ยนให้ใช้ `failure()` กับ user error
