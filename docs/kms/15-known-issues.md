# ปัญหาที่รู้แล้วและงานค้าง (Known Issues & Backlog) — ตรวจซ้ำที่ HEAD
> ตรวจล่าสุด: 2026-09-07 (commit d93a210d) · แถวที่ระบุวันที่ 2026-09-09 ตรวจซ้ำที่ commit 098107e1 — ผู้เขียน: AI reader; ทุกข้อเท็จจริงอ้าง path:line · ตรวจซ้ำโดย fact-checker

## 1. ขอบเขตและวิธีอ่าน

- แหล่งตั้งต้น: `docs/handoff/HANDOFF-2026-09-06.md` §4–§5 และ `docs/handoff/HANDOFF-RISKS-2026-09-05.md` ทุก section — ทุกข้อถูกเปิดไฟล์ที่อ้างซ้ำที่ HEAD `d93a210d` (ไม่เชื่อ handoff โดยไม่ดูโค้ด)
- สถานะ: **ยังอยู่** = โค้ด/runtime ยังแสดงปัญหา · **แก้แล้ว** = โค้ดที่ HEAD ไม่มีปัญหานั้นแล้ว · **ตรวจไม่ได้** = ต้องรัน/เข้าถึงสิ่งที่ session นี้ทำไม่ได้ (build ใน Docker, ssh .202, ฯลฯ)
- หลักฐาน runtime มาจาก local Docker stack แบบ read-only เท่านั้น (mongosh/psql/kafka-consumer-groups/docker logs) — ไม่ได้เขียน/restart/ลบอะไร
- กับดักการค้น: `.ignore` ที่ root ทำให้ `rg` ข้าม `backend/internal/goapi/process/build/` — ไฟล์ในบทความนี้ที่อยู่โฟลเดอร์นั้นตรวจด้วย `sed -n`/`rg --no-ignore`
- บทความนี้ **ไม่แก้โค้ด** — เสนอทางแก้เท่านั้น; ข้อที่ต้องตัดสินใจเชิงธุรกิจ/R1 ระบุ "ต้องถามลุงจืด"

## 2. ตาราง issue (re-verified 2026-09-07)

| id | issue | สถานะ | evidence path:line | ทางแก้ที่เสนอ | ต้องถามลุงจืด? |
|---|---|---|---|---|---|
| KI-01 | Mongo→PG projection `debtor` พัง: consumer INSERT คอลัมน์ `name0/createdat/updatedat` ที่ DDL ไม่มี (DDL มี `names JSONB`) และ `ON CONFLICT (code)` ทั้งที่ PK คือ `(guidfixed, code)` | ยังอยู่ | `backend/internal/goapi/handlers/kafka/debtor.go:110`; DDL `backend/internal/goapi/process/build/create-database.go:1407-1424`; topic ยัง register `backend/internal/goapi/handlers/kafka.go:956`; runtime: PG `demo.debtor` = 0 แถว ขณะ Mongo `debtors` = 38 doc | แก้ SQL ให้ตรง DDL (`names` JSONB + unique key ที่ตรง PK) หรือถอด consumer ถ้ายังไม่มี reader; เพิ่ม regression test แบบ `projection_reject_test.go` | ใช่ (ผูกกับคำถาม "PG เพื่ออะไร") |
| KI-02 | projection `creditor` พังแบบเดียวกัน (`name0`, `ON CONFLICT (code)`) | ยังอยู่ | `backend/internal/goapi/handlers/kafka/creditor.go:111`; DDL `create-database.go:1479-1496`; register `kafka.go:827`; runtime PG `demo.creditor` = 0 | เหมือน KI-01 | ใช่ |
| KI-03 | projection `erp_user` พัง: INSERT `position/department/approval_role/max_approval_amount` แต่ DDL มีแค่ `roworder/code/name/password`; ยังใส่ password default `"123456"` ลง PG | ยังอยู่ | `backend/internal/goapi/handlers/kafka/employee.go:114-119`; DDL `create-database.go:1227-1232`; register `kafka.go:921`; runtime PG `demo.erp_user` = 0 | แก้ DDL/SQL ให้ตรงกัน; เลิกเขียน password ลง projection (ผิดกฎ SECURITY ข้อ 10) | ใช่ (ต้องการ erp_user บน PG ไหม) |
| KI-04 | DLQ เป็น log-only: handler default คือ `LogDLQHandler`; หลัง retry หมด consumer log แล้ว offset เดินต่อ (segmentio/kafka-go `kafka_consumer.go:16`; `ReadMessage` auto-commit) → ตาราง `dead_letter_queue` ว่างตลอด | ยังอยู่ | `backend/internal/goapi/mydlq/dlq.go:91-100` (Send = log), `:131` (default handler); `backend/internal/goapi/handlers/kafka/utils.go:101-111`; loop `backend/internal/goapi/mykafkaconsumer/kafka_consumer.go:298` (ReadMessage), `:216-219` (error แค่ log); runtime `demo.dead_letter_queue` = 0 แถว | ใช้ `projection.Consume` (ack-after-success + `ErrRejected` ที่ `backend/internal/product/projection/consumer.go:14`) กับ topic อื่น หรือ implement DLQHandler ลง PG/Mongo + metric | ไม่ (R2) แต่ควรทำหลังตัดสิน KI-01–03 |
| KI-05 | Busy-poll: worker pool (auto = CPU×2, cap 32 → เครื่องนี้ 24) วน `GetActiveShops` บนตาราง `queues` ที่ว่าง ทุก 100 ms; processor ปลายทางเป็น TODO stub | ยังอยู่ | `backend/internal/goapi/workers/doc_processor.go:67` (OptimalWorkerCount 4..32 ×2.0), `:244-252` (poll + sleep 100ms); start ที่ `backend/internal/goapi/bootstrap.go:136`; stub `backend/internal/goapi/process/process_status.go:11-21`; runtime: `pg_stat_user_tables.queues` idx_scan = 159,834,945 (ตรวจซ้ำ; ตอนเขียนครั้งแรก 159,688,689), `xact_commit(postgres)` = 163,091,679 (ตอนเขียน 162,943,470); log mainapi "optimal workers: 24 (12 CPU cores)" | ปิด worker pool (ไม่ start ที่ bootstrap.go:136) หรือ backoff ≥ 5 s; ลบทั้ง pipeline ถ้าไม่มีแผน | ใช่ (ลบหรือเก็บ) |
| KI-06 | ClickHouse: goapi stub แล้ว แต่ legacy route ที่ใช้ CH ยัง register และจะ error เมื่อเรียก (`/productimport/*`, `/stockbalanceimport/*`, `/product/barcode2` hard-code holding) | ยังอยู่ | stub `backend/internal/goapi/myclickhouse/utils.go:234-241`, `backend/internal/goapi/handlers/health.go:90-93`; register `backend/main.go:520-521`; `backend/internal/productimport/services/productimport_service.go:157-158` (chRepo.List); `backend/internal/product/productbarcode/services/productbarcode_http_service.go:1883-1886`; route `backend/internal/product/productbarcode/productbarcode_http.go:102` | ตัดสิน 3 module legacy (ลบ หรือย้าย staging ไป Mongo) ก่อนถอด CH layer (ไฟล์ CH โดยตรงใน goapi 1,537 บรรทัด = `myclickhouse/` 921 + `handlers/clickhouse.go` 295 + `handlers/clond-clickhouse-database.go` 156 + `process/clickhouse-replace-process-stock-cost.go` 165; บวก `internal/report/reportqueryc` 1,141 บรรทัดที่ไม่ได้ register) | ใช่ (R1) |
| KI-07 | ClickHouse ยังฝังอยู่ใน infra/config: prod compose มี service + `depends_on` ทั้ง migrate/mainapi/worker, provision เขียน key CH, bootstrap มี block `clickhouse`, frontend settings มีหมวด ClickHouse | ยังอยู่ (dev local พักแล้ว) | `deploy/account/compose.yml:63,76,218,242,282`; `deploy/account/provision-server.sh:79-83,121-126`; `backend/bootstrap.json` และ `bootstrap.local.json` key `clickhouse.{host,port,user,password,database_name}` (ชื่อ key เท่านั้น); `frontend/src/lib/setup-config.ts:80-88`; `frontend/src/app/settings/settings-screen.tsx:124,166,508`; `backend/go.mod:137` (clickhouse-go v2.40.3), `:42` (ch-go); local: `backend/docker-compose.local.yml:6` comment "paused 2026-09-06"; runtime `docker ps` ไม่มี container clickhouse | ลำดับปลอดภัย: compose/provision → bootstrap+setup-config+frontend → โค้ด goapi → `go.mod` → `docs/kms/architecture/high-scale-multitenant-bi.md` | ใช่ (R1 ถอดถาวรหรือพักต่อ) |
| KI-08 | prod รัน projection 2 ชุดชนกัน: service `worker` ใช้ `DEV_API_MODE=1` (legacy consumers) เขียน PG เดียวกับ goapi consumers ใน `mainapi`; mode `""` จะรันทั้ง API และ consumers ใน process เดียว | ยังอยู่ | `deploy/account/compose.yml:271-275`; `backend/main.go:256` (mode ""/"2" = API), `:654` (mode ""/"1" = consumers) | หยุด `worker` mode 1 จนกว่าจะเหลือ projection ชุดเดียว | ใช่ (deploy = R0/R1) |
| KI-09 | คำสั่ง rebuild ทั้งหมด (`ReportPostHandler`, CommandID `rebuild`) ไม่ได้ register route ใน goapi | ยังอยู่ | นิยาม `backend/internal/goapi/handlers/commands.go:154,197`; route list `backend/internal/goapi/bootstrap.go:356-521` ไม่มี handler นี้ (`rg ReportPostHandler` เจอเฉพาะนิยามที่ `commands.go:154`) | เปิด route ที่มี auth หรือลบ handler + `build.DatabaseRebuild` ที่ไม่มีทางเข้า | ใช่ |
| KI-10 | `stockwaitprocess` ไม่มีใคร drain: `ProcessStockCostAll` ถูกเรียกจาก `DatabaseRebuild`/`CalcStockCostAll` เท่านั้น; `DatabaseRebuild` เข้าถึงได้เมื่อ `DatabaseChecker(..., true)` ซึ่งไม่มี caller ส่ง true | ยังอยู่ | `backend/internal/goapi/process/process-stock/product-calc-cost.go:49-55`; callers `create-database.go:2065` (ใน `DatabaseRebuild`), `:2370` (ใน `CalcStockCostAll`); gate `create-database.go:2035-2036`; caller ทั้ง 11 จุดส่ง `false` (เช่น `backend/internal/goapi/handlers/kafka/projection_reconcile.go:275`) | ตัดสินว่า stock-cost engine บน PG ยังอยู่ในแผนไหม; ถ้าไม่ ลบ pipeline | ใช่ |
| KI-11 | `docwaitprocess` drainer ลบเฉพาะ `transflag = 6`; tenant `test` ค้าง transflag 12 (5 แถว) และ 44 (3 แถว) | ยังอยู่ | `backend/internal/goapi/process/process-doc/process-doc-purchase-batch.go:100`; runtime `psql -d test`: `12|5`, `44|3`; `demo.docwaitprocess` = 0 | รวมกับ KI-10; ถ้าเก็บ ต้องมี drainer ต่อ transflag หรือ TTL | ใช่ |
| KI-12 | PG ค้างข้อมูล demo/C03: PG `productbarcode` C03 = 24 แถว แต่ Mongo `productbarcodes` (holdingcode demo, businesscode C03, ไม่ลบ) = 15 → ส่วนเกิน 9 (8850001000019…095) | ยังอยู่ | runtime psql `demo` vs mongosh `appdb.productbarcodes`; ทางล้าง `backend/internal/product/product/product_http.go:73` (`POST /product/resync`) + proxy `frontend/src/app/api/product/[[...productPath]]/route.ts:50-51` | เรียก `/product/resync` สำหรับ demo/C03 หรือ drop tenant DB ให้ `DatabaseChecker` สร้างใหม่ (ข้อมูล disposable) | ไม่ (R2 บน local) |
| KI-13 | log format ผิด `"กำลังประมวลผล create $0"` + echo payload ทั้งก้อน | ยังอยู่ | `backend/internal/goapi/handlers/kafka.go:191` | เปลี่ยนเป็น `%d bytes`/offset ไม่ log payload | ไม่ |
| KI-14 | dialog ลบ **สินค้า** ใช้ข้อความบาร์โค้ด ("ต้องการลบบาร์โค้ดที่เลือกจริงหรือไม่") เพราะ product-screen ใช้ `getBarcodeText` | ยังอยู่ | `frontend/src/app/menu/product-screen.tsx:198,918,968`; ข้อความ `frontend/src/lib/product-barcode/language.ts:83` | เพิ่ม key ข้อความสินค้าแยก (กฎ UX 40+ ข้อ 7) | ไม่ |
| KI-15 | "แถว unit picker ไม่มี accessible name" | ตรวจไม่ได้ (ไม่ reproduce) | picker ที่ product-screen ใช้คือ `MasterPicker` (`product-screen.tsx:2703`; เป้าหมาย `unit` ผ่าน `pickerTarget` ที่ `:1230`); ปุ่มแถวมีข้อความชื่อ/รหัสเป็น content (`frontend/src/components/product-barcode/master-picker.tsx:288-300`) → มี accessible name จาก content | ต้องระบุ picker/หน้าที่พบปัญหาจริง ก่อนแก้ | ใช่ (ขอตำแหน่ง) |
| KI-16 | `pkg/microservice/persister_mongo_test.go` มี vet warning เก่า | ตรวจไม่ได้ | ไฟล์มีอยู่ `backend/pkg/microservice/persister_mongo_test.go`; `go vet` ต้องรันใน Docker (ไม่ได้รันใน session นี้) | รัน vet ใน container ตาม `docs/handoff/HANDOFF-2026-09-06.md` §2 แล้วแก้เป็น commit เล็ก | ไม่ |
| KI-17 | tenant PG `test` schema เก่า: ตาราง `product` 11 คอลัมน์ ไม่มี `holding_code/businesscode` | ยังอยู่ | runtime `psql -d test information_schema.columns` (คอลัมน์ `%code%` = `itemcode,unitcode`) | drop DB `test` แล้วให้ `DatabaseChecker` สร้างใหม่ (disposable) | ไม่ (local) แต่ห้ามทำบน .202/prod โดยไม่ถาม |
| KI-18 | 15 package อยู่ใน test quarantine รอ business contract | ยังอยู่ | `backend/.ci/test-quarantine.txt` (15 รายการที่ไม่ใช่ comment) | ปลดทีละ package หลังยืนยัน contract | ใช่ |
| KI-19 | ตัวเลขบัญชียังเป็น float (ไม่มี currency/precision/rounding contract) | ยังอยู่ | ตัวอย่าง `backend/internal/product/productbarcode/models/product_barcode.go:291` (`Price float64`), `:492-493`; `create-database.go:1421` (`DOUBLE PRECISION`) | รอ contract ก่อนเปลี่ยนชนิดข้อมูล (R1) | ใช่ |
| KI-20 | Backup/restore drill ยังไม่เคยทำ; RPO/RTO/ปลายทาง backup ยังไม่ตกลง | ยังอยู่ | `docs/runbooks/RECOVERY-READINESS.md:7,31` | ตอบคำถาม §4 ข้อ 3 แล้วทำ drill บน environment แยก | ใช่ |
| KI-21 | Unit contract ไม่ตรงแบบ: โค้ดใช้ collection `units` + `unitcode` ระดับ holding; แบบ (MongoModel) ใช้ `unit_of_measure/code/businesscode` | ยังอยู่ | `backend/internal/product/unit/models/unit.go:9`; query `backend/internal/product/unit/repositories/unit_mongo_repository.go:80,99`; บันทึกความต่าง `backend/internal/product/product/outbox/README.md:104` | ห้ามย้าย scope tenant โดยเดา — รอคำตอบ | ใช่ |
| KI-22 | **ใหม่ (พบ 2026-09-07)**: outbox ของ organization (`holding/company/branch`) ถูกเขียนเป็น `PENDING` แต่ไม่มี dispatcher — worker ของ product จัดการเฉพาะ `aggregatetype=productprojection` → Mongo `outboxevents` PENDING 98 doc (attempts 0, ตั้งแต่ 2026-08-14 ถึง 2026-09-02) | ยังอยู่ | writer `backend/internal/shop/shop_service.go:115`, `backend/internal/organization/company/company_http.go:112-114`, `backend/internal/organization/branch/branch_http.go:125`, `backend/internal/organization/status_change.go:65,157`; dispatcher filter `backend/internal/product/product/outbox/outbox.go:21,192`; README `outbox/README.md:18` ("organization outboxes ... are excluded"); runtime mongosh: `PUBLISHED 7 / PENDING 98` แยกเป็น branch 28, company 44, holding 26 | ต้องรู้ก่อนว่า event เหล่านี้มี consumer ปลายทางไหม (`PublishOrOutbox` ที่ `backend/internal/organization/events/events.go:33` เขียน outbox เมื่อ Kafka ล้ม); ถ้าไม่มี → ลบ writer/ทำ dispatcher ที่ครอบ aggregatetype อื่น | ใช่ |
| KI-23 | GitHub Actions ล็อกเพราะ billing → CI ไม่รันตั้งแต่ 2026-09-03 | ยังอยู่ | `gh api .../check-runs/101277126610/annotations` = "The job was not started because your account is locked due to a billing issue."; ตรวจซ้ำ 2026-09-09: ยังมี run ใหม่ถูก trigger ตลอด แต่ job ไม่เคยเริ่ม — run ล่าสุด 2026-09-08 (`gh run view 34182717816`) ทุก job (backend-test, frontend-test, backend-projection-kafka-integration, backend-outbox-integration) fail ภายใน 3 วินาที ด้วย annotation เดิม (`gh api repos/:owner/:repo/check-runs/101924873905/annotations`) | ปลดล็อก billing (นอก repo) — ระหว่างนี้หลักฐาน build/test เป็น local เท่านั้น | ใช่ (บัญชี GitHub) |
| KI-24 | on-prem .202 ไม่ทราบสถานะ (ssh timeout 6 ก.ย.) และ prod `ENABLE_KAFKA`/bootstrap จริงยังไม่ตรวจ | ตรวจไม่ได้ | `docs/handoff/HANDOFF-RISKS-2026-09-05.md:28` (บันทึกเดิม); session นี้ไม่ได้ ssh | ลุงจืดเปิดทาง ssh/ให้ข้อมูล | ใช่ |
| KI-25 | `Persister.Transaction` กลืน error ของ GORM | แก้แล้ว | `backend/pkg/microservice/persister.go:346-354` คืน error จาก `db.Transaction` แล้ว | — | ไม่ |
| KI-26 | CI job Kafka พังจาก `mongo-init` รันซ้ำ | แก้แล้ว (ในโค้ด) | `.github/workflows/ci.yml:138-153` ใช้ `run --rm --no-deps tests`; แต่ยังพิสูจน์บน GitHub ไม่ได้เพราะ KI-23 | — | ไม่ |
| KI-27 | head-of-line block จาก message ที่ใช้ไม่ได้ใน projection ใหม่ | แก้แล้ว | `backend/internal/product/projection/consumer.go:14` (`ErrRejected`); ทดสอบ poison จริงตาม `docs/handoff/HANDOFF-2026-09-06.md` §3 ข้อ 3 | — | ไม่ |

## 3. Inventory ส่วนที่เกี่ยวข้อง

| module | หน้าที่ | entry/route | สถานะ | อ้างอิง |
|---|---|---|---|---|
| goapi Kafka consumers (`*-v1`) | Mongo→PG projection ทุก topic | `handlers/kafka.go` StartConsumers | LIVE (25 groups บน local, ทุกตัวลงท้าย `-v1`) | `backend/internal/goapi/handlers/kafka.go:827,921,956`; `kafka-consumer-groups --list` |
| debtor/creditor/erp_user projection | เขียนลูกหนี้/เจ้าหนี้/ผู้ใช้ลง PG | topics `when-debtor-created` ฯลฯ | LIVE แต่พัง 100% (KI-01–03) | `debtor.go:110`, `creditor.go:111`, `employee.go:119` |
| `mydlq` | Dead-letter | `SendToDLQ` จาก `handlers/kafka/utils.go` | STUBBED (log-only) | `backend/internal/goapi/mydlq/dlq.go:91,131` |
| worker pool `WorkerManager` | ประมวลผลคิว `queues` | start ใน `bootstrap.go:136` | LIVE แต่ไม่มีงาน (busy-poll) | `backend/internal/goapi/workers/doc_processor.go:67,244-252` |
| `process_status.go` | คำนวณสถานะ PO/SI | เรียกจาก worker | STUBBED (TODO) | `backend/internal/goapi/process/process_status.go:11-21` |
| ClickHouse layer goapi | เชื่อม CH | `myclickhouse`, `handlers/health.go` | STUBBED ("clickhouse is disabled") | `backend/internal/goapi/myclickhouse/utils.go:234-241`; `handlers/health.go:90-93` |
| `/productimport/*`, `/stockbalanceimport/*` | import ผ่าน staging บน CH | `backend/main.go:520-521` | LIVE route / DEAD backend (chRepo) | `productimport_service.go:157-158` |
| `/product/barcode2` | ค้นหาบาร์โค้ดจาก CH (hard-code holding) | `productbarcode_http.go:102` | LIVE route / DEAD backend | `productbarcode_http_service.go:1883-1886` |
| `internal/report/reportqueryc` | รายงานจาก CH | ไม่ได้ register | DEAD | `rg reportqueryc` นอกโฟลเดอร์ตัวเองเจอเฉพาะ swagger ที่ generate (`backend/docs/docs.go:28494`) ไม่มี import/route ใน `backend/main.go` (1,141 บรรทัด) |
| `ReportPostHandler` (rebuild) | rebuild PG ทั้ง holding | ไม่ได้ register | DEAD | `backend/internal/goapi/handlers/commands.go:154,197` |
| `POST /product/resync` | rebuild PG product/barcode ต่อบริษัท + queue snapshot | `product_http.go:73`; proxy frontend | LIVE | `backend/internal/product/product/product_http.go:73`; `frontend/src/app/api/product/[[...productPath]]/route.ts:50` |
| product outbox dispatcher | ส่ง `productprojection` PENDING → Kafka | Microservice background worker | LIVE (PUBLISHED 7 / pending productprojection 0) | `backend/internal/product/product/outbox/outbox.go:21,184,323` |
| organization outbox | เก็บ event holding/company/branch เมื่อส่ง Kafka ไม่ได้ | `events.go:33` | LIVE writer / ไม่มี dispatcher (KI-22) | `backend/internal/organization/events/events.go:33`; `outbox/README.md:18` |
| legacy consumers (`DEV_API_MODE=1`) | gorm projections ชุดเก่า | `backend/main.go:654`; prod `worker` | LIVE บน prod compose, ไม่รันบน local (mainapi local = mode 2) | `deploy/account/compose.yml:271-275`; `docker inspect mainapi` env `DEV_API_MODE=2` |

## 4. คำถามเปิดที่ต้องได้คำตอบจากลุงจืด (ยกมาจาก handoff + ยืนยันว่ายังไม่มีคำตอบใน repo)

1. **ตัวเลขบัญชี**: currency, precision/scale ของเงิน/จำนวน/ต้นทุน/FX, rounding mode และจุดปัดเศษ, trusted source — โค้ดยัง float (KI-19); ไม่มี SoT เพราะ `docs/` ถูกลบ (`AGENTS.md` ข้อ 1)
2. **Quarantine 15 package**: business contract ของแต่ละ package (`backend/.ci/test-quarantine.txt`) — ปลดได้ทีละตัวเมื่อยืนยัน
3. **Backup**: platform, source/target, format, RPO/RTO, หลักฐาน backup ล่าสุด → ทำ drill ตาม `docs/runbooks/RECOVERY-READINESS.md`
4. **Unit contract**: ยึด `units/unitcode` ระดับ holding (โค้ดปัจจุบัน `unit.go:9`) หรือ `unit_of_measure/code/businesscode` ระดับบริษัท (แบบ)
5. **ClickHouse**: ถอดถาวร (KI-06/07, R1) หรือพักต่อ; ถ้าถอด ให้ตัดสิน 3 module legacy ก่อน
6. **PostgreSQL เพื่ออะไร**: ตอนนี้ frontend ไม่เรียก endpoint ที่อ่าน PG (`docs/handoff/HANDOFF-RISKS-2026-09-05.md:10,13`); จะซ่อม projection (KI-01–03, 08–11) หรือหยุด projection ทุกอย่างยกเว้น product/barcode จนกว่าจะมี reader
7. **organization outbox (KI-22)**: event `holding/company/branch` มี consumer ปลายทางที่ต้องการจริงไหม ถ้าไม่มีควรลบ writer แทนการสร้าง dispatcher

## 5. หลักฐาน runtime (read-only, local stack, 2026-09-07)

- `docker ps`: `mainapi minio postgres redis kafka mongodb mongomodel` (+ container ของโปรเจ็คอื่น) — ไม่มี `clickhouse`
- `docker inspect mainapi` → `DEV_API_MODE=2`; log start 2026-09-05 23:27 "optimal workers: 24 (based on 12 CPU cores)"
- Mongo `appdb`: `debtors` 38 doc; `productbarcodes` demo = C01 15 / C02 15 / C03 15; `outboxevents` PUBLISHED 7 / PENDING 98 (branch 28, company 44, holding 26; attempts 0)
- PG `demo`: `debtor 0, creditor 0, erp_user 0, dead_letter_queue 0, stockwaitprocess 0, docwaitprocess 0`; `productbarcode` C01 15 / C02 15 / **C03 24**
- PG `test`: `docwaitprocess` transflag 12 = 5, 44 = 3; `product` มี 11 คอลัมน์ ไม่มี `holding_code/businesscode`
- PG `postgres`: `queues` seq_scan 3, idx_scan 159,834,945, n_live_tup 0; `xact_commit` 163,091,679 (ตรวจซ้ำโดย fact-checker; ตอนเขียนครั้งแรกในวันเดียวกัน 159,688,689 / 162,943,470; handoff 133M/136M — โตขึ้นต่อเนื่อง = busy-poll ยังทำงาน)
- `docker logs mainapi --since 72h | grep -c name0` = 0 (ไม่มี debtor event เข้ามาในช่วงนี้ ไม่ใช่หลักฐานว่าแก้แล้ว)
- Kafka consumer groups บน local: 25 กลุ่ม ลงท้าย `-v1` ทั้งหมด; ไม่มีกลุ่ม `*-projection` เพราะ legacy transport รันเฉพาะ mode 1

## 6. ช่องว่าง / สิ่งที่ยังไม่ตรวจ

- **ยังไม่ตรวจ**: `go vet`/`go test` ที่ HEAD (ต้องรันใน Docker golang:1.26 ตาม `docs/handoff/HANDOFF-2026-09-06.md` §2) → KI-16 และสถานะ quarantine เชิงผลรัน
- **ยังไม่ตรวจ**: on-prem 192.168.2.202 และ prod SGP1 159.223.43.229 (ไม่ได้ ssh) → ไม่ทราบว่า ClickHouse/worker mode 1/bootstrap จริงเป็นอย่างไร (KI-07, KI-08, KI-24)
- **ตรวจแล้ว (fact-checker)**: `deploy/account/compose.yml:329` (`mem_limit: 768m`) อยู่ใต้ service `frontend` ไม่ใช่ CH; ของ CH คือ `:76` (`mem_limit: 1g` ใต้ service `clickhouse`)
- **ยังไม่ตรวจ**: `shopuser_service.go` SaveUserPermissionShop fix (`backend/internal/shop/shopuser_service.go:311`) — เปิดแค่หัวฟังก์ชัน ไม่ได้ไล่ logic edit-target ทั้งหมด
- **ยังไม่ตรวจ**: มี client นอก web เรียก `/productimport` หรือไม่ (ต้องดู `shopuseraccesslogs` บน prod/.202)
- **ยังไม่ตรวจ**: consumer ปลายทางของ organization events (`companyTopicUpdated` ฯลฯ) — ระบุแค่ writer และการไม่มี dispatcher (KI-22)
- **ตัวเลขที่ระวัง**: จำนวน worker 24 ขึ้นกับ CPU ของเครื่อง (สูตร CPU×2 cap 32) — บน prod 4 vCPU จะได้ 8 ไม่ใช่ 24 แต่ยัง busy-poll เหมือนกัน
- คำถามให้ลุงจืด: ดู §4; ข้อที่ต้องตอบก่อนแตะโค้ด = 5 (ClickHouse), 6 (PG), 7 (organization outbox) เพราะกำหนดว่า KI-01–11 จะ "ซ่อม" หรือ "ลบ"
