# HANDOFF 2026-09-08 — เมนูเทียบ FlowAccount + PEAK และขอบเขตที่ตัดออก

> อ่านไฟล์นี้ก่อน แล้วค่อยเปิด `docs/handoff/HANDOFF-2026-09-06.md` (สถานะ repo/วิธีรัน/งานค้างฝั่ง backend ยังใช้ได้ทั้งหมด) และ `docs/handoff/HANDOFF-RISKS-2026-09-05.md` (ความเสี่ยง outbox/projection)
> กฎ project อยู่ที่ `AGENTS.md` (ไฟล์นั้นชนะเสมอ) · ฐานความรู้ `docs/kms/README.md` · skill `docs/skills/` (อ่านจาก disk ทุกครั้ง)

## 1. สิ่งที่ทำวันนี้

เทียบเมนู BC กับ FlowAccount + PEAK จากหน้าทางการ (411 ฟีเจอร์) ตรวจซ้ำแบบหักล้างทุกช่องว่าง แล้วเพิ่มเมนูที่ขาดจริงจนครบ 100% รวม **218** รายการ (เพิ่ม 15 + 18 รายการล่าสุด ปิดช่องว่างล็อตสินค้า บัตรเครดิต รวมจ่าย บัญชีสินทรัพย์ บุคลากรและผู้ใช้)

- รายละเอียด + ตารางว่าอะไรครอบคลุมแล้ว: `docs/kms/19-menu-coverage-flowaccount-peak.md`
- เหตุผลเชิงตัดสินใจ: `docs/kms/decisions/2026-09-08-menu-parity-flowaccount-peak.md`
- หลักฐานฝั่งคู่แข่ง (ของเดิมที่ AI ตัวอื่นทำไว้): `docs/features-flowaccount-peak/`
- วิธีเพิ่มเมนู (4 จุดที่ต้องแก้พร้อมกัน): `docs/skills/ui-scale-polish/SKILL.md` §8

## 2. ขอบเขตที่ "ตัดออก" — ห้าม AI ตัวไหนเพิ่มกลับเอง

| เรื่อง | สถานะ | เหตุผล |
|---|---|---|
| ระบบเงินเดือน (payroll run, สลิปเงินเดือน, ส่งไฟล์ธนาคารเงินเดือน) | **ไม่ทำ** (ลุงจืดสั่ง 2026-09-08) | ไม่อยู่ในขอบเขตผลิตภัณฑ์ |
| แบบยื่น ภ.ง.ด.1 / ภ.ง.ด.1ก | **ไม่ทำ** | เป็นผลจากเงินเดือน ไม่มีข้อมูลต้นทาง |
| ไฟล์นำส่งเงินสมทบประกันสังคม (สปส.) / กท.20 ก | **ไม่ทำ** | เหตุผลเดียวกัน |

ของที่ยังทำอยู่และอย่าสับสน: ภาษีหัก ณ ที่จ่ายของคู่ค้า (ภ.ง.ด.3/53 + 50 ทวิ), "เงินทดรองจ่ายพนักงาน" (การเงิน ไม่ใช่เงินเดือน), ทะเบียนพนักงาน `/employee` ที่อยู่ใน workspace wizard

การลบครั้งนี้กวาดถึง language key `payroll`, `employee_payroll`, `payroll_run`, `payroll_slip`, `vat_pnd1`, `sso_contribution_file` ใน `backend/assets/language/languages.tsv` แล้ว

## 3. งานค้างที่ทำต่อได้ทันที

1. **จอจริงของเมนูใหม่** — ทั้ง 15 รายการยังชี้ route ที่ไม่มีจอ แสดงการ์ด "Flutter route เดิม" (`frontend/src/app/menu/main-menu-screen.tsx:2786-2800`) ถ้าจะทำจอ ให้ทำทีละใบตามกฎ UI ใน `AGENTS.md` + skill
2. **คำถามที่ต้องได้คำตอบก่อนลงรายละเอียด** (อยู่ใน `docs/kms/18-decisions-and-agreements.md` §คำถาม): taxonomy XBRL ของกรมพัฒนาธุรกิจการค้า, รูปแบบไฟล์โอนเงินของธนาคารที่จะรองรับ, ระดับการอนุมัติรายจ่าย (ผู้อนุมัติเดียว vs หลายขั้น/วงเงิน), เทมเพลตนำเข้าเอกสารจากแพลตฟอร์มไหน
3. **เมนูปิดช่องว่างเพิ่ม 18 รายการเรียบร้อย** — ล็อตสินค้า/วันหมดอายุ, รายงานส่งอีเมลอัตโนมัติ, ส่งออก/สำรองข้อมูลกิจการ, บัตรเครดิตกิจการ, รวมจ่าย/รวมรับ, ทะเบียนพนักงาน, ผู้ใช้งาน, สิทธิ์ และ Audit log เปิดเป็นเมนูครบ 218 รายการแล้ว
4. **คำแปล 12 ภาษาของ key ใหม่** เขียนด้วย AI ยังไม่มีเจ้าของภาษาตรวจ (th/en ตรวจแล้ว)
5. **backend image ยังไม่มี key ใหม่** จนกว่าจะ rebuild — ตอนนี้ frontend fallback เป็นป้ายไทยในโค้ดจึงแสดงถูกอยู่แล้ว
6. งานค้างฝั่ง backend (projection drift ของ debtor/creditor/erp_user, DLQ log-only, busy-poll, ClickHouse ถอดถาวรหรือไม่) ยังเหมือนเดิมใน `HANDOFF-2026-09-06.md` §4

## 4. วิธีตรวจว่าไม่พัง

```bash
cd frontend && npx vitest run src/lib/menu-data.test.ts src/lib/menu-icons.test.ts src/lib/menu-usage.test.ts
```

แล้วเปิด `http://localhost:3000` → Demo Login → กลุ่มกิจการรุ่งเรือง (สาธิต) → C01 → สำนักงานใหญ่ → ช่องค้นหาเมนู พิมพ์ชื่อไทยที่เพิ่ม และกดปุ่มสลับธีมดูทั้ง light/dark
