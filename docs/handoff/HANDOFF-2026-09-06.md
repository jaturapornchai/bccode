# HANDOFF 2026-09-06 — สำหรับ AI/นักพัฒนาคนถัดไป (self-contained)

> **สถานะ ณ 6 ก.ย. 2026 — ไม่ใช่ไฟล์เริ่มอ่าน:** เริ่มที่ [`HANDOFF-2026-09-08.md`](HANDOFF-2026-09-08.md) (เมนู 224 รายการ + ขอบเขตที่ตัดออก) แล้วค่อยกลับมาไฟล์นี้เพื่อดูวิธีรัน local + งานค้างฝั่ง backend · เชิงลึก outbox/projection ดู [`HANDOFF-RISKS-2026-09-05.md`](HANDOFF-RISKS-2026-09-05.md)
> กฎ project: `CLAUDE.md` → `AGENTS.md` (UX ไทย 40+, premium UI, UAT ต้องตรวจ CRUD+Mongo ทีละ step, รูปเก็บ S3+thumbnail, ห้ามเดา)
> ยังไม่มี business-rule SoT (`docs/` ชุดเดิมถูกลบ 2026-09-03 กำลังสร้างใหม่) แต่ฐานความรู้ปัจจุบันอยู่ที่ `docs/kms/README.md` แล้ว — requirement ที่ไม่ชัด **ถามลุงจืด ห้ามเดา**

## 1. สถานะ repo

| รายการ | ค่า |
|---|---|
| branch / HEAD | `dev` — ค่าตอนเขียนคือ commit ที่เพิ่มไฟล์นี้ **ล้าสมัยแล้ว** ตรวจของจริงด้วย `git log -1` / `git status -sb` |
| push ล่าสุด | ตัวเลข `20e7666e` / `6394c78b` ในเอกสารนี้ **ล้าสมัยแล้ว** (ทั้งคู่อยู่บน `origin/dev` เรียบร้อย) — ตรวจด้วย `git log --oneline origin/dev -1` + `git rev-list --left-right --count origin/dev...dev` ก่อนตัดสินใจ push (R0 ต้องถามลุงจืด) |
| GitHub Actions | **ล็อกเพราะ billing** ตั้งแต่ 2026-09-03 (ทุก run fail ใน 4 วิ "account is locked due to a billing issue") → หลักฐาน build/test ทั้งหมดเป็น local เท่านั้น |
| DB/Kafka data | **disposable ทุก env (pre-launch)** — ไม่ทำ migration/backfill, drop แล้วสร้างใหม่ได้; แก้ schema/จอ/โค้ดไม่ต้องขอ แต่ deploy/push/ลบของจริงต้องถาม |

## 2. รัน local (เครื่อง dev Windows, Docker Desktop)

```bash
# backend stack (mongodb rs0, postgres :5432, kafka :9092, redis, minio :9100/:9001, mainapi :8888) — ClickHouse ถูกพักแล้ว (ดู §4)
cd backend && docker compose -f docker-compose.yml -f docker-compose.local.yml up -d
# rebuild mainapi หลังแก้ Go (~5 นาที)
docker compose -f docker-compose.yml -f docker-compose.local.yml build mainapi && docker compose -f docker-compose.yml -f docker-compose.local.yml up -d --no-deps mainapi
# frontend
cd frontend && npm run dev        # http://localhost:3000 (ห้ามใช้ next start ค้าง — จะไม่เห็นแก้)
```

- **Login ทดสอบ**: หน้า login กด "ทดลองใช้ระบบ (Demo)" (`POST /api/auth/demo-login`) → holding `demo` (บริษัท C01–C03, สาขา 00000). `demo/C02` มีสินค้า 15 / บาร์โค้ด 15 เหมาะกับ UAT
- Dev Login ต้องตั้ง `BCAI_DEV_LOGIN_ENABLED=true` + `BCAI_DEV_LOGIN_SECRET` ให้ตรงกันระหว่าง env ของ `next dev` กับ container `mainapi` (401 = secret drift → recreate container)
- Mongo: `docker exec mongodb mongosh --quiet appdb --eval '...'` (db `appdb`, tenant แยกด้วย field `holdingcode`/`businesscode`)
- PG: `docker exec postgres psql -U postgres -d <holdingcode>` — DB ต่อ holding (`demo`, `bc001` schema ปัจจุบัน; `test` schema เก่า ไม่มี holding_code/businesscode → ควร drop/สร้างใหม่)
- Kafka: `docker exec kafka kafka-consumer-groups --bootstrap-server localhost:9092 --describe --group <g>`; groups ของ goapi ลงท้าย `-v1`; ยิง message ทดสอบด้วย `kafka-console-producer`
- **Backend build/test ทำได้เฉพาะใน Docker** (Windows ไม่มี gcc → confluent-kafka CGO พัง):
  ```bash
  MSYS_NO_PATHCONV=1 docker run --rm -v D:/bccode/backend:/src -w /src -e GOFLAGS=-mod=mod golang:1.26 sh -c "apt-get update -qq >/dev/null 2>&1 && apt-get install -y -qq librdkafka-dev >/dev/null 2>&1; gofmt -l . ; go build ./... && go vet ./internal/goapi/... ./internal/product/... && go test ./internal/goapi/... ./internal/product/..."
  ```
  quarantine 15 package: `backend/.ci/test-quarantine.txt` (รอลุงจืดยืนยัน business contract ก่อนปลด)
- Integration suite จริง (Kafka 4.3.1 + Mongo 7 + PG 18): `backend/.ci/projection.compose.yml` → `docker compose -f backend/.ci/projection.compose.yml --profile test run --rm --no-deps tests` (ดู `backend/internal/product/product/outbox/README.md`)
- UI: โหลด skill `docs/skills/ui-scale-polish/SKILL.md` ก่อนแตะ UI; `frontend/src/app/globals.css` มี EOL ผสม → แก้ด้วย Node byte-preserving ไม่ใช้ Edit tool; ปุ่ม Tailwind ต้องใช้ `text-xs!`; ทดสอบ dark mode ด้วยการกดปุ่มสลับธีมจริง

## 3. งานที่เพิ่งเสร็จ (5–6 ก.ย.)

1. **Transactional outbox + projection fences** สำหรับ Product/Barcode/Unit (`ef0b62e7`): Mongo write + `outboxevents` ใน transaction เดียว → dispatcher → Kafka → consumer ใหม่แบบ ack-after-success (`internal/product/projection`) → PG upsert ใต้ advisory lock + `product_projection_fences`
2. **แก้ 8 ข้อจาก adversarial review** (`20e7666e`): `projection.ErrRejected` (ข้อความที่ใช้ไม่ได้ถูก log topic/partition/offset แล้ว ack แทนวนไม่จบ), legacy barcode reader ใช้ group `<id>-projection` แยกจาก librdkafka, ลบ registration ซ้ำใน warehouse group, cap advisory lock 256 แถว/transaction, retry `TransientTransactionError`, Resync ข้ามสินค้าที่ถูกลบ, CI mongo-init idempotent + `run --no-deps`
3. **UAT ผ่าน**: Product/Barcode Create→Update→Delete บน demo/C02 ตรวจ Mongo + outbox + PG ทีละ step; poison message จริงถูกข้าม; lag 0
4. **Audit บทบาท 3 store** (6 ก.ย.) — ผลอยู่ §4 และ `docs/handoff/HANDOFF-RISKS-2026-09-05.md` section "6 กันยายน 2026"
5. **พัก ClickHouse บนเครื่อง dev** (`6394c78b`): container ลบแล้ว, ตัดออกจาก `backend/docker-compose.local.yml`; mainapi cold start โดยไม่มี CH → healthy, consumers Stable

6. **งานจาก AI ตัวอื่นระหว่าง 6–7 ก.ย.** (commit `9ee21587`…`ff0f746c`): user shortcuts UI + icon/`!pl-10` fix, manage-shortcuts เป็น full-screen tab, data list row baseline, ลบ groupsubone/groupsubtwo ออกจาก product classification, productunit ไม่ผูก company access, กฎ Mandatory Skill Upgrade ใน AGENTS.md — รายละเอียดดู `git log`
7. **รวมเอกสารทั้งหมดใต้ `docs/` (7 ก.ย., commit `d93a210d` + `df7d9851`)**: skills → `docs/skills/`, ฐานความรู้ `docs/kms/` (18 บทความ + ADR + bugs; อ่านโค้ดทั้ง repo แล้ว fact-check), handoff/architecture/runbook/code-map ย้ายเข้า `docs/` — เริ่มอ่านที่ `docs/kms/README.md`

## 4. ข้อค้นพบสำคัญ (verify แล้วด้วยมือ) — backlog เรียงตามความสำคัญ

สรุปสั้น: **MongoDB = SoT จริง** (ทุกจอ frontend อ่าน Mongo) · **PostgreSQL = projection ที่เขียนอย่างเดียว ไม่มี reader และครึ่งหนึ่งพังเงียบ** · **ClickHouse = ไม่มีบทบาท** (goapi stub ตั้งแต่ commit `dcceb83f` 2026-05-31)

| # | เรื่อง | หลักฐาน | ทางแก้ที่เสนอ |
|---|---|---|---|
| P1 | **Mongo→PG debtor/creditor/erp_user พัง 100%** — consumer INSERT คอลัมน์ที่ DDL (GORM model) ไม่มี: `name0/createdat/updatedat`, `position/department/approval_role/max_approval_amount`; ยิง `when-debtor-created` จริงได้ `column "name0" ... does not exist` | `internal/goapi/handlers/kafka/debtor.go:110`, `creditor.go:111`, `employee.go:119`; DDL `internal/goapi/process/build/create-database.go:1405` | แก้ SQL ให้ตรง DDL (หรือถอด consumer ถ้าไม่มี reader) + เขียน regression test แบบ `projection_reject_test.go` |
| P1 | **DLQ เป็น log-only** → projection ที่ล้มหายเงียบ, offset commit ต่อ, ตาราง `dead_letter_queue` 0 แถวเสมอ | `internal/goapi/mydlq/dlq.go:91`; consumer loop `internal/goapi/mykafkaconsumer/kafka_consumer.go:326` | DLQ ลง PG/Mongo + metric lag/fail; หรือใช้ `projection.Consume` (ack-after-success) กับ topic อื่นด้วย |
| P2 | **Busy-poll**: worker pool 24 ตัว poll ตาราง `queues` ว่างใน DB `postgres` ทุก 100 ms → 133M index scans / 136M commits; processor ที่มันเรียกเป็น TODO stub | `internal/goapi/workers/doc_processor.go:239`, `internal/goapi/process/process_status.go:11` | ปิด worker pool หรือ backoff ≥ 5 วิ; ลบถ้าไม่มีแผนใช้ |
| P2 | **ClickHouse ถอดถาวร (R1 รอลุงจืด)** — legacy 3 route ยัง register แต่พังเมื่อเรียก (ไม่มี DDL ตาราง CH ใน repo): `/productimport/*` (13), `/stockbalanceimport/*` (9), `/product/barcode2` (hard-code holding); prod compose จอง RAM 1 GB/768 MB และ migrate/mainapi/worker `depends_on: clickhouse service_healthy` | `internal/productimport/services/productimport_service.go:158`, `internal/product/productbarcode/services/productbarcode_http_service.go:1886`, `deploy/account/compose.yml:63,216,240,280`, `deploy/account/provision-server.sh` (9 keys), `backend/bootstrap*.json` block `clickhouse`, `frontend/src/lib/setup-config.ts:81`, goapi CH layer ~2.8k บรรทัด (`internal/goapi/myclickhouse`, `mydb`, `handlers/clickhouse.go`, `clond-clickhouse-database.go`, `workers/db_log_cleaner.go`, `process/clickhouse-replace-process-stock-cost.go`), `internal/report/reportqueryc` (1.1k, ไม่ได้ register), `go.mod` clickhouse-go v2.40.3 + ch-go | ลำดับปลอดภัย: compose/provision → bootstrap+setupconfig+frontend → ลบโค้ด goapi + 20 call sites no-op + `StartBackgroundTask` → ตัดสิน 3 module legacy (ลบ; import ใหม่ใช้ outbox แล้ว) → `go.mod` → `docs/kms/architecture/high-scale-multitenant-bi.md` |
| P3 | **prod รัน projection 2 ชุดชนกัน**: `worker` (DEV_API_MODE=1) รัน legacy gorm consumers เขียน schema อีกชุดลง PG เดียวกับ goapi consumers ใน `mainapi`; คำสั่ง rebuild ทั้งหมด (`ReportPostHandler`) ไม่ได้ register route | `deploy/account/compose.yml:275`, `backend/main.go:654`, `internal/goapi/handlers/commands.go:197` | หยุด `worker` mode 1 จนกว่าจะเหลือชุดเดียว; เปิด route rebuild/resync ที่มี auth |
| P3 | `stockwaitprocess` ไม่มีใคร drain (เรียกได้จาก rebuild ที่ไม่ได้ register เท่านั้น); `docwaitprocess` ค้าง transflag 12/44 (drainer จัดการเฉพาะ 6) | `internal/goapi/process/process-stock/product-calc-cost.go:50`, `process/process-doc/process-doc-purchase-batch.go:100` | ตัดสินว่า stock-cost engine บน PG ยังอยู่ในแผนไหม; ถ้าไม่ ลบทั้ง pipeline |
| P4 | PG ค้างข้อมูล: demo/C03 มีบาร์โค้ด 9 แถวใน PG ที่ Mongo ไม่มี (8850001000019…095) | เทียบ `productbarcode` PG vs Mongo | ล้างผ่าน `/product/resync` (frontend มี proxy) หรือ drop tenant DB แล้วให้ `DatabaseChecker` สร้างใหม่ |
| P4 | จุกจิกที่ flag ไว้: log format ผิดที่ `internal/goapi/handlers/kafka.go:191` (echo payload ทั้งก้อน); dialog ลบสินค้าใช้คำว่า "ลบบาร์โค้ด"; แถว unit picker ไม่มี accessible name; `pkg/microservice/persister_mongo_test.go` vet warning; PG tenant `test` schema เก่า | — | แก้เป็น commit เล็กแยก |

กับดักที่เจอระหว่างงาน: `.ignore` ที่ root มี `**/build/` → **ripgrep ข้าม `internal/goapi/process/build/` ทั้งโฟลเดอร์** (ใช้ `rg --no-ignore`); `docker compose run` (v5.3) restart one-shot dependency ที่ exit แล้ว → ใช้ `--no-deps`; kafka-go และ librdkafka แชร์ consumer group ไม่ได้ (JoinGroup metadata คนละรูปแบบ); ฟอร์มแก้บาร์โค้ดใน browser ต้องพิมพ์คีย์จริงก่อนกดบันทึก (`form_input` อย่างเดียวไม่ยิง PUT); เมื่อเปิดแท็บสินค้าและบาร์โค้ดพร้อมกันจะมีปุ่ม "แก้ไข/ลบ" 2 ชุด — เช็ค header ก่อนกด

## 5. คำถามค้างที่ต้องได้คำตอบจากลุงจืดก่อนทำต่อ

1. **ตัวเลขบัญชี**: currency / precision / rounding contract (ยังไม่มี SoT)
2. **15 package ใน quarantine**: business contract ของแต่ละตัวคืออะไร จึงจะปลด quarantine ได้
3. **Backup**: platform / RPO / RTO (runbook `docs/runbooks/RECOVERY-READINESS.md` รอข้อมูลนี้)
4. **Unit contract**: `unit_of_measure/code/businesscode` vs `units/unitcode` (MongoModel partial index)
5. **ClickHouse**: ถอดถาวร (P2 ด้านบน) หรือพักต่อ
6. **PG ยังจำเป็นไหม / เพื่ออะไร**: ตอนนี้ไม่มี feature ใดอ่าน PG — จะลงทุนซ่อม projection (P1) หรือหยุด project ทุกอย่างยกเว้น product/barcode จนกว่าจะมี reader จริง

## 6. Infra (ไม่มี secret ในไฟล์นี้และใน repo)

- **on-prem 192.168.2.202**: Docker stack เดิม (postgres/clickhouse/mongodb/minio/mainapi/redis/kafka) — วันที่ 6 ก.ย. ssh timeout เข้าไม่ได้ → ไม่ทราบสถานะ stack/ClickHouse ที่นั่น (compose บน .202 เป็นคนละชุดกับ local; การพัก CH วันนี้ทำเฉพาะเครื่อง dev)
- **prod ใหม่ DigitalOcean SGP1 159.223.43.229** (4 vCPU/8 GB, Ubuntu 24.04): provision แล้ว (Docker+Caddy+ufw+fail2ban+swap, key-only ssh) **ยังไม่ deploy app**; compose ที่จะใช้ = `deploy/account/compose.yml` + `compose.8gb.yml`; prod เก่า 188.212.158.39 เข้าไม่ได้แล้ว
- โดเมน `app.bcaicloud.com` เลิกใช้; cloudflared tunnel `bcaicloud` ยังมี ใช้ซ้ำกับโดเมนใหม่ได้
- รูปภาพ: MinIO เท่านั้น (`POST /api/upload/image` → `/goapi/image/upload`) + thumbnail คู่เสมอ

## 7. วิธีทำงานที่ลุงจืดต้องการ (สำคัญกับ AI ตัวถัดไป)

- สื่อสารภาษาไทย สั้น ตรงประเด็น เรียก "ลุงจืด"; ทุก task จบด้วย ✅ ข้อดี / ⚠️ ข้อเสีย / 💡 คำแนะนำ
- **ห้ามบอกเสร็จโดยไม่มีหลักฐาน** (build/test/curl/query/log) · **ห้ามเดา** infra/requirement · scope ตามที่สั่ง เจอบั๊กอื่น flag ก่อนไม่แก้เอง · patch > refactor > rewrite
- ก่อนงานใหญ่/เสี่ยง: commit + push checkpoint ก่อน · R0 (push/deploy/ลบของจริง) ถามก่อน
- UAT ทุกครั้งต้อง Create→Read→Update→Delete ครบ + ยืนยันใน MongoDB ทันทีทีละ step + ล้างข้อมูลทดสอบด้วย id เท่านั้น
- ห้าม purge `outboxevents` history; ห้ามเก็บ/log secret; input ภายนอก validate เสมอ
- งาน UI ต้องผ่านกฎ "คนไทย 40+" + "พรีเมี่ยม" ใน `AGENTS.md` และอัปเดต `docs/skills/ui-scale-polish/SKILL.md` พร้อม commit
