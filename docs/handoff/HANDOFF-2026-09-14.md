# HANDOFF 2026-09-14 — prompt สำหรับ Gemini (แก้ต่อจาก code review)

> **อ่านไฟล์นี้ให้จบก่อนแตะโค้ด** ลำดับกฎ: `AGENTS.md` (ชนะเสมอ) → ไฟล์นี้ → `docs/kms/README.md` → `docs/skills/ui-scale-polish/SKILL.md` (ก่อนแตะ UI ทุกครั้ง)
> handoff เก่า `HANDOFF-2026-09-09.md` ยังใช้ได้เรื่อง verify/CI (ไม่มี CI — ต้อง `npm run verify` เองก่อน push)

---

## 0. สถานะตอนส่งมอบ (commit `83014952` บน `dev`, push แล้ว)

- งานค้างทั้งหมด (GL v2, ผังเมนู 9 โมดูล, ตารางที่เก็บสินค้า, แม่แบบธนาคารไทย, NumericInput) ถูก commit เป็น checkpoint เดียว
- Claude ทำ code review 8 มุม → verify 23 → **แก้แล้ว 8 จุด** (รายการ + เหตุผลใน `docs/kms/bugs/2026-09-14-code-review-gl-warehouse-fixes.md` และ README log 2026-09-14)
- หลักฐานที่ผ่านแล้ว: Go build/vet/test `internal/generalledger/...` ใน Docker ✅, `tsc --noEmit` ✅, vitest 470/470 ✅ (pre-push)
- **ยังไม่ได้ทำ**: UAT บน browser ของ 3 จุดที่แก้ (ตารางที่เก็บสินค้า, statement designer, NumericInput) และ `npm run verify:all` (backend ทั้งหมด)
- ไฟล์ขยะที่ root **ไม่ได้ commit**: `qa-coa-01-list-light-1600.png`, `qa-coa-snapshot-1.md`, `qa-coa-snapshot-2-form.md`, `tmp-patch-gl-masters.cjs`, `tmp-patch2.cjs` — ถามลุงจืดก่อนลบ (ไม่ใช่ของรอบนี้)

## 1. ห้ามทำ (เจอมาแล้ว)

- **ห้ามใส่ `NODE_ENV` gate ให้ `DevDomInspector` ใน `frontend/src/app/layout.tsx`** — reviewer จะ flag แต่ลุงจืดสั่งเปิดบน production (`docs/kms/decisions/2026-09-12-enable-dom-inspector-on-production.md`)
- **ห้ามเปลี่ยน default 2 ทศนิยมของ `NumericInput`** — มี test ยืนยัน (`numeric-input.test.ts`) แค่ห้ามปัดค่าที่เก็บ (แก้แล้ว)
- ห้าม derive Kafka group ของ GL จาก `CONSUMER_GROUP_NAME` (กลับไปเป็น apply ซ้ำ 2 รอบ) — ใช้ `gl-v2-projection` คงที่
- ห้าม `panic` ตอน register consumer ใน `backend/main.go`
- กฎเดิม: ห้ามอ้างชื่อโปรแกรมบัญชีรายอื่นในตลาด (pre-commit hook จับ), ห้าม hard-code สี, ไม่มี payroll, README log ทุก commit, `npm run verify` ก่อน push

## 2. งานที่ต้องทำต่อ — เรียงตามความสำคัญ

### A. ต้องถามลุงจืดก่อน (ห้ามเดา)
1. **เมนู `/line-oa` หายจากผังเมนูใหม่** — `AGENTS.md` บอกว่าต้องอยู่เมนูหลัก แต่ `frontend/src/lib/menu-data.ts` ตอนนี้มีแค่ 9 section ERP (po/bill/ap/ar/cash-bank/ic/fa/vat/gl, 224 รายการ, test ล็อกจำนวนไว้ที่ `menu-data.test.ts:77`) — ถามว่าวางกลุ่มไหน แล้วแก้ `menu-data.ts` + test count + `docs/kms/19-menu-coverage-market-standard.md` + AGENTS.md ในคอมมิตเดียว
2. **สิทธิ์ปุ่มของหน้าจอระดับ Holding** (`/employee`, `/user`, `/permissiongroup`) ไม่อยู่ใน `MENU_SECTIONS` → `frontend/src/lib/use-screen-actions.ts:37` fallback เป็น `ALL_SCREEN_ACTIONS` (ทุกคนเห็นปุ่มลบ) — ถามว่าจะ gate ด้วย role ADMIN ของ holding หรือเพิ่ม permission id ให้หน้าเหล่านี้

### B. บั๊กที่ verify แล้วว่าจริง แต่ยังไม่ได้แก้ (ทำได้เลย ไม่ต้องถาม)
3. `frontend/src/lib/general-ledger.ts:224` `reportCsv` — ใส่ `'` หน้าตัวเลขทุกช่อง Excel เปิดแล้วเป็นข้อความ `'1234.50` รวมยอดไม่ได้ → เอา apostrophe ออก (ถ้ากลัวเลขนำหน้าศูนย์หาย ให้ใส่เฉพาะคอลัมน์รหัส ไม่ใช่คอลัมน์ amount)
4. `backend/internal/generalledger/postgres.go:295` — search ใช้ `strpos(lower(code || ' ' || payload::text), ...)` ค้นทั้ง JSON รวม key → พิมพ์ `th`/`true`/`code` เจอทุกแถว → ค้นเฉพาะ `code` + ชื่อ (`payload->'names'`) หรือคอลัมน์ search_text ที่ projection สร้างให้
5. `backend/internal/generalledger/references.go:11` — ตรวจอ้างอิงก่อนลบบัญชีไม่รวม `gl_statement_templates.rows.accountcodes` → ลบบัญชีที่แม่แบบงบใช้ได้ แล้ว `calculateStatement` ได้ 0 เงียบ ๆ → เพิ่ม field นี้ในรายการตรวจ
6. `frontend/src/app/api/system-settings/[[...settingPath]]/route.ts:474-481` — proxy แต่ง `banknames` จาก `bankcode` เมื่อผู้ใช้ไม่กรอกชื่อธนาคาร ทำให้ผ่าน validation backend แล้วชื่อธนาคารกลายเป็นรหัส → ส่ง 400 ภาษาไทยกลับแทนการแต่งค่า
7. `frontend/src/app/system-settings/system-settings-screen.tsx:3583, 3695, 5573` + `system-setting-screens.ts:1742` — ปุ่ม "เพิ่มธนาคารไทยจากแม่แบบ" gate ด้วย `slug === "bank"` ซึ่งไม่มีอีกแล้ว (เหลือ `bookbankscreen`) → ฟีเจอร์ตาย + `saveThaiBanks` POST ไปผิด entity → ตัดสินใจกับลุงจืดว่าแม่แบบธนาคารยังต้องการไหม ถ้าไม่ ลบ dead code ทั้งชุด (`saveThaiBanks`, `ThaiBankTemplateDialog`, alias `bank` ใน `getSystemSettingConfig`)
8. `backend/internal/generalledger/processes.go:67,102` — `prepareProcess` นับ draft journal จาก Mongo คั่นกลางการอ่านยอดจาก PostgreSQL (ขัดกฎ 2-Tier "Zero Cross-DB Runtime Dependency" ใน AGENTS.md) → ย้ายเช็ค draft/opening ไปอ่านจาก projection ใน PG (มี `gl_records kind='journals'` อยู่แล้ว)
9. `frontend/src/app/gl/gl-journals.tsx:202, 219-221, 249, 458, 582` + `gl-masters.tsx`, `gl-statement-designer.tsx`, `account-search-dialog.tsx` — สี hard-code (`text-red-600`, `bg-amber-100`, `bg-emerald-500/10`) ขัดกฎพรีเมี่ยม → ใช้ token (`text-destructive`, `bg-primary/10`, `--panel`) ตาม `gl-masters.tsx:306` แล้วอัปเดต `docs/skills/ui-scale-polish/SKILL.md` + screenshot light/dark ตามกฎ
10. `frontend/src/app/gl/gl-journals.tsx:328`, `account-search-dialog.tsx:309,319` — ปุ่ม icon-only มี `title` แต่ไม่มี `aria-label` ไทย → เพิ่ม

### C. ประสิทธิภาพ/โครงสร้าง (ทำหลัง B; แต่ละข้อ = commit แยก, ห้ามรวม refactor ใหญ่)
11. `backend/internal/generalledger/httpapi/http.go:102` — `regexp.MustCompile` อยู่ใน closure รันทุก request → ย้ายเป็น package-level var (1 บรรทัด ทำก่อนเลย)
12. `http.go:98-121` — GL สร้าง `*sql.DB` pool ของตัวเองต่อ holding ซ้ำกับ `mypg.PgSqlFastConnect(holding)` (`backend/internal/goapi/mypg/fast_utils.go:39`) → ส่ง `mypg.PgSqlFastConnect` เป็น resolver ให้ `gl.NewPostgres` แทน (ลด connection ต่อ tenant ลงครึ่ง)
13. `backend/internal/generalledger/reports.go:141,186` — ทุกรายงานรัน CTE เดิม 2–3 รอบต่อหน้า (count+SUM, page, setTotals) → รวมเป็น statement เดียวด้วย `count(*) OVER()` / `SUM() OVER()`
14. `backend/internal/generalledger/errors.go` + `httpapi/http.go:290-343` — error envelope ของ GL คนละรูปกับ `backend/pkg/apperr` ที่ 14 package ใช้ → ย้ายไป `apperr.AppError`/`ToResponse()` แล้วลบ parser พิเศษฝั่ง FE (`general-ledger-api.ts:74 commandErrorInfo` ใช้ `extractMessage` จาก `workspace-api.ts:43`)
15. `frontend/src/app/gl/gl-common.tsx:425 useReferences` — 5 จอ GL โหลดผังบัญชีเต็ม (10k, หน้าละ 500 แบบ sequential) ทุกครั้งที่ mount และทุก `revision` → cache ระดับ module keyed ด้วย `sequence`; `general-ledger-api.ts:141 glAllRecords` ดึงหน้า 2..N แบบ `Promise.all` บน snapshot เดิม
16. splitter drag/keyboard/localStorage ถูก copy 6 ที่ (`system-settings-screen.tsx:889,962`, `manage-shortcuts-screen.tsx:~110`, `product-barcode-shelf-screen.tsx:~135`, `product-set-screen.tsx:~280`, `company-branch-tree-view.tsx:~720`) — `resizable-splitter.tsx` เป็นแค่ handle ไม่มี state → สร้าง hook `useSplitPercent({storageKey, defaultLeft, min, max})` ข้าง ๆ แล้วให้ทุกจอเรียก (อัปเดต skill ด้วย)
17. `frontend/src/app/gl/gl-common.tsx:157 formatAmountValue` ซ้ำกับ `general-ledger.ts:193 formatAmount` (คนละ edge case) → ใช้ตัวเดียว
18. `warehouse-tree-view.tsx:549-598 saveLocations` และ `system-settings-screen.tsx:~2570 saveThaiBanks` ยิงทีละแถวแบบ serial → `Promise.allSettled` ต่อเฟส (ระวัง: การอัปเดต state ต่อแถวที่เพิ่งแก้ต้องคงอยู่)
19. `frontend/src/lib/general-ledger.ts:445 calculateStatement` คำนวณงบใน browser — แก้ชั่วคราวให้อ่านครบทุกหน้าแล้ว แต่ระยะยาวควรเป็น endpoint `GET /gl/v2/reports/statement?template=<id>` ฝั่ง Go (มี CTE ใน `reports.go:256`) ตามกฎ "PostgreSQL = processing engine"

## 3. วิธี verify ก่อนบอกเสร็จ (ทุกข้อ)

```bash
cd D:/bccode && npm run verify
```

- backend เปลี่ยน → รันใน Docker (Windows native build พังเพราะ kafka CGO):
  `MSYS_NO_PATHCONV=1 docker run --rm -v D:/bccode/backend:/src -w /src -e GOFLAGS=-mod=mod golang:1.26 sh -c "apt-get update -qq >/dev/null 2>&1 && apt-get install -y -qq librdkafka-dev >/dev/null 2>&1; go build ./... && go vet ./internal/generalledger/... && go test ./internal/generalledger/..."`
- UI เปลี่ยน → screenshot light+dark × 1600/1280/1024/768 กดปุ่มสลับธีมจริง (กฎพรีเมี่ยมข้อ 8) + อัปเดต `docs/skills/ui-scale-polish/SKILL.md`
- เขียนข้อมูล → UAT CRUD + ตรวจ Mongo `appdb` ทีละ step (กฎ UAT)
- ทุก commit: README log (`### 2026-MM-DD — หัวข้อ`) + `git add README.md` ในคอมมิตเดียว; ไฟล์ ≥ 950 บรรทัด → `pwsh -NoProfile -File tools/gen-code-map.ps1`

## 4. ไฟล์ที่ต้องรู้จัก

| เรื่อง | ไฟล์ |
|---|---|
| GL backend | `backend/internal/generalledger/{models,mutations,processes,postgres,reports}.go`, `httpapi/http.go`, `kafkatransport/transport.go` |
| GL frontend | `frontend/src/app/gl/*.tsx`, `frontend/src/lib/general-ledger.ts`, `general-ledger-api.ts`, proxy `frontend/src/app/api/gl/[...glPath]/route.ts` |
| ผังเมนู | `frontend/src/lib/menu-data.ts` (+`.test.ts` ล็อก 224), `menu-screen-status.ts`, `use-screen-actions.ts` |
| คลังสินค้า | `frontend/src/app/system-settings/warehouse-tree-view.tsx`, backend `warehouse_location_http_service.go` (replace ทั้ง doc) |
| สถาปัตยกรรม GL | `docs/kms/architecture/2026-09-11-general-ledger-v2.md`, ADR ใน `docs/kms/decisions/2026-09-11-*` |
| บทเรียนรอบนี้ | `docs/kms/bugs/2026-09-14-code-review-gl-warehouse-fixes.md` |
