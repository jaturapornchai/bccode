---
source: backend/internal/product/productbarcode/models/product_barcode.go
tags: [datamodel, general-type]
---

# MarketplaceMediaAsset

โครงสร้าง `MarketplaceMediaAsset` จากโมดูล mainapi `productbarcode` มีฟิลด์ตามซอร์ส `product_barcode.go`

| Field | Type | bson | json | คำอธิบาย |
|---|---|---|---|---|
| Kind | string | kind | kind | ค่าของ Kind ตามฟิลด์ `kind` ในซอร์ส |
| URI | string | uri | uri | ค่าของ URI ตามฟิลด์ `uri` ในซอร์ส |
| ExternalID | string | externalid | externalid | ค่าของ ExternalID ตามฟิลด์ `externalid` ในซอร์ส |
| ExternalURL | string | externalurl | externalurl | ค่าของ ExternalURL ตามฟิลด์ `externalurl` ในซอร์ส |
| OptionCode | string | optioncode | optioncode | ค่าของ OptionCode ตามฟิลด์ `optioncode` ในซอร์ส |
| OptionValue | string | optionvalue | optionvalue | ค่าของ OptionValue ตามฟิลด์ `optionvalue` ในซอร์ส |
| SortOrder | int | sortorder | sortorder | ค่าของ SortOrder ตามฟิลด์ `sortorder` ในซอร์ส |
| AltText | string | alttext | alttext | ค่าของ AltText ตามฟิลด์ `alttext` ในซอร์ส |
| UseCase | string | usecase | usecase | ค่าของ UseCase ตามฟิลด์ `usecase` ในซอร์ส |
| MimeType | string | mimetype | mimetype | ค่าของ MimeType ตามฟิลด์ `mimetype` ในซอร์ส |
| Width | int | width | width | ค่าของ Width ตามฟิลด์ `width` ในซอร์ส |
| Height | int | height | height | ค่าของ Height ตามฟิลด์ `height` ในซอร์ส |
| LastImportedAt | string | lastimportedat | lastimportedat | ค่าของ LastImportedAt ตามฟิลด์ `lastimportedat` ในซอร์ส |

## ความสัมพันธ์

- ไม่มีฟิลด์ที่ใช้ struct ซ้อนตามประกาศนี้
